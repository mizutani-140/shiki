name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # TODO: Customize CI to your stack.
      # Examples:
      # - Node:
      #   - uses: actions/setup-node@v4
      #     with: { node-version: '20' }
      #   - run: npm ci
      #   - run: npm test
      #
      # - Python:
      #   - uses: actions/setup-python@v5
      #     with: { python-version: '3.11' }
      #   - run: pip install -r requirements.txt
      #   - run: pytest -q
      #
      # - Makefile:
      #   - run: make test

      - run: echo "Customize CI in .github/workflows/ci.yml"
