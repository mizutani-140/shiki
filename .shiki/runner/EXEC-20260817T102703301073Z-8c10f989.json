{
  "command": "codex exec - <.shiki/handoffs/T-20260817T063758155247Z-0b11207b-task.md>",
  "created_at": "2026-08-17T10:27:03+00:00",
  "goal_id": "G-20260817T063758153709Z-0302813a",
  "id": "EXEC-20260817T102703301073Z-8c10f989",
  "returncode": 0,
  "stderr": "2026-08-17T10:07:15.365489Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 95 column 5\nOpenAI Codex v0.144.6\n--------\nworkdir: /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\nmodel: gpt-5.6-sol\nprovider: openai\napproval: on-request\nsandbox: workspace-write [workdir, /tmp, $TMPDIR]\nreasoning effort: high\nreasoning summaries: none\nsession id: 01a00f30-8fca-7081-9def-5f2fb520eab6\n--------\nuser\n# Codex Task Handoff: T-20260817T063758155247Z-0b11207b\n\nGoal: G-20260817T063758153709Z-0302813a\nTask: T-20260817T063758155247Z-0b11207b\nRuntime: codex\nBranch: shiki/t-20260817t063758155247z-0b11207b-deny-the-sadr-0017-exemption-to-fork-prs-and-pin\n\n## Execution Protocol (the Shiki loop owns delivery)\n- Implement ONLY by editing files in this worktree to satisfy the Scope and Acceptance Checks below.\n- Do NOT run `git commit`, `git push`, `git checkout`/`git switch`, or any `gh` command.\n- Do NOT create, update, comment on, or merge a pull request.\n- The Shiki goal loop owns commit, push, PR creation (with the required MergeGate PR body) and merge; opening your own commit or PR breaks the loop's create_pr state transition and fails MergeGate.\n- Stay strictly within the declared Locks below; touch no files outside them.\n\n## Scope\nPort five capabilities from mizutani-140/CubeShare into this platform repository, one of them CORRECTED rather than copied.\n\n(a) FORK GUARD \u2014 the reason this task exists. Measured on origin/main: `git grep -c isCrossRepository -- scripts/ .github/ tests/` returns NOTHING (0 occurrences repo-wide; the same grep returns 63 files for `bookkeeping_closeout`, so the search works). And `mergegate_check.py` computes `carry_resolved_risk = \"low\" if bookkeeping_closeout else str((task or {}).get(\"risk_level\") or \"\")`. A PR shaped as a SADR-0017 bookkeeping closeout therefore resolves to `low` and skips the Guardian gate at any real risk level. This repository is PUBLIC with `allow_forking: true`, so a fork PR is an unauthenticated path to that shape.\n\nAdd the guard at BOTH enforcement points, in the `guardian_approval_signal.py` form:\n\n    if exemption and pr.get(\"isCrossRepository\") is not False:\n        exemption = False\n\nDO NOT copy CubeShare's `mergegate_check.py:3600`, which reads `if bookkeeping_closeout and pr and pr.get(...) is not False:`. That `and pr` short-circuits the guard on a falsy `pr`. On the Guardian leg the branch is unreachable, but `bookkeeping_closeout` also flows to `_frozen_spec_mutable_task_fields(...)` at `cs:3608`, under `if not migration_mode:` and OUTSIDE any `pr` guard. Use the un-short-circuited form at both sites.\n\nThe guard is fail-closed by design: `is not False` means absent/null/anything-but-`False` collapses the exemption, restoring the task's real risk. That is strictly stricter, never a bypass \u2014 but it means EVERY `gh pr view --json` field list that produces a PR snapshot must request `isCrossRepository`, or the exemption silently stops working on that path. The Python and every workflow field list MUST land in ONE commit.\n\nGuard scope, so it is not overclaimed: SADR-0017 bookkeeping exemption ONLY. The contract_approval carry and the SADR-0018 base-sync carry are out of scope.\n\n(b) IMMUTABLE MERGE-BASE PINNING. Resolve `mergeBaseOid` once in each gate workflow from `baseRefOid` plus `git merge-base`, inject it into the PR snapshot with jq, consume it in `mergegate_check.py` (a `_git_show_base`-style reader), and read the frozen tree with `git archive \"$merge_base\" .shiki`. Remove the `|| true` that swallows a failed resolution. Python and both workflows must land together or every migration PR blocks on the missing field.\n\n(c) CLOSEOUT CLASSIFIER HARDENING, MINUS THE RELAXATION. Port CubeShare's `is_bookkeeping_closeout` hardening: symlink-safe `lstat`/`S_ISREG` path proofs; `entry.status in {\"A\",\"M\"}` and `not old_path` so rename/copy/type-change legs are rejected (main rejects only `D`); the task's own worktree record returning False instead of passing; `type(...) is not int` for `expected_pr` so `True` and `\"12\"` no longer coerce; base goal status restricted to `{planned, ready, blocked}`; field-identity loops over base/head goal and lock; a completion-report content proof; a scorecard shape proof; a ledger-evidence proof; and a closing `set(changed_paths) != permitted_changed_paths`.\n\nEXCLUDE the lockless arm. CubeShare has `lock_terminal = lock_released or (lockless and not lock_path_in_diff)`; main has `if not task_changed or not lock_released: return False`. KEEP MAIN'S. CubeShare widened its own SADR-0017 exemption without amending SADR-0017 (the ADR is byte-identical on both sides). After this task, `grep -c \"lockless\\|lock_terminal\" scripts/mergegate_check.py` must be 0. The `_path_proven_absent` / `_path_is_regular_file` helpers still port \u2014 the symlink hardening uses them independently.\n\n(d) COMPLETION-REPORT RISK. Write the report's risk as the strongest of the goal's and every task's, across the `shiki_tasks.py` writer and the closeout classifier's equality check. Both legs in one commit, or the classifier fails on every outranked closeout.\n\n(e) DOCS. MG-25, MG-26 and the migration decision-control row; SADR-0012 STEP 5 ONLY. Step 3 documents the lockless closeout \u2014 main does not implement it, and publishing it would make CubeShare-local behaviour read as platform doctrine. MG-26 asserts registration on the immutable merge base, so it lands with or after (b), never before.\n\nMUST NOT: reflux `and pr`; add `lockless`/`lock_terminal`; touch SADR-0017 itself; port `exact_head_only`, `normal_frozen_spec_errors`, `exact_pr_link`, or any `spec_amendment`/`delivery_only` surface; add a second `git fetch origin`; weaken `tests/test_workflow_invocation_carry.py`'s `2`/`1` fetch counts or its `total == 4`; bind `_review_source` to a head SHA (a real but separate defect).\n\nTESTS. Fork fixtures at both enforcement points: `isCrossRepository: true` denies the exemption, and a snapshot with the field ABSENT also denies it. Assert `_bookkeeping_closeout_exemption` then resolves the task's real risk, not `low`. Assert every `gh pr view --json` list in both workflows contains `isCrossRepository`. Assert `grep -c \"lockless\\|lock_terminal\"` is 0 and that `if not task_changed or not lock_released:` is present verbatim. Symlinked, renamed, type-changed and coerced-`expected_pr` closeout fixtures all return False. A goal/task risk pair (`medium`/`critical`) produces `report[\"mergegate\"][\"risk\"] == \"critical\"` and the classifier's equality check passes on it. Keep the fetch-count invariants exactly as they are.\n\nREPRODUCIBILITY \u2014 what this task removes, and what to change to get the same result.\n  Symptom: on a PUBLIC repository, anyone may fork, open a PR shaped as a SADR-0017 bookkeeping closeout, and have `carry_resolved_risk` computed as `low`, bypassing the Guardian gate for a high- or critical-risk task.\n  Measured 2026-08-17: `isCrossRepository` 0 occurrences across scripts/, .github/ and tests/ on origin/main; `visibility: public`, `allow_forking: true`; 0 cross-repository PRs ever opened, so nothing has exploited it.\n  Root cause: the exemption never asks whether the PR came from a fork.\n  Correction this task makes over its source: CubeShare's `mergegate_check.py:3600` carries an `and pr` short-circuit its sibling at `guardian_approval_signal.py:336` does not; both sites here use the un-short-circuited form.\n  Reproducible when: a fork-PR fixture and a field-absent fixture both deny the exemption at both sites, `lockless`/`lock_terminal` appear nowhere, and the fetch-count invariants are untouched.\n\n## Acceptance Checks\n- A fork PR snapshot (isCrossRepository: true) denies the SADR-0017 exemption at BOTH enforcement points.\n- A snapshot with isCrossRepository ABSENT also denies the exemption at both points \u2014 fail-closed.\n- Neither enforcement point contains an `and pr` short-circuit before the field test.\n- With the exemption denied, _bookkeeping_closeout_exemption resolves the task's real risk_level, not \"low\".\n- Every gh pr view --json field list in shiki-mergegate.yml and shiki-cca-completion.yml contains isCrossRepository.\n- grep -c \"lockless\\|lock_terminal\" scripts/mergegate_check.py == 0, and `if not task_changed or not lock_released:` is present verbatim.\n- Symlinked, renamed, type-changed and coerced-expected_pr closeout fixtures all return False.\n- A goal/task risk pair (medium/critical) produces report[\"mergegate\"][\"risk\"] == \"critical\" and the closeout classifier's equality check passes on it.\n- A gate that resolves a base ref uses the immutable mergeBaseOid, and a failed resolution is not swallowed by `|| true`.\n- tests/test_workflow_invocation_carry.py still asserts count(\"git fetch origin\") == 2 in the CCA workflow, == 1 in the mergegate workflow, and total == 4.\n- docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md is unchanged, and SADR-0012 step 3 is not published.\n- python3 -m unittest discover -s tests, validate_shiki, and every scripts/test_shiki_*.sh pass.\n- No file outside the declared locks is changed.\n\n## Locks\n- path:scripts/mergegate_check.py\n- path:scripts/guardian_approval_signal.py\n- path:scripts/shiki_tasks.py\n- path:.github/workflows/shiki-mergegate.yml\n- path:.github/workflows/shiki-cca-completion.yml\n- path:tests/test_bookkeeping_closeout.py\n- path:tests/test_closeout.py\n- path:tests/test_guardian_signal_closeout.py\n- path:tests/test_paired_invariants.py\n- path:tests/test_memory_scorecard.py\n- path:tests/test_policy_job_merged_prs.py\n- path:tests/test_migration_mode.py\n- path:docs/agents/checklists.md\n- path:docs/agents/decision-control.md\n- path:docs/agents/triage-labels.md\n- path:docs/agents/implementation-policy.md\n- path:docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n- path:scripts/test_shiki_governance_evidence.sh\n- path:scripts/test_shiki_adversarial_state.sh\n\n## Required Skills\n- tdd\n- code-review\n\n## Distilled Rules\n\nnone applicable\n\nwarning: Under-development features enabled: chronicle. Under-development features are incomplete and may behave unpredictably. To suppress this warning, set `suppress_unstable_features_warning = true` in /Users/kio.mizutani/.codex/config.toml.\nwarning: Skill descriptions were shortened to fit the 2% skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.\nhook: SessionStart\nhook: SessionStart Completed\ncodex\nI\u2019m using the repository\u2019s Shiki execution skill plus the required TDD and code-review skills. I\u2019ll first verify the handoff, locks, worktree state, and relevant prior context; then I\u2019ll build failing tests, implement only within the locked paths, and run the full acceptance suite.\nexec\n/bin/zsh -lc \"sed -n '1,240p' .codex/skills/shiki/SKILL.md && sed -n '1,260p' /Users/kio.mizutani/skills/skills/engineering/tdd/SKILL.md && sed -n '1,260p' /Users/kio.mizutani/skills/skills/engineering/code-review/SKILL.md\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n exited 1 in 0ms:\n---\nname: shiki\ndescription: Use when the user invokes Shiki, /shiki, or asks to run the GitHub-first agentic engineering control plane for Goal Seek, Context and Impact, Task DAG, Codex implementation, CCA completion judgment, MergeGate, or bounded repair loops.\n---\n\n# Shiki\n\nShiki is the user's GitHub-first, runtime-agnostic control plane for agentic\nengineering.\n\n## Canonical Source Of Truth\n\n<!-- shiki-source-of-truth:start -->\n1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n4. `docs/adr/` records hard-to-reverse platform decisions.\n5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n<!-- shiki-source-of-truth:end -->\n\nWithin `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\nproduct decisions retain `NNNN-*.md` and the `ADR` namespace.\n\nCodex CLI does not register this skill as a custom `/shiki` slash command. When\nthe user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\nthat this is expected: invoke Shiki in Codex with natural language such as\n\"Shiki: create a new GitHub-backed target repo ...\" or run the shell command\n`shiki start ...`.\n\n## Start\n\nRun:\n\n```bash\nshiki status\nshiki doctor\n```\n\n`shiki status` reports the installed Shiki platform/template root and default\nconfig. Its `config.repo` value is not automatically the requested Target\nRepository. Do not route work to `/Users/kio.mizutani/shiki` or\n`mizutani-140/shiki` unless the user explicitly asks to work on the Shiki\nplatform repo itself.\n\nUse `shiki doctor` to distinguish Shiki availability from Agent Runtime\nauthentication. If Claude Code reports `Please run /login` or `API Error: 401\nInvalid authentication credentials`, the Claude Code adapter cannot run `/shiki`\nuntil `claude auth login` or `/login` succeeds. Do not block the Codex path on\nClaude auth; use `shiki start` from Codex or a terminal when Codex/GitHub are\nready.\n\nWhen `/shiki` or Shiki is invoked without a clear target, first establish\nwhether the user wants a new GitHub-backed Target Repository or an existing\nrepository. For new repo requests, collect the target path and GitHub slug before\ninspecting repo contents.\n\nThen inspect the selected target repository's `AGENTS.md`, `CLAUDE.md`,\n`CONTEXT.md`, `.shiki/`, `docs/agents/`, and open PR/issue state before changing\nfiles.\n\nIf Shiki is not installed in the target repository, do not give the user a\nmanual sequence. Ask for the missing repo and Goal values one question at a\ntime, then run the one-command entrypoint:\n\n```bash\nshiki start TARGET --repo OWNER/NAME --goal \"...\" --outcome \"...\"\n```\n\nThe default engineering Skill Gate directory is\n`/Users/kio.mizutani/Documents/lead-os/skills/engineering` when present. The\nstart record, plan, and handoff must preserve the selected skills directory.\n\n## Responsibilities\n\n- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n- Claude Code Action can act as GitHub-side CCA or reviewer.\n- CCA judges completion.\n- MergeGate authorizes state transitions and merge readiness.\n- GitHub branch protection is the hard gate.\n\n## Rules\n\n- For non-trivial goals, enter through `grill-with-docs`. In a bootstrapped Target Repository the default flow is Goal mode: Requirements Definition (grill dialogue + Context & Impact + PRD) ends with the operator's explicit approval = Spec Freeze, recorded as the plan's `spec_freeze` block (`status: frozen`, `approved_by`, `source`). Plans without it are rejected by `shiki plan ingest`/`run`. Codex satisfies the Context & Impact sweep requirement with an equivalent recorded exploration (the Claude Workflow tool is not available in Codex).\n- After Spec Freeze, scope-moving discoveries pause the task and return to the operator as a Spec Amendment; non-scope-moving interpretations are recorded in the Assumption Log.\n- `/shiki` should guide the user through missing repo/Goal answers one question at a time and then run `shiki start`; direct `init`, `plan`, and `run` calls are lower-level fallback commands.\n- Convert the settled `grill-with-docs` result into a machine-readable plan and run it with `shiki plan ingest` followed by `shiki run`.\n- For unattended execution, queue the plan with `shiki daemon enqueue-plan` and process it with `shiki daemon run`.\n- For headless runtime integration, use `shiki runner next` and `shiki runner execute` to pick up ready tasks and record execution evidence.\n- When a ready task is assigned to claude-code (the default), run the implementation adapter with `shiki runner claude --target TARGET --task-id T-XXXX`. When it is assigned to Codex, run `shiki runner codex --target TARGET --task-id T-XXXX` instead of handing the user a manual command. Both create or reuse the task worktree, invoke the headless runtime (`claude -p` or `codex exec`) with the handoff, and record runner evidence. Stop for user input only when the assigned runtime's auth/tooling is missing, dispatch is blocked, or Guardian approval is required.\n- Use Context and Impact before implementation.\n- Keep tasks as vertical slices with explicit locks and verification.\n- Use TDD for implementation work when behavior changes.\n- Do not call implementation complete until GitHub evidence, CCA, and MergeGate support it.\n- Do not use `shiki install-target` unless the user explicitly asks for local-only template copying.\n- Do not bypass branch protection or use admin merge.\n- If a workflow change needs a bootstrap exception, ask for explicit Guardian approval first.\n\n## Commands\n\n- `shiki install-global`\n- `shiki start /path/to/repo --repo OWNER/REPO --goal \"...\" --outcome \"...\"`\n- `shiki init /path/to/repo --repo OWNER/REPO`\n- `shiki preflight --require-github`\n- `shiki plan guide --prompt \"...\"`\n- `shiki plan ingest --plan-file PLAN.json`\n- `shiki run --plan P-0001`\n- `shiki daemon enqueue-plan --plan-file PLAN.json`\n- `shiki daemon run --once`\n- `shiki runner next`\n- `shiki runner execute --task-id T-0001 --command \"...\"`\n- `shiki runner claude --task-id T-0001`\n- `shiki runner codex --task-id T-0001`\n- `shiki loop step --goal-id G-0001`\n- `shiki loop run --goal-id G-0001`\n- `shiki smoke live --plan-file PLAN.json --dry-run`\n- `shiki smoke live --plan-file PLAN.json --execute-github`\n- `shiki smoke live --plan-file PLAN.json --execute-github --push-branch`\n- `shiki goal create --title ... --outcome ...`\n- `shiki issue plan --goal-id G-0001 --title ... --scope ... --acceptance-check ...`\n- `shiki lock acquire T-0001`\n- `shiki dispatch check T-0001`\n- `shiki worktree allocate T-0001`\n- `shiki github issue --task-id T-0001`\n- `shiki github pr --task-id T-0001`\n- `shiki handoff task T-0001`\n- `shiki handoff repair RP-0001`\n- `shiki repair packet --task-id T-0001 --pr 123 --minimal-change ... --verification-command ...`\n- `shiki task status T-0001 --status done`\n- `shiki goal complete G-0001`\n- `shiki status`\n---\nname: tdd\ndescription: Test-driven development with red-green-refactor loop. Use when user wants to build features or fix bugs using TDD, mentions \"red-green-refactor\", wants integration tests, or asks for test-first development.\n---\n\n# Test-Driven Development\n\n## Philosophy\n\n**Core principle**: Tests should verify behavior through public interfaces, not implementation details. Code can change entirely; tests shouldn't.\n\n**Good tests** are integration-style: they exercise real code paths through public APIs. They describe _what_ the system does, not _how_ it does it. A good test reads like a specification - \"user can checkout with valid cart\" tells you exactly what capability exists. These tests survive refactors because they don't care about internal structure.\n\n**Bad tests** are coupled to implementation. They mock internal collaborators, test private methods, or verify through external means (like querying a database directly instead of using the interface). The warning sign: your test breaks when you refactor, but behavior hasn't changed. If you rename an internal function and tests fail, those tests were testing implementation, not behavior.\n\nSee [tests.md](tests.md) for examples and [mocking.md](mocking.md) for mocking guidelines.\n\n## Anti-Pattern: Horizontal Slices\n\n**DO NOT write all tests first, then all implementation.** This is \"horizontal slicing\" - treating RED as \"write all tests\" and GREEN as \"write all code.\"\n\nThis produces **crap tests**:\n\n- Tests written in bulk test _imagined_ behavior, not _actual_ behavior\n- You end up testing the _shape_ of things (data structures, function signatures) rather than user-facing behavior\n- Tests become insensitive to real changes - they pass when behavior breaks, fail when behavior is fine\n- You outrun your headlights, committing to test structure before understanding the implementation\n\n**Correct approach**: Vertical slices via tracer bullets. One test \u2192 one implementation \u2192 repeat. Each test responds to what you learned from the previous cycle. Because you just wrote the code, you know exactly what behavior matters and how to verify it.\n\n```\nWRONG (horizontal):\n  RED:   test1, test2, test3, test4, test5\n  GREEN: impl1, impl2, impl3, impl4, impl5\n\nRIGHT (vertical):\n  RED\u2192GREEN: test1\u2192impl1\n  RED\u2192GREEN: test2\u2192impl2\n  RED\u2192GREEN: test3\u2192impl3\n  ...\n```\n\n## Workflow\n\n### 1. Planning\n\nWhen exploring the codebase, use the project's domain glossary so that test names and interface vocabulary match the project's language, and respect ADRs in the area you're touching.\n\nBefore writing any code:\n\n- [ ] Confirm with user what interface changes are needed\n- [ ] Confirm with user which behaviors to test (prioritize)\n- [ ] Identify opportunities for [deep modules](deep-modules.md) (small interface, deep implementation)\n- [ ] Design interfaces for [testability](interface-design.md)\n- [ ] List the behaviors to test (not implementation steps)\n- [ ] Get user approval on the plan\n\nAsk: \"What should the public interface look like? Which behaviors are most important to test?\"\n\n**You can't test everything.** Confirm with the user exactly which behaviors matter most. Focus testing effort on critical paths and complex logic, not every possible edge case.\n\n### 2. Tracer Bullet\n\nWrite ONE test that confirms ONE thing about the system:\n\n```\nRED:   Write test for first behavior \u2192 test fails\nGREEN: Write minimal code to pass \u2192 test passes\n```\n\nThis is your tracer bullet - proves the path works end-to-end.\n\n### 3. Incremental Loop\n\nFor each remaining behavior:\n\n```\nRED:   Write next test \u2192 fails\nGREEN: Minimal code to pass \u2192 passes\n```\n\nRules:\n\n- One test at a time\n- Only enough code to pass current test\n- Don't anticipate future tests\n- Keep tests focused on observable behavior\n\n### 4. Refactor\n\nAfter all tests pass, look for [refactor candidates](refactoring.md):\n\n- [ ] Extract duplication\n- [ ] Deepen modules (move complexity behind simple interfaces)\n- [ ] Apply SOLID principles where natural\n- [ ] Consider what new code reveals about existing code\n- [ ] Run tests after each refactor step\n\n**Never refactor while RED.** Get to GREEN first.\n\n## Checklist Per Cycle\n\n```\n[ ] Test describes behavior, not implementation\n[ ] Test uses public interface only\n[ ] Test would survive internal refactor\n[ ] Code is minimal for this test\n[ ] No speculative features added\n```\nsed: /Users/kio.mizutani/skills/skills/engineering/code-review/SKILL.md: No such file or directory\n\ncodex\nThe Shiki and TDD skill instructions are loaded. The required `code-review` skill is not present at the listed path or in the available-skill catalog, so I\u2019m locating the repository-configured copy before proceeding; if none exists, I\u2019ll record that Skill Gate limitation and perform the equivalent review gate manually.\nexec\n/bin/zsh -lc \"rg -n -i 'SADR-0017|reflux|fork guard|mergeBaseOid|bookkeeping closeout' /Users/kio.mizutani/.codex/memories/MEMORY.md\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n1:# Task Group: `CubeShare` / `shiki` upgrade recovery, CCA migration repair, and reflux planning\n26:## Task 3: Recover Actions capacity, independently review PR #331, and plan CubeShare/shiki reflux\n30:- extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md (cwd=/Users/kio.mizutani/CubeShare, /Users/kio.mizutani/shiki, rollout_path=extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md, updated_at=2026-08-17T00:00:00+00:00, thread_id=None, observed six-hour recovery/reflux arc) [skysight memory]\n34:- GitHub Pro, CubeShare PR #331, `L-20260810T103936853971Z-5282aeaf`, `ledger_evidence`, reflux-sweep, P0, P1, P2, P3, C1, C2, Shiki PR #330, `G-20260817T040326892610Z-af6effea`, `T-20260817T040326894136Z-0d640560`\n36:## Task 4: Keep public-repository fork guards and workflow exposure decisions explicit\n62:- when the original Shiki update expanded into many PRs, fixes, time, and cost, the user asked to step back and organize the situation -> prefer a concrete divergence/reflux plan over continuing local patch accumulation. [Task 3] [skysight memory]\n71:- After an upgrade becomes a fork, use a three-way reflux audit: compare CubeShare platform state with shiki main, classify upstream candidates versus CubeShare-only work, upstream selected fixes, then normalize/reinstall/verify downstream. Do not upstream a relaxation that weakens public platform gates. [Task 3] [skysight memory]\n75:- For Shiki P1, port a repaired fork guard: `guardian_approval_signal.py:336` had the intended `isCrossRepository` pattern, but `mergegate_check.py:3600`'s `bookkeeping_closeout and pr and ...` short-circuits when `pr` is absent. [Task 4] [skysight memory]\n\nexec\n/bin/zsh -lc \"rg --files /Users/kio.mizutani/skills /Users/kio.mizutani/.codex/skills /Users/kio.mizutani/.agents/skills 2>/dev/null | rg '(\"'^|/)code-review/SKILL'\"\\\\.md\"'$|code.review.*SKILL'\"\\\\.md\"'$'\"'\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n exited 1 in 0ms:\nexec\n/bin/zsh -lc \"rg -n 'T-20260817T063758155247Z-0b11207b|G-20260817T063758153709Z-0302813a' .shiki 2>/dev/null | head -80\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n.shiki/tasks/T-20260817T063758155247Z-0b11207b.json:22:  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n.shiki/tasks/T-20260817T063758155247Z-0b11207b.json:23:  \"id\": \"T-20260817T063758155247Z-0b11207b\",\n.shiki/locks/T-20260817T063758155247Z-0b11207b.json:3:  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n.shiki/locks/T-20260817T063758155247Z-0b11207b.json:27:  \"task_id\": \"T-20260817T063758155247Z-0b11207b\"\n.shiki/goals/G-20260817T063758153709Z-0302813a.json:35:  \"id\": \"G-20260817T063758153709Z-0302813a\",\n.shiki/dag/G-20260817T063758153709Z-0302813a.json:3:  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n.shiki/dag/G-20260817T063758153709Z-0302813a.json:5:    \"T-20260817T063758155247Z-0b11207b\"\n.shiki/ledger/L-20260817T063758154615Z-5322dcab.json:4:    \".shiki/goals/G-20260817T063758153709Z-0302813a.json\",\n.shiki/ledger/L-20260817T063758154615Z-5322dcab.json:7:  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n.shiki/ledger/L-20260817T063758155305Z-197baec8.json:4:    \".shiki/tasks/T-20260817T063758155247Z-0b11207b.json\"\n.shiki/ledger/L-20260817T063758155305Z-197baec8.json:6:  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n.shiki/ledger/L-20260817T063758155305Z-197baec8.json:10:  \"task_id\": \"T-20260817T063758155247Z-0b11207b\",\n\nexec\n/bin/zsh -lc 'git rev-parse --show-toplevel && git status --short && codex --version && codex login status && python3 --version && node --version && jq --version' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 138ms:\nLogged in using ChatGPT\nPython 3.14.6\nv26.5.0\njq-1.8.1\n\ncodex\nPreflight confirms the assigned Codex runtime and task/lock records exist. The worktree already contains uncommitted changes in nearly all locked implementation files plus Shiki-owned task/lock metadata; I\u2019m treating those as pre-existing state, preserving the metadata, and auditing the patch before adding or correcting anything. No `code-review` skill file was found, so the final pre-PR review will use the repository\u2019s documented review/checklist surfaces directly.\n  \u2192 Audit pre-existing patch, task contract, source decisions, and locked-file boundaries\n  \u2022 Run focused tests to establish RED failures, then implement each vertical behavior slice within locks\n  \u2022 Run targeted and full acceptance suites, inspect immutable/fork/closeout invariants\n  \u2022 Perform the required code-review gate manually and report durable evidence/blockers\n2026-08-17T10:07:47.468718Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1,100p' /Users/kio.mizutani/.codex/memories/MEMORY.md && sed -n '1,130p' /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n# Task Group: `CubeShare` / `shiki` upgrade recovery, CCA migration repair, and reflux planning\n\nscope: Separate a downstream Shiki upgrade from product changes, repair CCA migration context fail-closed, and classify CubeShare/shiki divergence before upstreaming.\napplies_to: cwd=/Users/kio.mizutani/CubeShare, /Users/kio.mizutani/shiki; reuse_rule=re-query exact PR heads, billing, auth, task/ledger mirrors, locks, and repository visibility; observed UI state is not current-state proof\n\n## Task 1: Audit and isolate CubeShare Shiki upgrade changes\n\n### rollout_summary_files\n\n- rollout_summaries/2026-08-07T06-52-32-ThXB-cubeshare_shiki_upgrade_and_cca_migration_repair.md (cwd=/Users/kio.mizutani/CubeShare, rollout_path=/Users/kio.mizutani/.codex/sessions/2026/08/07/rollout-2026-08-07T15-52-32-019fdafe-b027-7560-b635-dbf87a53c99b.jsonl, updated_at=2026-08-10T10:45:29+00:00, thread_id=019fdafe-b027-7560-b635-dbf87a53c99b, partial; local fixes verified but GitHub governance/CI incomplete)\n\n### keywords\n\n- Shiki upgrade, `.new`, `.gitignore`, `shiki doctor`, unregistered worktrees, `git add scripts/`, Approval Records, Contract PR, locks\n\n## Task 2: Repair delivery-only CCA migration context without weakening gates\n\n### rollout_summary_files\n\n- rollout_summaries/2026-08-07T06-52-32-ThXB-cubeshare_shiki_upgrade_and_cca_migration_repair.md (cwd=/Users/kio.mizutani/CubeShare, rollout_path=/Users/kio.mizutani/.codex/sessions/2026/08/07/rollout-2026-08-07T15-52-32-019fdafe-b027-7560-b635-dbf87a53c99b.jsonl, updated_at=2026-08-10T10:45:29+00:00, thread_id=019fdafe-b027-7560-b635-dbf87a53c99b, PR #331 blocked externally after bounded repair)\n\n### keywords\n\n- `migration_context`, delivery_only, `SHIKI_TRUSTED_BASE_REPOSITORY`, `SHIKI_TRUSTED_BASE_REVISION`, `delivery-only migration compatibility requires an immutable trusted base`, PR #331, `ledger_evidence`, GitHub Actions billing\n\n## Task 3: Recover Actions capacity, independently review PR #331, and plan CubeShare/shiki reflux\n\n### rollout_summary_files\n\n- extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md (cwd=/Users/kio.mizutani/CubeShare, /Users/kio.mizutani/shiki, rollout_path=extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md, updated_at=2026-08-17T00:00:00+00:00, thread_id=None, observed six-hour recovery/reflux arc) [skysight memory]\n\n### keywords\n\n- GitHub Pro, CubeShare PR #331, `L-20260810T103936853971Z-5282aeaf`, `ledger_evidence`, reflux-sweep, P0, P1, P2, P3, C1, C2, Shiki PR #330, `G-20260817T040326892610Z-af6effea`, `T-20260817T040326894136Z-0d640560`\n\n## Task 4: Keep public-repository fork guards and workflow exposure decisions explicit\n\n### rollout_summary_files\n\n- extensions/skysight/resources/2026-08-17T06-30-00-IeOJ-10min-memory-summary.md (cwd=/Users/kio.mizutani/CubeShare, /Users/kio.mizutani/shiki, rollout_path=extensions/skysight/resources/2026-08-17T06-30-00-IeOJ-10min-memory-summary.md, updated_at=2026-08-17T06:30:00+00:00, thread_id=None, observed exposure/P1 planning; owner decisions remain) [skysight memory]\n\n### keywords\n\n- `.github/workflows/claude.yml`, `issue_comment`, `pull_request_review_comment`, `WORKFLOW_CONTRACTS`, `isCrossRepository`, `guardian_approval_signal.py:336`, `mergegate_check.py:3600`, `and pr`, `exact_head_only`, `approvers.users`\n\n## Task 5: Observe Guardian/CCA planning and runner-cost boundaries without treating UI evidence as execution proof\n\n### rollout_summary_files\n\n- extensions/chronicle/resources/2026-08-12T02-03-00-BAsI-10min-memory-summary.md (cwd=claude/guardian-approval-planning-13e494, rollout_path=extensions/chronicle/resources/2026-08-12T02-03-00-BAsI-10min-memory-summary.md, updated_at=2026-08-12T02:03:00+00:00, thread_id=None, observed CCA-06/rerun cost investigation) [chronicle memory]\n- extensions/chronicle/resources/2026-08-12T04-44-00-gRPU-10min-memory-summary.md (cwd=/Users/kio.mizutani/shiki, rollout_path=extensions/chronicle/resources/2026-08-12T04-44-00-gRPU-10min-memory-summary.md, updated_at=2026-08-12T04:44:00+00:00, thread_id=None, observed SADR/ADR and PR #331 planning) [chronicle memory]\n- extensions/chronicle/resources/2026-08-12T08-25-00-elmG-10min-memory-summary.md (cwd=/Users/kio.mizutani/shiki, rollout_path=extensions/chronicle/resources/2026-08-12T08-25-00-elmG-10min-memory-summary.md, updated_at=2026-08-12T08:25:00+00:00, thread_id=None, observed framework-alignment research) [chronicle memory]\n\n### keywords\n\n- `Guardian\u627f\u8a8d\u306e\u4e8b\u524d\u53d6\u5f97\u8a08\u753b`, CCA-06, `rerun_cca`, `types: [labeled]`, PR #328, PR #331, Issue #322, SADR, `docs/research/2026-08-07-framework-alignment-and-evolution.md`, GitHub Actions minutes\n\n## User preferences\n\n- when the Shiki upgrade mixed with product changes, the user required upgrade and product changes to be separated into distinct commits -> classify files before staging; never use broad staging in a mixed worktree. [Task 1]\n- when workflow reruns were considered after billing recovery, the user clarified they should happen in the pre-fork Codex chat -> preserve task/thread ownership and do not rerun from an unrelated forked context. [Task 3] [skysight memory]\n- when the original Shiki update expanded into many PRs, fixes, time, and cost, the user asked to step back and organize the situation -> prefer a concrete divergence/reflux plan over continuing local patch accumulation. [Task 3] [skysight memory]\n\n## Reusable knowledge\n\n- `shiki doctor` can pass local contract validation yet remain non-green because of unregistered worktrees or invalid GitHub/Claude auth. Treat doctor, local tests, GitHub state, mirror state, and CI as separate evidence surfaces. `shiki status` describes the platform repo/config, not automatically the target repo. [Task 1]\n- Existing-target upgrades can overwrite `.gitignore`; compare installed and target exclusions. CubeShare required the union retaining `node_modules`, `dist`, `.cloud-run-url`, credential-file names, Shiki session state, and `.ai/logs`; all eight ignore probes then passed. [Task 1]\n- CCA first/retry should consume one workflow-owned structured `migration_context`; only `verified=true`, `mode=delivery_only` migrations bypass task-local lifecycle/skill/PR-ledger bytes. Normal or unverified work retains the ordinary blockers. [Task 2]\n- Generated temporary targets need CI-equivalent immutable trusted-base variables. With `SHIKI_TRUSTED_BASE_REPOSITORY` and `SHIKI_TRUSTED_BASE_REVISION`, the unique failing test and the full 1,165-test suite passed; without them, failure was expected rather than a source defect. [Task 2]\n- Ledger repair is not finished until the current Task mirror retains the Ledger ID after synchronization. PR #331 evidence showed that a worktree-side registration could be dropped later; independently review all blocking check results rather than calling it a single blocker. [Task 2] [Task 3] [skysight memory]\n- After an upgrade becomes a fork, use a three-way reflux audit: compare CubeShare platform state with shiki main, classify upstream candidates versus CubeShare-only work, upstream selected fixes, then normalize/reinstall/verify downstream. Do not upstream a relaxation that weakens public platform gates. [Task 3] [skysight memory]\n- P0 CCA rerun economy scope was workflow concurrency, cancelled/skipped/stale check bucketing, label-trigger filtering, and delaying CCA judgment until required checks settle. Its Contract PR #330 was observed merged after 5/5 checks; later implementation/closeout results were still being monitored. [Task 3] [skysight memory]\n- Observed Aug 12 planning separated GitHub Actions runner availability from local/Claude judgment and gate metadata. Inspect CCA-06 races, `rerun_cca`, and label-trigger behavior before proposing more reruns; observed UI/billing data is not current-state proof. [Task 5] [chronicle memory]\n- Before a public-visibility change, treat `.github/workflows/claude.yml` as a concrete exposure item: observed `issue_comment`/`pull_request_review_comment` triggers, broad write-style permissions, and comment-body-only gating need repair and validator coverage via `WORKFLOW_CONTRACTS`. The observed low exposure depended on the repository remaining private. [Task 4] [skysight memory]\n- For Shiki P1, port a repaired fork guard: `guardian_approval_signal.py:336` had the intended `isCrossRepository` pattern, but `mergegate_check.py:3600`'s `bookkeeping_closeout and pr and ...` short-circuits when `pr` is absent. [Task 4] [skysight memory]\n\n## Failures and how to do differently\n\n- Symptom: broad `git add scripts/` in a dirty upgrade checkout. Cause: unrelated untracked product-operation scripts. Fix: stage only the classified, declared upgrade paths; do not commit `.new` candidates. [Task 1]\n- Symptom: a generated-target test says `delivery-only migration compatibility requires an immutable trusted base`. Cause: missing trusted-base environment, even on clean `origin/main`. Fix: supply the exact CI-equivalent variables before diagnosing source code. [Task 2]\n- Symptom: all GitHub jobs fail before step 0 with billing/spending-limit output. Cause: external Actions capacity, not implementation. Fix: report the exact platform blocker, restore capacity with the user, then consume a new rerun/repair attempt. [Task 2]\n- Symptom: `shiki run` is executed before `shiki contract open`. Cause: sequencing error creates stale goal/task IDs. Fix: open/validate the contract first and move aside stale IDs before dispatch. [Task 3] [skysight memory]\n- Symptom: no amount of extra locks unblocks a cross-task mirror rewrite. Cause: the observed rule did not inspect locks. Fix: classify the independent rule/blocker rather than widening locks. [Task 3] [skysight memory]\n\n# Task Group: `CubeShare` DWD receipt PDF extraction, classification, and CCA-only PR handling\n\nscope: Representative-mailbox receipt extraction/classification into Drive/Firestore, plus the narrowly separate PR #330 CCA rerun boundary.\napplies_to: cwd=/Users/kio.mizutani/CubeShare; reuse_rule=reuse code paths and scope boundaries, but re-check target mailbox delegation, Drive folders, PR heads, billing, and current workflow state before acting\n\n## Task 1: Plan DWD receipt backfill for the representative mailbox\n\n### rollout_summary_files\n\n- rollout_summaries/2026-08-10T06-16-26-DDix-cubeshare_dwd_receipt_backfill_and_cca_scope_correction.md (cwd=/Users/kio.mizutani/CubeShare, rollout_path=/Users/kio.mizutani/.codex/sessions/2026/08/10/rollout-2026-08-10T15-16-26-019fea50-b770-77b0-9a63-36fe3b928e11.jsonl, updated_at=2026-08-12T00:33:22+00:00, thread_id=019fea50-b770-77b0-9a63-36fe3b928e11, partial; implementation not completed)\n- extensions/chronicle/resources/2026-08-12T00-27-00-ybjn-10min-memory-summary.md (cwd=/Users/kio.mizutani/CubeShare, rollout_path=extensions/chronicle/resources/2026-08-12T00-27-00-ybjn-10min-memory-summary.md, updated_at=2026-08-12T00:27:00+00:00, thread_id=None, observed dedicated Cloud Run Job/Scheduler design) [chronicle memory]\n\n### keywords\n\n- DWD, `DWD_SERVICE_ACCOUNT_KEY`, `subject`, `k.shimamura@cube-group.co.jp`, `getAuthClient`, `ACCESS_TOKEN_SCOPE_INSUFFICIENT`, VISA, AMEX, `VendorInvoiceEngine`, \u9818\u53ce\u66f8\u5206\u985e\n\n---\ntitle: Shiki and CubeShare Recovery\ndescription: You moved from GitHub billing recovery into a long Shiki/CubeShare governance and PR-repair workflow. You upgraded GitHub capacity, verified CubeShare PR state, planned Shiki reflux work, merged the Shiki P0 contract PR, dispatched implementation, and monitored follow-on PR checks.\napplications: [com.google.Chrome, com.openai.codex, com.apple.systempreferences, com.apple.dock, com.microsoft.VSCode, com.anthropic.claudefordesktop, jp.kiok.nani, com.apple.controlcenter]\nsuggestion:\n  type: skill\n  name: Shiki divergence audit\n  description: Turn my workflow for comparing a forked project against shiki main and classifying reflux candidates into a reusable audit skill.\n---\n\n## Memory summary\n\nThe user spent the six-hour window on a connected Shiki/CubeShare recovery and hardening arc. The work began with GitHub billing and plan investigation to restore Actions capacity for `mizutani-140/CubeShare`, then moved through PR `#331` validation, independent Claude review of Codex\u2019s completion report, and a broader reset of how CubeShare\u2019s Shiki upgrade had become a fork/reflux problem. By the end of the window, the user had merged `mizutani-140/shiki` PR `#330`, dispatched the follow-on P0 implementation task for CCA re-run economy, monitored Shiki PR `#331` and closeout PR `#332`, and kept CubeShare PRs `#326`, `#330`, and `#331` as adjacent unresolved work.\n\nA major decision in the middle of the window was to stop treating CubeShare\u2019s current state as a simple local repair and instead classify CubeShare/shiki divergence before upstreaming selected fixes into shiki main. Claude\u2019s `reflux-sweep` produced a five-task structure split across shiki (`P1`, `P2`, `P3`) and CubeShare (`C1`, `C2`), with a new `P0` added first for CCA rerun behavior. Later work showed P0\u2019s contract PR `#330` pass checks and merge, after which the implementation loop created or monitored Shiki PR `#331` and its closeout PR `#332`.\n\n### Relevant prior context\n\nNo relevant prior context predating this six-hour window is established from the supplied child summaries beyond the fact that the Shiki/CubeShare work was already in progress at the start. The earliest captured summary begins with GitHub billing/pricing checks and Claude Desktop Shiki setup/research rather than a settled prior task state.\n\n### Important non-obvious context about the user\n\n- `mizutani-140/CubeShare`: main downstream repository involved in the Shiki upgrade, PR check recovery, and fork/reflux planning.\n- `mizutani-140/shiki`: upstream/platform repository where the user shifted P0/P1/P2/P3 hardening work.\n- `GitHub Pro`: the user upgraded the `mizutani-140` account to restore Actions/check capacity after plan or billing limits blocked PR validation.\n- CubeShare PR `#331`: repeatedly reviewed as `T-20260810T093132083502Z-10598506: CCA delivery-only migration context repair`; remained blocked/unstable around ledger-evidence and operator-decision issues.\n- CubeShare PR `#326`: mirror-migration-related PR; Claude later reported head `d06cdd9c` passed Shiki validation while base `772b2cbb` failed on `ledger.schema.json`.\n- CubeShare PR `#330`: open spec amendment PR titled `spec(shiki): add workflow invocation fixture amendment`, from `codex/g321-workflow-invocation-fixture-amendment`.\n- Shiki PR `#330`: contract PR `contract: register CCA re-run economy goal and task contract`; merged during the window after `5 / 5 checks OK`.\n- Shiki PR `#331`: implementation PR for the P0 CCA re-run economy work; checks were being monitored after the contract PR merge.\n- Shiki PR `#332`: closeout PR being polled near the end of the window, with closeout head `20afe0bb7e6d`.\n- `G-20260817T040326892610Z-af6effea` / `T-20260817T040326894136Z-0d640560`: active Shiki goal/task identifiers for the P0 work.\n- `/Users/kio.mizutani/shiki`: local shiki checkout used in visible Claude commands and polling.\n- `/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main`: coordinator worktree used for shiki loop/contract workflow.\n- `.claude/worktrees/.worktrees/shiki-t-20260817t040326894136z-0d640560-cancel-s`: worktree path shown for dispatched Codex implementation.\n- `scripts/shiki_loop.py`, `tests/test_workflow_invocation.py`, `tests/test_loop_engine.py`: core files in the planned P0 scope.\n- `Post Guardian blockers comment`: workflow step under review because its condition changed from `if: always()` to `if: failure()`.\n- `SADR-0017`: visible in later closeout discussion as a bookkeeping closeout exemption whose fork guard gap became a priority question.\n- `isCrossRepository`: CubeShare had a fork guard around Guardian approval logic that shiki main lacked in the observed comparison.\n- `Claude Desktop`, `Codex`, and `Chrome`: the user coordinated implementation and review through Claude/Codex while verifying GitHub PR/check state in Chrome.\n\n## Recording summary\n\n### Billing recovery and PR #331 validation\n\nThe window began with the user checking GitHub billing, Copilot, GitHub pricing, Enterprise trial, Team, Pro, and Actions billing information. They used Codex to ask whether a GitHub Enterprise trial, Team, Copilot Pro, or GitHub Pro would unblock Actions for `mizutani-140/CubeShare`. The investigation narrowed to GitHub Pro for the personal account rather than Enterprise trial or Copilot Pro.\n\nThe user completed a GitHub Free-to-Pro upgrade flow in Chrome for the `mizutani-140` personal account. Afterward, Codex-visible state indicated that plan restrictions had changed: a previous branch-protection API result changed from a plan-limited `403` style to an unset `404` style, PR `#331` remained open at head `926563c...`, and previous checks still reflected billing-stop failures. The user clarified that workflow reruns should occur in the pre-fork Codex chat, not the current forked chat.\n\nCodex later showed PR `#331` checks progressing on exact head `926563c05262235cb7099cadffbdc0d448b5a776` against base/current main `72c96e5995610e91fce253ed62659568597e0e76`. CI, Claude review, and Validate Shiki mirror succeeded, while MergeGate metadata failed, CCA returned `repair_required`, MergeGate policy skipped, and the CCA Review Bridge skipped. The core ledger issue was `L-20260810T103936853971Z-5282aeaf` appearing in the PR diff but not in task `T-20260810T093132083502Z-10598506` `ledger_evidence`.\n\n### Independent Claude review of Codex\u2019s report\n\nThe user pasted Codex\u2019s PR `#331` completion report into Claude Desktop and asked Claude to scrutinize it. Claude ran a multi-agent `verify-331` workflow with Measure, Refute, and Synthesize phases. The verification focused on whether Codex\u2019s \u201csingle real blocker\u201d framing was accurate and whether the ledger-evidence repair was technically and procedurally legitimate.\n\nClaude\u2019s synthesis corrected the earlier framing. It reported multiple blocking non-pass results rather than one, identified `PR-10` as missing from the repair packet, and noted that previous manual worktree-side registration of `5282aeaf` had already been removed by a later goal-loop-related commit. It also distinguished MergeGate metadata failure from the skipped policy check and observed that GitHub branch protection was not enforcing MergeGate at that point.\n\nThe user then asked Claude to step back and organize the situation because the original goal had been only to update Shiki in CubeShare, but the process had expanded into many PRs, fixes, time, and cost. Claude\u2019s reset analysis said the original upgrade PR `#293` had been merged earlier, current CubeShare passed under its local forked validator, but CubeShare had effectively become a Shiki fork carrying platform fixes absent from upstream.\n\n### CubeShare/shiki reflux planning\n\nThe user pushed Claude toward choosing a concrete plan for Codex implementation. Claude reframed the problem as a reflux or three-way merge audit: compare CubeShare\u2019s local Shiki platform state against shiki main, identify which divergences should upstream into shiki, normalize CubeShare afterward, then reinstall/verify.\n\nClaude started a `reflux-sweep` workflow with six classification agents: `map:mergegate`, `map:validate`, `map:migrations`, `map:loop-and-friends`, `map:cubeshare-only`, and `map:declarative`. The workflow progressed through Classify, Refute, and Synthesize. The synthesis corrected earlier assumptions about when divergence was introduced, highlighted a Guardian approval gap involving `guardian_approval_signal.py:336` and `isCrossRepository`, and warned against upstreaming relaxations that would weaken platform gates.\n\nThe resulting structure separated shiki-side tasks `P1`, `P2`, `P3` from CubeShare-side tasks `C1`, `C2`. The user approved addressing the CCA rerun problem first, creating a new P0 before the rest of the DAG. Spec Amendment and migration-mode choices remained unresolved and were treated as later blockers for CubeShare work rather than blockers for P0/P1/P2/P3/C1.\n\n### Shiki P0 contract PR\n\nClaude diagnosed P0 as a shiki-side CCA rerun economy fix. The visible scope included duplicate/rerun behavior from label-triggered CCA runs, required checks still pending when CCA evaluates CCA-06, workflow concurrency, and `scripts/shiki_loop.py` bucketing of cancelled/skipped/stale check states. The planned implementation files included `scripts/shiki_loop.py`, `tests/test_workflow_invocation.py`, and `tests/test_loop_engine.py`.\n\nThe user approved the P0 scope. Claude created and validated `P-CCARUN-draft.json`, selected `SADR-0011-autonomous-quality-gate-evidence.md` as the PRD reference, and prepared a handoff. Claude then opened shiki Contract PR `#330` on branch `shiki/contract-g-20260817t040326892610z-af6effea`. A sequencing issue occurred first: `shiki run` had been executed before `shiki contract open`, creating old IDs that Claude moved aside; the active IDs became `G-20260817T040326892610Z-af6effea` and `T-20260817T040326894136Z-0d640560`.\n\nThe user reviewed PR `#330` in Chrome. It initially had failing Shiki MergeGate and Shiki CCA Completion checks, tied to an inbox draft included in the contract commit. A corrected head `ec4ada6` later reran checks successfully. GitHub Actions then showed newer successful Shiki Claude Review, Shiki CCA Completion, Shiki MergeGate, and Shiki Validate runs. The PR list showed `5 / 5 checks OK`.\n\n### Shiki P0 implementation dispatch and PR monitoring\n\nThe user merged Shiki PR `#330` after checks were green. Claude recorded the merge at commit `c1e55ef88471`, synced the coordinator to `origin/main`, acquired 9 locks, allocated the P0 worktree, confirmed `dispatchable: true`, and started a background Codex run for `T-20260817T040326894136Z-0d640560`.\n\nThe dispatched work covered four implementation points: workflow concurrency/cancel-in-progress, `_check_bucket` handling for cancelled/skipped/stale states, label-trigger filtering while preserving `guardian:approved` and mergegate labels, and starting CCA judging after required checks were finalized. Claude later reported review and validation green: `unittest` ran `1154` tests with `OK`, plus validate, actionlint, and goal-loop checks passed. It also corrected an earlier verification caveat about pipeline exit status.\n\nA design-sensitive change surfaced during review: `Post Guardian blockers comment` changed from `if: always()` to `if: failure()`. Claude described this as useful for avoiding stale comments on cancelled or superseded runs under `cancel-in-progress`, while flagging that it came from broad acceptance wording and needed operator visibility.\n\nClaude then monitored Shiki PR `#331`, polling checks for commit `9965b6c7`. Visible state showed MergeGate metadata and Claude review successful, while Validate Shiki mirror and CCA verdict were still in progress. Claude interpreted the in-progress CCA verdict as likely evidence of the new settle-gate behavior waiting for required checks rather than failing early on pending checks, though the final result was not yet visible in that segment.\n\nNear the end, Claude showed the prior Shiki PR had reached merge verification and moved into a closeout PR/check-polling flow. Shiki PR `#332` was being polled from `/Users/kio.mizutani/shiki`, with closeout head `20afe0bb7e6d`. The closeout discussion highlighted `SADR-0017` bookkeeping closeout exemption behavior and a fork-guard gap in shiki main.\n\n### CubeShare dependency review and remaining blockers\n\nIn parallel with Shiki P0 work, Claude ran or monitored a `cubeshare-dag` workflow to measure Task `#298`, PR `#326`, and PR `#331`. It moved from Measure to Refute, with a notable conclusion recorded in `memory/mirror-rewrite-blocked-independently-of-locks.md`: cross-task mirror rewrites were blocked by a rule that did not inspect locks, so widening locks would not solve that issue.\n\nClaude later reported that CubeShare PR `#326` head `d06cdd9c` passed Shiki validation with exit `0`, while base `772b2cbb` failed on a `ledger.schema.json` enum mismatch. It described PR `#326` as mirror migration complete and metadata green, but the CubeShare plan was more complex than a simple three-node DAG. PR `#331` was treated as a stopped state rather than a normal dispatchable implementation task because some required changes needed operator action or limited authorization.\n\nThe user inspected the CubeShare pull requests list in Chrome. Six open PRs were visible, including `#331`, `#330`, `#328`, `#326`, `#140`, and `#139`. The user opened CubeShare PR `#330`, titled `spec(shiki): add workflow invocation fixture amendment`, which was open and able to merge from `codex/g321-workflow-invocation-fixture-amendment`, with labels `guardian:approved` and `mergegate:spec_amendment`.\n\n## Citations\n\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T00-10-00-TXyz-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T00-50-00-iJWc-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T01-10-00-eWXl-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T01-40-00-viXA-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T02-20-00-xOxT-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T03-00-00-DopM-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T04-00-00-alds-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T04-30-00-Teyl-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T05-10-00-XJOa-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T05-40-00-Tvha-10min-memory-summary.md\n- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T05-50-00-XaeK-10min-memory-summary.md\nexec\n/bin/zsh -lc 'git diff -- scripts/mergegate_check.py scripts/guardian_approval_signal.py scripts/shiki_tasks.py .github/workflows/shiki-mergegate.yml .github/workflows/shiki-cca-completion.yml' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 57081e3..04719fe 100644\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -52,13 +52,21 @@ jobs:\n           REPOSITORY: ${{ github.repository }}\n           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n         run: |\n+          set -euo pipefail\n           rm -rf .shiki/gha\n           mkdir -p .shiki/gha\n-          gh pr view \"$PR_NUMBER\" --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n+          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n+          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n           git fetch origin \"$base_ref\"\n-          git diff --name-only \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files.txt\n-          git diff --name-status \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files-status.txt\n+          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n+          printf '%s\\n' \"$merge_base\" > .shiki/gha/merge-base-oid.txt\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n+          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n+          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n \n           # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n           # mergegate job builds them below. The Guardian-approval signal must\n@@ -70,7 +78,7 @@ jobs:\n           # the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n           # Implementation-PR merge proof: resolve the task id with the SAME\n           # semantics mergegate_check.py uses (first T-<4+ digits> OR\n@@ -85,7 +93,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\n@@ -218,7 +226,7 @@ jobs:\n           while true; do\n             gh pr view \"$PR_NUMBER\" \\\n               --repo \"$REPOSITORY\" \\\n-              --json headRefOid,statusCheckRollup \\\n+              --json headRefOid,statusCheckRollup,isCrossRepository \\\n               > .shiki/gha/pr-settling.json\n \n             observed_head=\"$(jq -r '.headRefOid // \"\"' .shiki/gha/pr-settling.json)\"\n@@ -267,9 +275,13 @@ jobs:\n \n           # The judge must consume the settled rollup, not the snapshot gathered at\n           # the beginning of this concurrently-triggered workflow run.\n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n           gh pr view \"$PR_NUMBER\" \\\n             --repo \"$REPOSITORY\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n \n       - name: Prepare CCA transport schema\n         id: schema\n@@ -706,10 +718,14 @@ jobs:\n             exit 1\n           fi\n \n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n           gh pr view \"$PR_NUMBER\" \\\n             --repo \"$REPOSITORY\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n-            > .shiki/gha/pr.json\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n+            > .shiki/gha/pr-refresh.json\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr-refresh.json > .shiki/gha/pr.json\n+          rm .shiki/gha/pr-refresh.json\n \n           echo \"::endgroup::\"\n \n@@ -782,7 +798,7 @@ jobs:\n           mkdir -p .shiki/gha/base-shiki\n \n           gh pr view \"$PR_NUMBER\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n             > .shiki/gha/live-pr.json\n \n           gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/comments\" \\\n@@ -797,16 +813,21 @@ jobs:\n \n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/live-pr.json)\"\n           head_oid=\"$(jq -r '.headRefOid' .shiki/gha/live-pr.json)\"\n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n \n           git fetch origin \"$base_ref\"\n \n-          git diff --name-only \"origin/${base_ref}...HEAD\" \\\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/live-pr.json > .shiki/gha/live-pr-with-merge-base.json\n+          mv .shiki/gha/live-pr-with-merge-base.json .shiki/gha/live-pr.json\n+\n+          git diff --name-only \"${merge_base}...HEAD\" \\\n             > .shiki/gha/live-changed-files.txt\n \n-          git diff --name-status \"origin/${base_ref}...HEAD\" \\\n+          git diff --name-status \"${merge_base}...HEAD\" \\\n             > .shiki/gha/live-changed-files-status.txt\n \n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n           current_head=\"$(git rev-parse HEAD)\"\n           if [ \"$head_oid\" != \"$current_head\" ]; then\n@@ -849,7 +870,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\ndiff --git a/.github/workflows/shiki-mergegate.yml b/.github/workflows/shiki-mergegate.yml\nindex 9ee3d28..128e912 100644\n--- a/.github/workflows/shiki-mergegate.yml\n+++ b/.github/workflows/shiki-mergegate.yml\n@@ -49,13 +49,20 @@ jobs:\n           PR_NUMBER: ${{ github.event.pull_request.number }}\n           REPOSITORY: ${{ github.repository }}\n         run: |\n+          set -euo pipefail\n           rm -rf .shiki/gha\n           mkdir -p .shiki/gha\n-          gh pr view \"$PR_NUMBER\" --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n+          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n+          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n           git fetch origin \"$base_ref\"\n-          git diff --name-only \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files.txt\n-          git diff --name-status \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files-status.txt\n+          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n+          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n+          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n \n           # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n           # Guardian gate from the Goal's frozen-plan risk in THIS metadata job,\n@@ -69,7 +76,7 @@ jobs:\n           gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/events\" > .shiki/gha/live-guardian-events.json || echo \"[]\" > .shiki/gha/live-guardian-events.json\n           gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/timeline\" -H \"Accept: application/vnd.github+json\" > .shiki/gha/live-guardian-timeline.json || echo \"[]\" > .shiki/gha/live-guardian-timeline.json\n \n-          # Base .shiki snapshot from origin/<base_ref>, built the SAME way\n+          # Base .shiki snapshot from the PR's immutable merge base, built the SAME way\n           # shiki-cca-completion.yml builds it. mergegate_check.py needs it to\n           # verify task-field immutability for a post_merge_reconcile PR and to\n           # resolve the SADR-0017 bookkeeping-closeout exemption; without it a\n@@ -78,7 +85,7 @@ jobs:\n           # would go shallow.\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n       - name: MergeGate metadata check\n         env:\n@@ -103,7 +110,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex 1692489..c5236ac 100644\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -330,6 +330,10 @@ def main(argv: list[str] | None = None) -> int:\n         changed_files_status=args.changed_files_status,\n         merged_prs=args.merged_prs,\n     )\n+    # A fork PR is never eligible for the SADR-0017 exemption. The snapshot must\n+    # prove the negative; absent/null/anything-but-False fails closed.\n+    if exemption and pr.get(\"isCrossRepository\") is not False:\n+        exemption = False\n     # The risk that actually gates Guardian approval: \"low\" for a proven closeout,\n     # otherwise the real task risk. task_risk is still reported as risk_level for\n     # audit. PR labels may only ESCALATE the effective risk (a maintainer can force\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 3fecaa8..4cbc73b 100755\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -8,6 +8,7 @@ from dataclasses import dataclass\n import json\n import re\n import shutil\n+import stat\n import subprocess\n import sys\n import tempfile\n@@ -1279,29 +1280,26 @@ def declared_migration_ids(body: str) -> list[str]:\n     return [match.group(1) for match in _MIGRATION_ID_LINE.finditer(body)]\n \n \n-def _git_show_base(target: Path, base_ref: str, repo_path: str) -> str:\n-    \"\"\"Read ``repo_path`` from the base branch via a read-only ``git show``.\n-\n-    Tries ``origin/<base_ref>`` first \u2014 the MergeGate workflow fetches it with\n-    ``fetch-depth: 0`` (shiki-mergegate.yml) \u2014 then the bare ``<base_ref>`` as a\n-    fallback for a local base branch. Raises ``FileNotFoundError`` naming every\n-    ref it tried when neither resolves the path.\"\"\"\n-    errors: list[str] = []\n-    for ref in (f\"origin/{base_ref}\", base_ref):\n-        result = subprocess.run(\n-            [\"git\", \"show\", f\"{ref}:{repo_path}\"],\n-            cwd=str(target),\n-            capture_output=True,\n-            text=True,\n-        )\n-        if result.returncode == 0:\n-            return result.stdout\n-        errors.append(f\"{ref}: {result.stderr.strip() or 'not found'}\")\n-    raise FileNotFoundError(f\"could not read {repo_path} from the base branch ({'; '.join(errors)})\")\n+def _git_show_merge_base(target: Path, merge_base_oid: str, repo_path: str) -> str:\n+    \"\"\"Read ``repo_path`` from one immutable, workflow-resolved merge base.\"\"\"\n+    if not re.fullmatch(r\"[0-9a-fA-F]{40,64}\", merge_base_oid):\n+        raise FileNotFoundError(f\"invalid immutable mergeBaseOid {merge_base_oid!r}\")\n+    result = subprocess.run(\n+        [\"git\", \"show\", f\"{merge_base_oid}:{repo_path}\"],\n+        cwd=str(target),\n+        capture_output=True,\n+        text=True,\n+    )\n+    if result.returncode == 0:\n+        return result.stdout\n+    detail = result.stderr.strip() or \"not found\"\n+    raise FileNotFoundError(\n+        f\"could not read {repo_path} from immutable mergeBaseOid {merge_base_oid} ({detail})\"\n+    )\n \n \n-def resolve_base_migration_registry(target: Path, base_ref: str) -> tuple[Any, ...]:\n-    \"\"\"Resolve the migration registry from the BASE branch's\n+def resolve_base_migration_registry(target: Path, merge_base_oid: str) -> tuple[Any, ...]:\n+    \"\"\"Resolve the migration registry from the immutable merge base's\n     ``scripts/shiki_migrations.py`` \u2014 NEVER the PR head's.\n \n     THIS IS THE SECURITY PROPERTY OF MIGRATION MODE. Do NOT simplify this to import\n@@ -1320,7 +1318,7 @@ def resolve_base_migration_registry(target: Path, base_ref: str) -> tuple[Any, .\n     its only external dependency (``shiki_process``) resolves from ``scripts/`` on\n     ``sys.path``, infrastructure that carries no migration logic.\n     \"\"\"\n-    source = _git_show_base(target, base_ref, \"scripts/shiki_migrations.py\")\n+    source = _git_show_merge_base(target, merge_base_oid, \"scripts/shiki_migrations.py\")\n     # Execute the base source as a REAL module object registered in sys.modules:\n     # dataclass field resolution (Python 3.12+) looks the class's module up there,\n     # so a plain-dict namespace would fail on the module's ``@dataclass Migration``.\n@@ -1399,14 +1397,17 @@ def enforce_migration(\n     migration_id = ids[0]\n \n     if base_registry is None:\n-        base_ref = str(pr.get(\"baseRefName\") or \"\")\n-        if not base_ref:\n-            blocking.append(\"migration mode requires the PR baseRefName to resolve the base branch migration registry\")\n+        merge_base_oid = str(pr.get(\"mergeBaseOid\") or \"\")\n+        if not merge_base_oid:\n+            blocking.append(\n+                \"migration mode requires the workflow-resolved immutable mergeBaseOid \"\n+                \"to resolve the trusted migration registry\"\n+            )\n             return\n         try:\n-            base_registry = resolve_base_migration_registry(target, base_ref)\n+            base_registry = resolve_base_migration_registry(target, merge_base_oid)\n         except Exception as error:  # noqa: BLE001 - resolving base code can fail many ways; fail closed with a named reason, never crash the gate\n-            blocking.append(f\"migration mode could not resolve the base branch migration registry: {error}\")\n+            blocking.append(f\"migration mode could not resolve the immutable merge-base migration registry: {error}\")\n             return\n \n     by_id = {str(migration.id): migration for migration in base_registry}\n@@ -1976,6 +1977,28 @@ def _builtin_guardian_risk_required(risk_labels: list[str]) -> bool:\n _CLOSEOUT_MUTABLE_TASK_FIELDS = frozenset({\"status\", \"expected_pr\", \"closeout_pr\", \"ledger_evidence\", \"expected_branch\"})\n \n \n+def _path_proven_absent(path: Path) -> bool:\n+    \"\"\"Prove repository-path absence without following a dangling symlink.\"\"\"\n+    try:\n+        path.lstat()\n+    except FileNotFoundError:\n+        try:\n+            return stat.S_ISDIR(path.parent.lstat().st_mode)\n+        except OSError:\n+            return False\n+    except OSError:\n+        return False\n+    return False\n+\n+\n+def _path_is_regular_file(path: Path) -> bool:\n+    \"\"\"Require a regular repository file without following symlinks.\"\"\"\n+    try:\n+        return stat.S_ISREG(path.lstat().st_mode)\n+    except OSError:\n+        return False\n+\n+\n def _closeout_completes_goal(target: Path, goal_id: str) -> bool | None:\n     \"\"\"Whether this closeout completes its goal: every task node of the goal's DAG\n     is ``done`` at HEAD (this task set ``done`` by the closeout). Returns None when\n@@ -2043,10 +2066,21 @@ def is_bookkeeping_closeout(\n     try:\n         if base_shiki is None or not base_shiki.exists():\n             return False\n-        if not isinstance(task, dict) or str(task.get(\"id\") or \"\") != task_id or not goal_id:\n+        if (\n+            not isinstance(task, dict)\n+            or str(task.get(\"id\") or \"\") != task_id\n+            or str(task.get(\"goal_id\") or \"\") != goal_id\n+            or not goal_id\n+        ):\n             return False\n-        base_task = load_json(base_shiki / \"tasks\" / f\"{task_id}.json\")\n-        if not isinstance(base_task, dict):\n+        task_file = f\".shiki/tasks/{task_id}.json\"\n+        base_task_path = base_shiki / \"tasks\" / f\"{task_id}.json\"\n+        head_task_path = target / task_file\n+        if not (_path_is_regular_file(base_task_path) and _path_is_regular_file(head_task_path)):\n+            return False\n+        base_task = load_json(base_task_path)\n+        head_task = load_json(head_task_path)\n+        if not isinstance(base_task, dict) or not isinstance(head_task, dict) or head_task != task:\n             return False\n \n         # Condition 4: every task-file key outside the mutable set is the frozen\n@@ -2060,13 +2094,22 @@ def is_bookkeeping_closeout(\n         # Condition 5 (task leg): status transitions review -> done, exactly.\n         if str(base_task.get(\"status\")) != \"review\" or str(task.get(\"status\")) != \"done\":\n             return False\n+        if type(task.get(\"expected_pr\")) is not int:\n+            return False\n+        expected_branch = task.get(\"expected_branch\")\n+        if not isinstance(expected_branch, str) or not expected_branch.strip():\n+            return False\n+        if (\n+            (\"closeout_pr\" in base_task) != (\"closeout_pr\" in task)\n+            or base_task.get(\"closeout_pr\") != task.get(\"closeout_pr\")\n+        ):\n+            return False\n \n         # Condition 6: the implementation PR is named by the BASE expected_pr (a\n         # field the PR head cannot write) and must be proven merged.\n-        try:\n-            impl_pr_num = int(base_task.get(\"expected_pr\"))\n-        except (TypeError, ValueError):\n+        if type(base_task.get(\"expected_pr\")) is not int:\n             return False\n+        impl_pr_num = base_task[\"expected_pr\"]\n         if impl_pr_num not in (merged_pr_numbers or set()):\n             return False\n \n@@ -2075,7 +2118,6 @@ def is_bookkeeping_closeout(\n         if completes_goal is None:\n             return False\n \n-        task_file = f\".shiki/tasks/{task_id}.json\"\n         lock_file = f\".shiki/locks/{task_id}.json\"\n         goal_file = f\".shiki/goals/{goal_id}.json\"\n         worktree_file = f\".shiki/worktrees/{task_id}.json\"\n@@ -2084,60 +2126,99 @@ def is_bookkeeping_closeout(\n         lock_released = False\n         goal_completed = False\n         reports_added = 0\n+        completion_report_path: str | None = None\n+        completion_ledger_id: str | None = None\n+        base_goal_for_closeout: dict[str, Any] | None = None\n+        head_goal_for_closeout: dict[str, Any] | None = None\n         changed_paths: list[str] = []\n+        changed_ledger_paths: list[str] = []\n \n         for entry in changed_files_status:\n             path = normalize_repo_path(entry.path)\n             old_path = normalize_repo_path(entry.old_path or \"\")\n-            # Condition 1: every changed path (both legs of a rename) under .shiki/.\n-            for candidate in (candidate for candidate in (path, old_path) if candidate):\n-                if not candidate.startswith(\".shiki/\"):\n-                    return False\n-                changed_paths.append(candidate)\n-            # Condition 2: no deletion (a rename splits into a delete leg too).\n-            if entry.status == \"D\":\n+            # Conditions 1-2: only direct .shiki path additions/modifications;\n+            # delete, rename, copy and type-change legs are never terminal state.\n+            if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            changed_paths.append(path)\n \n             if path == task_file:\n+                if entry.status != \"M\":\n+                    return False\n                 task_changed = True\n             elif path == lock_file:\n                 # Condition 5 (lock leg): active -> released, proven against base.\n-                base_lock = load_json(base_shiki / \"locks\" / f\"{task_id}.json\")\n-                head_lock = load_json(target / path)\n+                base_lock_path = base_shiki / \"locks\" / f\"{task_id}.json\"\n+                head_lock_path = target / path\n+                if (\n+                    entry.status != \"M\"\n+                    or not _path_is_regular_file(base_lock_path)\n+                    or not _path_is_regular_file(head_lock_path)\n+                ):\n+                    return False\n+                base_lock = load_json(base_lock_path)\n+                head_lock = load_json(head_lock_path)\n                 if not (isinstance(base_lock, dict) and base_lock.get(\"state\") == \"active\"):\n                     return False\n                 if not (isinstance(head_lock, dict) and head_lock.get(\"state\") == \"released\"):\n                     return False\n+                if any(\n+                    lock.get(\"task_id\") != task_id or lock.get(\"goal_id\") != goal_id\n+                    for lock in (base_lock, head_lock)\n+                ):\n+                    return False\n+                for field in (set(base_lock) | set(head_lock)) - {\"state\"}:\n+                    if base_lock.get(field) != head_lock.get(field):\n+                        return False\n                 lock_released = True\n             elif path == goal_file:\n                 # Condition 5 (goal leg): a goal transition is allowed ONLY when\n                 # this task completes its goal, and must be -> complete.\n-                if not completes_goal:\n+                base_goal_path = base_shiki / \"goals\" / f\"{goal_id}.json\"\n+                head_goal_path = target / path\n+                if (\n+                    entry.status != \"M\"\n+                    or not completes_goal\n+                    or not _path_is_regular_file(base_goal_path)\n+                    or not _path_is_regular_file(head_goal_path)\n+                ):\n                     return False\n-                base_goal = load_json(base_shiki / \"goals\" / f\"{goal_id}.json\")\n-                head_goal = load_json(target / path)\n-                if not (isinstance(head_goal, dict) and str(head_goal.get(\"status\")) == \"complete\"):\n+                base_goal = load_json(base_goal_path)\n+                head_goal = load_json(head_goal_path)\n+                if not (\n+                    isinstance(base_goal, dict)\n+                    and base_goal.get(\"id\") == goal_id\n+                    and str(base_goal.get(\"status\")) in {\"planned\", \"ready\", \"blocked\"}\n+                ):\n                     return False\n-                if isinstance(base_goal, dict) and str(base_goal.get(\"status\")) == \"complete\":\n-                    # Already complete on base: not a real completion transition.\n+                if not (\n+                    isinstance(head_goal, dict)\n+                    and head_goal.get(\"id\") == goal_id\n+                    and str(head_goal.get(\"status\")) == \"complete\"\n+                ):\n                     return False\n+                for field in (set(base_goal) | set(head_goal)) - {\"status\", \"ledger_evidence\"}:\n+                    if base_goal.get(field) != head_goal.get(field):\n+                        return False\n+                base_goal_for_closeout = base_goal\n+                head_goal_for_closeout = head_goal\n                 goal_completed = True\n             elif path.startswith(\".shiki/reports/\") and path.endswith(\".json\"):\n                 # A scorecard is ADDED by goal completion; a modified report is not\n                 # part of the terminal set.\n-                if entry.status != \"A\":\n+                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n                     return False\n                 reports_added += 1\n+                completion_report_path = path\n             elif path == worktree_file:\n-                # This task's own worktree record is benign id-scoped mirror state\n-                # (no governance transition); condition 3 still covers it.\n-                pass\n+                return False\n             elif path.startswith(\".shiki/ledger/\") and path.endswith(\".json\"):\n                 # Ledgers are append-only mirror bookkeeping (condition 3 and the\n                 # untrusted-mutation gate police them); an ADD carries no transition,\n                 # but a modify is never part of a closeout's terminal set.\n-                if entry.status != \"A\":\n+                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n                     return False\n+                changed_ledger_paths.append(path)\n             else:\n                 # Any other .shiki path (dag, memories, guardian-policy, another\n                 # task/goal/lock, a plan, \u2026) is not part of the terminal set.\n@@ -2161,11 +2242,167 @@ def is_bookkeeping_closeout(\n             # EXACTLY ONE added scorecard report.\n             if not goal_completed or reports_added != 1:\n                 return False\n+            if not (\n+                isinstance(base_goal_for_closeout, dict)\n+                and isinstance(head_goal_for_closeout, dict)\n+                and completion_report_path\n+            ):\n+                return False\n+\n+            dag = load_dag(target, goal_id)\n+            dag_nodes = dag.get(\"nodes\") if isinstance(dag, dict) else None\n+            if not isinstance(dag_nodes, list) or not dag_nodes:\n+                return False\n+            goal_task_ids = [str(node) for node in dag_nodes]\n+            goal_tasks = [load_task(target, node_id) for node_id in goal_task_ids]\n+            if not all(isinstance(goal_task, dict) for goal_task in goal_tasks):\n+                return False\n+\n+            report = load_json(target / completion_report_path)\n+            report_mergegate = report.get(\"mergegate\") if isinstance(report, dict) else None\n+            scorecard = report.get(\"scorecard\") if isinstance(report, dict) else None\n+            scorecard_tasks = scorecard.get(\"tasks\") if isinstance(scorecard, dict) else None\n+            expected_task_paths = {\n+                f\".shiki/tasks/{goal_task_id}.json\" for goal_task_id in goal_task_ids\n+            }\n+            report_evidence = (\n+                {str(value) for value in report.get(\"evidence\") or []}\n+                if isinstance(report, dict)\n+                else set()\n+            )\n+            effective_risk = str(head_goal_for_closeout.get(\"risk_level\") or \"\")\n+            for goal_task in goal_tasks:\n+                if not isinstance(goal_task, dict):\n+                    return False\n+                effective_risk = _max_risk(effective_risk, str(goal_task.get(\"risk_level\") or \"\"))\n+            if not (\n+                isinstance(report, dict)\n+                and report.get(\"id\") == Path(completion_report_path).stem\n+                and report.get(\"goal_id\") == goal_id\n+                and report.get(\"status\") == \"complete\"\n+                and report_evidence == expected_task_paths\n+                and report.get(\"blocking_reasons\") == []\n+                and isinstance(report_mergegate, dict)\n+                and report_mergegate.get(\"dependencies\") == \"pass\"\n+                and report_mergegate.get(\"locks\") == \"pass\"\n+                and report_mergegate.get(\"checks\") == \"pass\"\n+                and report_mergegate.get(\"review\") == \"recorded\"\n+                and report_mergegate.get(\"ledger\") == \"pass\"\n+                and report_mergegate.get(\"risk\") == effective_risk\n+            ):\n+                return False\n+            scorecard_complete = bool(\n+                isinstance(scorecard, dict)\n+                and scorecard.get(\"goal_id\") == goal_id\n+                and isinstance(scorecard_tasks, dict)\n+                and type(scorecard_tasks.get(\"total\")) is int\n+                and type(scorecard_tasks.get(\"completed\")) is int\n+                and type(scorecard_tasks.get(\"failed\")) is int\n+                and scorecard_tasks.get(\"total\") == len(goal_task_ids)\n+                and scorecard_tasks.get(\"completed\") == len(goal_task_ids)\n+                and scorecard_tasks.get(\"failed\") == 0\n+            )\n+            degraded_warnings = scorecard.get(\"warnings\") if isinstance(scorecard, dict) else None\n+            scorecard_degraded = bool(\n+                isinstance(scorecard, dict)\n+                and set(scorecard) == {\"goal_id\", \"warnings\"}\n+                and scorecard.get(\"goal_id\") == goal_id\n+                and isinstance(degraded_warnings, list)\n+                and len(degraded_warnings) == 1\n+                and isinstance(degraded_warnings[0], str)\n+                and degraded_warnings[0].startswith(\"scorecard generation failed:\")\n+            )\n+            if not (scorecard_complete or scorecard_degraded):\n+                return False\n+\n+            base_goal_ledger = base_goal_for_closeout.get(\"ledger_evidence\")\n+            head_goal_ledger = head_goal_for_closeout.get(\"ledger_evidence\")\n+            if not isinstance(base_goal_ledger, list) or not isinstance(head_goal_ledger, list):\n+                return False\n+            if head_goal_ledger[: len(base_goal_ledger)] != base_goal_ledger:\n+                return False\n+            added_goal_ledger_ids = head_goal_ledger[len(base_goal_ledger) :]\n+            if len(added_goal_ledger_ids) != 1 or not isinstance(added_goal_ledger_ids[0], str):\n+                return False\n+            completion_ledger_id = added_goal_ledger_ids[0]\n+            completion_ledger_path = target / \".shiki\" / \"ledger\" / f\"{completion_ledger_id}.json\"\n+            if not _path_is_regular_file(completion_ledger_path):\n+                return False\n+            completion_ledger = load_json(completion_ledger_path)\n+            completion_evidence = completion_ledger.get(\"evidence\") if isinstance(completion_ledger, dict) else None\n+            if not (\n+                isinstance(completion_ledger, dict)\n+                and completion_ledger.get(\"id\") == completion_ledger_id\n+                and completion_ledger.get(\"goal_id\") == goal_id\n+                and completion_ledger.get(\"task_id\") is None\n+                and completion_ledger.get(\"type\") == \"completion\"\n+                and completion_evidence == [completion_report_path]\n+            ):\n+                return False\n         else:\n             # A non-completing closeout must carry NO goal transition and NO report.\n             if goal_completed or reports_added != 0:\n                 return False\n \n+        base_task_ledger = base_task.get(\"ledger_evidence\")\n+        head_task_ledger = task.get(\"ledger_evidence\")\n+        if not isinstance(base_task_ledger, list) or not isinstance(head_task_ledger, list):\n+            return False\n+        if head_task_ledger[: len(base_task_ledger)] != base_task_ledger:\n+            return False\n+        added_task_ledger_ids = head_task_ledger[len(base_task_ledger) :]\n+        if (\n+            not all(isinstance(value, str) for value in added_task_ledger_ids)\n+            or len(added_task_ledger_ids) != len(set(added_task_ledger_ids))\n+        ):\n+            return False\n+        self_reference_ids: list[str] = []\n+        for added_ledger_id in added_task_ledger_ids:\n+            if not isinstance(added_ledger_id, str):\n+                return False\n+            if added_ledger_id == completion_ledger_id:\n+                continue\n+            added_ledger_path = target / \".shiki\" / \"ledger\" / f\"{added_ledger_id}.json\"\n+            if not _path_is_regular_file(added_ledger_path):\n+                return False\n+            added_ledger = load_json(added_ledger_path)\n+            self_reference_evidence = added_ledger.get(\"evidence\") if isinstance(added_ledger, dict) else None\n+            required_self_reference_evidence = {task_file, lock_file}\n+            if not (\n+                isinstance(added_ledger, dict)\n+                and added_ledger.get(\"id\") == added_ledger_id\n+                and added_ledger.get(\"goal_id\") == goal_id\n+                and added_ledger.get(\"task_id\") == task_id\n+                and added_ledger.get(\"type\") == \"lock\"\n+                and isinstance(self_reference_evidence, list)\n+                and all(isinstance(value, str) for value in self_reference_evidence)\n+                and len(self_reference_evidence) == len(required_self_reference_evidence)\n+                and set(self_reference_evidence) == required_self_reference_evidence\n+            ):\n+                return False\n+            self_reference_ids.append(added_ledger_id)\n+        if len(self_reference_ids) != 1:\n+            return False\n+        permitted_ledger_ids = {self_reference_ids[0]}\n+        if completion_ledger_id is not None:\n+            permitted_ledger_ids.add(completion_ledger_id)\n+        permitted_ledger_paths = {\n+            f\".shiki/ledger/{ledger_id}.json\" for ledger_id in permitted_ledger_ids\n+        }\n+        if (\n+            set(added_task_ledger_ids) != permitted_ledger_ids\n+            or set(changed_ledger_paths) != permitted_ledger_paths\n+        ):\n+            return False\n+\n+        permitted_changed_paths = {task_file, lock_file, *permitted_ledger_paths}\n+        if completes_goal:\n+            if completion_report_path is None:\n+                return False\n+            permitted_changed_paths.update({goal_file, completion_report_path})\n+        if set(changed_paths) != permitted_changed_paths:\n+            return False\n+\n         return True\n     except (OSError, ValueError, TypeError):\n         # Unreadable/malformed evidence is fail-closed: NOT a bookkeeping closeout.\n@@ -3222,6 +3459,10 @@ def main() -> int:\n                 changed_files_status=files_status,\n                 merged_pr_numbers=merged_prs,\n             )\n+            # The exemption is same-repository only and fail-closed: the live PR\n+            # snapshot must explicitly prove isCrossRepository=false.\n+            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\n+                bookkeeping_closeout = False\n \n             ledger_entries = load_ledger_entries(target, task, warnings, blocking)\n             if pr:\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex d531052..ca917c3 100644\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -23,6 +23,8 @@ from shiki_state import append_ledger_entry, new_control_id\n _ID_SUFFIX = r\"(?:[0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})\"\n _TASK_ID_RE = re.compile(rf\"^T-{_ID_SUFFIX}$\")\n \n+_RISK_ORDER = {\"low\": 0, \"medium\": 1, \"high\": 2, \"critical\": 3}\n+\n # Contract PR mode (SADR-0015). The body marker DECLARES intent; the\n # maintainer-applied label is the independent second factor that AUTHORIZES the\n # mode. `shiki contract open` must never apply the label to its own PR \u2014 doing so\n@@ -1391,6 +1393,13 @@ def cmd_goal_complete(args: argparse.Namespace) -> int:\n         blocking.append(f\"incomplete tasks: {', '.join(incomplete)}\")\n \n     status = \"blocked\" if blocking else \"complete\"\n+    effective_risk = str(goal.get(\"risk_level\") or \"\")\n+    for task in tasks:\n+        task_risk = str(task.get(\"risk_level\") or \"\")\n+        if _RISK_ORDER.get(task_risk, -1) > _RISK_ORDER.get(effective_risk, -1):\n+            effective_risk = task_risk\n+    if not effective_risk:\n+        effective_risk = \"low\"\n     report_id = next_control_id(target, \"R\")\n     report = {\n         \"id\": report_id,\n@@ -1405,7 +1414,7 @@ def cmd_goal_complete(args: argparse.Namespace) -> int:\n             \"checks\": \"pass\" if not blocking else \"blocked\",\n             \"review\": \"recorded\",\n             \"ledger\": \"pass\",\n-            \"risk\": goal.get(\"risk_level\", \"low\"),\n+            \"risk\": effective_risk,\n         },\n         \"created_at\": utc_now(),\n     }\n\nexec\n/bin/zsh -lc 'git diff --stat && git diff --name-only && git diff -- .shiki/tasks/T-20260817T063758155247Z-0b11207b.json .shiki/locks/T-20260817T063758155247Z-0b11207b.json' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n .github/workflows/shiki-cca-completion.yml         |  49 ++-\n .github/workflows/shiki-mergegate.yml              |  19 +-\n .../tasks/T-20260817T063758155247Z-0b11207b.json   |   9 +-\n .../SADR-0012-autonomous-loop-closeout-to-main.md  |   2 +-\n docs/agents/checklists.md                          |   2 +\n docs/agents/decision-control.md                    |   1 +\n docs/agents/implementation-policy.md               |   7 +\n docs/agents/triage-labels.md                       |   4 +\n scripts/guardian_approval_signal.py                |   4 +\n scripts/mergegate_check.py                         | 349 +++++++++++++++++----\n scripts/shiki_tasks.py                             |  11 +-\n tests/test_bookkeeping_closeout.py                 |  95 +++++-\n tests/test_guardian_signal_closeout.py             |  74 ++++-\n tests/test_memory_scorecard.py                     |  32 ++\n tests/test_migration_mode.py                       |  37 ++-\n tests/test_paired_invariants.py                    |  50 +++\n 16 files changed, 648 insertions(+), 97 deletions(-)\n.github/workflows/shiki-cca-completion.yml\n.github/workflows/shiki-mergegate.yml\n.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/agents/checklists.md\ndocs/agents/decision-control.md\ndocs/agents/implementation-policy.md\ndocs/agents/triage-labels.md\nscripts/guardian_approval_signal.py\nscripts/mergegate_check.py\nscripts/shiki_tasks.py\ntests/test_bookkeeping_closeout.py\ntests/test_guardian_signal_closeout.py\ntests/test_memory_scorecard.py\ntests/test_migration_mode.py\ntests/test_paired_invariants.py\ndiff --git a/.shiki/tasks/T-20260817T063758155247Z-0b11207b.json b/.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\nindex 46b959a..3beb259 100644\n--- a/.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n+++ b/.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n@@ -22,7 +22,8 @@\n   \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n   \"id\": \"T-20260817T063758155247Z-0b11207b\",\n   \"ledger_evidence\": [\n-    \"L-20260817T063758155305Z-197baec8\"\n+    \"L-20260817T063758155305Z-197baec8\",\n+    \"L-20260817T100648130920Z-5568a6ab\"\n   ],\n   \"locks\": [\n     \"path:scripts/mergegate_check.py\",\n@@ -41,7 +42,9 @@\n     \"path:docs/agents/decision-control.md\",\n     \"path:docs/agents/triage-labels.md\",\n     \"path:docs/agents/implementation-policy.md\",\n-    \"path:docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\"\n+    \"path:docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\",\n+    \"path:scripts/test_shiki_governance_evidence.sh\",\n+    \"path:scripts/test_shiki_adversarial_state.sh\"\n   ],\n   \"non_goals\": [\n     \"Refluxing the `and pr` short-circuit\",\n@@ -59,7 +62,7 @@\n   ],\n   \"risk_level\": \"high\",\n   \"scope\": \"Port five capabilities from mizutani-140/CubeShare into this platform repository, one of them CORRECTED rather than copied.\\n\\n(a) FORK GUARD \\u2014 the reason this task exists. Measured on origin/main: `git grep -c isCrossRepository -- scripts/ .github/ tests/` returns NOTHING (0 occurrences repo-wide; the same grep returns 63 files for `bookkeeping_closeout`, so the search works). And `mergegate_check.py` computes `carry_resolved_risk = \\\"low\\\" if bookkeeping_closeout else str((task or {}).get(\\\"risk_level\\\") or \\\"\\\")`. A PR shaped as a SADR-0017 bookkeeping closeout therefore resolves to `low` and skips the Guardian gate at any real risk level. This repository is PUBLIC with `allow_forking: true`, so a fork PR is an unauthenticated path to that shape.\\n\\nAdd the guard at BOTH enforcement points, in the `guardian_approval_signal.py` form:\\n\\n    if exemption and pr.get(\\\"isCrossRepository\\\") is not False:\\n        exemption = False\\n\\nDO NOT copy CubeShare's `mergegate_check.py:3600`, which reads `if bookkeeping_closeout and pr and pr.get(...) is not False:`. That `and pr` short-circuits the guard on a falsy `pr`. On the Guardian leg the branch is unreachable, but `bookkeeping_closeout` also flows to `_frozen_spec_mutable_task_fields(...)` at `cs:3608`, under `if not migration_mode:` and OUTSIDE any `pr` guard. Use the un-short-circuited form at both sites.\\n\\nThe guard is fail-closed by design: `is not False` means absent/null/anything-but-`False` collapses the exemption, restoring the task's real risk. That is strictly stricter, never a bypass \\u2014 but it means EVERY `gh pr view --json` field list that produces a PR snapshot must request `isCrossRepository`, or the exemption silently stops working on that path. The Python and every workflow field list MUST land in ONE commit.\\n\\nGuard scope, so it is not overclaimed: SADR-0017 bookkeeping exemption ONLY. The contract_approval carry and the SADR-0018 base-sync carry are out of scope.\\n\\n(b) IMMUTABLE MERGE-BASE PINNING. Resolve `mergeBaseOid` once in each gate workflow from `baseRefOid` plus `git merge-base`, inject it into the PR snapshot with jq, consume it in `mergegate_check.py` (a `_git_show_base`-style reader), and read the frozen tree with `git archive \\\"$merge_base\\\" .shiki`. Remove the `|| true` that swallows a failed resolution. Python and both workflows must land together or every migration PR blocks on the missing field.\\n\\n(c) CLOSEOUT CLASSIFIER HARDENING, MINUS THE RELAXATION. Port CubeShare's `is_bookkeeping_closeout` hardening: symlink-safe `lstat`/`S_ISREG` path proofs; `entry.status in {\\\"A\\\",\\\"M\\\"}` and `not old_path` so rename/copy/type-change legs are rejected (main rejects only `D`); the task's own worktree record returning False instead of passing; `type(...) is not int` for `expected_pr` so `True` and `\\\"12\\\"` no longer coerce; base goal status restricted to `{planned, ready, blocked}`; field-identity loops over base/head goal and lock; a completion-report content proof; a scorecard shape proof; a ledger-evidence proof; and a closing `set(changed_paths) != permitted_changed_paths`.\\n\\nEXCLUDE the lockless arm. CubeShare has `lock_terminal = lock_released or (lockless and not lock_path_in_diff)`; main has `if not task_changed or not lock_released: return False`. KEEP MAIN'S. CubeShare widened its own SADR-0017 exemption without amending SADR-0017 (the ADR is byte-identical on both sides). After this task, `grep -c \\\"lockless\\\\|lock_terminal\\\" scripts/mergegate_check.py` must be 0. The `_path_proven_absent` / `_path_is_regular_file` helpers still port \\u2014 the symlink hardening uses them independently.\\n\\n(d) COMPLETION-REPORT RISK. Write the report's risk as the strongest of the goal's and every task's, across the `shiki_tasks.py` writer and the closeout classifier's equality check. Both legs in one commit, or the classifier fails on every outranked closeout.\\n\\n(e) DOCS. MG-25, MG-26 and the migration decision-control row; SADR-0012 STEP 5 ONLY. Step 3 documents the lockless closeout \\u2014 main does not implement it, and publishing it would make CubeShare-local behaviour read as platform doctrine. MG-26 asserts registration on the immutable merge base, so it lands with or after (b), never before.\\n\\nMUST NOT: reflux `and pr`; add `lockless`/`lock_terminal`; touch SADR-0017 itself; port `exact_head_only`, `normal_frozen_spec_errors`, `exact_pr_link`, or any `spec_amendment`/`delivery_only` surface; add a second `git fetch origin`; weaken `tests/test_workflow_invocation_carry.py`'s `2`/`1` fetch counts or its `total == 4`; bind `_review_source` to a head SHA (a real but separate defect).\\n\\nTESTS. Fork fixtures at both enforcement points: `isCrossRepository: true` denies the exemption, and a snapshot with the field ABSENT also denies it. Assert `_bookkeeping_closeout_exemption` then resolves the task's real risk, not `low`. Assert every `gh pr view --json` list in both workflows contains `isCrossRepository`. Assert `grep -c \\\"lockless\\\\|lock_terminal\\\"` is 0 and that `if not task_changed or not lock_released:` is present verbatim. Symlinked, renamed, type-changed and coerced-`expected_pr` closeout fixtures all return False. A goal/task risk pair (`medium`/`critical`) produces `report[\\\"mergegate\\\"][\\\"risk\\\"] == \\\"critical\\\"` and the classifier's equality check passes on it. Keep the fetch-count invariants exactly as they are.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: on a PUBLIC repository, anyone may fork, open a PR shaped as a SADR-0017 bookkeeping closeout, and have `carry_resolved_risk` computed as `low`, bypassing the Guardian gate for a high- or critical-risk task.\\n  Measured 2026-08-17: `isCrossRepository` 0 occurrences across scripts/, .github/ and tests/ on origin/main; `visibility: public`, `allow_forking: true`; 0 cross-repository PRs ever opened, so nothing has exploited it.\\n  Root cause: the exemption never asks whether the PR came from a fork.\\n  Correction this task makes over its source: CubeShare's `mergegate_check.py:3600` carries an `and pr` short-circuit its sibling at `guardian_approval_signal.py:336` does not; both sites here use the un-short-circuited form.\\n  Reproducible when: a fork-PR fixture and a field-absent fixture both deny the exemption at both sites, `lockless`/`lock_terminal` appear nowhere, and the fetch-count invariants are untouched.\",\n-  \"status\": \"planned\",\n+  \"status\": \"running\",\n   \"test_command\": \"python3 -m unittest discover -s tests && python3 scripts/validate_shiki.py --target . && for s in scripts/test_shiki_*.sh; do bash \\\"$s\\\" || exit 1; done\",\n   \"title\": \"Deny the SADR-0017 exemption to fork PRs and pin the judged base\"\n }\n\nexec\n/bin/zsh -lc 'shiki status && shiki doctor' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n2026-08-17T10:07:57.853363Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 1782ms:\nShiki doctor\ntarget: /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\nstatus: fail\nusable entrypoints: cli, codex\n\nconfig:\n- [pass] Shiki config: .shiki/config.yaml parses successfully.\nentrypoint:\n- [pass] Shiki CLI entrypoint: Shiki CLI entrypoint is available.\nauth:\n- [pass] Codex CLI auth: Codex CLI is installed and authenticated.\n- [warn] Claude CLI auth: Claude CLI is unavailable or unauthenticated.\n  remediation: Run `claude auth login` in a terminal or `/login` inside Claude Code, then rerun `/shiki`.\n- [warn] GitHub CLI auth: GitHub CLI is unavailable or unauthenticated.\n  remediation: Run `gh auth login -h github.com` before Shiki creates GitHub repositories, issues, PRs, or branch protection.\nprovider:\n- [warn] Repository provider config: .shiki/repo.json is missing; this target may be a legacy Shiki repository or not fully initialized.\n  remediation: Run `shiki init TARGET --repo OWNER/NAME --execute` or record provider metadata for this repository.\ngit:\n- [pass] Git repository: Target is a git repository.\n- [pass] Current branch: Current branch is shiki/t-20260817t063758155247z-0b11207b-deny-the-sadr-0017-exemption-to-fork-prs-and-pin.\n- [warn] Origin remote: Origin remote exists, but provider config is missing so it cannot be checked.\n  remediation: Add .shiki/repo.json provider metadata.\nworktrees:\n- [fail] Worktree registry: Unregistered git worktrees: /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/contract-4479b0d3, /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260729t092307427377z-c8531f7e-let-the-, /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260731t095819930606z-2c95fc6d-backfill, /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260801t041653348134z-bf8a3c4b-enforce-, /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260801t041653423255z-2798773b-register, /Users/kio.mizutani/shiki/.claude/worktrees/autonomous-agent-framework-research-567983, /Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main, /Users/kio.mizutani/shiki/.claude/worktrees/guardian-approval-planning-13e494, /private/tmp/claude-501/-Users-kio-mizutani-shiki--claude-worktrees-guardian-approval-planning-13e494/49ff5832-43c7-4aa1-8b06-b388e0342527/scratchpad/ctrl, /private/tmp/claude-501/-Users-kio-mizutani-shiki--claude-worktrees-guardian-approval-planning-13e494/49ff5832-43c7-4aa1-8b06-b388e0342527/scratchpad/shiki-main.\n  remediation: Remove the worktree (`git worktree remove`) or register it with `shiki worktree allocate`.\nworkflows:\n- [pass] Required workflow files: Required workflow files are present.\nchecks:\n- [pass] Required check names: Required checks match workflow job display names.\nworkflows:\n- [pass] Workflow structural parse: Workflow files parse with the dependency-free parser.\ncodeowners:\n- [pass] CODEOWNERS governance: CODEOWNERS covers critical Shiki paths.\nmanifest:\n- [pass] Shiki manifest layout: Manifest-required directories/files are present and runtime-only evidence is not committed.\nstate_classes:\n- [pass] Shiki state class manifest: State classes are loaded and required trust classes exist.\n- [pass] Unknown tracked .shiki paths: Tracked .shiki paths are represented by the manifest.\n- [pass] Runtime-only state classes: Runtime-only/cache/local-only classes are not committed.\n- [pass] Append-only evidence state class: .shiki/ledger is classified as append-only-evidence.\nmigrations:\n- [pass] Migration registry: Migration registry imports and dependencies are valid.\n- [pass] Migration state: .shiki/migrations/state.json is present and valid.\n- [pass] Pending migrations: No pending migrations.\nguardian:\n- [pass] Guardian policy: Guardian policy parses and validates.\n- [pass] Guardian approvers: Guardian users/teams are configured.\n- [pass] Guardian solo maintainer policy: Solo maintainer policy is explicit.\nevidence_integrity:\n- [pass] CCA evidence manifest: CCA evidence manifest schema and workflow/MergeGate wiring are present.\nruntime:\n- [pass] Runtime registry: Runtime registry imports and declares supported runtimes.\n- [pass] Runtime assignments: Config and task runtime assignments are valid.\ncontract:\n- [pass] Shiki contract validation: validate_shiki.py passed.\ngithub:\n- [skip] GitHub online checks: Online GitHub checks were skipped; pass --online to enable them.\n\nblocking reasons:\n- Claude Code is not authenticated; /shiki cannot run inside Claude Code until Claude Code login succeeds.\n- GitHub CLI is not authenticated or token is invalid: github.com\n\nClaude Code supports `/shiki` through its command file. Codex uses the installed Shiki skill through natural language, not a `/shiki` slash command. Use `shiki start` from Codex or a terminal when a slash command is unavailable.\n\ncodex\nThe pre-existing patch already contains most requested implementation and regression tests, including the corrected un-short-circuited fork guard and strongest-risk report logic. I\u2019m now using those tests as the current RED baseline: any failures will be repaired one behavior slice at a time, while separately checking that the patch did not import the prohibited lockless or out-of-scope relaxations.\nexec\n/bin/zsh -lc 'python3 -m unittest -v tests.test_bookkeeping_closeout tests.test_closeout tests.test_guardian_signal_closeout tests.test_paired_invariants tests.test_memory_scorecard tests.test_policy_job_merged_prs tests.test_migration_mode tests.test_workflow_invocation_carry' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n exited 1 in 0ms:\ntest_bookkeeping_closeout (unittest.loader._FailedTest.test_bookkeeping_closeout) ... ERROR\ntest_closeout (unittest.loader._FailedTest.test_closeout) ... ERROR\ntest_guardian_signal_closeout (unittest.loader._FailedTest.test_guardian_signal_closeout) ... ERROR\ntest_paired_invariants (unittest.loader._FailedTest.test_paired_invariants) ... ERROR\ntest_memory_scorecard (unittest.loader._FailedTest.test_memory_scorecard) ... ERROR\ntest_policy_job_merged_prs (unittest.loader._FailedTest.test_policy_job_merged_prs) ... ERROR\ntest_migration_mode (unittest.loader._FailedTest.test_migration_mode) ... ERROR\ntest_workflow_invocation_carry (unittest.loader._FailedTest.test_workflow_invocation_carry) ... ERROR\n\n======================================================================\nERROR: test_bookkeeping_closeout (unittest.loader._FailedTest.test_bookkeeping_closeout)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_bookkeeping_closeout\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\", line 36, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_closeout (unittest.loader._FailedTest.test_closeout)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_closeout\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_closeout.py\", line 43, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_guardian_signal_closeout (unittest.loader._FailedTest.test_guardian_signal_closeout)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_guardian_signal_closeout\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 31, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_paired_invariants (unittest.loader._FailedTest.test_paired_invariants)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_paired_invariants\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_paired_invariants.py\", line 43, in <module>\n    import shiki_test_support  # noqa: F401  (puts scripts/ on sys.path)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_memory_scorecard (unittest.loader._FailedTest.test_memory_scorecard)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_memory_scorecard\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_memory_scorecard.py\", line 19, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_policy_job_merged_prs (unittest.loader._FailedTest.test_policy_job_merged_prs)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_policy_job_merged_prs\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_policy_job_merged_prs.py\", line 38, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_migration_mode (unittest.loader._FailedTest.test_migration_mode)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_migration_mode\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_migration_mode.py\", line 34, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n======================================================================\nERROR: test_workflow_invocation_carry (unittest.loader._FailedTest.test_workflow_invocation_carry)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_workflow_invocation_carry\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_workflow_invocation_carry.py\", line 34, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n----------------------------------------------------------------------\nRan 8 tests in 0.000s\n\nFAILED (errors=8)\n\n2026-08-17T10:08:05.277092Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nThat first focused command used unittest\u2019s dotted-module mode, but this repository\u2019s tests bootstrap imports only through discovery/file execution; the errors were harness invocation errors, not product failures. I\u2019m rerunning with the repository\u2019s supported discovery interface before drawing any RED/GREEN conclusion.\nexec\n/bin/zsh -lc 'for pattern in test_bookkeeping_closeout.py test_closeout.py test_guardian_signal_closeout.py test_paired_invariants.py test_memory_scorecard.py test_policy_job_merged_prs.py test_migration_mode.py test_workflow_invocation_carry.py; do python3 -m unittest discover -s tests -p \"$pattern\" -v || exit 1; done' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n2026-08-17T10:08:16.670759Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 5700ms:\nok\ntest_missing_base_snapshot_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_missing_base_snapshot_disqualifies) ... ok\ntest_missing_dag_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_missing_dag_disqualifies) ... ok\ntest_non_shiki_file_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_non_shiki_file_disqualifies) ... ok\ntest_positive_completing_is_classified_and_needs_no_guardian (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_completing_is_classified_and_needs_no_guardian) ... ok\ntest_positive_expected_branch_differs_is_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_expected_branch_differs_is_classified) ... ok\ntest_positive_noncompleting_is_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_noncompleting_is_classified) ... ok\ntest_renamed_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_renamed_shiki_path_disqualifies) ... ok\ntest_report_without_completion_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_report_without_completion_disqualifies) ... ok\ntest_risk_level_downgrade_is_not_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_risk_level_downgrade_is_not_classified) ... ok\ntest_second_scorecard_report_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_second_scorecard_report_disqualifies) ... ok\ntest_strongest_goal_or_task_risk_matches_completion_report (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_strongest_goal_or_task_risk_matches_completion_report) ... ok\ntest_symlinked_task_file_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_symlinked_task_file_disqualifies) ... ok\ntest_task_head_status_not_done_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_task_head_status_not_done_disqualifies) ... ok\ntest_type_changed_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_type_changed_shiki_path_disqualifies) ... ok\ntest_unreadable_base_task_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_unreadable_base_task_disqualifies) ... ok\ntest_worktree_record_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_worktree_record_disqualifies) ... ok\n\n----------------------------------------------------------------------\nRan 31 tests in 0.153s\n\nOK\ntest_repoint_resets_count_and_closeout_cca_race_reruns (test_closeout.CloseoutCcaRerunResetTests.test_repoint_resets_count_and_closeout_cca_race_reruns) ... ok\ntest_genuinely_differing_file_conflicts_normally_not_a_refusal (test_closeout.CloseoutCoordinatorMergeTests.test_genuinely_differing_file_conflicts_normally_not_a_refusal) ... ok\ntest_merge_origin_main_succeeds_after_closeout_and_again_next_cycle (test_closeout.CloseoutCoordinatorMergeTests.test_merge_origin_main_succeeds_after_closeout_and_again_next_cycle) ... ok\ntest_control_both_tasks_on_main_both_done_completes (test_closeout.CompletesGoalTests.test_control_both_tasks_on_main_both_done_completes) ... ok\ntest_multitask_first_task_only_on_main_does_not_complete (test_closeout.CompletesGoalTests.test_multitask_first_task_only_on_main_does_not_complete) ... ok\ntest_single_task_goal_completes (test_closeout.CompletesGoalTests.test_single_task_goal_completes) ... ok\ntest_goal_complete_leaves_unrelated_goal_b_untouched (test_closeout.GoalCompleteMirrorSyncTests.test_goal_complete_leaves_unrelated_goal_b_untouched) ... ok\ntest_goal_complete_syncs_completing_task_to_main_and_commits_only_shiki (test_closeout.GoalCompleteMirrorSyncTests.test_goal_complete_syncs_completing_task_to_main_and_commits_only_shiki) ... ok\ntest_dispatch_clears_pointer_and_next_cycle_cuts_fresh_pr (test_closeout.ReopenedTaskDoesNotCloseoutTests.test_dispatch_clears_pointer_and_next_cycle_cuts_fresh_pr) ... ok\ntest_first_cycle_decides_dispatch_not_closeout (test_closeout.ReopenedTaskDoesNotCloseoutTests.test_first_cycle_decides_dispatch_not_closeout) ... ok\n\n----------------------------------------------------------------------\nRan 10 tests in 4.598s\n\nOK\ntest_absent_cross_repository_field_denies_exemption_and_uses_real_risk (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_absent_cross_repository_field_denies_exemption_and_uses_real_risk) ... ok\ntest_cross_repository_pr_denies_exemption_and_uses_real_risk (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_cross_repository_pr_denies_exemption_and_uses_real_risk) ... ok\ntest_deletion_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_deletion_requires_guardian) ... ok\ntest_governance_field_edit_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_governance_field_edit_requires_guardian) ... ok\ntest_maintainer_label_still_forces_gate_on_exempt_pr (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_maintainer_label_still_forces_gate_on_exempt_pr) ... ok\ntest_missing_base_snapshot_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_missing_base_snapshot_requires_guardian) ... ok\ntest_missing_changed_files_status_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_missing_changed_files_status_requires_guardian) ... ok\ntest_non_shiki_path_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_non_shiki_path_requires_guardian) ... ok\ntest_non_terminal_status_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_non_terminal_status_requires_guardian) ... ok\ntest_nonexistent_base_snapshot_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_nonexistent_base_snapshot_requires_guardian) ... ok\ntest_nonexistent_changed_files_status_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_nonexistent_changed_files_status_requires_guardian) ... ok\ntest_parity_signal_matches_mergegate_and_is_never_more_permissive (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_parity_signal_matches_mergegate_and_is_never_more_permissive) ... ok\ntest_proven_completing_critical_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_completing_critical_closeout_not_required) ... ok\ntest_proven_noncompleting_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_noncompleting_closeout_not_required) ... ok\ntest_second_scorecard_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_second_scorecard_requires_guardian) ... ok\ntest_signal_records_exemption_applied_flag (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_signal_records_exemption_applied_flag) ... ok\ntest_unmerged_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unmerged_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_task_id_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_task_id_requires_guardian) ... ok\n\n----------------------------------------------------------------------\nRan 19 tests in 0.118s\n\nOK\ntest_declared_subset_is_consistent_with_implemented (test_paired_invariants.Pair01SchemaKeywords.test_declared_subset_is_consistent_with_implemented) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair01SchemaKeywords.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair01SchemaKeywords.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair02CheckNameLiterals.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair02CheckNameLiterals.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair03MirrorDirs.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair03MirrorDirs.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair04PrBodyHeadings.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair04PrBodyHeadings.test_holds) ... ok\ntest_code_sites_are_set_equal (test_paired_invariants.Pair05CcaVerdictValues.test_code_sites_are_set_equal) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair05CcaVerdictValues.test_divergence_is_detected) ... ok\ntest_workflow_and_prose_are_contained (test_paired_invariants.Pair05CcaVerdictValues.test_workflow_and_prose_are_contained) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair06RiskVocabulary.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair06RiskVocabulary.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair07CodeReviewVerdicts.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair07CodeReviewVerdicts.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair08DefaultRequiredChecks.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair08DefaultRequiredChecks.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair09MemoryVocabulary.test_divergence_is_detected) ... ok\ntest_shipped_memory_records_validate (test_paired_invariants.Pair09MemoryVocabulary.test_shipped_memory_records_validate) ... ok\ntest_statuses_and_areas_are_set_equal (test_paired_invariants.Pair09MemoryVocabulary.test_statuses_and_areas_are_set_equal) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair10WorkflowFiles.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair10WorkflowFiles.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair11RepairSkillSubset.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair11RepairSkillSubset.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair12ChecklistIds.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair12ChecklistIds.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair13GuardianIdentity.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair13GuardianIdentity.test_holds) ... ok\ntest_divergence_is_detected (test_paired_invariants.Pair14SmokeStartRequired.test_divergence_is_detected) ... ok\ntest_holds (test_paired_invariants.Pair14SmokeStartRequired.test_holds) ... ok\ntest_both_exemption_sites_fail_closed_without_pr_short_circuit (test_paired_invariants.Pair15ForkAndMergeBaseWorkflowEvidence.test_both_exemption_sites_fail_closed_without_pr_short_circuit) ... ok\ntest_closeout_keeps_mandatory_lock_release_and_no_relaxation (test_paired_invariants.Pair15ForkAndMergeBaseWorkflowEvidence.test_closeout_keeps_mandatory_lock_release_and_no_relaxation) ... ok\ntest_every_pr_view_json_field_list_requests_fork_identity (test_paired_invariants.Pair15ForkAndMergeBaseWorkflowEvidence.test_every_pr_view_json_field_list_requests_fork_identity) ... ok\ntest_workflows_archive_the_pinned_merge_base_without_swallowing_failure (test_paired_invariants.Pair15ForkAndMergeBaseWorkflowEvidence.test_workflows_archive_the_pinned_merge_base_without_swallowing_failure) ... ok\n\n----------------------------------------------------------------------\nRan 35 tests in 0.015s\n\nOK\ntest_report_uses_strongest_goal_or_task_risk (test_memory_scorecard.GoalCompleteContractTests.test_report_uses_strongest_goal_or_task_risk) ... ok\ntest_scorecard_in_report_file_and_not_on_stdout (test_memory_scorecard.GoalCompleteContractTests.test_scorecard_in_report_file_and_not_on_stdout) ... ok\ntest_counts_from_ledger_and_task_state_only (test_memory_scorecard.ScorecardTests.test_counts_from_ledger_and_task_state_only) ... ok\ntest_missing_categories_are_zero_with_warnings (test_memory_scorecard.ScorecardTests.test_missing_categories_are_zero_with_warnings) ... ok\ntest_raw_memory_is_not_a_counting_source (test_memory_scorecard.ScorecardTests.test_raw_memory_is_not_a_counting_source) ... ok\ntest_single_memory_does_not_yield_suggestion (test_memory_scorecard.SuggestionTests.test_single_memory_does_not_yield_suggestion) ... ok\ntest_suggestion_does_not_change_memory_status (test_memory_scorecard.SuggestionTests.test_suggestion_does_not_change_memory_status) ... ok\ntest_suggestion_from_recurring_raw_memories_includes_mem_ids (test_memory_scorecard.SuggestionTests.test_suggestion_from_recurring_raw_memories_includes_mem_ids) ... ok\n\n----------------------------------------------------------------------\nRan 8 tests in 0.106s\n\nOK\ntest_merge_proof_invocations_receive_base_shiki_and_merged_prs (test_policy_job_merged_prs.WorkflowText.test_merge_proof_invocations_receive_base_shiki_and_merged_prs) ... ok\ntest_merged_prs_included_only_when_state_merged (test_policy_job_merged_prs.WorkflowText.test_merged_prs_included_only_when_state_merged) ... ok\ntest_merged_prs_not_nested_in_post_merge_reconcile_marker (test_policy_job_merged_prs.WorkflowText.test_merged_prs_not_nested_in_post_merge_reconcile_marker) ... ok\ntest_merged_prs_read_from_base_snapshot_not_head (test_policy_job_merged_prs.WorkflowText.test_merged_prs_read_from_base_snapshot_not_head) ... ok\ntest_resolution_matches_prepare_cca_context (test_policy_job_merged_prs.WorkflowText.test_resolution_matches_prepare_cca_context) ... ok\n\n----------------------------------------------------------------------\nRan 5 tests in 0.001s\n\nOK\ntest_missing_immutable_merge_base_oid_blocks_registry_resolution (test_migration_mode.BaseRegistryResolutionTests.test_missing_immutable_merge_base_oid_blocks_registry_resolution) ... ok\ntest_registry_is_read_from_base_not_head (test_migration_mode.BaseRegistryResolutionTests.test_registry_is_read_from_base_not_head) ... ok\ntest_self_defining_migration_pr_blocks_because_id_absent_from_base (test_migration_mode.BaseRegistryResolutionTests.test_self_defining_migration_pr_blocks_because_id_absent_from_base) ... ok\ntest_absent_line_yields_nothing (test_migration_mode.DeclaredMigrationIdTests.test_absent_line_yields_nothing) ... ok\ntest_mid_sentence_mention_is_not_a_declaration (test_migration_mode.DeclaredMigrationIdTests.test_mid_sentence_mention_is_not_a_declaration) ... ok\ntest_single_line_is_parsed (test_migration_mode.DeclaredMigrationIdTests.test_single_line_is_parsed) ... ok\ntest_two_lines_are_both_returned (test_migration_mode.DeclaredMigrationIdTests.test_two_lines_are_both_returned) ... ok\ntest_label_without_marker_does_not_enter_mode (test_migration_mode.MigrationDecisionTests.test_label_without_marker_does_not_enter_mode) ... ok\ntest_marker_and_label_enables_mode (test_migration_mode.MigrationDecisionTests.test_marker_and_label_enables_mode) ... ok\ntest_marker_quoted_in_code_span_is_not_a_directive (test_migration_mode.MigrationDecisionTests.test_marker_quoted_in_code_span_is_not_a_directive) ... ok\ntest_marker_without_label_blocks_with_the_label_message (test_migration_mode.MigrationDecisionTests.test_marker_without_label_blocks_with_the_label_message) ... ok\ntest_mid_sentence_marker_in_prose_is_not_a_directive (test_migration_mode.MigrationDecisionTests.test_mid_sentence_marker_in_prose_is_not_a_directive) ... ok\ntest_this_tasks_own_impl_pr_body_does_not_enter_or_error (test_migration_mode.MigrationDecisionTests.test_this_tasks_own_impl_pr_body_does_not_enter_or_error) ... ok\ntest_already_applied_migration_blocks (test_migration_mode.MigrationGateTests.test_already_applied_migration_blocks) ... ok\ntest_conforming_pr_passes (test_migration_mode.MigrationGateTests.test_conforming_pr_passes) ... ok\ntest_declared_id_absent_from_base_registry_blocks (test_migration_mode.MigrationGateTests.test_declared_id_absent_from_base_registry_blocks) ... ok\ntest_destructive_migration_blocks (test_migration_mode.MigrationGateTests.test_destructive_migration_blocks) ... ok\ntest_duplicated_migration_line_blocks (test_migration_mode.MigrationGateTests.test_duplicated_migration_line_blocks) ... ok\ntest_extra_changed_file_blocks_naming_the_path (test_migration_mode.MigrationGateTests.test_extra_changed_file_blocks_naming_the_path) ... ok\ntest_migration_whose_apply_raises_fails_closed (test_migration_mode.MigrationGateTests.test_migration_whose_apply_raises_fails_closed) ... ok\ntest_missing_base_snapshot_blocks (test_migration_mode.MigrationGateTests.test_missing_base_snapshot_blocks) ... ok\ntest_missing_migration_line_blocks (test_migration_mode.MigrationGateTests.test_missing_migration_line_blocks) ... ok\ntest_missing_produced_file_blocks_naming_the_path (test_migration_mode.MigrationGateTests.test_missing_produced_file_blocks_naming_the_path) ... ok\ntest_non_shiki_file_blocks (test_migration_mode.MigrationGateTests.test_non_shiki_file_blocks) ... ok\ntest_single_changed_byte_blocks_naming_the_path (test_migration_mode.MigrationGateTests.test_single_changed_byte_blocks_naming_the_path) ... ok\n\n----------------------------------------------------------------------\nRan 25 tests in 0.256s\n\nOK\ntest_each_step_exports_default_branch_and_references_it (test_workflow_invocation_carry.EveryInvocationCarriesBothFlags.test_each_step_exports_default_branch_and_references_it) ... ok\ntest_enumerates_exactly_four_and_all_carry_both_flags (test_workflow_invocation_carry.EveryInvocationCarriesBothFlags.test_enumerates_exactly_four_and_all_carry_both_flags) ... ok\ntest_metadata_run_body_uses_shell_var_not_expression (test_workflow_invocation_carry.MergegateRunBodiesStayExpressionFree.test_metadata_run_body_uses_shell_var_not_expression) ... ok\ntest_no_base_fetch_carries_depth (test_workflow_invocation_carry.NoShallowOrSecondBaseFetch.test_no_base_fetch_carries_depth) ... ok\ntest_no_second_base_fetch_added (test_workflow_invocation_carry.NoShallowOrSecondBaseFetch.test_no_second_base_fetch_added) ... ok\ntest_dropping_either_flag_from_any_invocation_fails (test_workflow_invocation_carry.ValidateShikiGuardsEveryInvocation.test_dropping_either_flag_from_any_invocation_fails) ... ok\ntest_repo_workflows_satisfy_contract (test_workflow_invocation_carry.ValidateShikiGuardsEveryInvocation.test_repo_workflows_satisfy_contract) ... ok\n\n----------------------------------------------------------------------\nRan 7 tests in 0.032s\n\nOK\n\nexec\n/bin/zsh -lc \"rg -n 'def _git_show_base|def _path_proven_absent|def _path_is_regular_file|def _max_risk|def is_bookkeeping_closeout|bookkeeping_closeout =|carry_resolved_risk|if not task_changed or not lock_released|lockless|lock_terminal|exact_head_only|normal_frozen_spec_errors|exact_pr_link|spec_amendment|delivery_only' scripts/mergegate_check.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n925:def _max_risk(first: str, second: str) -> str:\n1980:def _path_proven_absent(path: Path) -> bool:\n1994:def _path_is_regular_file(path: Path) -> bool:\n2022:def is_bookkeeping_closeout(\n2237:        if not task_changed or not lock_released:\n2531:    carry_resolved_risk = \"low\" if bookkeeping_closeout else str((task or {}).get(\"risk_level\") or \"\")\n2543:        resolved_risk=carry_resolved_risk,\n3157:    bookkeeping_closeout = False\n3453:            bookkeeping_closeout = is_bookkeeping_closeout(\n3465:                bookkeeping_closeout = False\n\nexec\n/bin/zsh -lc \"rg -n -C 4 'isCrossRepository|mergeBaseOid|merge_base|git archive|git merge-base|gh pr view --json|git fetch origin|\\\\|\\\\| true' scripts/mergegate_check.py scripts/guardian_approval_signal.py .github/workflows/shiki-mergegate.yml .github/workflows/shiki-cca-completion.yml tests/test_*.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n.github/workflows/shiki-cca-completion.yml-54-        run: |\n.github/workflows/shiki-cca-completion.yml-55-          set -euo pipefail\n.github/workflows/shiki-cca-completion.yml-56-          rm -rf .shiki/gha\n.github/workflows/shiki-cca-completion.yml-57-          mkdir -p .shiki/gha\n.github/workflows/shiki-cca-completion.yml:58:          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n.github/workflows/shiki-cca-completion.yml-59-          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n.github/workflows/shiki-cca-completion.yml-60-          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n.github/workflows/shiki-cca-completion.yml-61-          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n.github/workflows/shiki-cca-completion.yml:62:          git fetch origin \"$base_ref\"\n.github/workflows/shiki-cca-completion.yml:63:          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n.github/workflows/shiki-cca-completion.yml:64:          printf '%s\\n' \"$merge_base\" > .shiki/gha/merge-base-oid.txt\n.github/workflows/shiki-cca-completion.yml:65:          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n.github/workflows/shiki-cca-completion.yml-66-            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n.github/workflows/shiki-cca-completion.yml-67-          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n.github/workflows/shiki-cca-completion.yml:68:          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n.github/workflows/shiki-cca-completion.yml:69:          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n.github/workflows/shiki-cca-completion.yml-70-\n.github/workflows/shiki-cca-completion.yml-71-          # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n.github/workflows/shiki-cca-completion.yml-72-          # mergegate job builds them below. The Guardian-approval signal must\n.github/workflows/shiki-cca-completion.yml-73-          # resolve a proven closeout as low risk exactly as the MergeGate policy\n--\n.github/workflows/shiki-cca-completion.yml-77-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n.github/workflows/shiki-cca-completion.yml-78-          # the base fetch would go shallow).\n.github/workflows/shiki-cca-completion.yml-79-          rm -rf .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml-80-          mkdir -p .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml:81:          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml-82-\n.github/workflows/shiki-cca-completion.yml-83-          # Implementation-PR merge proof: resolve the task id with the SAME\n.github/workflows/shiki-cca-completion.yml-84-          # semantics mergegate_check.py uses (first T-<4+ digits> OR\n.github/workflows/shiki-cca-completion.yml-85-          # T-<timestamp>-<hex>), read the BASE snapshot's expected_pr (a field the\n--\n.github/workflows/shiki-cca-completion.yml-87-          # only when gh proves it MERGED. Fails closed to \"\" on any missing or\n.github/workflows/shiki-cca-completion.yml-88-          # unresolvable input, so the exemption is never granted without proof.\n.github/workflows/shiki-cca-completion.yml-89-          MERGED_PRS=\"\"\n.github/workflows/shiki-cca-completion.yml-90-          body=\"$(jq -r '.body // \"\"' .shiki/gha/pr.json)\"\n.github/workflows/shiki-cca-completion.yml:91:          task_id=\"$(printf '%s' \"$body\" | grep -oE 'T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})' | head -1 || true)\"\n.github/workflows/shiki-cca-completion.yml-92-          base_task=\".shiki/gha/base-shiki/.shiki/tasks/${task_id}.json\"\n.github/workflows/shiki-cca-completion.yml-93-          if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n.github/workflows/shiki-cca-completion.yml-94-            base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n.github/workflows/shiki-cca-completion.yml-95-            if [ -n \"$base_pr\" ]; then\n.github/workflows/shiki-cca-completion.yml:96:              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n.github/workflows/shiki-cca-completion.yml-97-              if [ \"$state\" = \"MERGED\" ]; then\n.github/workflows/shiki-cca-completion.yml-98-                MERGED_PRS=\"$base_pr\"\n.github/workflows/shiki-cca-completion.yml-99-              fi\n.github/workflows/shiki-cca-completion.yml-100-            fi\n--\n.github/workflows/shiki-cca-completion.yml-121-            jq -n '{applies: false, reason: \"contract approval does not apply: no Shiki task id in the PR body\"}' > \"$approval_file\"\n.github/workflows/shiki-cca-completion.yml-122-          elif [ -z \"$ca_base_ref\" ]; then\n.github/workflows/shiki-cca-completion.yml-123-            jq -n --arg t \"$task_id\" '{task_id: $t, applies: false, reason: \"contract approval does not apply: no base ref resolved\"}' > \"$approval_file\"\n.github/workflows/shiki-cca-completion.yml-124-          else\n.github/workflows/shiki-cca-completion.yml:125:            adding_commit=\"$(git log --diff-filter=A --format=%H \"origin/${ca_base_ref}\" -- \".shiki/tasks/${task_id}.json\" 2>/dev/null | tail -1 || true)\"\n.github/workflows/shiki-cca-completion.yml-126-            if [ -z \"$adding_commit\" ]; then\n.github/workflows/shiki-cca-completion.yml-127-              jq -n --arg t \"$task_id\" '{task_id: $t, merged: false, applies: false, reason: \"contract approval does not apply: no commit adds this task file on the base branch (a first-registration PR is never covered)\"}' > \"$approval_file\"\n.github/workflows/shiki-cca-completion.yml-128-            else\n.github/workflows/shiki-cca-completion.yml-129-              pulls=\"$(gh api \"repos/${REPOSITORY}/commits/${adding_commit}/pulls\" 2>/dev/null || echo \"[]\")\"\n--\n.github/workflows/shiki-cca-completion.yml-225-          deadline=$((SECONDS + WAIT_TIMEOUT_SECONDS))\n.github/workflows/shiki-cca-completion.yml-226-          while true; do\n.github/workflows/shiki-cca-completion.yml-227-            gh pr view \"$PR_NUMBER\" \\\n.github/workflows/shiki-cca-completion.yml-228-              --repo \"$REPOSITORY\" \\\n.github/workflows/shiki-cca-completion.yml:229:              --json headRefOid,statusCheckRollup,isCrossRepository \\\n.github/workflows/shiki-cca-completion.yml-230-              > .shiki/gha/pr-settling.json\n.github/workflows/shiki-cca-completion.yml-231-\n.github/workflows/shiki-cca-completion.yml-232-            observed_head=\"$(jq -r '.headRefOid // \"\"' .shiki/gha/pr-settling.json)\"\n.github/workflows/shiki-cca-completion.yml-233-            if [ \"$observed_head\" != \"$EXPECTED_HEAD_SHA\" ]; then\n--\n.github/workflows/shiki-cca-completion.yml-274-          done\n.github/workflows/shiki-cca-completion.yml-275-\n.github/workflows/shiki-cca-completion.yml-276-          # The judge must consume the settled rollup, not the snapshot gathered at\n.github/workflows/shiki-cca-completion.yml-277-          # the beginning of this concurrently-triggered workflow run.\n.github/workflows/shiki-cca-completion.yml:278:          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n.github/workflows/shiki-cca-completion.yml-279-          gh pr view \"$PR_NUMBER\" \\\n.github/workflows/shiki-cca-completion.yml-280-            --repo \"$REPOSITORY\" \\\n.github/workflows/shiki-cca-completion.yml:281:            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n.github/workflows/shiki-cca-completion.yml:282:          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n.github/workflows/shiki-cca-completion.yml-283-            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n.github/workflows/shiki-cca-completion.yml-284-          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n.github/workflows/shiki-cca-completion.yml-285-\n.github/workflows/shiki-cca-completion.yml-286-      - name: Prepare CCA transport schema\n--\n.github/workflows/shiki-cca-completion.yml-586-          # a PR #179 failure mode). The comments feed was fetched above. Select on\n.github/workflows/shiki-cca-completion.yml-587-          # AUTHOR as well as the marker: only github-actions[bot]'s OWN status\n.github/workflows/shiki-cca-completion.yml-588-          # comment may be PATCHed, so a human who quotes the marker is never\n.github/workflows/shiki-cca-completion.yml-589-          # overwritten; when no bot comment exists a fresh one is posted below.\n.github/workflows/shiki-cca-completion.yml:590:          existing_id=\"$(jq -r 'map(select(((.body // \"\") | contains(\"<!-- shiki:guardian-status -->\")) and ((.user.login // \"\") == \"github-actions[bot]\"))) | (.[0].id // empty)' .shiki/gha/live-guardian-comments.json 2>/dev/null || true)\"\n.github/workflows/shiki-cca-completion.yml-591-          if [ -n \"${existing_id:-}\" ]; then\n.github/workflows/shiki-cca-completion.yml-592-            # Read the markdown as a raw string into the request body so nothing in\n.github/workflows/shiki-cca-completion.yml-593-            # the comment is misinterpreted as a typed field.\n.github/workflows/shiki-cca-completion.yml-594-            jq -n --rawfile body .shiki/gha/guardian-status-comment.md '{body: $body}' \\\n--\n.github/workflows/shiki-cca-completion.yml-639-          if gh api \"repos/${REPOSITORY}/actions/permissions/workflow\" > .shiki/gha/workflow-permissions.json 2> .shiki/gha/workflow-permissions.err; then\n.github/workflows/shiki-cca-completion.yml-640-            cat .shiki/gha/workflow-permissions.json\n.github/workflows/shiki-cca-completion.yml-641-          else\n.github/workflows/shiki-cca-completion.yml-642-            echo \"::warning::Could not introspect repository workflow permissions with this token.\"\n.github/workflows/shiki-cca-completion.yml:643:            cat .shiki/gha/workflow-permissions.err || true\n.github/workflows/shiki-cca-completion.yml-644-          fi\n.github/workflows/shiki-cca-completion.yml-645-          echo \"::endgroup::\"\n.github/workflows/shiki-cca-completion.yml-646-\n.github/workflows/shiki-cca-completion.yml-647-          gh api \"repos/${REPOSITORY}/pulls/${PR_NUMBER}/reviews\" > .shiki/gha/reviews-before-bridge.json\n--\n.github/workflows/shiki-cca-completion.yml-684-                --data @.shiki/gha/create-review-request.json\n.github/workflows/shiki-cca-completion.yml-685-            )\"\n.github/workflows/shiki-cca-completion.yml-686-\n.github/workflows/shiki-cca-completion.yml-687-            echo \"REST create review HTTP status: ${http_status}\"\n.github/workflows/shiki-cca-completion.yml:688:            cat .shiki/gha/create-review-response.json || true\n.github/workflows/shiki-cca-completion.yml-689-\n.github/workflows/shiki-cca-completion.yml-690-            case \"$http_status\" in\n.github/workflows/shiki-cca-completion.yml-691-              200|201)\n.github/workflows/shiki-cca-completion.yml-692-                echo \"CCA Review Bridge REST approval submitted.\"\n--\n.github/workflows/shiki-cca-completion.yml-717-            echo \"::error::CCA Review Bridge did not produce a visible APPROVED review.\"\n.github/workflows/shiki-cca-completion.yml-718-            exit 1\n.github/workflows/shiki-cca-completion.yml-719-          fi\n.github/workflows/shiki-cca-completion.yml-720-\n.github/workflows/shiki-cca-completion.yml:721:          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n.github/workflows/shiki-cca-completion.yml-722-          gh pr view \"$PR_NUMBER\" \\\n.github/workflows/shiki-cca-completion.yml-723-            --repo \"$REPOSITORY\" \\\n.github/workflows/shiki-cca-completion.yml:724:            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n.github/workflows/shiki-cca-completion.yml-725-            > .shiki/gha/pr-refresh.json\n.github/workflows/shiki-cca-completion.yml:726:          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n.github/workflows/shiki-cca-completion.yml-727-            .shiki/gha/pr-refresh.json > .shiki/gha/pr.json\n.github/workflows/shiki-cca-completion.yml-728-          rm .shiki/gha/pr-refresh.json\n.github/workflows/shiki-cca-completion.yml-729-\n.github/workflows/shiki-cca-completion.yml-730-          echo \"::endgroup::\"\n--\n.github/workflows/shiki-cca-completion.yml-797-          rm -rf .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml-798-          mkdir -p .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml-799-\n.github/workflows/shiki-cca-completion.yml-800-          gh pr view \"$PR_NUMBER\" \\\n.github/workflows/shiki-cca-completion.yml:801:            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n.github/workflows/shiki-cca-completion.yml-802-            > .shiki/gha/live-pr.json\n.github/workflows/shiki-cca-completion.yml-803-\n.github/workflows/shiki-cca-completion.yml-804-          gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/comments\" \\\n.github/workflows/shiki-cca-completion.yml-805-            > .shiki/gha/live-guardian-comments.json\n--\n.github/workflows/shiki-cca-completion.yml-812-            > .shiki/gha/live-guardian-timeline.json\n.github/workflows/shiki-cca-completion.yml-813-\n.github/workflows/shiki-cca-completion.yml-814-          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/live-pr.json)\"\n.github/workflows/shiki-cca-completion.yml-815-          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/live-pr.json)\"\n.github/workflows/shiki-cca-completion.yml:816:          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n.github/workflows/shiki-cca-completion.yml-817-\n.github/workflows/shiki-cca-completion.yml:818:          git fetch origin \"$base_ref\"\n.github/workflows/shiki-cca-completion.yml-819-\n.github/workflows/shiki-cca-completion.yml:820:          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n.github/workflows/shiki-cca-completion.yml-821-            .shiki/gha/live-pr.json > .shiki/gha/live-pr-with-merge-base.json\n.github/workflows/shiki-cca-completion.yml-822-          mv .shiki/gha/live-pr-with-merge-base.json .shiki/gha/live-pr.json\n.github/workflows/shiki-cca-completion.yml-823-\n.github/workflows/shiki-cca-completion.yml:824:          git diff --name-only \"${merge_base}...HEAD\" \\\n.github/workflows/shiki-cca-completion.yml-825-            > .shiki/gha/live-changed-files.txt\n.github/workflows/shiki-cca-completion.yml-826-\n.github/workflows/shiki-cca-completion.yml:827:          git diff --name-status \"${merge_base}...HEAD\" \\\n.github/workflows/shiki-cca-completion.yml-828-            > .shiki/gha/live-changed-files-status.txt\n.github/workflows/shiki-cca-completion.yml-829-\n.github/workflows/shiki-cca-completion.yml:830:          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml-831-\n.github/workflows/shiki-cca-completion.yml-832-          current_head=\"$(git rev-parse HEAD)\"\n.github/workflows/shiki-cca-completion.yml-833-          if [ \"$head_oid\" != \"$current_head\" ]; then\n.github/workflows/shiki-cca-completion.yml-834-            echo \"::error::Live PR headRefOid ${head_oid} does not match checked out HEAD ${current_head}\"\n--\n.github/workflows/shiki-cca-completion.yml-864-          # referenced PR merged \u2014 so hoisting the resolution out of the marker\n.github/workflows/shiki-cca-completion.yml-865-          # test is additive and changes no existing behaviour.\n.github/workflows/shiki-cca-completion.yml-866-          MERGED_PRS=\"\"\n.github/workflows/shiki-cca-completion.yml-867-          body=\"$(jq -r '.body // \"\"' .shiki/gha/live-pr.json)\"\n.github/workflows/shiki-cca-completion.yml:868:          task_id=\"$(printf '%s' \"$body\" | grep -oE 'T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})' | head -1 || true)\"\n.github/workflows/shiki-cca-completion.yml-869-          base_task=\".shiki/gha/base-shiki/.shiki/tasks/${task_id}.json\"\n.github/workflows/shiki-cca-completion.yml-870-          if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n.github/workflows/shiki-cca-completion.yml-871-            base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n.github/workflows/shiki-cca-completion.yml-872-            if [ -n \"$base_pr\" ]; then\n.github/workflows/shiki-cca-completion.yml:873:              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n.github/workflows/shiki-cca-completion.yml-874-              if [ \"$state\" = \"MERGED\" ]; then\n.github/workflows/shiki-cca-completion.yml-875-                MERGED_PRS=\"$base_pr\"\n.github/workflows/shiki-cca-completion.yml-876-              fi\n.github/workflows/shiki-cca-completion.yml-877-            fi\n--\n.github/workflows/shiki-cca-completion.yml-892-            jq -n '{applies: false, reason: \"contract approval does not apply: no Shiki task id in the PR body\"}' > \"$approval_file\"\n.github/workflows/shiki-cca-completion.yml-893-          elif [ -z \"$ca_base_ref\" ]; then\n.github/workflows/shiki-cca-completion.yml-894-            jq -n --arg t \"$task_id\" '{task_id: $t, applies: false, reason: \"contract approval does not apply: no base ref resolved\"}' > \"$approval_file\"\n.github/workflows/shiki-cca-completion.yml-895-          else\n.github/workflows/shiki-cca-completion.yml:896:            adding_commit=\"$(git log --diff-filter=A --format=%H \"origin/${ca_base_ref}\" -- \".shiki/tasks/${task_id}.json\" 2>/dev/null | tail -1 || true)\"\n.github/workflows/shiki-cca-completion.yml-897-            if [ -z \"$adding_commit\" ]; then\n.github/workflows/shiki-cca-completion.yml-898-              jq -n --arg t \"$task_id\" '{task_id: $t, merged: false, applies: false, reason: \"contract approval does not apply: no commit adds this task file on the base branch (a first-registration PR is never covered)\"}' > \"$approval_file\"\n.github/workflows/shiki-cca-completion.yml-899-            else\n.github/workflows/shiki-cca-completion.yml-900-              pulls=\"$(gh api \"repos/${REPOSITORY}/commits/${adding_commit}/pulls\" 2>/dev/null || echo \"[]\")\"\n--\n.github/workflows/shiki-mergegate.yml-51-        run: |\n.github/workflows/shiki-mergegate.yml-52-          set -euo pipefail\n.github/workflows/shiki-mergegate.yml-53-          rm -rf .shiki/gha\n.github/workflows/shiki-mergegate.yml-54-          mkdir -p .shiki/gha\n.github/workflows/shiki-mergegate.yml:55:          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n.github/workflows/shiki-mergegate.yml-56-          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n.github/workflows/shiki-mergegate.yml-57-          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n.github/workflows/shiki-mergegate.yml-58-          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n.github/workflows/shiki-mergegate.yml:59:          git fetch origin \"$base_ref\"\n.github/workflows/shiki-mergegate.yml:60:          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n.github/workflows/shiki-mergegate.yml:61:          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n.github/workflows/shiki-mergegate.yml-62-            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n.github/workflows/shiki-mergegate.yml-63-          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n.github/workflows/shiki-mergegate.yml:64:          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n.github/workflows/shiki-mergegate.yml:65:          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n.github/workflows/shiki-mergegate.yml-66-\n.github/workflows/shiki-mergegate.yml-67-          # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n.github/workflows/shiki-mergegate.yml-68-          # Guardian gate from the Goal's frozen-plan risk in THIS metadata job,\n.github/workflows/shiki-mergegate.yml-69-          # independent of any CCA verdict, so the check needs the same live\n--\n.github/workflows/shiki-mergegate.yml-84-          # The fetch above already ran \u2014 do NOT add a second, or the base fetch\n.github/workflows/shiki-mergegate.yml-85-          # would go shallow.\n.github/workflows/shiki-mergegate.yml-86-          rm -rf .shiki/gha/base-shiki\n.github/workflows/shiki-mergegate.yml-87-          mkdir -p .shiki/gha/base-shiki\n.github/workflows/shiki-mergegate.yml:88:          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n.github/workflows/shiki-mergegate.yml-89-\n.github/workflows/shiki-mergegate.yml-90-      - name: MergeGate metadata check\n.github/workflows/shiki-mergegate.yml-91-        env:\n.github/workflows/shiki-mergegate.yml-92-          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}\n--\n.github/workflows/shiki-mergegate.yml-104-          # post_merge_reconcile PR consumes this to prove its referenced PR\n.github/workflows/shiki-mergegate.yml-105-          # merged; without it the metadata check can never pass one.\n.github/workflows/shiki-mergegate.yml-106-          MERGED_PRS=\"\"\n.github/workflows/shiki-mergegate.yml-107-          body=\"$(jq -r '.body // \"\"' .shiki/gha/pr.json 2>/dev/null || echo \"\")\"\n.github/workflows/shiki-mergegate.yml:108:          task_id=\"$(printf '%s' \"$body\" | grep -oE 'T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})' | head -1 || true)\"\n.github/workflows/shiki-mergegate.yml-109-          base_task=\".shiki/gha/base-shiki/.shiki/tasks/${task_id}.json\"\n.github/workflows/shiki-mergegate.yml-110-          if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n.github/workflows/shiki-mergegate.yml-111-            base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n.github/workflows/shiki-mergegate.yml-112-            if [ -n \"$base_pr\" ]; then\n.github/workflows/shiki-mergegate.yml:113:              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n.github/workflows/shiki-mergegate.yml-114-              if [ \"$state\" = \"MERGED\" ]; then\n.github/workflows/shiki-mergegate.yml-115-                MERGED_PRS=\"$base_pr\"\n.github/workflows/shiki-mergegate.yml-116-              fi\n.github/workflows/shiki-mergegate.yml-117-            fi\n--\n.github/workflows/shiki-mergegate.yml-132-            jq -n '{applies: false, reason: \"contract approval does not apply: no Shiki task id in the PR body\"}' > \"$approval_file\"\n.github/workflows/shiki-mergegate.yml-133-          elif [ -z \"$ca_base_ref\" ]; then\n.github/workflows/shiki-mergegate.yml-134-            jq -n --arg t \"$task_id\" '{task_id: $t, applies: false, reason: \"contract approval does not apply: no base ref resolved\"}' > \"$approval_file\"\n.github/workflows/shiki-mergegate.yml-135-          else\n.github/workflows/shiki-mergegate.yml:136:            adding_commit=\"$(git log --diff-filter=A --format=%H \"origin/${ca_base_ref}\" -- \".shiki/tasks/${task_id}.json\" 2>/dev/null | tail -1 || true)\"\n.github/workflows/shiki-mergegate.yml-137-            if [ -z \"$adding_commit\" ]; then\n.github/workflows/shiki-mergegate.yml-138-              jq -n --arg t \"$task_id\" '{task_id: $t, merged: false, applies: false, reason: \"contract approval does not apply: no commit adds this task file on the base branch (a first-registration PR is never covered)\"}' > \"$approval_file\"\n.github/workflows/shiki-mergegate.yml-139-            else\n.github/workflows/shiki-mergegate.yml-140-              pulls=\"$(gh api \"repos/${REPOSITORY}/commits/${adding_commit}/pulls\" 2>/dev/null || echo \"[]\")\"\n--\ntests/test_closeout.py-123-        _git(self.target, \"update-ref\", \"refs/remotes/origin/main\", \"HEAD\")\ntests/test_closeout.py-124-\ntests/test_closeout.py-125-    def use_github_fetch_url(self) -> None:\ntests/test_closeout.py-126-        # After main is established, mirror the production runtime: a GitHub fetch\ntests/test_closeout.py:127:        # URL (so require_github_first_target passes and `git fetch origin main` is\ntests/test_closeout.py-128-        # a harmless no-op) and the bare repo as the push URL (so real pushes work).\ntests/test_closeout.py-129-        _git(self.target, \"remote\", \"set-url\", \"origin\", \"https://github.com/o/r.git\")\ntests/test_closeout.py-130-        _git(self.target, \"remote\", \"set-url\", \"--push\", \"origin\", str(self.remote))\ntests/test_closeout.py-131-\n--\ntests/test_closeout.py-483-    repositories were fast-forward-only. Driven against a REAL repo + bare origin,\ntests/test_closeout.py-484-    twice in a row, plus the exceptional genuinely-differing-file case.\"\"\"\ntests/test_closeout.py-485-\ntests/test_closeout.py-486-    def _merge_origin_main(self, env: CloseoutEnv):\ntests/test_closeout.py:487:        # `git fetch origin` hits the fake GitHub URL and no-ops; the authoritative\ntests/test_closeout.py-488-        # origin/main tracking ref is set directly by the closeout simulation below.\ntests/test_closeout.py-489-        _git(env.target, \"fetch\", \"origin\", check=False)\ntests/test_closeout.py-490-        return _git(env.target, \"merge\", \"origin/main\", check=False)\ntests/test_closeout.py-491-\n--\ntests/test_cca_head_sha_injection.py-245-    def test_neither_base_fetch_is_shallow(self):\ntests/test_cca_head_sha_injection.py-246-        fetch_lines = [\ntests/test_cca_head_sha_injection.py-247-            line\ntests/test_cca_head_sha_injection.py-248-            for line in self.text.splitlines()\ntests/test_cca_head_sha_injection.py:249:            if 'git fetch origin \"$base_ref\"' in line\ntests/test_cca_head_sha_injection.py-250-        ]\ntests/test_cca_head_sha_injection.py-251-        self.assertEqual(\ntests/test_cca_head_sha_injection.py-252-            len(fetch_lines),\ntests/test_cca_head_sha_injection.py-253-            2,\n--\ntests/test_dispatch_state_sync.py-484-    return target, remote\ntests/test_dispatch_state_sync.py-485-\ntests/test_dispatch_state_sync.py-486-\ntests/test_dispatch_state_sync.py-487-def _use_github_fetch_url(target: Path, remote: Path) -> None:\ntests/test_dispatch_state_sync.py:488:    # Mirror the production runtime: a GitHub fetch URL (so `git fetch origin main`\ntests/test_dispatch_state_sync.py-489-    # is a harmless no-op) with the bare repo as the push URL (so real pushes work).\ntests/test_dispatch_state_sync.py-490-    _git(target, \"remote\", \"set-url\", \"origin\", \"https://github.com/o/r.git\")\ntests/test_dispatch_state_sync.py-491-    _git(target, \"remote\", \"set-url\", \"--push\", \"origin\", str(remote))\ntests/test_dispatch_state_sync.py-492-\n--\nscripts/mergegate_check.py-1279-    \"\"\"Every id declared on its own ``Migration: <id>`` line in the PR body.\"\"\"\nscripts/mergegate_check.py-1280-    return [match.group(1) for match in _MIGRATION_ID_LINE.finditer(body)]\nscripts/mergegate_check.py-1281-\nscripts/mergegate_check.py-1282-\nscripts/mergegate_check.py:1283:def _git_show_merge_base(target: Path, merge_base_oid: str, repo_path: str) -> str:\nscripts/mergegate_check.py-1284-    \"\"\"Read ``repo_path`` from one immutable, workflow-resolved merge base.\"\"\"\nscripts/mergegate_check.py:1285:    if not re.fullmatch(r\"[0-9a-fA-F]{40,64}\", merge_base_oid):\nscripts/mergegate_check.py:1286:        raise FileNotFoundError(f\"invalid immutable mergeBaseOid {merge_base_oid!r}\")\nscripts/mergegate_check.py-1287-    result = subprocess.run(\nscripts/mergegate_check.py:1288:        [\"git\", \"show\", f\"{merge_base_oid}:{repo_path}\"],\nscripts/mergegate_check.py-1289-        cwd=str(target),\nscripts/mergegate_check.py-1290-        capture_output=True,\nscripts/mergegate_check.py-1291-        text=True,\nscripts/mergegate_check.py-1292-    )\nscripts/mergegate_check.py-1293-    if result.returncode == 0:\nscripts/mergegate_check.py-1294-        return result.stdout\nscripts/mergegate_check.py-1295-    detail = result.stderr.strip() or \"not found\"\nscripts/mergegate_check.py-1296-    raise FileNotFoundError(\nscripts/mergegate_check.py:1297:        f\"could not read {repo_path} from immutable mergeBaseOid {merge_base_oid} ({detail})\"\nscripts/mergegate_check.py-1298-    )\nscripts/mergegate_check.py-1299-\nscripts/mergegate_check.py-1300-\nscripts/mergegate_check.py:1301:def resolve_base_migration_registry(target: Path, merge_base_oid: str) -> tuple[Any, ...]:\nscripts/mergegate_check.py-1302-    \"\"\"Resolve the migration registry from the immutable merge base's\nscripts/mergegate_check.py-1303-    ``scripts/shiki_migrations.py`` \u2014 NEVER the PR head's.\nscripts/mergegate_check.py-1304-\nscripts/mergegate_check.py-1305-    THIS IS THE SECURITY PROPERTY OF MIGRATION MODE. Do NOT simplify this to import\n--\nscripts/mergegate_check.py-1317-    the MergeGate workflow already performs) and executed in an isolated namespace;\nscripts/mergegate_check.py-1318-    its only external dependency (``shiki_process``) resolves from ``scripts/`` on\nscripts/mergegate_check.py-1319-    ``sys.path``, infrastructure that carries no migration logic.\nscripts/mergegate_check.py-1320-    \"\"\"\nscripts/mergegate_check.py:1321:    source = _git_show_merge_base(target, merge_base_oid, \"scripts/shiki_migrations.py\")\nscripts/mergegate_check.py-1322-    # Execute the base source as a REAL module object registered in sys.modules:\nscripts/mergegate_check.py-1323-    # dataclass field resolution (Python 3.12+) looks the class's module up there,\nscripts/mergegate_check.py-1324-    # so a plain-dict namespace would fail on the module's ``@dataclass Migration``.\nscripts/mergegate_check.py-1325-    # ``shiki_process`` \u2014 the module's only external dependency \u2014 resolves from\n--\nscripts/mergegate_check.py-1396-        return\nscripts/mergegate_check.py-1397-    migration_id = ids[0]\nscripts/mergegate_check.py-1398-\nscripts/mergegate_check.py-1399-    if base_registry is None:\nscripts/mergegate_check.py:1400:        merge_base_oid = str(pr.get(\"mergeBaseOid\") or \"\")\nscripts/mergegate_check.py:1401:        if not merge_base_oid:\nscripts/mergegate_check.py-1402-            blocking.append(\nscripts/mergegate_check.py:1403:                \"migration mode requires the workflow-resolved immutable mergeBaseOid \"\nscripts/mergegate_check.py-1404-                \"to resolve the trusted migration registry\"\nscripts/mergegate_check.py-1405-            )\nscripts/mergegate_check.py-1406-            return\nscripts/mergegate_check.py-1407-        try:\nscripts/mergegate_check.py:1408:            base_registry = resolve_base_migration_registry(target, merge_base_oid)\nscripts/mergegate_check.py-1409-        except Exception as error:  # noqa: BLE001 - resolving base code can fail many ways; fail closed with a named reason, never crash the gate\nscripts/mergegate_check.py-1410-            blocking.append(f\"migration mode could not resolve the immutable merge-base migration registry: {error}\")\nscripts/mergegate_check.py-1411-            return\nscripts/mergegate_check.py-1412-\n--\nscripts/mergegate_check.py-3459-                changed_files_status=files_status,\nscripts/mergegate_check.py-3460-                merged_pr_numbers=merged_prs,\nscripts/mergegate_check.py-3461-            )\nscripts/mergegate_check.py-3462-            # The exemption is same-repository only and fail-closed: the live PR\nscripts/mergegate_check.py:3463:            # snapshot must explicitly prove isCrossRepository=false.\nscripts/mergegate_check.py:3464:            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\nscripts/mergegate_check.py-3465-                bookkeeping_closeout = False\nscripts/mergegate_check.py-3466-\nscripts/mergegate_check.py-3467-            ledger_entries = load_ledger_entries(target, task, warnings, blocking)\nscripts/mergegate_check.py-3468-            if pr:\n--\nscripts/guardian_approval_signal.py-331-        merged_prs=args.merged_prs,\nscripts/guardian_approval_signal.py-332-    )\nscripts/guardian_approval_signal.py-333-    # A fork PR is never eligible for the SADR-0017 exemption. The snapshot must\nscripts/guardian_approval_signal.py-334-    # prove the negative; absent/null/anything-but-False fails closed.\nscripts/guardian_approval_signal.py:335:    if exemption and pr.get(\"isCrossRepository\") is not False:\nscripts/guardian_approval_signal.py-336-        exemption = False\nscripts/guardian_approval_signal.py-337-    # The risk that actually gates Guardian approval: \"low\" for a proven closeout,\nscripts/guardian_approval_signal.py-338-    # otherwise the real task risk. task_risk is still reported as risk_level for\nscripts/guardian_approval_signal.py-339-    # audit. PR labels may only ESCALATE the effective risk (a maintainer can force\n--\ntests/test_paired_invariants.py-854-\ntests/test_paired_invariants.py-855-    def test_every_pr_view_json_field_list_requests_fork_identity(self):\ntests/test_paired_invariants.py-856-        for workflow in self.WORKFLOWS:\ntests/test_paired_invariants.py-857-            field_lists = self._gh_pr_view_field_lists(read_text(workflow))\ntests/test_paired_invariants.py:858:            self.assertTrue(field_lists, f\"{workflow.name}: no gh pr view --json field list found\")\ntests/test_paired_invariants.py-859-            for fields in field_lists:\ntests/test_paired_invariants.py:860:                self.assertIn(\"isCrossRepository\", fields, f\"{workflow.name}: {sorted(fields)}\")\ntests/test_paired_invariants.py-861-\ntests/test_paired_invariants.py-862-    def test_both_exemption_sites_fail_closed_without_pr_short_circuit(self):\ntests/test_paired_invariants.py-863-        mergegate = read_text(SCRIPTS_DIR / \"mergegate_check.py\")\ntests/test_paired_invariants.py-864-        signal = read_text(SCRIPTS_DIR / \"guardian_approval_signal.py\")\ntests/test_paired_invariants.py-865-        self.assertIn(\ntests/test_paired_invariants.py:866:            'if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:',\ntests/test_paired_invariants.py-867-            mergegate,\ntests/test_paired_invariants.py-868-        )\ntests/test_paired_invariants.py-869-        self.assertNotIn(\"bookkeeping_closeout and pr and pr.get\", mergegate)\ntests/test_paired_invariants.py-870-        self.assertIn(\ntests/test_paired_invariants.py:871:            'if exemption and pr.get(\"isCrossRepository\") is not False:',\ntests/test_paired_invariants.py-872-            signal,\ntests/test_paired_invariants.py-873-        )\ntests/test_paired_invariants.py-874-        self.assertNotIn(\"exemption and pr and pr.get\", signal)\ntests/test_paired_invariants.py-875-\n--\ntests/test_paired_invariants.py-877-        mergegate = read_text(SCRIPTS_DIR / \"mergegate_check.py\")\ntests/test_paired_invariants.py-878-        self.assertIsNone(re.search(r\"lockless|lock_terminal\", mergegate))\ntests/test_paired_invariants.py-879-        self.assertIn(\"if not task_changed or not lock_released:\", mergegate)\ntests/test_paired_invariants.py-880-\ntests/test_paired_invariants.py:881:    def test_workflows_archive_the_pinned_merge_base_without_swallowing_failure(self):\ntests/test_paired_invariants.py-882-        for workflow in self.WORKFLOWS:\ntests/test_paired_invariants.py-883-            text = read_text(workflow)\ntests/test_paired_invariants.py:884:            self.assertIn('git archive \"$merge_base\" .shiki', text, workflow.name)\ntests/test_paired_invariants.py:885:            self.assertNotIn('git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki || true', text)\ntests/test_paired_invariants.py:886:            self.assertIn(\"mergeBaseOid\", text, workflow.name)\ntests/test_paired_invariants.py-887-\ntests/test_paired_invariants.py-888-\ntests/test_paired_invariants.py-889-if __name__ == \"__main__\":\ntests/test_paired_invariants.py-890-    unittest.main()\n--\ntests/test_workflow_invocation_carry.py-210-    def test_no_second_base_fetch_added(self) -> None:\ntests/test_workflow_invocation_carry.py-211-        # The carry reuses each job's existing base fetch. The CCA workflow keeps\ntests/test_workflow_invocation_carry.py-212-        # exactly two (one per job); the MergeGate metadata workflow keeps exactly\ntests/test_workflow_invocation_carry.py-213-        # one. A second fetch would go shallow by default and defeat the proof.\ntests/test_workflow_invocation_carry.py:214:        self.assertEqual(CCA_WF.read_text(encoding=\"utf-8\").count(\"git fetch origin\"), 2)\ntests/test_workflow_invocation_carry.py:215:        self.assertEqual(MERGEGATE_WF.read_text(encoding=\"utf-8\").count(\"git fetch origin\"), 1)\ntests/test_workflow_invocation_carry.py-216-\ntests/test_workflow_invocation_carry.py-217-\ntests/test_workflow_invocation_carry.py-218-class ValidateShikiGuardsEveryInvocation(unittest.TestCase):\ntests/test_workflow_invocation_carry.py-219-    def test_repo_workflows_satisfy_contract(self) -> None:\n--\ntests/test_migration_mode.py-466-            # Base branch: the registry WITHOUT the attacker migration.\ntests/test_migration_mode.py-467-            (scripts / \"shiki_migrations.py\").write_text(_BASE_MIGRATIONS_SRC, encoding=\"utf-8\")\ntests/test_migration_mode.py-468-            _git(repo, \"add\", \"-A\")\ntests/test_migration_mode.py-469-            _git(repo, \"commit\", \"-qm\", \"base migrations\")\ntests/test_migration_mode.py:470:            merge_base_oid = subprocess.run(\ntests/test_migration_mode.py-471-                [\"git\", \"rev-parse\", \"HEAD\"], cwd=repo, check=True, capture_output=True, text=True\ntests/test_migration_mode.py-472-            ).stdout.strip()\ntests/test_migration_mode.py-473-            # Head branch: adds the attacker migration to its own registry.\ntests/test_migration_mode.py-474-            _git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\ntests/test_migration_mode.py-475-            (scripts / \"shiki_migrations.py\").write_text(_HEAD_MIGRATIONS_SRC, encoding=\"utf-8\")\ntests/test_migration_mode.py-476-            _git(repo, \"add\", \"-A\")\ntests/test_migration_mode.py-477-            _git(repo, \"commit\", \"-qm\", \"head adds a self-defining migration\")\ntests/test_migration_mode.py-478-\ntests/test_migration_mode.py:479:            registry = resolve_base_migration_registry(repo, merge_base_oid)\ntests/test_migration_mode.py-480-        ids = {str(migration.id) for migration in registry}\ntests/test_migration_mode.py-481-        self.assertIn(BASELINE, ids)\ntests/test_migration_mode.py-482-        self.assertNotIn(MIG_ID, ids, \"the head-defined migration must be absent from the base registry\")\ntests/test_migration_mode.py-483-\n--\ntests/test_migration_mode.py-490-            # A base .shiki snapshot for the gate to apply against.\ntests/test_migration_mode.py-491-            _seed_base(repo)\ntests/test_migration_mode.py-492-            _git(repo, \"add\", \"-A\")\ntests/test_migration_mode.py-493-            _git(repo, \"commit\", \"-qm\", \"base\")\ntests/test_migration_mode.py:494:            merge_base_oid = subprocess.run(\ntests/test_migration_mode.py-495-                [\"git\", \"rev-parse\", \"HEAD\"], cwd=repo, check=True, capture_output=True, text=True\ntests/test_migration_mode.py-496-            ).stdout.strip()\ntests/test_migration_mode.py-497-            _git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\ntests/test_migration_mode.py-498-            # The PR head both DEFINES the migration and delivers a .shiki change,\n--\ntests/test_migration_mode.py-506-                target=repo,\ntests/test_migration_mode.py-507-                pr={\ntests/test_migration_mode.py-508-                    \"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\ntests/test_migration_mode.py-509-                    \"baseRefName\": \"unresolvable-moving-ref\",\ntests/test_migration_mode.py:510:                    \"mergeBaseOid\": merge_base_oid,\ntests/test_migration_mode.py-511-                },\ntests/test_migration_mode.py-512-                base_shiki=repo / \".shiki\",\ntests/test_migration_mode.py-513-                changed_files_status=_conforming_changes(),\ntests/test_migration_mode.py-514-                blocking=blocking,\n--\ntests/test_migration_mode.py-518-            any(\"is not registered in the base branch\" in b and MIG_ID in b for b in blocking),\ntests/test_migration_mode.py-519-            blocking,\ntests/test_migration_mode.py-520-        )\ntests/test_migration_mode.py-521-\ntests/test_migration_mode.py:522:    def test_missing_immutable_merge_base_oid_blocks_registry_resolution(self) -> None:\ntests/test_migration_mode.py-523-        with tempfile.TemporaryDirectory() as tmp:\ntests/test_migration_mode.py-524-            repo = self._repo(tmp)\ntests/test_migration_mode.py-525-            scripts = repo / \"scripts\"\ntests/test_migration_mode.py-526-            scripts.mkdir()\n--\ntests/test_migration_mode.py-537-                blocking=blocking,\ntests/test_migration_mode.py-538-                warnings=[],\ntests/test_migration_mode.py-539-            )\ntests/test_migration_mode.py-540-        self.assertTrue(\ntests/test_migration_mode.py:541:            any(\"immutable mergeBaseOid\" in reason for reason in blocking),\ntests/test_migration_mode.py-542-            blocking,\ntests/test_migration_mode.py-543-        )\ntests/test_migration_mode.py-544-\ntests/test_migration_mode.py-545-\n--\ntests/test_sync_proof.py-523-        # by the read-only allowlist (below) and the orphan test, which observe\ntests/test_sync_proof.py-524-        # every subcommand actually spawned; here we forbid the two things a spy\ntests/test_sync_proof.py-525-        # cannot see: a swallowed failure and a smuggled network client import.\ntests/test_sync_proof.py-526-        src = Path(shiki_sync_proof.__file__).read_text(encoding=\"utf-8\")\ntests/test_sync_proof.py:527:        self.assertNotIn(\"|| true\", src)\ntests/test_sync_proof.py-528-        self.assertIsNone(re.search(r\"except\\s*:\", src), \"no bare except\")\ntests/test_sync_proof.py-529-        self.assertIsNone(\ntests/test_sync_proof.py-530-            re.search(r\"except[^\\n]*:\\s*(pass|continue)\\b\", src),\ntests/test_sync_proof.py-531-            \"no except that silently continues\",\n--\ntests/test_guardian_signal_closeout.py-229-            \"reviews\": [],\ntests/test_guardian_signal_closeout.py-230-            \"body\": (f\"Closeout {TASK_ID} for goal {GOAL_ID}.\" if body is _SENTINEL else body),\ntests/test_guardian_signal_closeout.py-231-        }\ntests/test_guardian_signal_closeout.py-232-        if self.is_cross_repository is not _SENTINEL:\ntests/test_guardian_signal_closeout.py:233:            pr[\"isCrossRepository\"] = self.is_cross_repository\ntests/test_guardian_signal_closeout.py-234-        path = self.target / \"pr.json\"\ntests/test_guardian_signal_closeout.py-235-        path.write_text(json.dumps(pr), encoding=\"utf-8\")\ntests/test_guardian_signal_closeout.py-236-        return path\ntests/test_guardian_signal_closeout.py-237-\n--\ntests/test_repair_delivery.py-97-        self._register_goal_on_main(risk=risk)\ntests/test_repair_delivery.py-98-\ntests/test_repair_delivery.py-99-        # Push URL is the bare remote (so the loop's real git push works); the\ntests/test_repair_delivery.py-100-        # fetch URL is GitHub-shaped so require_github_first_target passes and the\ntests/test_repair_delivery.py:101:        # closeout's `git fetch origin main` is a harmless no-op (the fake `pr\ntests/test_repair_delivery.py-102-        # merge` advances the origin/main tracking ref instead).\ntests/test_repair_delivery.py-103-        _git(self.target, \"remote\", \"set-url\", \"origin\", \"https://github.com/o/r.git\")\ntests/test_repair_delivery.py-104-        _git(self.target, \"remote\", \"set-url\", \"--push\", \"origin\", str(self.remote))\ntests/test_repair_delivery.py-105-\n\nexec\n/bin/zsh -lc 'git diff -- tests/test_bookkeeping_closeout.py tests/test_guardian_signal_closeout.py tests/test_paired_invariants.py tests/test_memory_scorecard.py tests/test_migration_mode.py' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex aceda0c..b412816 100644\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -140,13 +140,18 @@ class Closeout:\n         _write(self.base / \"locks\" / f\"{TASK_ID}.json\",\n                {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"active\", \"owner\": \"shiki-run\"})\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n-               {\"id\": GOAL_ID, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"high\"})\n+               {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n+                \"ledger_evidence\": []})\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n-        head_ledgers = [COMPLETION_LEDGER, PULL_LEDGER] if completes else [PULL_LEDGER]\n+        head_ledgers = (\n+            [\"L-base00001\", COMPLETION_LEDGER, PULL_LEDGER]\n+            if completes\n+            else [\"L-base00001\", PULL_LEDGER]\n+        )\n         self.head_task = self._task(status=\"done\", expected_pr=CLOSEOUT_PR, expected_branch=CLOSEOUT_BRANCH,\n-                                    ledger_evidence=head_ledgers, closeout_pr=CLOSEOUT_PR)\n+                                    ledger_evidence=head_ledgers)\n         _write(self.target / \".shiki\" / \"tasks\" / f\"{TASK_ID}.json\", self.head_task)\n         _write(self.target / \".shiki\" / \"locks\" / f\"{TASK_ID}.json\",\n                {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"released\", \"owner\": \"shiki-run\"})\n@@ -158,19 +163,40 @@ class Closeout:\n                 \"type\": \"completion\", \"summary\": \"goal complete\", \"evidence\": [REPORT_REL]})\n         _write(self.target / \".shiki\" / \"ledger\" / f\"{PULL_LEDGER}.json\",\n                {\"id\": PULL_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"lock\",\n-                \"summary\": \"closeout /pull\", \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"]})\n+                \"summary\": \"closeout /pull\", \"evidence\": [\n+                    f\".shiki/tasks/{TASK_ID}.json\",\n+                    f\".shiki/locks/{TASK_ID}.json\",\n+                ]})\n \n         if completes:\n             _write(self.target / \".shiki\" / \"dag\" / f\"{GOAL_ID}.json\",\n                    {\"goal_id\": GOAL_ID, \"nodes\": [TASK_ID], \"edges\": []})\n             _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n-                   {\"id\": GOAL_ID, \"status\": \"complete\", \"title\": \"g\", \"risk_level\": \"high\"})\n-            _write(self.target / REPORT_REL, {\"id\": REPORT_ID, \"goal_id\": GOAL_ID, \"status\": \"complete\"})\n+                   {\"id\": GOAL_ID, \"status\": \"complete\", \"title\": \"g\", \"risk_level\": \"high\",\n+                    \"ledger_evidence\": [COMPLETION_LEDGER]})\n+            _write(self.target / REPORT_REL, {\n+                \"id\": REPORT_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"status\": \"complete\",\n+                \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"],\n+                \"blocking_reasons\": [],\n+                \"mergegate\": {\n+                    \"dependencies\": \"pass\",\n+                    \"locks\": \"pass\",\n+                    \"checks\": \"pass\",\n+                    \"review\": \"recorded\",\n+                    \"ledger\": \"pass\",\n+                    \"risk\": \"high\",\n+                },\n+                \"scorecard\": {\n+                    \"goal_id\": GOAL_ID,\n+                    \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1},\n+                },\n+            })\n             self.changed = [\n                 cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                 cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n                 cf(\"M\", f\".shiki/goals/{GOAL_ID}.json\"),\n-                cf(\"A\", f\".shiki/worktrees/{TASK_ID}.json\"),\n                 cf(\"A\", REPORT_REL),\n                 cf(\"A\", f\".shiki/ledger/{COMPLETION_LEDGER}.json\"),\n                 cf(\"A\", f\".shiki/ledger/{PULL_LEDGER}.json\"),\n@@ -183,7 +209,8 @@ class Closeout:\n             _write(self.target / \".shiki\" / \"tasks\" / f\"{SIBLING_ID}.json\",\n                    {\"id\": SIBLING_ID, \"goal_id\": GOAL_ID, \"status\": \"running\", \"title\": \"s\"})\n             _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n-                   {\"id\": GOAL_ID, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"high\"})\n+                   {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n+                    \"ledger_evidence\": []})\n             self.changed = [\n                 cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                 cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n@@ -299,6 +326,25 @@ class BookkeepingCloseoutTest(unittest.TestCase):\n         # The single decision point still exempts it (no Guardian required).\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_strongest_goal_or_task_risk_matches_completion_report(self) -> None:\n+        c = self.new().build(completes=True)\n+        base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n+        base_task[\"risk_level\"] = \"critical\"\n+        _write(c.base / \"tasks\" / f\"{TASK_ID}.json\", base_task)\n+        c.head_task[\"risk_level\"] = \"critical\"\n+        c.rewrite_head_task()\n+        for goal_path in (\n+            c.base / \"goals\" / f\"{GOAL_ID}.json\",\n+            c.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n+        ):\n+            goal = json.loads(goal_path.read_text())\n+            goal[\"risk_level\"] = \"medium\"\n+            _write(goal_path, goal)\n+        report = json.loads((c.target / REPORT_REL).read_text())\n+        report[\"mergegate\"][\"risk\"] = \"critical\"\n+        _write(c.target / REPORT_REL, report)\n+        self.assertTrue(c.classify())\n+\n     # --- disqualifiers (each in isolation; assert the Guardian fallback) ---\n \n     def _assert_disqualified(self, c: Closeout) -> None:\n@@ -319,6 +365,39 @@ class BookkeepingCloseoutTest(unittest.TestCase):\n         c.changed.append(cf(\"D\", f\".shiki/ledger/{PULL_LEDGER}.json\"))\n         self._assert_disqualified(c)\n \n+    def test_renamed_shiki_path_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        c.changed[0] = cf(\"R\", f\".shiki/tasks/{TASK_ID}.json\", \".shiki/tasks/T-0000.json\")\n+        self._assert_disqualified(c)\n+\n+    def test_type_changed_shiki_path_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        c.changed[0] = cf(\"T\", f\".shiki/tasks/{TASK_ID}.json\")\n+        self._assert_disqualified(c)\n+\n+    def test_symlinked_task_file_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        task_path = c.target / \".shiki\" / \"tasks\" / f\"{TASK_ID}.json\"\n+        target_path = task_path.with_name(\"task-target.json\")\n+        target_path.write_bytes(task_path.read_bytes())\n+        task_path.unlink()\n+        task_path.symlink_to(target_path.name)\n+        self._assert_disqualified(c)\n+\n+    def test_coerced_expected_pr_disqualifies(self) -> None:\n+        for value in (True, str(IMPL_PR)):\n+            with self.subTest(value=value):\n+                c = self.new().build_noncompleting()\n+                base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n+                base_task[\"expected_pr\"] = value\n+                _write(c.base / \"tasks\" / f\"{TASK_ID}.json\", base_task)\n+                self._assert_disqualified(c)\n+\n+    def test_worktree_record_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        c.changed.append(cf(\"A\", f\".shiki/worktrees/{TASK_ID}.json\"))\n+        self._assert_disqualified(c)\n+\n     def test_file_outside_effective_locks_disqualifies(self) -> None:\n         # A .shiki ledger that is NOT in the task's ledger_evidence is under .shiki\n         # (condition 1 passes) but outside the derived mirror locks (condition 3).\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex a8bfaec..6424517 100644\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -108,6 +108,7 @@ class Scenario:\n         self.changed: list[ChangedFile] = []\n         self.head_task: dict = {}\n         self.labels: list[str] = []\n+        self.is_cross_repository = False\n \n     # --- construction -----------------------------------------------------\n \n@@ -127,12 +128,17 @@ class Scenario:\n         _write(self.base / \"locks\" / f\"{TASK_ID}.json\",\n                {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"active\", \"owner\": \"shiki-run\"})\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n-               {\"id\": GOAL_ID, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n-        head_ledgers = [COMPLETION_LEDGER, PULL_LEDGER] if completes else [PULL_LEDGER]\n+        head_ledgers = (\n+            [\"L-base00002\", COMPLETION_LEDGER, PULL_LEDGER]\n+            if completes\n+            else [\"L-base00002\", PULL_LEDGER]\n+        )\n         self.head_task = self._task(status=\"done\", expected_pr=CLOSEOUT_PR,\n-                                    ledger_evidence=head_ledgers, closeout_pr=CLOSEOUT_PR)\n+                                    ledger_evidence=head_ledgers)\n         self.rewrite_head_task()\n         _write(self.target / \".shiki\" / \"locks\" / f\"{TASK_ID}.json\",\n                {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"released\", \"owner\": \"shiki-run\"})\n@@ -141,19 +147,40 @@ class Scenario:\n                 \"type\": \"completion\", \"summary\": \"goal complete\", \"evidence\": [REPORT_REL]})\n         _write(self.target / \".shiki\" / \"ledger\" / f\"{PULL_LEDGER}.json\",\n                {\"id\": PULL_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"lock\",\n-                \"summary\": \"closeout /pull\", \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"]})\n+                \"summary\": \"closeout /pull\", \"evidence\": [\n+                    f\".shiki/tasks/{TASK_ID}.json\",\n+                    f\".shiki/locks/{TASK_ID}.json\",\n+                ]})\n \n         if completes:\n             _write(self.target / \".shiki\" / \"dag\" / f\"{GOAL_ID}.json\",\n                    {\"goal_id\": GOAL_ID, \"nodes\": [TASK_ID], \"edges\": []})\n             _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n-                   {\"id\": GOAL_ID, \"status\": \"complete\", \"title\": \"g\", \"risk_level\": \"critical\"})\n-            _write(self.target / REPORT_REL, {\"id\": REPORT_ID, \"goal_id\": GOAL_ID, \"status\": \"complete\"})\n+                   {\"id\": GOAL_ID, \"status\": \"complete\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                    \"ledger_evidence\": [COMPLETION_LEDGER]})\n+            _write(self.target / REPORT_REL, {\n+                \"id\": REPORT_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"status\": \"complete\",\n+                \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"],\n+                \"blocking_reasons\": [],\n+                \"mergegate\": {\n+                    \"dependencies\": \"pass\",\n+                    \"locks\": \"pass\",\n+                    \"checks\": \"pass\",\n+                    \"review\": \"recorded\",\n+                    \"ledger\": \"pass\",\n+                    \"risk\": \"critical\",\n+                },\n+                \"scorecard\": {\n+                    \"goal_id\": GOAL_ID,\n+                    \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1},\n+                },\n+            })\n             self.changed = [\n                 cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                 cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n                 cf(\"M\", f\".shiki/goals/{GOAL_ID}.json\"),\n-                cf(\"A\", f\".shiki/worktrees/{TASK_ID}.json\"),\n                 cf(\"A\", REPORT_REL),\n                 cf(\"A\", f\".shiki/ledger/{COMPLETION_LEDGER}.json\"),\n                 cf(\"A\", f\".shiki/ledger/{PULL_LEDGER}.json\"),\n@@ -164,7 +191,8 @@ class Scenario:\n             _write(self.target / \".shiki\" / \"tasks\" / f\"{SIBLING_ID}.json\",\n                    {\"id\": SIBLING_ID, \"goal_id\": GOAL_ID, \"status\": \"running\", \"title\": \"s\"})\n             _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n-                   {\"id\": GOAL_ID, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+                   {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                    \"ledger_evidence\": []})\n             self.changed = [\n                 cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                 cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n@@ -201,6 +229,8 @@ class Scenario:\n             \"reviews\": [],\n             \"body\": (f\"Closeout {TASK_ID} for goal {GOAL_ID}.\" if body is _SENTINEL else body),\n         }\n+        if self.is_cross_repository is not _SENTINEL:\n+            pr[\"isCrossRepository\"] = self.is_cross_repository\n         path = self.target / \"pr.json\"\n         path.write_text(json.dumps(pr), encoding=\"utf-8\")\n         return path\n@@ -336,6 +366,34 @@ class GuardianSignalCloseoutTest(unittest.TestCase):\n         s.merged = set()  # the base expected_pr (impl PR) is not proven merged\n         self._assert_disqualified(s)\n \n+    def test_cross_repository_pr_denies_exemption_and_uses_real_risk(self) -> None:\n+        s = self.new().build()\n+        self.assertTrue(s.classify(), \"the SADR-0017 shape itself should be proven\")\n+        s.is_cross_repository = True\n+        signal = s.run_signal()\n+        self.assertTrue(signal[\"required\"])\n+        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n+        self.assertEqual(signal[\"risk_level\"], \"critical\")\n+        self.assertEqual(\n+            sig._carry_resolved_risk(\n+                shiki_root=str(s.target),\n+                base_shiki=str(s.base),\n+                pr_body=f\"Closeout {TASK_ID} for goal {GOAL_ID}.\",\n+                task_risk=\"critical\",\n+                exemption=signal[\"bookkeeping_closeout_exemption\"],\n+            ),\n+            \"critical\",\n+        )\n+\n+    def test_absent_cross_repository_field_denies_exemption_and_uses_real_risk(self) -> None:\n+        s = self.new().build()\n+        self.assertTrue(s.classify(), \"the SADR-0017 shape itself should be proven\")\n+        s.is_cross_repository = _SENTINEL\n+        signal = s.run_signal()\n+        self.assertTrue(signal[\"required\"])\n+        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n+        self.assertEqual(signal[\"risk_level\"], \"critical\")\n+\n     # --- missing / unbuildable inputs fail closed -------------------------\n \n     def test_missing_base_snapshot_requires_guardian(self) -> None:\ndiff --git a/tests/test_memory_scorecard.py b/tests/test_memory_scorecard.py\nindex 7b0249e..8ec9670 100644\n--- a/tests/test_memory_scorecard.py\n+++ b/tests/test_memory_scorecard.py\n@@ -144,6 +144,38 @@ class GoalCompleteContractTests(unittest.TestCase):\n             self.assertIn(\"scorecard\", report)\n             self.assertEqual(report[\"scorecard\"][\"goal_id\"], GOAL_ID)\n \n+    def test_report_uses_strongest_goal_or_task_risk(self) -> None:\n+        import argparse\n+        import subprocess\n+        from shiki_tasks import cmd_goal_complete\n+\n+        with tempfile.TemporaryDirectory() as tmp:\n+            root = Path(tmp)\n+            _seed_goal(root)\n+            goal_path = root / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\"\n+            goal = json.loads(goal_path.read_text(encoding=\"utf-8\"))\n+            goal[\"risk_level\"] = \"medium\"\n+            goal_path.write_text(json.dumps(goal), encoding=\"utf-8\")\n+            task_id = \"T-20260613T000000000000Z-0000c002\"\n+            task = _task(root, task_id, status=\"done\")\n+            task[\"risk_level\"] = \"critical\"\n+            (root / \".shiki\" / \"tasks\" / f\"{task_id}.json\").write_text(\n+                json.dumps(task), encoding=\"utf-8\"\n+            )\n+            subprocess.run([\"git\", \"init\", \"-b\", \"main\"], cwd=root, capture_output=True, check=True)\n+            subprocess.run(\n+                [\"git\", \"remote\", \"add\", \"origin\", \"https://github.com/example/x.git\"],\n+                cwd=root,\n+                capture_output=True,\n+                check=True,\n+            )\n+            output = io.StringIO()\n+            with redirect_stdout(output):\n+                cmd_goal_complete(argparse.Namespace(target=str(root), goal_id=GOAL_ID, summary=None))\n+            report_path = Path(json.loads(output.getvalue())[\"report_file\"])\n+            report = json.loads(report_path.read_text(encoding=\"utf-8\"))\n+            self.assertEqual(report[\"mergegate\"][\"risk\"], \"critical\")\n+\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_migration_mode.py b/tests/test_migration_mode.py\nindex 1ce3a7d..caffbe6 100644\n--- a/tests/test_migration_mode.py\n+++ b/tests/test_migration_mode.py\n@@ -467,13 +467,16 @@ class BaseRegistryResolutionTests(unittest.TestCase):\n             (scripts / \"shiki_migrations.py\").write_text(_BASE_MIGRATIONS_SRC, encoding=\"utf-8\")\n             _git(repo, \"add\", \"-A\")\n             _git(repo, \"commit\", \"-qm\", \"base migrations\")\n+            merge_base_oid = subprocess.run(\n+                [\"git\", \"rev-parse\", \"HEAD\"], cwd=repo, check=True, capture_output=True, text=True\n+            ).stdout.strip()\n             # Head branch: adds the attacker migration to its own registry.\n             _git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n             (scripts / \"shiki_migrations.py\").write_text(_HEAD_MIGRATIONS_SRC, encoding=\"utf-8\")\n             _git(repo, \"add\", \"-A\")\n             _git(repo, \"commit\", \"-qm\", \"head adds a self-defining migration\")\n \n-            registry = resolve_base_migration_registry(repo, \"main\")\n+            registry = resolve_base_migration_registry(repo, merge_base_oid)\n         ids = {str(migration.id) for migration in registry}\n         self.assertIn(BASELINE, ids)\n         self.assertNotIn(MIG_ID, ids, \"the head-defined migration must be absent from the base registry\")\n@@ -488,6 +491,9 @@ class BaseRegistryResolutionTests(unittest.TestCase):\n             _seed_base(repo)\n             _git(repo, \"add\", \"-A\")\n             _git(repo, \"commit\", \"-qm\", \"base\")\n+            merge_base_oid = subprocess.run(\n+                [\"git\", \"rev-parse\", \"HEAD\"], cwd=repo, check=True, capture_output=True, text=True\n+            ).stdout.strip()\n             _git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n             # The PR head both DEFINES the migration and delivers a .shiki change,\n             # declaring the id it just invented.\n@@ -498,7 +504,11 @@ class BaseRegistryResolutionTests(unittest.TestCase):\n             # base_registry=None => the gate resolves it from base via git show.\n             enforce_migration(\n                 target=repo,\n-                pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+                pr={\n+                    \"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+                    \"baseRefName\": \"unresolvable-moving-ref\",\n+                    \"mergeBaseOid\": merge_base_oid,\n+                },\n                 base_shiki=repo / \".shiki\",\n                 changed_files_status=_conforming_changes(),\n                 blocking=blocking,\n@@ -509,6 +519,29 @@ class BaseRegistryResolutionTests(unittest.TestCase):\n             blocking,\n         )\n \n+    def test_missing_immutable_merge_base_oid_blocks_registry_resolution(self) -> None:\n+        with tempfile.TemporaryDirectory() as tmp:\n+            repo = self._repo(tmp)\n+            scripts = repo / \"scripts\"\n+            scripts.mkdir()\n+            (scripts / \"shiki_migrations.py\").write_text(_BASE_MIGRATIONS_SRC, encoding=\"utf-8\")\n+            _seed_base(repo)\n+            _git(repo, \"add\", \"-A\")\n+            _git(repo, \"commit\", \"-qm\", \"base\")\n+            blocking: list[str] = []\n+            enforce_migration(\n+                target=repo,\n+                pr={\"body\": f\"Migration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+                base_shiki=repo / \".shiki\",\n+                changed_files_status=_conforming_changes(),\n+                blocking=blocking,\n+                warnings=[],\n+            )\n+        self.assertTrue(\n+            any(\"immutable mergeBaseOid\" in reason for reason in blocking),\n+            blocking,\n+        )\n+\n \n if __name__ == \"__main__\":\n     unittest.main()\ndiff --git a/tests/test_paired_invariants.py b/tests/test_paired_invariants.py\nindex 3a022fc..256f146 100644\n--- a/tests/test_paired_invariants.py\n+++ b/tests/test_paired_invariants.py\n@@ -836,5 +836,55 @@ class Pair14SmokeStartRequired(PairInvariant):\n             self.assert_all_equal(sites, relation=\"set-equality\", fix=\"n/a\")\n \n \n+class Pair15ForkAndMergeBaseWorkflowEvidence(PairInvariant):\n+    WORKFLOWS = (\n+        WORKFLOWS_DIR / \"shiki-mergegate.yml\",\n+        WORKFLOWS_DIR / \"shiki-cca-completion.yml\",\n+    )\n+\n+    @staticmethod\n+    def _gh_pr_view_field_lists(text: str) -> list[set[str]]:\n+        fields: list[set[str]] = []\n+        for match in re.finditer(r'gh pr view\\s+\"\\$[^\\\"]+\"', text):\n+            command_tail = text[match.start() : match.start() + 500]\n+            json_match = re.search(r\"--json\\s+([^\\s\\\\]+)\", command_tail)\n+            if json_match:\n+                fields.append(set(json_match.group(1).split(\",\")))\n+        return fields\n+\n+    def test_every_pr_view_json_field_list_requests_fork_identity(self):\n+        for workflow in self.WORKFLOWS:\n+            field_lists = self._gh_pr_view_field_lists(read_text(workflow))\n+            self.assertTrue(field_lists, f\"{workflow.name}: no gh pr view --json field list found\")\n+            for fields in field_lists:\n+                self.assertIn(\"isCrossRepository\", fields, f\"{workflow.name}: {sorted(fields)}\")\n+\n+    def test_both_exemption_sites_fail_closed_without_pr_short_circuit(self):\n+        mergegate = read_text(SCRIPTS_DIR / \"mergegate_check.py\")\n+        signal = read_text(SCRIPTS_DIR / \"guardian_approval_signal.py\")\n+        self.assertIn(\n+            'if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:',\n+            mergegate,\n+        )\n+        self.assertNotIn(\"bookkeeping_closeout and pr and pr.get\", mergegate)\n+        self.assertIn(\n+            'if exemption and pr.get(\"isCrossRepository\") is not False:',\n+            signal,\n+        )\n+        self.assertNotIn(\"exemption and pr and pr.get\", signal)\n+\n+    def test_closeout_keeps_mandatory_lock_release_and_no_relaxation(self):\n+        mergegate = read_text(SCRIPTS_DIR / \"mergegate_check.py\")\n+        self.assertIsNone(re.search(r\"lockless|lock_terminal\", mergegate))\n+        self.assertIn(\"if not task_changed or not lock_released:\", mergegate)\n+\n+    def test_workflows_archive_the_pinned_merge_base_without_swallowing_failure(self):\n+        for workflow in self.WORKFLOWS:\n+            text = read_text(workflow)\n+            self.assertIn('git archive \"$merge_base\" .shiki', text, workflow.name)\n+            self.assertNotIn('git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki || true', text)\n+            self.assertIn(\"mergeBaseOid\", text, workflow.name)\n+\n+\n if __name__ == \"__main__\":\n     unittest.main()\n\nexec\n/bin/zsh -lc 'git diff -- docs/agents/checklists.md docs/agents/decision-control.md docs/agents/triage-labels.md docs/agents/implementation-policy.md docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md && git diff --check' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ndiff --git a/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex afa0bcf..db5a208 100644\n--- a/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -31,7 +31,7 @@ Mechanics (single-task and last-task-of-a-goal; see Consequences for multi-task)\n    - if `closeout_pr` is set \u2192 action `mark_done` (the closeout PR merged; completion is on main).\n 3. `create_closeout_pr` builds a normal-mode closeout PR in a **fresh worktree cut from `origin/main`** on the deterministic branch `shiki/<task>-closeout`: set `task.status=done`, lock `state=released`, and \u2014 only if this task makes every goal task `done` \u2014 run `cmd_goal_complete` (scorecard report + completion ledger + `goal.status=complete`) **in that worktree** so the coupling is satisfied on the HEAD. It then opens the PR, records the `/pull/N` self-reference ledger, and in the coordinator sets `task.closeout_pr` and repoints `task.expected_pr` (+ `expected_branch`) to the closeout PR so the existing snapshot/merge machinery drives it.\n 4. While `closeout_pr` is set and not merged, the loop gates on the closeout PR's required checks with **no repair path** (a bookkeeping PR has no implementation to repair): green \u2192 `merge`; a CCA same-head race \u2192 one `rerun_cca`; any genuine check failure \u2192 `stop_blocked` (recorded for diagnosis); else `wait_checks`.\n-5. Loop-executed tasks declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout stages (goal, dag, lock, report, ledgers), so `files_outside_locks` is satisfied without per-file lock bookkeeping.\n+5. MergeGate derives id-scoped mirror locks from the frozen task identity and unions them with the task's declared locks. Those derived locks cover every `.shiki` file a closeout may stage (task, the required existing lock release, goal, report, and ledgers), while the classifier still requires both `task_changed` and `lock_released` and rejects worktree-record changes.\n \n Re-entrancy: `create_closeout_pr` is gated on `not task.closeout_pr`, so it is reached only when no closeout PR has been recorded. The closeout branch name is deterministic, so an existing PR for it means a PRIOR run was interrupted mid-effector (after `gh pr create` but before recording `closeout_pr`) and its HEAD may be incomplete \u2014 missing the repointed `expected_pr` or the `/pull` self-reference ledger that MergeGate requires. Silently adopting such a PR would block MergeGate forever with no repair path (the closeout phase has no auto-repair). The effector therefore **fails closed to `stop_blocked` for a recorded operator reconcile** (verify/repair the existing PR's HEAD and set `task.closeout_pr`, or close it and re-run) rather than adopting a possibly-broken PR. No force-push or branch deletion is performed (the constitution forbids them without Guardian authorization). Safe automatic adoption \u2014 verifying the existing PR's HEAD carries `expected_pr` + the `/pull` ledger before reusing it \u2014 is a deferred enhancement.\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 0c5ec6c..b2b021a 100644\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -227,6 +227,8 @@ ledger entries.\n | MG-19 | Runtime registry names, config role assignments, task `assigned_runtime` values, docs, and install/staging coverage remain consistent. | blocking |\n | MG-20 | GitHub provider configuration validates provider kind, host, remote protocol, repo slug, canonical remote URL, API base URL, and legacy `.shiki/repo.json` compatibility. | blocking |\n | MG-21 | `.shiki` state classes classify changed paths, block unknown/runtime/cache/local-only paths, and preserve append-only-evidence rules. | blocking |\n+| MG-25 | Repository branch protection or a ruleset requires Shiki checks; otherwise the absent hard enforcement is recorded as a rollout blocker and the Goal is not claimed enforceable. | blocking for governance rollout completion |\n+| MG-26 | Migration mode has the exact marker, maintainer-applied label, one non-destructive migration ID registered on the immutable merge base, a snapshot archived from that same merge base, no changes outside `.shiki/**`, and a path/byte-exact re-run result; normal Task, CCA, review, and Guardian gates remain active. | blocking when applicable |\n \n ## GC \u2014 Goal Completion Checklist\n \ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 8c94ecd..97e1b86 100644\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -15,6 +15,7 @@ Core rule:\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\n+| Can a repository-wide mirror migration run? | Planner proposes; maintainer selects mode; MergeGate proves output | After the migration code and reviewed old-value inventory are already on the immutable merge base | Task/Goal contract, migration ID, immutable merge-base snapshot, exact `.shiki/**` diff | migration ready / blocked | `mergegate:migration`, merge-base registry re-run, CCA, review, Guardian policy |\n | Can work be split into issues? | Planner | After PRD or settled Goal | Acceptance criteria, non-goals, dependencies | Vertical-slice issues | `to-issues`, issue templates |\n | Is an issue AFK-ready or HITL? | Triager | Issue triage | Scope, decisions remaining, testability, risk | `ready-for-agent` or HITL/blocker label | triage labels, dispatch check |\n | Which runtime should execute? | Runtime Router | After issue is AFK-ready | Task type, risk, skills, repo constraints | Runtime assignment | task metadata, handoff schema |\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex ed66e23..707349c 100644\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -285,6 +285,13 @@ MergeGate allows merge only when:\n - PR metadata is complete.\n - No architecture gate remains unresolved.\n \n+For Migration mode, the implementation PR is a pure `.shiki/**` delivery. It\n+names one non-destructive migration already registered on the immutable merge\n+base, and MergeGate reproduces that migration from the `.shiki` snapshot archived\n+from the same pinned commit before comparing every changed path and byte. The\n+specialized scope proof does not waive Task, dependency, lock, CCA, review, or\n+Guardian requirements.\n+\n ## Phase 11 \u2014 Goal Completion Judgment\n \n After all task PRs merge, judge the parent Goal:\ndiff --git a/docs/agents/triage-labels.md b/docs/agents/triage-labels.md\nindex 6940778..fe8d566 100644\n--- a/docs/agents/triage-labels.md\n+++ b/docs/agents/triage-labels.md\n@@ -29,6 +29,10 @@ Target Repositories may customize labels, but the Shiki Template assumes these c\n - `mergegate:waiting` - merge evidence is incomplete.\n - `mergegate:blocked` - check, lock, dependency, risk, or review blocks merge.\n - `mergegate:ready` - MergeGate conditions are satisfied.\n+- `mergegate:migration` - a configured maintainer selected the repository-wide\n+  mirror migration route. The PR must still bind to a Task, CCA, review, and\n+  Guardian policy; its `.shiki/**` diff must equal one migration registered on\n+  the immutable merge base, path-for-path and byte-for-byte.\n - `repair:needed` - a bounded Repair Loop is required.\n \n ## Skill Gate\n\n2026-08-17T10:08:29.279020Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'git diff -- .github/workflows/shiki-mergegate.yml' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ndiff --git a/.github/workflows/shiki-mergegate.yml b/.github/workflows/shiki-mergegate.yml\nindex 9ee3d28..128e912 100644\n--- a/.github/workflows/shiki-mergegate.yml\n+++ b/.github/workflows/shiki-mergegate.yml\n@@ -49,13 +49,20 @@ jobs:\n           PR_NUMBER: ${{ github.event.pull_request.number }}\n           REPOSITORY: ${{ github.repository }}\n         run: |\n+          set -euo pipefail\n           rm -rf .shiki/gha\n           mkdir -p .shiki/gha\n-          gh pr view \"$PR_NUMBER\" --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n+          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n+          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n           git fetch origin \"$base_ref\"\n-          git diff --name-only \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files.txt\n-          git diff --name-status \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files-status.txt\n+          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n+          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n+          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n \n           # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n           # Guardian gate from the Goal's frozen-plan risk in THIS metadata job,\n@@ -69,7 +76,7 @@ jobs:\n           gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/events\" > .shiki/gha/live-guardian-events.json || echo \"[]\" > .shiki/gha/live-guardian-events.json\n           gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/timeline\" -H \"Accept: application/vnd.github+json\" > .shiki/gha/live-guardian-timeline.json || echo \"[]\" > .shiki/gha/live-guardian-timeline.json\n \n-          # Base .shiki snapshot from origin/<base_ref>, built the SAME way\n+          # Base .shiki snapshot from the PR's immutable merge base, built the SAME way\n           # shiki-cca-completion.yml builds it. mergegate_check.py needs it to\n           # verify task-field immutability for a post_merge_reconcile PR and to\n           # resolve the SADR-0017 bookkeeping-closeout exemption; without it a\n@@ -78,7 +85,7 @@ jobs:\n           # would go shallow.\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n       - name: MergeGate metadata check\n         env:\n@@ -103,7 +110,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\n\nexec\n/bin/zsh -lc 'git diff -- .github/workflows/shiki-cca-completion.yml' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 57081e3..04719fe 100644\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -52,13 +52,21 @@ jobs:\n           REPOSITORY: ${{ github.repository }}\n           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n         run: |\n+          set -euo pipefail\n           rm -rf .shiki/gha\n           mkdir -p .shiki/gha\n-          gh pr view \"$PR_NUMBER\" --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n+          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n+          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n           git fetch origin \"$base_ref\"\n-          git diff --name-only \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files.txt\n-          git diff --name-status \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files-status.txt\n+          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n+          printf '%s\\n' \"$merge_base\" > .shiki/gha/merge-base-oid.txt\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n+          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n+          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n \n           # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n           # mergegate job builds them below. The Guardian-approval signal must\n@@ -70,7 +78,7 @@ jobs:\n           # the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n           # Implementation-PR merge proof: resolve the task id with the SAME\n           # semantics mergegate_check.py uses (first T-<4+ digits> OR\n@@ -85,7 +93,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\n@@ -218,7 +226,7 @@ jobs:\n           while true; do\n             gh pr view \"$PR_NUMBER\" \\\n               --repo \"$REPOSITORY\" \\\n-              --json headRefOid,statusCheckRollup \\\n+              --json headRefOid,statusCheckRollup,isCrossRepository \\\n               > .shiki/gha/pr-settling.json\n \n             observed_head=\"$(jq -r '.headRefOid // \"\"' .shiki/gha/pr-settling.json)\"\n@@ -267,9 +275,13 @@ jobs:\n \n           # The judge must consume the settled rollup, not the snapshot gathered at\n           # the beginning of this concurrently-triggered workflow run.\n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n           gh pr view \"$PR_NUMBER\" \\\n             --repo \"$REPOSITORY\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n \n       - name: Prepare CCA transport schema\n         id: schema\n@@ -706,10 +718,14 @@ jobs:\n             exit 1\n           fi\n \n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n           gh pr view \"$PR_NUMBER\" \\\n             --repo \"$REPOSITORY\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n-            > .shiki/gha/pr.json\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n+            > .shiki/gha/pr-refresh.json\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr-refresh.json > .shiki/gha/pr.json\n+          rm .shiki/gha/pr-refresh.json\n \n           echo \"::endgroup::\"\n \n@@ -782,7 +798,7 @@ jobs:\n           mkdir -p .shiki/gha/base-shiki\n \n           gh pr view \"$PR_NUMBER\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n             > .shiki/gha/live-pr.json\n \n           gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/comments\" \\\n@@ -797,16 +813,21 @@ jobs:\n \n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/live-pr.json)\"\n           head_oid=\"$(jq -r '.headRefOid' .shiki/gha/live-pr.json)\"\n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n \n           git fetch origin \"$base_ref\"\n \n-          git diff --name-only \"origin/${base_ref}...HEAD\" \\\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/live-pr.json > .shiki/gha/live-pr-with-merge-base.json\n+          mv .shiki/gha/live-pr-with-merge-base.json .shiki/gha/live-pr.json\n+\n+          git diff --name-only \"${merge_base}...HEAD\" \\\n             > .shiki/gha/live-changed-files.txt\n \n-          git diff --name-status \"origin/${base_ref}...HEAD\" \\\n+          git diff --name-status \"${merge_base}...HEAD\" \\\n             > .shiki/gha/live-changed-files-status.txt\n \n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n           current_head=\"$(git rev-parse HEAD)\"\n           if [ \"$head_oid\" != \"$current_head\" ]; then\n@@ -849,7 +870,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\n\nexec\n/bin/zsh -lc \"sed -n '880,955p' scripts/mergegate_check.py; sed -n '1960,2285p' scripts/mergegate_check.py; sed -n '3120,3485p' scripts/mergegate_check.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\nexec\n/bin/zsh -lc \"sed -n '285,360p' scripts/guardian_approval_signal.py; sed -n '360,430p' scripts/guardian_approval_signal.py; rg -n '_bookkeeping_closeout_exemption|bookkeeping_closeout_exemption|carry_resolved_risk' scripts tests\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n    parser.add_argument(\n        \"--base-sync-carry\",\n        action=\"store_true\",\n        help=\"Carry a Guardian comment approval across a proven pure base sync (guardian_comment_carried); requires --default-branch.\",\n    )\n    parser.add_argument(\n        \"--default-branch\",\n        default=\"\",\n        help=\"Default branch name the carry proves a pure sync against. Empty disables the carry.\",\n    )\n    parser.add_argument(\"--output\", required=True)\n    args = parser.parse_args(argv)\n\n    pr = _load_json(args.pr_json)\n    if not isinstance(pr, dict):\n        signal = {\n            \"required\": True,\n            \"approved\": False,\n            \"error\": \"pr.json missing or invalid\",\n            \"bookkeeping_closeout_exemption\": False,\n        }\n        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n        return 0\n\n    # Authoritative risk comes from the task resolved by the PR-body task id \u2014\n    # exactly how the MergeGate policy check resolves it. When the risk cannot be\n    # determined (no task id in the body, missing/corrupt task file, or no\n    # risk_level), FAIL CLOSED: require Guardian approval rather than let an\n    # undetermined risk collapse to \"not required\" (which would let the CCA treat\n    # CCA-08 as not applicable for an unapproved high/critical PR).\n    head_sha = str(pr.get(\"headRefOid\") or \"\")\n    pr_body = str(pr.get(\"body\") or \"\")\n    task_risk = _resolve_task_risk_level(args.shiki_root, pr_body)\n\n    # SADR-0017: a PROVEN bookkeeping closeout carries no implementation and\n    # inherits no new risk, so at this single Guardian decision point the task's\n    # risk is evaluated as if low \u2014 IDENTICALLY to the MergeGate policy check,\n    # which calls the same is_bookkeeping_closeout classifier over the same inputs.\n    # It fails closed to \"no exemption\" whenever any input is absent or unbuildable,\n    # so a missing base snapshot / changed-files status / merge proof requires\n    # Guardian approval exactly as before.\n    exemption = _bookkeeping_closeout_exemption(\n        shiki_root=args.shiki_root,\n        pr_body=pr_body,\n        base_shiki=args.base_shiki,\n        changed_files_status=args.changed_files_status,\n        merged_prs=args.merged_prs,\n    )\n    # A fork PR is never eligible for the SADR-0017 exemption. The snapshot must\n    # prove the negative; absent/null/anything-but-False fails closed.\n    if exemption and pr.get(\"isCrossRepository\") is not False:\n        exemption = False\n    # The risk that actually gates Guardian approval: \"low\" for a proven closeout,\n    # otherwise the real task risk. task_risk is still reported as risk_level for\n    # audit. PR labels may only ESCALATE the effective risk (a maintainer can force\n    # the gate even on an exempted PR) and are ignored when the effective risk is\n    # undetermined (fail closed wins) \u2014 exactly as in MergeGate's\n    # _guardian_risk_labels.\n    effective_risk = \"low\" if exemption else task_risk\n    risk_unknown = effective_risk is None\n    labels = _label_names(pr) + ([effective_risk, f\"risk:{effective_risk}\"] if effective_risk else [])\n\n    try:\n        policy = load_guardian_policy_file(Path(args.guardian_policy))\n    except GuardianPolicyError as error:\n        # Fail closed: an undetermined risk or an unreadable policy on a\n        # high/critical PR must never let the CCA see approval.\n        required = risk_unknown or _builtin_high_or_critical(labels)\n        signal = {\n            \"required\": required,\n            \"approved\": not required,\n            \"error\": f\"guardian policy unreadable: {error}\",\n            \"head_sha\": head_sha,\n            \"bookkeeping_closeout_exemption\": exemption,\n        }\n        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n        return 0\n\n    policy_errors = validate_guardian_policy(policy)\n    required = risk_unknown or _builtin_high_or_critical(labels) or risk_requires_guardian(labels, policy)\n    if policy_errors and required:\n        signal = {\n            \"required\": True,\n            \"approved\": False,\n            \"error\": \"; \".join(policy_errors),\n            \"head_sha\": head_sha,\n            \"bookkeeping_closeout_exemption\": exemption,\n        }\n        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n        return 0\n\n    if not required:\n        # Risk is KNOWN and below the Guardian threshold (low/medium, or a proven\n        # bookkeeping closeout evaluated as if low).\n        note = f\"Guardian approval not required for risk level {task_risk!r}\"\n        if exemption:\n            note += \" (SADR-0017 bookkeeping-closeout exemption applied)\"\n        signal = {\n            \"required\": False,\n            \"approved\": True,\n            \"sources\": [],\n            \"ai_reviewers\": [],\n            \"approvers\": [],\n            \"head_sha\": head_sha,\n            \"bookkeeping_closeout_exemption\": exemption,\n            \"note\": note,\n        }\n        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n        return 0\n\n    # SADR-0015 Contract Approval OR-branch. The Guardian requirement is satisfied\n    # by EITHER a live PR approval (below) OR a proven Contract Approval \u2014 the SAME\n    # OR the MergeGate policy check applies, so the two Guardian decision points\n    # never diverge. When it applies, report the requirement satisfied with the\n    # ``contract_approval`` source the CCA prompt reads, and DO NOT consult the\n    # live-approval evidence (the approval was recorded on the merged Contract PR).\n    # Absent/invalid proof => None => the live-approval path below runs unchanged.\n    contract = _contract_approval_result(\n        shiki_root=args.shiki_root,\n        pr_body=pr_body,\n        base_shiki=args.base_shiki,\n        changed_files_status=args.changed_files_status,\n        contract_approval=args.contract_approval,\n    )\n    if contract is not None and contract.applies:\n        signal = {\n            \"required\": True,\n            \"approved\": True,\n            \"sources\": list(contract.sources),\n            \"ai_reviewers\": [],\n            \"approvers\": [],\n            \"head_sha\": head_sha,\n            \"expected_repository\": args.expected_repository,\n            \"risk_level\": task_risk,\n            \"risk_determined\": not risk_unknown,\n            \"bookkeeping_closeout_exemption\": exemption,\n            \"contract_approval\": True,\n            \"note\": (\n                \"Guardian approval satisfied by Contract Approval (SADR-0015): the task contract was \"\n                \"registered and Guardian-approved before dispatch\"\n            ),\n        }\n        Path(args.output).write_text(json.dumps(signal, indent=2, ensure_ascii=False) + \"\\n\", encoding=\"utf-8\")\n        return 0\n\n    comments = _as_list(_load_json(args.guardian_comments))\nscripts/guardian_approval_signal.py:106:def _carry_resolved_risk(*, shiki_root: str, base_shiki: str, pr_body: str, task_risk: str | None, exemption: bool) -> str:\nscripts/guardian_approval_signal.py:147:def _bookkeeping_closeout_exemption(\nscripts/guardian_approval_signal.py:304:            \"bookkeeping_closeout_exemption\": False,\nscripts/guardian_approval_signal.py:326:    exemption = _bookkeeping_closeout_exemption(\nscripts/guardian_approval_signal.py:358:            \"bookkeeping_closeout_exemption\": exemption,\nscripts/guardian_approval_signal.py:371:            \"bookkeeping_closeout_exemption\": exemption,\nscripts/guardian_approval_signal.py:389:            \"bookkeeping_closeout_exemption\": exemption,\nscripts/guardian_approval_signal.py:420:            \"bookkeeping_closeout_exemption\": exemption,\nscripts/guardian_approval_signal.py:451:        resolved_risk=_carry_resolved_risk(\nscripts/guardian_approval_signal.py:472:        \"bookkeeping_closeout_exemption\": exemption,\ntests/test_guardian_signal_closeout.py:314:        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:319:        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:326:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:375:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:378:            sig._carry_resolved_risk(\ntests/test_guardian_signal_closeout.py:383:                exemption=signal[\"bookkeeping_closeout_exemption\"],\ntests/test_guardian_signal_closeout.py:394:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:403:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:409:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:415:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:421:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:432:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:440:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:448:        self.assertIn(\"bookkeeping_closeout_exemption\", proven)\ntests/test_guardian_signal_closeout.py:449:        self.assertTrue(proven[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:454:        self.assertIn(\"bookkeeping_closeout_exemption\", disq)\ntests/test_guardian_signal_closeout.py:455:        self.assertFalse(disq[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:466:        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:528:                    signal[\"bookkeeping_closeout_exemption\"], exempt,\nscripts/mergegate_check.py:2531:    carry_resolved_risk = \"low\" if bookkeeping_closeout else str((task or {}).get(\"risk_level\") or \"\")\nscripts/mergegate_check.py:2543:        resolved_risk=carry_resolved_risk,\n\n succeeded in 0ms:\n    \"\"\"\n    path = normalize_repo_path(entry.path)\n    plan_id = Path(path).stem\n    if not allow_source_plan:\n        blocking.append(\n            f\"{mode} must not change {path}; carrying the Goal's frozen source plan \"\n            \"is not enabled for this registration\"\n        )\n        return\n    if entry.status != \"A\":\n        blocking.append(\n            f\"{mode} may only ADD the Goal's spec-frozen source plan, not modify {path}\"\n        )\n        return\n    if not source_plan_id:\n        blocking.append(\n            f\"{mode} cannot resolve the Goal's source_plan, so the carried plan {path} is not admissible\"\n        )\n        return\n    if plan_id != source_plan_id:\n        blocking.append(\n            f\"{mode} may only carry the Goal's source_plan {source_plan_id}, not {plan_id}\"\n        )\n        return\n    plan = load_plan(target, plan_id)\n    if not isinstance(plan, dict):\n        blocking.append(f\"{mode} source plan {path} is not a JSON object\")\n        return\n    if str(plan.get(\"id\") or \"\") != plan_id:\n        blocking.append(\n            f\"{mode} source plan filename {path} does not match its id {plan.get('id')!r}\"\n        )\n    spec_freeze = plan.get(\"spec_freeze\")\n    if not isinstance(spec_freeze, dict) or spec_freeze.get(\"status\") != \"frozen\":\n        blocking.append(\n            f\"{mode} source plan {path} must be spec-frozen (spec_freeze.status=frozen) to be carried\"\n        )\n\n\n# Risk ordering, low to high. Used to force the Guardian gate from the STRONGER\n# of a Contract PR's goal-file risk and its immutable frozen-plan risk, so a\n# downgraded goal file can never resolve to a weaker gate than the frozen plan.\n_RISK_ORDER = {\"low\": 0, \"medium\": 1, \"high\": 2, \"critical\": 3}\n\n\ndef _max_risk(first: str, second: str) -> str:\n    return first if _RISK_ORDER.get(first, -1) >= _RISK_ORDER.get(second, -1) else second\n\n\ndef contract_guardian_risk(target: Path, goal_id: str) -> str | None:\n    \"\"\"Resolve the risk level that forces the Guardian gate for a Contract PR.\n\n    The authority is the goal's spec-frozen source_plan (immutable, on the base\n    branch), never the mutable goal file alone. Returns the stronger of the frozen\n    plan's risk and the goal file's risk, or None when the goal cannot be\n    resolved (in which case the contract validator has already blocked).\"\"\"\n    goal = load_goal(target, goal_id)\n    if not isinstance(goal, dict):\n        return None\n    goal_risk = str(goal.get(\"risk_level\") or \"\")\n    plan_id = str(goal.get(\"source_plan\") or \"\")\n    plan = load_plan(target, plan_id) if plan_id else None\n    plan_risk = str(plan.get(\"risk_level\") or \"\") if isinstance(plan, dict) else \"\"\n    resolved = _max_risk(plan_risk, goal_risk)\n    return resolved or None\n\n\n# A task file an amendment MODIFIES freezes these fields to the base snapshot: they\n# are the loop-owned bookkeeping (expected_pr/closeout_pr/expected_branch/\n# ledger_evidence) that an amendment \u2014 which re-opens a task for re-dispatch, not\n# a completion \u2014 must never rewrite. This is the SADR-0017 mutable set minus\n# ``status`` (which the amendment MAY move, but only to planned/ready below).\n_AMENDMENT_TASK_FROZEN_FIELDS: tuple[str, ...] = (\n    \"expected_pr\",\n    \"closeout_pr\",\n    \"expected_branch\",\n# snapshot and the PR head. Every OTHER key is the frozen governance contract\n# (scope, non_goals, required_skills, risk_level, locks, acceptance_checks,\n# test_command, title, dependencies, \u2026) and must be byte-identical, so the\n# exemption can never ride a smuggled contract edit.\n#\n# ``expected_branch`` belongs here \u2014 it is NOT a governance field. The goal loop\n# cuts the closeout onto ``shiki/<task_id>-closeout`` and rewrites the closeout\n# task file's ``expected_branch`` to that new branch on EVERY closeout by\n# construction (scripts/shiki_loop.py), because MergeGate independently requires a\n# PR's task ``expected_branch`` to equal the PR's actual head ref: the metadata gate\n# in ``main`` blocks when ``expected_branch != headRefName``.\n# So every real closeout differs from its base on ``expected_branch``, and without\n# this entry condition 4 would reject every closeout before any other condition is\n# reached \u2014 the exact defect SADR-0017 exists to remove. That independent head-ref\n# binding is load-bearing for THIS whitelist entry: a PR cannot set\n# ``expected_branch`` to a branch it is not on, so admitting the field here cannot\n# let a closeout impersonate another branch. Do not weaken that binding.\n_CLOSEOUT_MUTABLE_TASK_FIELDS = frozenset({\"status\", \"expected_pr\", \"closeout_pr\", \"ledger_evidence\", \"expected_branch\"})\n\n\ndef _path_proven_absent(path: Path) -> bool:\n    \"\"\"Prove repository-path absence without following a dangling symlink.\"\"\"\n    try:\n        path.lstat()\n    except FileNotFoundError:\n        try:\n            return stat.S_ISDIR(path.parent.lstat().st_mode)\n        except OSError:\n            return False\n    except OSError:\n        return False\n    return False\n\n\ndef _path_is_regular_file(path: Path) -> bool:\n    \"\"\"Require a regular repository file without following symlinks.\"\"\"\n    try:\n        return stat.S_ISREG(path.lstat().st_mode)\n    except OSError:\n        return False\n\n\ndef _closeout_completes_goal(target: Path, goal_id: str) -> bool | None:\n    \"\"\"Whether this closeout completes its goal: every task node of the goal's DAG\n    is ``done`` at HEAD (this task set ``done`` by the closeout). Returns None when\n    it cannot be established (missing/empty DAG, or an unresolvable node) so the\n    caller fails closed. The DAG is read at HEAD, but a bookkeeping closeout may\n    not change the DAG (``is_bookkeeping_closeout`` rejects any ``.shiki/dag`` path\n    in the diff), so the DAG this reads is the authoritative base DAG.\"\"\"\n    dag = load_dag(target, goal_id)\n    nodes = dag.get(\"nodes\") if isinstance(dag, dict) else None\n    if not isinstance(nodes, list) or not nodes:\n        return None\n    for node in nodes:\n        node_task = load_task(target, str(node))\n        if not isinstance(node_task, dict):\n            return None\n        if str(node_task.get(\"status\")) != \"done\":\n            return False\n    return True\n\n\ndef is_bookkeeping_closeout(\n    *,\n    target: Path,\n    task: dict[str, Any] | None,\n    task_id: str,\n    goal_id: str,\n    base_shiki: Path | None,\n    changed_files_status: list[ChangedFile],\n    merged_pr_numbers: set[int],\n) -> bool:\n    \"\"\"Classify whether a PR is a pure bookkeeping closeout (SADR-0017).\n\n    A bookkeeping closeout carries ONLY the terminal state that a merged task's\n    completion writes to ``main``: the task moves ``review -> done``, its lock\n    ``active -> released``, and \u2014 ONLY when this task completes its goal \u2014 the goal\n    moves ``-> complete`` with exactly one added scorecard report. It contains zero\n    implementation, so it needed no code review to produce and inherits no new\n    risk; a qualifying PR is therefore evaluated as if the task were low risk at\n    the single Guardian decision point (``enforce_guardian_policy``), while every\n    OTHER gate runs unchanged.\n\n    Every condition is proven from the diff, the base snapshot, and live PR state\n    (``merged_pr_numbers``) \u2014 NEVER from the PR body, a label, a marker, or any\n    task field the PR head can write (note: this function never reads the PR at\n    all). It FAILS CLOSED on every axis: any condition that cannot be established\n    (missing base snapshot, unreadable task file, unresolvable implementation PR,\n    missing DAG, \u2026) returns False, and the PR inherits the task's real risk exactly\n    as before.\n\n    The six SADR-0017 conditions:\n      1. every changed path is under ``.shiki/`` (one byte outside disqualifies);\n      2. no path is deleted;\n      3. every changed path is inside the task's declared locks unioned with its\n         derived id-scoped mirror locks (the existing files-outside-locks rule);\n      4. the task file's governance fields are byte-identical to the base snapshot\n         \u2014 only the ``_CLOSEOUT_MUTABLE_TASK_FIELDS`` (status, expected_pr,\n         closeout_pr, ledger_evidence and expected_branch \u2014 see that definition for\n         why the branch rewrite is safe) may differ;\n      5. the transitions are exactly the terminal set: task ``review -> done``,\n         lock ``active -> released``, and ONLY when this task completes its goal,\n         goal ``-> complete`` with exactly one added scorecard report;\n      6. the task's implementation PR (named by the BASE snapshot's expected_pr,\n         which the PR head cannot write) is proven merged.\n    \"\"\"\n    try:\n        if base_shiki is None or not base_shiki.exists():\n            return False\n        if (\n            not isinstance(task, dict)\n            or str(task.get(\"id\") or \"\") != task_id\n            or str(task.get(\"goal_id\") or \"\") != goal_id\n            or not goal_id\n        ):\n            return False\n        task_file = f\".shiki/tasks/{task_id}.json\"\n        base_task_path = base_shiki / \"tasks\" / f\"{task_id}.json\"\n        head_task_path = target / task_file\n        if not (_path_is_regular_file(base_task_path) and _path_is_regular_file(head_task_path)):\n            return False\n        base_task = load_json(base_task_path)\n        head_task = load_json(head_task_path)\n        if not isinstance(base_task, dict) or not isinstance(head_task, dict) or head_task != task:\n            return False\n\n        # Condition 4: every task-file key outside the mutable set is the frozen\n        # governance contract and must equal the base snapshot exactly.\n        for key in set(base_task) | set(task):\n            if key in _CLOSEOUT_MUTABLE_TASK_FIELDS:\n                continue\n            if base_task.get(key) != task.get(key):\n                return False\n\n        # Condition 5 (task leg): status transitions review -> done, exactly.\n        if str(base_task.get(\"status\")) != \"review\" or str(task.get(\"status\")) != \"done\":\n            return False\n        if type(task.get(\"expected_pr\")) is not int:\n            return False\n        expected_branch = task.get(\"expected_branch\")\n        if not isinstance(expected_branch, str) or not expected_branch.strip():\n            return False\n        if (\n            (\"closeout_pr\" in base_task) != (\"closeout_pr\" in task)\n            or base_task.get(\"closeout_pr\") != task.get(\"closeout_pr\")\n        ):\n            return False\n\n        # Condition 6: the implementation PR is named by the BASE expected_pr (a\n        # field the PR head cannot write) and must be proven merged.\n        if type(base_task.get(\"expected_pr\")) is not int:\n            return False\n        impl_pr_num = base_task[\"expected_pr\"]\n        if impl_pr_num not in (merged_pr_numbers or set()):\n            return False\n\n        # Whether this closeout completes its goal, judged from the HEAD DAG.\n        completes_goal = _closeout_completes_goal(target, goal_id)\n        if completes_goal is None:\n            return False\n\n        lock_file = f\".shiki/locks/{task_id}.json\"\n        goal_file = f\".shiki/goals/{goal_id}.json\"\n        worktree_file = f\".shiki/worktrees/{task_id}.json\"\n\n        task_changed = False\n        lock_released = False\n        goal_completed = False\n        reports_added = 0\n        completion_report_path: str | None = None\n        completion_ledger_id: str | None = None\n        base_goal_for_closeout: dict[str, Any] | None = None\n        head_goal_for_closeout: dict[str, Any] | None = None\n        changed_paths: list[str] = []\n        changed_ledger_paths: list[str] = []\n\n        for entry in changed_files_status:\n            path = normalize_repo_path(entry.path)\n            old_path = normalize_repo_path(entry.old_path or \"\")\n            # Conditions 1-2: only direct .shiki path additions/modifications;\n            # delete, rename, copy and type-change legs are never terminal state.\n            if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                return False\n            changed_paths.append(path)\n\n            if path == task_file:\n                if entry.status != \"M\":\n                    return False\n                task_changed = True\n            elif path == lock_file:\n                # Condition 5 (lock leg): active -> released, proven against base.\n                base_lock_path = base_shiki / \"locks\" / f\"{task_id}.json\"\n                head_lock_path = target / path\n                if (\n                    entry.status != \"M\"\n                    or not _path_is_regular_file(base_lock_path)\n                    or not _path_is_regular_file(head_lock_path)\n                ):\n                    return False\n                base_lock = load_json(base_lock_path)\n                head_lock = load_json(head_lock_path)\n                if not (isinstance(base_lock, dict) and base_lock.get(\"state\") == \"active\"):\n                    return False\n                if not (isinstance(head_lock, dict) and head_lock.get(\"state\") == \"released\"):\n                    return False\n                if any(\n                    lock.get(\"task_id\") != task_id or lock.get(\"goal_id\") != goal_id\n                    for lock in (base_lock, head_lock)\n                ):\n                    return False\n                for field in (set(base_lock) | set(head_lock)) - {\"state\"}:\n                    if base_lock.get(field) != head_lock.get(field):\n                        return False\n                lock_released = True\n            elif path == goal_file:\n                # Condition 5 (goal leg): a goal transition is allowed ONLY when\n                # this task completes its goal, and must be -> complete.\n                base_goal_path = base_shiki / \"goals\" / f\"{goal_id}.json\"\n                head_goal_path = target / path\n                if (\n                    entry.status != \"M\"\n                    or not completes_goal\n                    or not _path_is_regular_file(base_goal_path)\n                    or not _path_is_regular_file(head_goal_path)\n                ):\n                    return False\n                base_goal = load_json(base_goal_path)\n                head_goal = load_json(head_goal_path)\n                if not (\n                    isinstance(base_goal, dict)\n                    and base_goal.get(\"id\") == goal_id\n                    and str(base_goal.get(\"status\")) in {\"planned\", \"ready\", \"blocked\"}\n                ):\n                    return False\n                if not (\n                    isinstance(head_goal, dict)\n                    and head_goal.get(\"id\") == goal_id\n                    and str(head_goal.get(\"status\")) == \"complete\"\n                ):\n                    return False\n                for field in (set(base_goal) | set(head_goal)) - {\"status\", \"ledger_evidence\"}:\n                    if base_goal.get(field) != head_goal.get(field):\n                        return False\n                base_goal_for_closeout = base_goal\n                head_goal_for_closeout = head_goal\n                goal_completed = True\n            elif path.startswith(\".shiki/reports/\") and path.endswith(\".json\"):\n                # A scorecard is ADDED by goal completion; a modified report is not\n                # part of the terminal set.\n                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n                    return False\n                reports_added += 1\n                completion_report_path = path\n            elif path == worktree_file:\n                return False\n            elif path.startswith(\".shiki/ledger/\") and path.endswith(\".json\"):\n                # Ledgers are append-only mirror bookkeeping (condition 3 and the\n                # untrusted-mutation gate police them); an ADD carries no transition,\n                # but a modify is never part of a closeout's terminal set.\n                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n                    return False\n                changed_ledger_paths.append(path)\n            else:\n                # Any other .shiki path (dag, memories, guardian-policy, another\n                # task/goal/lock, a plan, \u2026) is not part of the terminal set.\n                return False\n\n        # Condition 3: every changed path inside the task's declared locks unioned\n        # with its derived id-scoped mirror locks. HEAD locks equal base locks\n        # (frozen by condition 4), so a PR cannot widen them to sneak a file in.\n        declared_locks = [str(lock) for lock in task.get(\"locks\") or []]\n        effective_locks = list(dict.fromkeys([*declared_locks, *_derive_task_mirror_locks(target, task)]))\n        if files_outside_locks(changed_paths, effective_locks):\n            return False\n\n        # The task's review -> done transition is the closeout; its lock release is\n        # a merged task's residue. Both are mandatory.\n        if not task_changed or not lock_released:\n            return False\n\n        if completes_goal:\n            # A completing closeout MUST carry the goal -> complete transition and\n            # EXACTLY ONE added scorecard report.\n            if not goal_completed or reports_added != 1:\n                return False\n            if not (\n                isinstance(base_goal_for_closeout, dict)\n                and isinstance(head_goal_for_closeout, dict)\n                and completion_report_path\n            ):\n                return False\n\n            dag = load_dag(target, goal_id)\n            dag_nodes = dag.get(\"nodes\") if isinstance(dag, dict) else None\n            if not isinstance(dag_nodes, list) or not dag_nodes:\n                return False\n            goal_task_ids = [str(node) for node in dag_nodes]\n            goal_tasks = [load_task(target, node_id) for node_id in goal_task_ids]\n            if not all(isinstance(goal_task, dict) for goal_task in goal_tasks):\n                return False\n\n            report = load_json(target / completion_report_path)\n            report_mergegate = report.get(\"mergegate\") if isinstance(report, dict) else None\n            scorecard = report.get(\"scorecard\") if isinstance(report, dict) else None\n            scorecard_tasks = scorecard.get(\"tasks\") if isinstance(scorecard, dict) else None\n            expected_task_paths = {\n                f\".shiki/tasks/{goal_task_id}.json\" for goal_task_id in goal_task_ids\n            }\n            report_evidence = (\n                {str(value) for value in report.get(\"evidence\") or []}\n                if isinstance(report, dict)\n                else set()\n            )\n            effective_risk = str(head_goal_for_closeout.get(\"risk_level\") or \"\")\n            for goal_task in goal_tasks:\n                if not isinstance(goal_task, dict):\n                    return False\n                effective_risk = _max_risk(effective_risk, str(goal_task.get(\"risk_level\") or \"\"))\n            if not (\n                isinstance(report, dict)\n                and report.get(\"id\") == Path(completion_report_path).stem\n                and report.get(\"goal_id\") == goal_id\n                and report.get(\"status\") == \"complete\"\n                and report_evidence == expected_task_paths\n                and report.get(\"blocking_reasons\") == []\n                and isinstance(report_mergegate, dict)\n    parser.add_argument(\"--allow-missing-cca\", action=\"store_true\")\n    # Base-sync carry (guardian_comment_carried). Inert unless BOTH are given: the\n    # flag is off by default and --default-branch defaults to empty, so an\n    # invocation that omits them is byte-for-byte the pre-carry gate.\n    parser.add_argument(\n        \"--base-sync-carry\",\n        action=\"store_true\",\n        help=\"Carry a Guardian comment approval across a proven pure base sync (guardian_comment_carried); requires --default-branch.\",\n    )\n    parser.add_argument(\n        \"--default-branch\",\n        default=\"\",\n        help=\"Default branch name; the base the carry proves a pure sync against. Empty disables the carry.\",\n    )\n    args = parser.parse_args()\n\n    target = Path(args.target).resolve()\n    blocking: list[str] = []\n    warnings: list[str] = []\n    resolved_task_id: str | None = None\n    resolved_goal_id: str | None = None\n    reconcile_mode = False\n    post_merge_mode = False\n    contract_mode = False\n    amendment_mode = False\n    migration_mode = False\n    # PR numbers proven merged (live PR state gathered by the workflow). Used by\n    # post_merge_reconcile to prove its referenced PR merged, and by the bookkeeping\n    # closeout classifier to prove the task's implementation PR merged (SADR-0017).\n    merged_prs = {\n        int(token.strip())\n        for token in str(args.merged_prs or \"\").split(\",\")\n        if token.strip().isdigit()\n    }\n    # Whether the (normal single-task) PR is a proven bookkeeping closeout, so the\n    # Guardian gate evaluates it as if the task were low risk (SADR-0017). Stays\n    # False for contract / goal_reconcile / post_merge_reconcile PRs.\n    bookkeeping_closeout = False\n    manifest: dict[str, Any] | None = None\n    try:\n        manifest = load_manifest(target)\n    except ManifestError as error:\n        blocking.append(str(error))\n\n    pr = load_json(Path(args.pr_json))\n    body = \"\"\n    if pr:\n        body = str(pr.get(\"body\") or \"\")\n        resolved_task_id = first_match(TASK_ID, body)\n        resolved_goal_id = first_match(GOAL_ID, body)\n        reconcile_mode, reconcile_error = goal_reconcile_decision(pr)\n        post_merge_mode, post_merge_error = post_merge_reconcile_decision(pr)\n        contract_mode, contract_error = contract_decision(pr)\n        amendment_mode, amendment_error = amendment_decision(pr)\n        migration_mode, migration_error = migration_decision(pr)\n        if reconcile_error:\n            blocking.append(reconcile_error)\n        if post_merge_error:\n            blocking.append(post_merge_error)\n        if contract_error:\n            blocking.append(contract_error)\n        if amendment_error:\n            blocking.append(amendment_error)\n        if migration_error:\n            blocking.append(migration_error)\n        if reconcile_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both goal_reconcile and post_merge_reconcile\")\n        if contract_mode and reconcile_mode:\n            blocking.append(\"a PR cannot be both contract and goal_reconcile\")\n        if contract_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both contract and post_merge_reconcile\")\n        if amendment_mode and reconcile_mode:\n            blocking.append(\"a PR cannot be both amendment and goal_reconcile\")\n        if amendment_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both amendment and post_merge_reconcile\")\n        if amendment_mode and contract_mode:\n            blocking.append(\"a PR cannot be both amendment and contract\")\n        if migration_mode and reconcile_mode:\n            blocking.append(\"a PR cannot be both migration and goal_reconcile\")\n        if migration_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both migration and post_merge_reconcile\")\n        if migration_mode and contract_mode:\n            blocking.append(\"a PR cannot be both migration and contract\")\n        if migration_mode and amendment_mode:\n            blocking.append(\"a PR cannot be both migration and amendment\")\n        if args.expected_head_sha:\n            pr_head = str(pr.get(\"headRefOid\") or \"\")\n            if not pr_head:\n                blocking.append(\"PR headRefOid is missing\")\n            elif pr_head != args.expected_head_sha:\n                blocking.append(f\"PR headRefOid {pr_head} does not match expected checked-out HEAD {args.expected_head_sha}\")\n        if reconcile_mode:\n            # A goal_reconcile PR is goal-scoped (frozen-plan task registration);\n            # it does not carry a single implementation task id.\n            if not resolved_goal_id:\n                blocking.append(\"goal_reconcile PR body does not contain a Shiki goal id like G-0001\")\n        elif contract_mode:\n            # A Contract PR is goal-scoped (frozen-plan task-contract registration);\n            # it carries no single implementation task id (SADR-0015).\n            if not resolved_goal_id:\n                blocking.append(\"contract PR body does not contain a Shiki goal id like G-0001\")\n        elif amendment_mode:\n            # An Amendment PR is goal-scoped (amends the goal's spec-frozen source\n            # plan and re-opens this goal's tasks); it carries no single\n            # implementation task id.\n            if not resolved_goal_id:\n                blocking.append(\"amendment PR body does not contain a Shiki goal id like G-0001\")\n        elif post_merge_mode:\n            # A post_merge_reconcile PR is task-scoped (reconcile a merged task).\n            if not resolved_task_id:\n                blocking.append(\"post_merge_reconcile PR body does not contain a Shiki task id like T-0001\")\n        elif migration_mode:\n            # A Migration PR is repository-wide (a mirror-scoped re-run proof); it\n            # references NEITHER a task nor a goal id \u2014 it declares a `Migration: <id>`\n            # line, validated in enforce_migration.\n            pass\n        else:\n            if not resolved_task_id:\n                blocking.append(\"PR body does not contain a Shiki task id like T-0001\")\n            if not resolved_goal_id:\n                blocking.append(\"PR body does not contain a Shiki goal id like G-0001\")\n        for heading in [\"Scope\", \"Acceptance\", \"Evidence\", \"MergeGate\"]:\n            if heading.lower() not in body.lower() and not has_heading(body, heading):\n                blocking.append(f\"PR body is missing {heading} section\")\n    else:\n        warnings.append(f\"PR JSON not found at {args.pr_json}; skipping PR metadata checks\")\n    enforce_required_check_definitions(target, blocking)\n\n    task: dict[str, Any] | None = None\n    ledger_entries: list[dict[str, Any]] = []\n    if reconcile_mode and pr:\n        # goal_reconcile is a goal-scoped, frozen-plan registration PR; it uses a\n        # dedicated deny-by-default validator instead of the single-task readiness\n        # flow (no implementation task, CCA, locks, or guardian gate).\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_goal_reconcile(\n            target=target,\n            goal_id=resolved_goal_id or \"\",\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n        )\n    elif contract_mode and pr:\n        # Contract mode (SADR-0015): a goal-scoped, spec-frozen task-contract\n        # registration PR. It uses the generalized deny-by-default validator and\n        # forces the Guardian gate from the Goal's frozen-plan risk BEFORE any\n        # implementation is dispatched. The Guardian gate here is INDEPENDENT of a\n        # CCA verdict: a Contract PR carries no implementation for CCA to judge, so\n        # requiring a verdict to reach the gate (as the single-task flow does)\n        # would let a high-risk contract merge unapproved when CCA is absent.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_contract(\n            target=target,\n            goal_id=resolved_goal_id or \"\",\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n            # The contract flow is the one path that carries a Goal's frozen plan\n            # to the default branch (SADR-0015 plan-reachability gap); enable it.\n            allow_source_plan_carry=True,\n        )\n        if resolved_goal_id:\n            contract_risk = contract_guardian_risk(target, resolved_goal_id)\n            if contract_risk not in _RISK_ORDER:\n                # Fail closed: if the Guardian risk cannot be resolved from the\n                # goal's frozen plan, the gate cannot be evaluated, so the contract\n                # must not slip through as if it were unguarded. (enforce_contract\n                # already blocks a risk-less goal registration; this guards the\n                # guardian input independently in case the goal is on base.)\n                blocking.append(\n                    f\"contract PR Guardian risk for goal {resolved_goal_id} could not be resolved \"\n                    f\"from its frozen plan (got {contract_risk!r}); the Guardian gate cannot be evaluated\"\n                )\n            else:\n                enforce_guardian_policy(\n                    pr=pr,\n                    task={\"risk_level\": contract_risk},\n                    target=target,\n                    guardian_policy=args.guardian_policy,\n                    guardian_comments=args.guardian_comments,\n                    guardian_events=args.guardian_events,\n                    guardian_timeline=args.guardian_timeline,\n                    blocking=blocking,\n                    warnings=warnings,\n                    expected_repository=args.expected_repository,\n                    base_sync_carry=args.base_sync_carry,\n                    default_branch=args.default_branch,\n                )\n    elif amendment_mode and pr:\n        # Amendment mode (SADR-0009/SADR-0015): a goal-scoped PR that corrects a\n        # spec-frozen Goal's contract on a protected branch \u2014 an append to the\n        # source plan's spec_freeze.amendments plus the task files bound to the\n        # amended plan. Deny-by-default validator; Guardian gate forced from the\n        # goal's frozen plan and INDEPENDENT of a CCA verdict (an amendment carries\n        # no implementation for CCA to judge). Like contract mode, the Guardian\n        # requirement runs here rather than in the single-task CCA flow.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        base_shiki_amend = Path(args.base_shiki) if args.base_shiki else None\n        enforce_amendment(\n            target=target,\n            goal_id=resolved_goal_id or \"\",\n            base_shiki=base_shiki_amend,\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n        )\n        if resolved_goal_id:\n            # Reuse contract_guardian_risk on BOTH the head and the base snapshot\n            # and take the stronger, so an amendment that WEAKENS its own plan's\n            # risk_level can never lower its own Guardian gate below what the\n            # pre-amendment (base) contract required. No second resolution \u2014 the\n            # existing resolver runs against each root and _max_risk combines them.\n            head_risk = contract_guardian_risk(target, resolved_goal_id) or \"\"\n            base_risk = \"\"\n            if base_shiki_amend is not None and base_shiki_amend.exists():\n                base_risk = contract_guardian_risk(base_shiki_amend.parent, resolved_goal_id) or \"\"\n            amendment_risk = _max_risk(head_risk, base_risk) or None\n            if amendment_risk not in _RISK_ORDER:\n                # Fail closed: an unresolvable amendment risk means the Guardian gate\n                # cannot be evaluated, so the amendment must not slip through as if\n                # it were unguarded.\n                blocking.append(\n                    f\"amendment PR Guardian risk for goal {resolved_goal_id} could not be resolved \"\n                    f\"from its frozen plan (got {amendment_risk!r}); the Guardian gate cannot be evaluated\"\n                )\n            else:\n                enforce_guardian_policy(\n                    pr=pr,\n                    task={\"risk_level\": amendment_risk},\n                    target=target,\n                    guardian_policy=args.guardian_policy,\n                    guardian_comments=args.guardian_comments,\n                    guardian_events=args.guardian_events,\n                    guardian_timeline=args.guardian_timeline,\n                    blocking=blocking,\n                    warnings=warnings,\n                    expected_repository=args.expected_repository,\n                )\n    elif migration_mode and pr:\n        # Migration mode: a repository-wide mirror change whose sole proof\n        # obligation is that the .shiki diff EQUALS the re-run output of a migration\n        # registered on the BASE branch (see enforce_migration). It carries no\n        # implementation task, CCA verdict, or Guardian gate \u2014 the maintainer label\n        # is the authorization and the base-registered re-run is the proof.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_migration(\n            target=target,\n            pr=pr,\n            base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n        )\n    elif post_merge_mode and pr:\n        # post_merge_reconcile is a task-scoped reconcile of a merged task's\n        # residual lock / status; a dedicated deny-by-default validator.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_post_merge_reconcile(\n            target=target,\n            task_id=resolved_task_id or \"\",\n            base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n            merged_pr_numbers=merged_prs,\n            pr_body=body,\n        )\n    elif resolved_task_id:\n        task = load_task(target, resolved_task_id)\n        if task is None:\n            blocking.append(f\"Task file not found for {resolved_task_id}\")\n        else:\n            task_goal_id = str(task.get(\"goal_id\") or \"\")\n            if resolved_goal_id and task_goal_id != resolved_goal_id:\n                blocking.append(f\"PR goal id {resolved_goal_id} does not match task goal_id {task_goal_id}\")\n            if not load_goal(target, task_goal_id):\n                blocking.append(f\"Goal file not found for {task_goal_id}\")\n\n            if pr:\n                pr_number = pr.get(\"number\")\n                expected_pr = task.get(\"expected_pr\")\n                if expected_pr is not None and pr_number is not None and expected_pr != pr_number:\n                    blocking.append(f\"Task expected_pr {expected_pr} does not match PR #{pr_number}\")\n                elif expected_pr is None:\n                    warnings.append(f\"Task {resolved_task_id} has no expected_pr recorded\")\n\n                head_ref = pr.get(\"headRefName\")\n                expected_branch = task.get(\"expected_branch\")\n                if expected_branch and head_ref and expected_branch != head_ref:\n                    blocking.append(f\"Task expected_branch {expected_branch!r} does not match PR head {head_ref!r}\")\n\n            for dependency_id in task.get(\"dependencies\") or []:\n                dependency = load_task(target, str(dependency_id))\n                if dependency is None:\n                    blocking.append(f\"Task dependency {dependency_id} has no task file\")\n                elif dependency.get(\"status\") != \"done\":\n                    blocking.append(f\"Task dependency {dependency_id} is not done: {dependency.get('status')!r}\")\n\n            files = changed_files(Path(args.changed_files))\n            files_status = parse_changed_files_status(Path(args.changed_files_status), files)\n            locks = [str(lock) for lock in task.get(\"locks\") or []]\n            if files and not locks:\n                blocking.append(f\"Task {resolved_task_id} has no locks but PR changes files\")\n            # The task's declared (product) locks stay exactly what the plan froze;\n            # the .shiki evidence the loop syncs onto the branch is covered by the\n            # id-scoped mirror set derived from the task's own ids/ledgers at read\n            # time. Union the two for the files-outside-locks gate only \u2014 lock\n            # CONFLICT detection stays on the declared locks, since shared mirror\n            # evidence is not a contended product resource.\n            effective_locks = list(dict.fromkeys([*locks, *_derive_task_mirror_locks(target, task)]))\n            # Judge the lock-scope gates against the WHOLE diff, both legs of every\n            # rename included. Using the name-only `files` list would let a rename\n            # move a file out of locked scope unseen (git shows only the new leg).\n            scope_paths = lock_scope_paths(files_status)\n            for path in files_outside_locks(scope_paths, effective_locks):\n                blocking.append(f\"Changed file {path} is outside declared task locks\")\n            blocking.extend(active_lock_conflicts(target, resolved_task_id, locks, scope_paths))\n\n            # SADR-0017: a proven bookkeeping closeout (task terminal state only, no\n            # implementation) is evaluated as if low risk at the Guardian gate\n            # below. Computed here (where the head task, base snapshot, diff and\n            # merged-PR proof are all in hand); consulted only by\n            # enforce_guardian_policy. Read-only \u2014 it never adds a blocking reason.\n            bookkeeping_closeout = is_bookkeeping_closeout(\n                target=target,\n                task=task,\n                task_id=resolved_task_id,\n                goal_id=task_goal_id,\n                base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                changed_files_status=files_status,\n                merged_pr_numbers=merged_prs,\n            )\n            # The exemption is same-repository only and fail-closed: the live PR\n            # snapshot must explicitly prove isCrossRepository=false.\n            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\n                bookkeeping_closeout = False\n\n            ledger_entries = load_ledger_entries(target, task, warnings, blocking)\n            if pr:\n                enforce_untrusted_shiki_mutations(\n                    target=target,\n                    manifest=manifest,\n                    base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                    changed_files_status=files_status,\n                    task=task,\n                    goal_id=task_goal_id,\n                    task_id=resolved_task_id,\n                    pr=pr,\n                    blocking=blocking,\n                    warnings=warnings,\n                )\n                # SADR-0015 Contract immutability on the NORMAL task-PR path: a PR\n                # that changes its own task file may move only bookkeeping \u2014 its\n                # frozen governance contract is bound to the base snapshot, and its\n                # risk_level may never resolve weaker than base. Only this branch\n                # calls it (contract / goal_reconcile / post_merge_reconcile use\n\n2026-08-17T10:08:39.925677Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1,340p' .github/workflows/shiki-cca-completion.yml; sed -n '680,910p' .github/workflows/shiki-cca-completion.yml; sed -n '1,150p' .github/workflows/shiki-mergegate.yml; sed -n '2380,2585p' scripts/mergegate_check.py; sed -n '1,190p' scripts/guardian_approval_signal.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\nname: Shiki CCA Completion\n\non:\n  pull_request:\n    types:\n      - opened\n      - synchronize\n      - reopened\n      - ready_for_review\n      - edited\n      - labeled\n      - unlabeled\n  workflow_dispatch:\n    inputs:\n      pr_number:\n        description: Pull request number to judge.\n        required: true\n        type: string\n\nconcurrency:\n  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}\n  cancel-in-progress: true\n\npermissions:\n  contents: read\n  pull-requests: write\n  issues: write\n  checks: write\n  actions: read\n  id-token: write\n\njobs:\n  cca:\n    name: CCA verdict\n    runs-on: ubuntu-latest\n    if: github.event_name == 'workflow_dispatch' || (github.event.pull_request.draft == false && ((github.event.action != 'labeled' && github.event.action != 'unlabeled') || github.event.label.name == 'guardian:approved' || startsWith(github.event.label.name, 'mergegate:')))\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n          ref: ${{ github.event.pull_request.head.sha || github.sha }}\n\n      - name: Validate Shiki artifacts\n        run: python3 scripts/validate_shiki.py\n\n      - name: Prepare CCA context\n        shell: bash\n        env:\n          GH_TOKEN: ${{ github.token }}\n          PR_NUMBER: ${{ github.event.pull_request.number || inputs.pr_number }}\n          REPOSITORY: ${{ github.repository }}\n          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n        run: |\n          set -euo pipefail\n          rm -rf .shiki/gha\n          mkdir -p .shiki/gha\n          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n          git fetch origin \"$base_ref\"\n          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n          printf '%s\\n' \"$merge_base\" > .shiki/gha/merge-base-oid.txt\n          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n\n          # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n          # mergegate job builds them below. The Guardian-approval signal must\n          # resolve a proven closeout as low risk exactly as the MergeGate policy\n          # check does; without these inputs the CCA path could not classify a\n          # closeout at all, so the exemption would be unreachable and a closeout\n          # would still return needs_guardian. Base .shiki snapshot from\n          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n          # the base fetch would go shallow).\n          rm -rf .shiki/gha/base-shiki\n          mkdir -p .shiki/gha/base-shiki\n          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n\n          # Implementation-PR merge proof: resolve the task id with the SAME\n          # semantics mergegate_check.py uses (first T-<4+ digits> OR\n          # T-<timestamp>-<hex>), read the BASE snapshot's expected_pr (a field the\n          # PR head cannot write, unlike the head's) and pass it as --merged-prs\n          # only when gh proves it MERGED. Fails closed to \"\" on any missing or\n          # unresolvable input, so the exemption is never granted without proof.\n          MERGED_PRS=\"\"\n          body=\"$(jq -r '.body // \"\"' .shiki/gha/pr.json)\"\n          task_id=\"$(printf '%s' \"$body\" | grep -oE 'T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})' | head -1 || true)\"\n          base_task=\".shiki/gha/base-shiki/.shiki/tasks/${task_id}.json\"\n          if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n            base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n            if [ -n \"$base_pr\" ]; then\n              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n              if [ \"$state\" = \"MERGED\" ]; then\n                MERGED_PRS=\"$base_pr\"\n              fi\n            fi\n          fi\n\n          # SADR-0015 Contract Approval carry. Build the registration proof the\n          # evaluator (shiki_contract_approval.evaluate_contract_approval) reads via\n          # --contract-approval, from evidence neither the PR head nor the PR body\n          # can forge. Resolve the task id with the SAME regex used above; find the\n          # commit that ADDED .shiki/tasks/<task_id>.json on the base branch with\n          # `git log --diff-filter=A` over the base ref the fetch above already\n          # retrieved (do NOT add a second fetch and do NOT use --depth: a shallow\n          # base makes this return nothing and silently defeats the carry, the exact\n          # defect PR #206 removed); resolve the MERGED PR behind that commit and its\n          # guardian:approved label straight from the GitHub API. Field names match\n          # the evaluator (guardian_source, adding_commit_pr) \u2014 the authority over the\n          # looser list in the task handoff. EVERY failure path writes a well-formed\n          # JSON object whose values DEFEAT the carry (the evaluator recomputes\n          # applies) with a reason, never an absent or partial file a reader could\n          # treat as permissive.\n          approval_file=.shiki/gha/contract-approval.json\n          ca_base_ref=\"$(jq -r '.baseRefName // \"\"' .shiki/gha/pr.json 2>/dev/null || echo \"\")\"\n          if [ -z \"$task_id\" ]; then\n            jq -n '{applies: false, reason: \"contract approval does not apply: no Shiki task id in the PR body\"}' > \"$approval_file\"\n          elif [ -z \"$ca_base_ref\" ]; then\n            jq -n --arg t \"$task_id\" '{task_id: $t, applies: false, reason: \"contract approval does not apply: no base ref resolved\"}' > \"$approval_file\"\n          else\n            adding_commit=\"$(git log --diff-filter=A --format=%H \"origin/${ca_base_ref}\" -- \".shiki/tasks/${task_id}.json\" 2>/dev/null | tail -1 || true)\"\n            if [ -z \"$adding_commit\" ]; then\n              jq -n --arg t \"$task_id\" '{task_id: $t, merged: false, applies: false, reason: \"contract approval does not apply: no commit adds this task file on the base branch (a first-registration PR is never covered)\"}' > \"$approval_file\"\n            else\n              pulls=\"$(gh api \"repos/${REPOSITORY}/commits/${adding_commit}/pulls\" 2>/dev/null || echo \"[]\")\"\n              reg_pr=\"$(printf '%s' \"$pulls\" | jq 'map(select(.merged_at != null)) | (.[0].number // empty)' 2>/dev/null || echo \"\")\"\n              if [ -z \"$reg_pr\" ]; then\n                jq -n --arg t \"$task_id\" --arg c \"$adding_commit\" '{task_id: $t, adding_commit: $c, merged: false, applies: false, reason: \"contract approval does not apply: no merged PR is associated with the adding commit\"}' > \"$approval_file\"\n              else\n                guardian_approved=\"$(printf '%s' \"$pulls\" | jq --argjson n \"$reg_pr\" 'any(.[]; .number == $n and ((.labels // []) | any(.name == \"guardian:approved\")))' 2>/dev/null || echo false)\"\n                jq -n \\\n                  --arg t \"$task_id\" \\\n                  --argjson pr \"$reg_pr\" \\\n                  --arg c \"$adding_commit\" \\\n                  --argjson ga \"$guardian_approved\" \\\n                  '{\n                     task_id: $t,\n                     pr: $pr,\n                     merged: true,\n                     adding_commit: $c,\n                     adding_commit_pr: $pr,\n                     guardian_approved: $ga,\n                     guardian_source: (if $ga then \"guardian_label\" else \"\" end),\n                     applies: $ga,\n                     reason: (if $ga\n                       then \"task contract registered and Guardian-approved (guardian:approved label) on merged Contract PR \\($pr) before dispatch\"\n                       else \"contract approval does not apply: registering PR \\($pr) carries no guardian:approved label\" end)\n                   }' > \"$approval_file\"\n              fi\n            fi\n          fi\n          echo \"::group::contract-approval carry\"\n          cat \"$approval_file\"\n          echo \"::endgroup::\"\n\n          # Deterministic Guardian-approval signal for CCA-08. The CCA must not\n          # judge Guardian approval by LLM-reading raw comments; it reads the\n          # result of the SAME authoritative evaluate_guardian_approval the\n          # MergeGate policy check uses (recognizes label/review/comment OR an\n          # external AI guardian review artifact, SADR-0010). The policy check\n          # remains the authoritative gate.\n          #\n          # SADR-0018 base-sync carry (guardian_comment_carried). --base-sync-carry\n          # + --default-branch let the signal carry a stale Guardian comment\n          # approval across a proven pure base sync, so it matches the gate rather\n          # than under-reporting. The proof reads git history over\n          # origin/<default_branch>; the base fetch above already retrieved it \u2014\n          # do NOT add a second fetch and do NOT use --depth (a shallow base makes\n          # the proof unresolvable and silently disables the carry, the exact\n          # defect PR #206 removed). Inert unless BOTH flags are present.\n          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/comments\" > .shiki/gha/live-guardian-comments.json || echo \"[]\" > .shiki/gha/live-guardian-comments.json\n          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/events\" > .shiki/gha/live-guardian-events.json || echo \"[]\" > .shiki/gha/live-guardian-events.json\n          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/timeline\" -H \"Accept: application/vnd.github+json\" > .shiki/gha/live-guardian-timeline.json || echo \"[]\" > .shiki/gha/live-guardian-timeline.json\n          python3 scripts/guardian_approval_signal.py \\\n            --pr-json .shiki/gha/pr.json \\\n            --guardian-policy .shiki/guardian-policy.json \\\n            --guardian-comments .shiki/gha/live-guardian-comments.json \\\n            --guardian-events .shiki/gha/live-guardian-events.json \\\n            --guardian-timeline .shiki/gha/live-guardian-timeline.json \\\n            --expected-repository \"${REPOSITORY}\" \\\n            --shiki-root . \\\n            --base-shiki .shiki/gha/base-shiki/.shiki \\\n            --changed-files-status .shiki/gha/changed-files-status.txt \\\n            --merged-prs \"$MERGED_PRS\" \\\n            --contract-approval .shiki/gha/contract-approval.json \\\n            --base-sync-carry \\\n            --default-branch \"$DEFAULT_BRANCH\" \\\n            --output .shiki/gha/guardian-approval.json\n          echo \"::group::guardian-approval signal\"\n          cat .shiki/gha/guardian-approval.json\n          echo \"::endgroup::\"\n\n      - name: Wait for required checks to settle\n        shell: bash\n        env:\n          GH_TOKEN: ${{ github.token }}\n          PR_NUMBER: ${{ github.event.pull_request.number || inputs.pr_number }}\n          REPOSITORY: ${{ github.repository }}\n          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}\n          WAIT_TIMEOUT_SECONDS: \"900\"\n          WAIT_INTERVAL_SECONDS: \"15\"\n        run: |\n          set -euo pipefail\n\n          # Read the repository's required-check contract rather than maintaining\n          # another list in this workflow. The two checks produced by this workflow\n          # cannot settle before its judge: CCA verdict is the current job and the\n          # policy check is downstream of it.\n          python3 - <<'PY' > .shiki/gha/required-checks.txt\n          import sys\n\n          sys.path.insert(0, \"scripts\")\n          from validate_shiki import config_required_checks\n\n          own_checks = {\"CCA verdict\", \"MergeGate policy check\"}\n          for check in config_required_checks():\n              if check not in own_checks:\n                  print(check)\n          PY\n\n          deadline=$((SECONDS + WAIT_TIMEOUT_SECONDS))\n          while true; do\n            gh pr view \"$PR_NUMBER\" \\\n              --repo \"$REPOSITORY\" \\\n              --json headRefOid,statusCheckRollup,isCrossRepository \\\n              > .shiki/gha/pr-settling.json\n\n            observed_head=\"$(jq -r '.headRefOid // \"\"' .shiki/gha/pr-settling.json)\"\n            if [ \"$observed_head\" != \"$EXPECTED_HEAD_SHA\" ]; then\n              echo \"::error::PR head ${observed_head} does not match expected head ${EXPECTED_HEAD_SHA}; refusing to judge stale check state.\"\n              exit 1\n            fi\n\n            pending=\"\"\n            while IFS= read -r check_name; do\n              [ -n \"$check_name\" ] || continue\n              state=\"$(\n                jq -r --arg name \"$check_name\" '\n                  [.statusCheckRollup[]?\n                    | select((.name // .context // \"\") == $name)]\n                  | if length == 0 then\n                      \"MISSING\"\n                    else\n                      (sort_by(.startedAt // .createdAt // \"\") | last\n                        | if has(\"status\") then (.status // \"MISSING\")\n                          else (.state // \"MISSING\")\n                          end)\n                    end\n                ' .shiki/gha/pr-settling.json\n              )\"\n              case \"$state\" in\n                COMPLETED|SUCCESS|FAILURE|ERROR)\n                  ;;\n                *)\n                  pending=\"${pending}${pending:+, }${check_name}=${state}\"\n                  ;;\n              esac\n            done < .shiki/gha/required-checks.txt\n\n            if [ -z \"$pending\" ]; then\n              echo \"Required checks are terminal on head ${EXPECTED_HEAD_SHA}; refreshing CCA context.\"\n              break\n            fi\n            if (( SECONDS >= deadline )); then\n              echo \"::error::Timed out after ${WAIT_TIMEOUT_SECONDS}s waiting for required checks on head ${EXPECTED_HEAD_SHA}: ${pending}\"\n              exit 1\n            fi\n            echo \"Waiting for required checks on head ${EXPECTED_HEAD_SHA}: ${pending}\"\n            sleep \"$WAIT_INTERVAL_SECONDS\"\n          done\n\n          # The judge must consume the settled rollup, not the snapshot gathered at\n          # the beginning of this concurrently-triggered workflow run.\n          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n          gh pr view \"$PR_NUMBER\" \\\n            --repo \"$REPOSITORY\" \\\n            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n\n      - name: Prepare CCA transport schema\n        id: schema\n        shell: bash\n        run: |\n          cat > \"$RUNNER_TEMP/cca-transport.schema.json\" <<'JSON'\n          {\n            \"type\": \"object\",\n            \"required\": [\n              \"verdict\",\n              \"summary\",\n              \"goal_id\",\n              \"task_id\",\n              \"pr\",\n              \"head_sha\",\n              \"can_merge\",\n              \"checklist\",\n              \"acceptance\",\n              \"mergegate\",\n              \"confidence\"\n            ],\n            \"properties\": {\n              \"verdict\": {\n                \"enum\": [\n                  \"complete\",\n                  \"repair_required\",\n                  \"blocked\",\n                  \"needs_guardian\",\n                  \"insufficient_evidence\"\n                ]\n              },\n              \"summary\": { \"type\": \"string\" },\n              \"goal_id\": { \"type\": \"string\" },\n              \"task_id\": { \"type\": \"string\" },\n              \"pr\": { \"type\": \"integer\" },\n              \"head_sha\": { \"type\": \"string\" },\n              \"can_merge\": { \"type\": \"boolean\" },\n              \"blocking_reason\": { \"type\": \"string\" },\n              \"checklist\": {\n                \"type\": \"array\",\n                \"minItems\": 1,\n                \"items\": {\n                  \"type\": \"object\",\n                  \"required\": [\"id\", \"status\", \"blocking\"],\n                  \"properties\": {\n                    \"id\": { \"type\": \"string\" },\n                    \"status\": {\n                      \"type\": \"string\",\n                      \"enum\": [\"pass\", \"fail\", \"insufficient_evidence\", \"not_applicable\"]\n                    },\n                    \"blocking\": { \"type\": \"boolean\" },\n                    \"evidence\": { \"type\": \"string\" },\n                    \"reason\": { \"type\": \"string\" }\n                  },\n                  \"additionalProperties\": true\n                }\n                -H \"Accept: application/vnd.github+json\" \\\n                -H \"Authorization: Bearer ${GH_TOKEN}\" \\\n                -H \"X-GitHub-Api-Version: 2022-11-28\" \\\n                \"https://api.github.com/repos/${REPOSITORY}/pulls/${PR_NUMBER}/reviews\" \\\n                --data @.shiki/gha/create-review-request.json\n            )\"\n\n            echo \"REST create review HTTP status: ${http_status}\"\n            cat .shiki/gha/create-review-response.json || true\n\n            case \"$http_status\" in\n              200|201)\n                echo \"CCA Review Bridge REST approval submitted.\"\n                ;;\n              *)\n                echo \"::error::CCA Review Bridge REST approval failed with HTTP ${http_status}.\"\n                echo \"::error::If this is 403 after can_approve_pull_request_reviews=true, GITHUB_TOKEN cannot create the required approval in this repository context. Use a non-author reviewer bot token or GitHub App token.\"\n                exit 1\n                ;;\n            esac\n          else\n            echo \"CCA Review Bridge approval already exists for ${bot_login}; skipping create-review call.\"\n          fi\n\n          sleep 5\n\n          gh api \"repos/${REPOSITORY}/pulls/${PR_NUMBER}/reviews\" > .shiki/gha/reviews-after-bridge.json\n\n          approved_after=\"$(\n            jq -r --arg login \"$bot_login\" '\n              [.[]? | select((.state // \"\" | ascii_upcase) == \"APPROVED\") | select(.user.login == $login)] | length\n            ' .shiki/gha/reviews-after-bridge.json\n          )\"\n\n          echo \"APPROVED reviews by ${bot_login} after bridge: ${approved_after}\"\n\n          if [ \"$approved_after\" = \"0\" ]; then\n            echo \"::error::CCA Review Bridge did not produce a visible APPROVED review.\"\n            exit 1\n          fi\n\n          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n          gh pr view \"$PR_NUMBER\" \\\n            --repo \"$REPOSITORY\" \\\n            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n            > .shiki/gha/pr-refresh.json\n          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n            .shiki/gha/pr-refresh.json > .shiki/gha/pr.json\n          rm .shiki/gha/pr-refresh.json\n\n          echo \"::endgroup::\"\n\n      - name: Build CCA evidence manifest\n        run: |\n          python3 scripts/build_cca_evidence_manifest.py \\\n            --repo \"${{ github.repository }}\" \\\n            --pr \"${{ github.event.pull_request.number || inputs.pr_number }}\" \\\n            --head-sha \"${{ github.event.pull_request.head.sha || github.sha }}\" \\\n            --workflow-name \"Shiki CCA Completion\" \\\n            --run-id \"${{ github.run_id }}\" \\\n            --run-attempt \"${{ github.run_attempt }}\" \\\n            --event-name \"${{ github.event_name }}\" \\\n            --artifact-name \"shiki-cca-evidence\" \\\n            --evidence-dir .shiki/gha \\\n            --output .shiki/gha/cca-evidence-manifest.json\n\n      - name: Upload CCA evidence\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: shiki-cca-evidence\n          path: .shiki/gha\n\n  mergegate:\n    name: MergeGate policy check\n    runs-on: ubuntu-latest\n    needs: cca\n    # MergeGate only reads PR state to reach a verdict; it must never mutate the\n    # PR. Pin this job to read-only, overriding the workflow write grants the CCA\n    # job needs (enforced by WORKFLOW_CONTRACTS job_permissions in validate_shiki).\n    permissions:\n      contents: read\n      pull-requests: read\n      issues: read\n      checks: read\n      actions: read\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n          ref: ${{ github.event.pull_request.head.sha || github.sha }}\n\n      - name: Reset CCA evidence runtime directory\n        run: |\n          rm -rf .shiki/gha\n          mkdir -p .shiki/gha\n\n      - name: Download CCA evidence\n        uses: actions/download-artifact@v4\n        with:\n          name: shiki-cca-evidence\n          path: .shiki/gha\n\n      - name: Validate Shiki artifacts\n        run: python3 scripts/validate_shiki.py\n\n      - name: Refresh live PR state\n        shell: bash\n        env:\n          GH_TOKEN: ${{ github.token }}\n          PR_NUMBER: ${{ github.event.pull_request.number || inputs.pr_number }}\n        run: |\n          set -euo pipefail\n\n          rm -f .shiki/gha/live-pr.json .shiki/gha/live-changed-files.txt .shiki/gha/live-changed-files-status.txt\n          rm -f .shiki/gha/live-guardian-comments.json .shiki/gha/live-guardian-events.json .shiki/gha/live-guardian-timeline.json\n          rm -rf .shiki/gha/base-shiki\n          mkdir -p .shiki/gha/base-shiki\n\n          gh pr view \"$PR_NUMBER\" \\\n            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n            > .shiki/gha/live-pr.json\n\n          gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/comments\" \\\n            > .shiki/gha/live-guardian-comments.json\n\n          gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/events\" \\\n            > .shiki/gha/live-guardian-events.json\n\n          gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/timeline\" \\\n            -H \"Accept: application/vnd.github+json\" \\\n            > .shiki/gha/live-guardian-timeline.json\n\n          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/live-pr.json)\"\n          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/live-pr.json)\"\n          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n\n          git fetch origin \"$base_ref\"\n\n          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n            .shiki/gha/live-pr.json > .shiki/gha/live-pr-with-merge-base.json\n          mv .shiki/gha/live-pr-with-merge-base.json .shiki/gha/live-pr.json\n\n          git diff --name-only \"${merge_base}...HEAD\" \\\n            > .shiki/gha/live-changed-files.txt\n\n          git diff --name-status \"${merge_base}...HEAD\" \\\n            > .shiki/gha/live-changed-files-status.txt\n\n          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n\n          current_head=\"$(git rev-parse HEAD)\"\n          if [ \"$head_oid\" != \"$current_head\" ]; then\n            echo \"::error::Live PR headRefOid ${head_oid} does not match checked out HEAD ${current_head}\"\n            exit 1\n          fi\n\n      - name: Enforce MergeGate\n        env:\n          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}\n          GH_TOKEN: ${{ github.token }}\n          REPOSITORY: ${{ github.repository }}\n          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n        run: |\n          set -euo pipefail\n\n          # Implementation-PR merge proof, resolved for EVERY judged PR (not only\n          # post_merge_reconcile), the SAME way the Prepare CCA context step\n          # resolves it for the CCA signal. Resolve the task id with the SAME\n          # semantics mergegate_check.py uses (TASK_ID = T-<4+ digits> OR\n          # T-<timestamp>-<hex>, first match; the validator also rejects a body\n          # with more than one task id, so this first-match can never disagree\n          # with it), read the BASE snapshot's expected_pr (a field the PR head\n          # cannot write, unlike the head's) and pass it as --merged-prs only when\n          # gh proves it MERGED. Fails closed to \"\" on any missing or unresolvable\n          # input.\n          #\n          # This must run for every mode. SADR-0017's bookkeeping-closeout\n          # exemption (condition 6) needs the implementation PR proven merged, and\n          # a closeout carries NO post_merge_reconcile marker; gating this behind\n          # the marker left MERGED_PRS empty for closeouts, so the exemption could\n          # never be established. post_merge_reconcile PRs still receive exactly\n          # this value \u2014 that mode already consumes --merged-prs to prove its\n          # referenced PR merged \u2014 so hoisting the resolution out of the marker\n          # test is additive and changes no existing behaviour.\n          MERGED_PRS=\"\"\n          body=\"$(jq -r '.body // \"\"' .shiki/gha/live-pr.json)\"\n          task_id=\"$(printf '%s' \"$body\" | grep -oE 'T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})' | head -1 || true)\"\n          base_task=\".shiki/gha/base-shiki/.shiki/tasks/${task_id}.json\"\n          if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n            base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n            if [ -n \"$base_pr\" ]; then\n              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n              if [ \"$state\" = \"MERGED\" ]; then\n                MERGED_PRS=\"$base_pr\"\n              fi\n            fi\n          fi\n\n          # SADR-0015 Contract Approval carry, built the SAME way the CCA context\n          # step builds it (from the base-branch adding commit and the MERGED PR\n          # behind it) and passed to mergegate_check.py via --contract-approval. The\n          # base fetch in \"Refresh live PR state\" already retrieved origin/<base_ref>\n          # \u2014 reuse it; do NOT add a second fetch and do NOT use --depth (a shallow\n          # base makes `git log --diff-filter=A` return nothing and silently defeats\n          # the carry, the exact defect PR #206 removed). Field names match the\n          # evaluator (guardian_source, adding_commit_pr). EVERY failure path writes a\n          # well-formed JSON object whose values DEFEAT the carry with a reason.\n          approval_file=.shiki/gha/contract-approval.json\n          ca_base_ref=\"$(jq -r '.baseRefName // \"\"' .shiki/gha/live-pr.json 2>/dev/null || echo \"\")\"\n          if [ -z \"$task_id\" ]; then\n            jq -n '{applies: false, reason: \"contract approval does not apply: no Shiki task id in the PR body\"}' > \"$approval_file\"\n          elif [ -z \"$ca_base_ref\" ]; then\n            jq -n --arg t \"$task_id\" '{task_id: $t, applies: false, reason: \"contract approval does not apply: no base ref resolved\"}' > \"$approval_file\"\n          else\n            adding_commit=\"$(git log --diff-filter=A --format=%H \"origin/${ca_base_ref}\" -- \".shiki/tasks/${task_id}.json\" 2>/dev/null | tail -1 || true)\"\n            if [ -z \"$adding_commit\" ]; then\n              jq -n --arg t \"$task_id\" '{task_id: $t, merged: false, applies: false, reason: \"contract approval does not apply: no commit adds this task file on the base branch (a first-registration PR is never covered)\"}' > \"$approval_file\"\n            else\n              pulls=\"$(gh api \"repos/${REPOSITORY}/commits/${adding_commit}/pulls\" 2>/dev/null || echo \"[]\")\"\n              reg_pr=\"$(printf '%s' \"$pulls\" | jq 'map(select(.merged_at != null)) | (.[0].number // empty)' 2>/dev/null || echo \"\")\"\n              if [ -z \"$reg_pr\" ]; then\n                jq -n --arg t \"$task_id\" --arg c \"$adding_commit\" '{task_id: $t, adding_commit: $c, merged: false, applies: false, reason: \"contract approval does not apply: no merged PR is associated with the adding commit\"}' > \"$approval_file\"\n              else\n                guardian_approved=\"$(printf '%s' \"$pulls\" | jq --argjson n \"$reg_pr\" 'any(.[]; .number == $n and ((.labels // []) | any(.name == \"guardian:approved\")))' 2>/dev/null || echo false)\"\n                jq -n \\\n                  --arg t \"$task_id\" \\\n                  --argjson pr \"$reg_pr\" \\\n                  --arg c \"$adding_commit\" \\\n                  --argjson ga \"$guardian_approved\" \\\nname: Shiki MergeGate\n\non:\n  pull_request:\n    types:\n      - opened\n      - synchronize\n      - reopened\n      - ready_for_review\n      - edited\n      - labeled\n      - unlabeled\n  workflow_dispatch:\n\nconcurrency:\n  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}\n  cancel-in-progress: true\n\nenv:\n  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true\n\npermissions:\n  contents: read\n  pull-requests: read\n  issues: read\n  checks: read\n  actions: read\n\njobs:\n  mergegate:\n    name: MergeGate metadata check\n    runs-on: ubuntu-latest\n    if: github.event_name == 'workflow_dispatch' || ((github.event.action != 'labeled' && github.event.action != 'unlabeled') || github.event.label.name == 'guardian:approved' || startsWith(github.event.label.name, 'mergegate:'))\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v5\n        with:\n          fetch-depth: 0\n          ref: ${{ github.event.pull_request.head.sha || github.sha }}\n\n      - name: Validate Shiki artifacts\n        run: python3 scripts/validate_shiki.py\n\n      - name: Prepare PR context\n        if: github.event_name == 'pull_request'\n        shell: bash\n        env:\n          GH_TOKEN: ${{ github.token }}\n          PR_NUMBER: ${{ github.event.pull_request.number }}\n          REPOSITORY: ${{ github.repository }}\n        run: |\n          set -euo pipefail\n          rm -rf .shiki/gha\n          mkdir -p .shiki/gha\n          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n          git fetch origin \"$base_ref\"\n          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n\n          # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n          # Guardian gate from the Goal's frozen-plan risk in THIS metadata job,\n          # independent of any CCA verdict, so the check needs the same live\n          # comment/label/timeline evidence shiki-cca-completion.yml gathers.\n          # Without these files a high/critical contract mode PR blocks on a\n          # MISSING INPUT (\"Guardian comments evidence file is missing\"), not a\n          # missing approval. Fall back to an empty array so a fetch failure is a\n          # deterministic \"no approval evidence\", never a broken run.\n          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/comments\" > .shiki/gha/live-guardian-comments.json || echo \"[]\" > .shiki/gha/live-guardian-comments.json\n          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/events\" > .shiki/gha/live-guardian-events.json || echo \"[]\" > .shiki/gha/live-guardian-events.json\n          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/timeline\" -H \"Accept: application/vnd.github+json\" > .shiki/gha/live-guardian-timeline.json || echo \"[]\" > .shiki/gha/live-guardian-timeline.json\n\n          # Base .shiki snapshot from the PR's immutable merge base, built the SAME way\n          # shiki-cca-completion.yml builds it. mergegate_check.py needs it to\n          # verify task-field immutability for a post_merge_reconcile PR and to\n          # resolve the SADR-0017 bookkeeping-closeout exemption; without it a\n          # post_merge_reconcile PR blocks on \"requires a base .shiki snapshot\".\n          # The fetch above already ran \u2014 do NOT add a second, or the base fetch\n          # would go shallow.\n          rm -rf .shiki/gha/base-shiki\n          mkdir -p .shiki/gha/base-shiki\n          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n\n      - name: MergeGate metadata check\n        env:\n          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}\n          GH_TOKEN: ${{ github.token }}\n          REPOSITORY: ${{ github.repository }}\n          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n        run: |\n          set -euo pipefail\n\n          # Implementation-PR merge proof, resolved the SAME way the CCA workflow\n          # resolves it (TASK_ID = T-<4+ digits> OR T-<timestamp>-<hex>, first\n          # match; read expected_pr from the BASE snapshot task file \u2014 a field the\n          # PR head cannot forge \u2014 and include the PR only when gh proves it\n          # MERGED). Fails closed to \"\" on any missing or unresolvable input. A\n          # post_merge_reconcile PR consumes this to prove its referenced PR\n          # merged; without it the metadata check can never pass one.\n          MERGED_PRS=\"\"\n          body=\"$(jq -r '.body // \"\"' .shiki/gha/pr.json 2>/dev/null || echo \"\")\"\n          task_id=\"$(printf '%s' \"$body\" | grep -oE 'T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})' | head -1 || true)\"\n          base_task=\".shiki/gha/base-shiki/.shiki/tasks/${task_id}.json\"\n          if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n            base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n            if [ -n \"$base_pr\" ]; then\n              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n              if [ \"$state\" = \"MERGED\" ]; then\n                MERGED_PRS=\"$base_pr\"\n              fi\n            fi\n          fi\n\n          # SADR-0015 Contract Approval carry, built the SAME way shiki-cca-completion.yml\n          # builds it (from the base-branch adding commit and the MERGED PR behind it)\n          # and passed to mergegate_check.py via --contract-approval. The base fetch in\n          # \"Prepare PR context\" already retrieved origin/<base_ref> \u2014 reuse it; do NOT\n          # add a second fetch and do NOT use --depth (a shallow base makes\n          # `git log --diff-filter=A` return nothing and silently defeats the carry, the\n          # exact defect PR #206 removed). Field names match the evaluator\n          # (guardian_source, adding_commit_pr). EVERY failure path writes a well-formed\n          # JSON object whose values DEFEAT the carry with a reason.\n          approval_file=.shiki/gha/contract-approval.json\n          ca_base_ref=\"$(jq -r '.baseRefName // \"\"' .shiki/gha/pr.json 2>/dev/null || echo \"\")\"\n          if [ -z \"$task_id\" ]; then\n            jq -n '{applies: false, reason: \"contract approval does not apply: no Shiki task id in the PR body\"}' > \"$approval_file\"\n          elif [ -z \"$ca_base_ref\" ]; then\n            jq -n --arg t \"$task_id\" '{task_id: $t, applies: false, reason: \"contract approval does not apply: no base ref resolved\"}' > \"$approval_file\"\n          else\n            adding_commit=\"$(git log --diff-filter=A --format=%H \"origin/${ca_base_ref}\" -- \".shiki/tasks/${task_id}.json\" 2>/dev/null | tail -1 || true)\"\n            if [ -z \"$adding_commit\" ]; then\n              jq -n --arg t \"$task_id\" '{task_id: $t, merged: false, applies: false, reason: \"contract approval does not apply: no commit adds this task file on the base branch (a first-registration PR is never covered)\"}' > \"$approval_file\"\n            else\n              pulls=\"$(gh api \"repos/${REPOSITORY}/commits/${adding_commit}/pulls\" 2>/dev/null || echo \"[]\")\"\n              reg_pr=\"$(printf '%s' \"$pulls\" | jq 'map(select(.merged_at != null)) | (.[0].number // empty)' 2>/dev/null || echo \"\")\"\n              if [ -z \"$reg_pr\" ]; then\n                jq -n --arg t \"$task_id\" --arg c \"$adding_commit\" '{task_id: $t, adding_commit: $c, merged: false, applies: false, reason: \"contract approval does not apply: no merged PR is associated with the adding commit\"}' > \"$approval_file\"\n              else\n                guardian_approved=\"$(printf '%s' \"$pulls\" | jq --argjson n \"$reg_pr\" 'any(.[]; .number == $n and ((.labels // []) | any(.name == \"guardian:approved\")))' 2>/dev/null || echo false)\"\n                jq -n \\\n                  --arg t \"$task_id\" \\\n                  --argjson pr \"$reg_pr\" \\\n                  --arg c \"$adding_commit\" \\\n                  --argjson ga \"$guardian_approved\" \\\n                and set(self_reference_evidence) == required_self_reference_evidence\n            ):\n                return False\n            self_reference_ids.append(added_ledger_id)\n        if len(self_reference_ids) != 1:\n            return False\n        permitted_ledger_ids = {self_reference_ids[0]}\n        if completion_ledger_id is not None:\n            permitted_ledger_ids.add(completion_ledger_id)\n        permitted_ledger_paths = {\n            f\".shiki/ledger/{ledger_id}.json\" for ledger_id in permitted_ledger_ids\n        }\n        if (\n            set(added_task_ledger_ids) != permitted_ledger_ids\n            or set(changed_ledger_paths) != permitted_ledger_paths\n        ):\n            return False\n\n        permitted_changed_paths = {task_file, lock_file, *permitted_ledger_paths}\n        if completes_goal:\n            if completion_report_path is None:\n                return False\n            permitted_changed_paths.update({goal_file, completion_report_path})\n        if set(changed_paths) != permitted_changed_paths:\n            return False\n\n        return True\n    except (OSError, ValueError, TypeError):\n        # Unreadable/malformed evidence is fail-closed: NOT a bookkeeping closeout.\n        return False\n\n\ndef _load_guardian_evidence(\n    *,\n    path: str,\n    target: Path,\n    required: bool,\n    blocking: list[str],\n    warnings: list[str],\n    description: str,\n) -> list[dict[str, Any]]:\n    evidence_path = Path(path)\n    if not evidence_path.is_absolute():\n        evidence_path = target / evidence_path\n    if not evidence_path.exists():\n        message = f\"Guardian {description} evidence file is missing at {evidence_path}\"\n        if required:\n            blocking.append(message)\n        else:\n            warnings.append(message)\n        return []\n    try:\n        return load_json_list(evidence_path)\n    except (OSError, json.JSONDecodeError, ValueError) as error:\n        message = f\"Guardian {description} evidence file is invalid: {error}\"\n        if required:\n            blocking.append(message)\n        else:\n            warnings.append(message)\n        return []\n\n\ndef enforce_guardian_policy(\n    *,\n    pr: dict[str, Any],\n    task: dict[str, Any] | None,\n    target: Path,\n    guardian_policy: str,\n    guardian_comments: str,\n    guardian_events: str,\n    guardian_timeline: str,\n    blocking: list[str],\n    warnings: list[str],\n    expected_repository: str = \"\",\n    bookkeeping_closeout: bool = False,\n    contract_approval: Any = None,\n    base_sync_carry: bool = False,\n    default_branch: str = \"\",\n) -> None:\n    risk_labels = _guardian_risk_labels(pr, task, bookkeeping_closeout=bookkeeping_closeout)\n    requires_guardian = _builtin_guardian_risk_required(risk_labels)\n    try:\n        policy = load_guardian_policy_file(_guardian_policy_path(target, guardian_policy))\n    except GuardianPolicyError as error:\n        if requires_guardian:\n            blocking.append(f\"Guardian policy is required for high/critical risk PRs: {error}\")\n        else:\n            warnings.append(f\"Guardian policy could not be loaded; skipping Guardian check for non-high-risk PR: {error}\")\n        return\n    policy_errors = validate_guardian_policy(policy)\n    if policy_errors:\n        if requires_guardian:\n            blocking.extend(f\"Guardian policy validation failed: {error}\" for error in policy_errors)\n        else:\n            warnings.extend(f\"Guardian policy validation warning: {error}\" for error in policy_errors)\n        return\n    requires_guardian = requires_guardian or risk_requires_guardian(risk_labels, policy)\n    if not requires_guardian:\n        return\n\n    # SADR-0015 Contract Approval OR-branch (\"Evaluation placement\"). The Guardian\n    # requirement is satisfied by EITHER a live PR approval (the\n    # ``evaluate_guardian_approval`` path below) OR a proven Contract Approval: a\n    # normal implementation PR whose task contract was registered to the default\n    # branch and Guardian-approved BEFORE dispatch. ``evaluate_guardian_approval``\n    # is never consulted or modified here, so a defect in the contract path\n    # degrades to \"does not apply\" and the live-approval path below runs\n    # byte-for-byte as today. When the carry applies, live-approval evidence is\n    # not required at all \u2014 the approval was recorded on the merged Contract PR.\n    if contract_approval is not None and getattr(contract_approval, \"applies\", False):\n        warnings.append(\n            \"Guardian approval satisfied by \"\n            + \", \".join(contract_approval.sources)\n            + \" (SADR-0015 Contract Approval: the task contract was registered and Guardian-approved before dispatch)\"\n        )\n        return\n\n    blocker_count_before_evidence = len(blocking)\n    comments = _load_guardian_evidence(\n        path=guardian_comments,\n        target=target,\n        required=True,\n        blocking=blocking,\n        warnings=warnings,\n        description=\"comments\",\n    )\n    events = _load_guardian_evidence(\n        path=guardian_events,\n        target=target,\n        required=True,\n        blocking=blocking,\n        warnings=warnings,\n        description=\"label events\",\n    )\n    timeline = _load_guardian_evidence(\n        path=guardian_timeline,\n        target=target,\n        required=False,\n        blocking=blocking,\n        warnings=warnings,\n        description=\"timeline\",\n    )\n    if len(blocking) > blocker_count_before_evidence:\n        return\n\n    reviews = [review for review in pr.get(\"reviews\") or [] if isinstance(review, dict)]\n    head_sha = str(pr.get(\"headRefOid\") or \"\")\n    # The carry evaluates the TASK's resolved risk (a proven bookkeeping closeout\n    # is treated as low here, identically to _guardian_risk_labels), never the PR\n    # labels \u2014 a label escalation is refused inside the evaluator. The git proof\n    # runs in-process against ``target`` and is never persisted.\n    carry_resolved_risk = \"low\" if bookkeeping_closeout else str((task or {}).get(\"risk_level\") or \"\")\n    result = evaluate_guardian_approval(\n        policy=policy,\n        pr=pr,\n        reviews=reviews,\n        comments=comments,\n        label_events=events + timeline,\n        head_sha=head_sha,\n        expected_repo=expected_repository,\n        base_sync_carry=base_sync_carry,\n        default_branch=default_branch,\n        carry_target=target,\n        resolved_risk=carry_resolved_risk,\n    )\n    warnings.extend(result.warnings)\n    if not result.approved:\n        blocking.extend(result.blockers or (\"Guardian approval is required but policy-backed evidence is missing\",))\n    else:\n        approver_desc = \", \".join(result.approvers) if result.approvers else \"\"\n        if result.ai_reviewers:\n            # External AI guardian review: record the AI reviewer identity\n            # distinctly (reviewer_type=external_ai_model), never as a human.\n            ai_desc = \"reviewer_type=external_ai_model (\" + \", \".join(result.ai_reviewers) + \")\"\n            approver_desc = (approver_desc + \"; \" + ai_desc) if approver_desc else ai_desc\n        warnings.append(\n            \"Guardian approval satisfied by \"\n            + \", \".join(result.sources)\n            + \" from \"\n            + (approver_desc or \"<unknown>\")\n        )\n\n\ndef load_ledger_entries(target: Path, task: dict[str, Any], warnings: list[str], blocking: list[str]) -> list[dict[str, Any]]:\n    entries: list[dict[str, Any]] = []\n    for ledger_id in task.get(\"ledger_evidence\") or []:\n        if not isinstance(ledger_id, str):\n            blocking.append(\"Task ledger_evidence contains a non-string entry\")\n            continue\n        path = target / \".shiki\" / \"ledger\" / f\"{ledger_id}.json\"\n        entry = load_json(path)\n        if entry is None:\n            blocking.append(f\"Task ledger evidence {ledger_id} is missing at {path}\")\n            continue\n        entries.append(entry)\n    if not entries:\n        blocking.append(\"Task has no ledger evidence entries\")\n    return entries\n\n\ndef normalize_repo_path(path: str) -> str:\n    normalized = path.strip().replace(\"\\\\\", \"/\")\n    while normalized.startswith(\"./\"):\n        normalized = normalized[2:]\n    return normalized\n\n#!/usr/bin/env python3\n\"\"\"Emit a deterministic Guardian-approval signal for the CCA judge.\n\nThe CCA completion judge must not decide Guardian approval by interpreting raw\nPR comments with an LLM \u2014 \"LLM outputs may vary. State transitions must not\nvary.\" This helper runs the SAME authoritative ``evaluate_guardian_approval``\nused by the MergeGate policy check against the live PR comments/events, and\nwrites a small JSON signal the CCA reads for CCA-08.\n\nIt never approves on its own: it reports whether a recorded authority (human\nlabel/review/comment OR an external AI guardian review artifact, SADR-0010)\napproved the exact current head. The MergeGate policy check remains the\nauthoritative gate; this only lets the CCA see the same result deterministically.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport re\nfrom pathlib import Path\nfrom typing import Any\n\nfrom shiki_guardian import (\n    GuardianPolicyError,\n    evaluate_guardian_approval,\n    load_guardian_policy_file,\n    risk_requires_guardian,\n    validate_guardian_policy,\n)\n\n# The bookkeeping-closeout exemption (SADR-0017) must resolve risk IDENTICALLY on\n# this signal path and on the MergeGate policy check. Importing and calling the\n# SAME classifier \u2014 never a second copy of its six conditions \u2014 is what keeps the\n# two Guardian decision points from diverging. ``parse_changed_files_status``\n# comes from the same module so the diff is parsed exactly as MergeGate parses it.\nfrom mergegate_check import (\n    is_bookkeeping_closeout,\n    parse_changed_files_status,\n    resolved_guardian_risk_never_weaker,\n)\n\n# The canonical Shiki task-id pattern, identical to the MergeGate policy check's\n# TASK_ID. The signal MUST resolve risk the same way MergeGate does so the two\n# gates never diverge.\n_ID_SUFFIX = r\"(?:[0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})\"\n_TASK_ID_RE = re.compile(rf\"\\bT-{_ID_SUFFIX}\\b\")\n\n\ndef _load_json(path: str) -> Any:\n    if not path:\n        return None\n    p = Path(path)\n    if not p.exists():\n        return None\n    try:\n        return json.loads(p.read_text(encoding=\"utf-8\"))\n    except (OSError, ValueError):\n        return None\n\n\ndef _as_list(value: Any) -> list[dict[str, Any]]:\n    return [item for item in value if isinstance(item, dict)] if isinstance(value, list) else []\n\n\ndef _label_names(pr: dict[str, Any]) -> list[str]:\n    names: list[str] = []\n    for label in pr.get(\"labels\") or []:\n        if isinstance(label, dict):\n            label = label.get(\"name\")\n        if label:\n            names.append(str(label).strip().lower())\n    return names\n\n\ndef _builtin_high_or_critical(labels: list[str]) -> bool:\n    normalized = {label.removeprefix(\"risk:\") for label in labels}\n    return bool(normalized.intersection({\"high\", \"critical\"}))\n\n\ndef _resolve_task_risk_level(shiki_root: str, pr_body: str) -> str | None:\n    \"\"\"Resolve the task risk the SAME way the MergeGate policy check does: by the\n    first Shiki task id in the PR body (not by an expected_pr glob, which fails\n    open on a null/stale/string expected_pr or a corrupt task file).\n\n    Returns the lowercase ``risk_level`` of the resolved task, or ``None`` when it\n    cannot be determined \u2014 in which case the caller MUST fail closed (require\n    Guardian approval) rather than letting an undetermined risk collapse to\n    \"not required\".\n    \"\"\"\n    match = _TASK_ID_RE.search(pr_body or \"\")\n    if not match:\n        return None\n    task = _load_json(str(Path(shiki_root) / \".shiki\" / \"tasks\" / f\"{match.group(0)}.json\"))\n    if not isinstance(task, dict):\n        return None\n    risk = str(task.get(\"risk_level\") or \"\").strip().lower()\n    return risk or None\n\n\ndef _parse_merged_prs(value: str) -> set[int]:\n    \"\"\"Parse the comma-separated ``--merged-prs`` list exactly as MergeGate does.\"\"\"\n    return {int(token.strip()) for token in str(value or \"\").split(\",\") if token.strip().isdigit()}\n\n\ndef _carry_resolved_risk(*, shiki_root: str, base_shiki: str, pr_body: str, task_risk: str | None, exemption: bool) -> str:\n    \"\"\"Resolve the carry's Guardian risk the SAME way the MergeGate gate does.\n\n    The gate floors the head task risk against the base snapshot\n    (``resolved_guardian_risk_never_weaker`` at the normal-path call site), so a PR\n    that LOWERED its own ``risk_level`` still gates at the base risk. The signal\n    must apply the identical floor, or it would be MORE permissive than the gate: a\n    base=critical / head=high task would carry in the signal while the gate refuses\n    it. A proven bookkeeping closeout is evaluated as low, identically to the gate.\n    Falls back to the head risk when the task cannot be resolved (fail closed via\n    the caller's required-gate).\n    \"\"\"\n    if exemption:\n        return \"low\"\n    match = _TASK_ID_RE.search(pr_body or \"\")\n    if not match:\n        return str(task_risk or \"\")\n    task_id = match.group(0)\n    head_task = _load_json(str(Path(shiki_root) / \".shiki\" / \"tasks\" / f\"{task_id}.json\"))\n    if not isinstance(head_task, dict):\n        return str(task_risk or \"\")\n    base_task = _load_json(str(Path(base_shiki) / \"tasks\" / f\"{task_id}.json\")) if base_shiki else None\n    return resolved_guardian_risk_never_weaker(base_task if isinstance(base_task, dict) else None, head_task)\n\n\ndef _load_changed_files_status(path: str) -> list | None:\n    \"\"\"Parse the git ``--name-status`` changed-files file into ChangedFile entries.\n\n    Returns ``None`` when the path is empty or the file is absent, so the caller\n    FAILS CLOSED (no exemption) rather than classifying against an assumed diff. A\n    real-but-empty status list is intentionally NOT ``None``: the classifier will\n    simply find no terminal transitions and reject it.\n    \"\"\"\n    if not path:\n        return None\n    status_path = Path(path)\n    if not status_path.exists():\n        return None\n    return parse_changed_files_status(status_path, [])\n\n\ndef _bookkeeping_closeout_exemption(\n    *,\n    shiki_root: str,\n    pr_body: str,\n    base_shiki: str,\n    changed_files_status: str,\n    merged_prs: str,\n) -> bool:\n    \"\"\"Whether this PR is a proven SADR-0017 bookkeeping closeout.\n\n    The entire decision is delegated to the SAME ``is_bookkeeping_closeout``\n    classifier the MergeGate policy check calls, over the SAME inputs (the base\n    ``.shiki`` snapshot, the changed-files status, and the merged-PR proof). Reusing\n    the one classifier \u2014 never a second copy of its six conditions \u2014 is what keeps\n    this signal and MergeGate from diverging.\n\n    FAILS CLOSED (returns ``False``, i.e. no exemption) on every axis: no task id\n    in the PR body, an unreadable/missing head task file, or a missing changed-files\n    status file returns ``False`` here; a missing/unreadable base snapshot,\n    unresolvable implementation PR, or any disqualifying diff returns ``False`` from\n    the classifier itself. The classifier never reads the PR, a label, or a marker,\n    so only durable evidence can grant the exemption.\n    \"\"\"\n    match = _TASK_ID_RE.search(pr_body or \"\")\n    if not match:\n        return False\n    task_id = match.group(0)\n    target = Path(shiki_root)\n    task = _load_json(str(target / \".shiki\" / \"tasks\" / f\"{task_id}.json\"))\n    if not isinstance(task, dict):\n        return False\n    files_status = _load_changed_files_status(changed_files_status)\n    if files_status is None:\n        return False\n    return is_bookkeeping_closeout(\n        target=target,\n        task=task,\n        task_id=task_id,\n        goal_id=str(task.get(\"goal_id\") or \"\"),\n        base_shiki=Path(base_shiki) if base_shiki else None,\n        changed_files_status=files_status,\n        merged_pr_numbers=_parse_merged_prs(merged_prs),\n    )\n\n\n2026-08-17T10:08:45.445953Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"nl -ba scripts/guardian_approval_signal.py | sed -n '292,380p'; nl -ba scripts/mergegate_check.py | sed -n '650,760p'; nl -ba scripts/mergegate_check.py | sed -n '3420,3480p'; rg -n 'resolve_base_migration_registry|mergeBaseOid|immutable mergeBaseOid' scripts/mergegate_check.py tests/test_migration_mode.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n   292\t        default=\"\",\n   293\t        help=\"Default branch name the carry proves a pure sync against. Empty disables the carry.\",\n   294\t    )\n   295\t    parser.add_argument(\"--output\", required=True)\n   296\t    args = parser.parse_args(argv)\n   297\t\n   298\t    pr = _load_json(args.pr_json)\n   299\t    if not isinstance(pr, dict):\n   300\t        signal = {\n   301\t            \"required\": True,\n   302\t            \"approved\": False,\n   303\t            \"error\": \"pr.json missing or invalid\",\n   304\t            \"bookkeeping_closeout_exemption\": False,\n   305\t        }\n   306\t        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n   307\t        return 0\n   308\t\n   309\t    # Authoritative risk comes from the task resolved by the PR-body task id \u2014\n   310\t    # exactly how the MergeGate policy check resolves it. When the risk cannot be\n   311\t    # determined (no task id in the body, missing/corrupt task file, or no\n   312\t    # risk_level), FAIL CLOSED: require Guardian approval rather than let an\n   313\t    # undetermined risk collapse to \"not required\" (which would let the CCA treat\n   314\t    # CCA-08 as not applicable for an unapproved high/critical PR).\n   315\t    head_sha = str(pr.get(\"headRefOid\") or \"\")\n   316\t    pr_body = str(pr.get(\"body\") or \"\")\n   317\t    task_risk = _resolve_task_risk_level(args.shiki_root, pr_body)\n   318\t\n   319\t    # SADR-0017: a PROVEN bookkeeping closeout carries no implementation and\n   320\t    # inherits no new risk, so at this single Guardian decision point the task's\n   321\t    # risk is evaluated as if low \u2014 IDENTICALLY to the MergeGate policy check,\n   322\t    # which calls the same is_bookkeeping_closeout classifier over the same inputs.\n   323\t    # It fails closed to \"no exemption\" whenever any input is absent or unbuildable,\n   324\t    # so a missing base snapshot / changed-files status / merge proof requires\n   325\t    # Guardian approval exactly as before.\n   326\t    exemption = _bookkeeping_closeout_exemption(\n   327\t        shiki_root=args.shiki_root,\n   328\t        pr_body=pr_body,\n   329\t        base_shiki=args.base_shiki,\n   330\t        changed_files_status=args.changed_files_status,\n   331\t        merged_prs=args.merged_prs,\n   332\t    )\n   333\t    # A fork PR is never eligible for the SADR-0017 exemption. The snapshot must\n   334\t    # prove the negative; absent/null/anything-but-False fails closed.\n   335\t    if exemption and pr.get(\"isCrossRepository\") is not False:\n   336\t        exemption = False\n   337\t    # The risk that actually gates Guardian approval: \"low\" for a proven closeout,\n   338\t    # otherwise the real task risk. task_risk is still reported as risk_level for\n   339\t    # audit. PR labels may only ESCALATE the effective risk (a maintainer can force\n   340\t    # the gate even on an exempted PR) and are ignored when the effective risk is\n   341\t    # undetermined (fail closed wins) \u2014 exactly as in MergeGate's\n   342\t    # _guardian_risk_labels.\n   343\t    effective_risk = \"low\" if exemption else task_risk\n   344\t    risk_unknown = effective_risk is None\n   345\t    labels = _label_names(pr) + ([effective_risk, f\"risk:{effective_risk}\"] if effective_risk else [])\n   346\t\n   347\t    try:\n   348\t        policy = load_guardian_policy_file(Path(args.guardian_policy))\n   349\t    except GuardianPolicyError as error:\n   350\t        # Fail closed: an undetermined risk or an unreadable policy on a\n   351\t        # high/critical PR must never let the CCA see approval.\n   352\t        required = risk_unknown or _builtin_high_or_critical(labels)\n   353\t        signal = {\n   354\t            \"required\": required,\n   355\t            \"approved\": not required,\n   356\t            \"error\": f\"guardian policy unreadable: {error}\",\n   357\t            \"head_sha\": head_sha,\n   358\t            \"bookkeeping_closeout_exemption\": exemption,\n   359\t        }\n   360\t        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n   361\t        return 0\n   362\t\n   363\t    policy_errors = validate_guardian_policy(policy)\n   364\t    required = risk_unknown or _builtin_high_or_critical(labels) or risk_requires_guardian(labels, policy)\n   365\t    if policy_errors and required:\n   366\t        signal = {\n   367\t            \"required\": True,\n   368\t            \"approved\": False,\n   369\t            \"error\": \"; \".join(policy_errors),\n   370\t            \"head_sha\": head_sha,\n   371\t            \"bookkeeping_closeout_exemption\": exemption,\n   372\t        }\n   373\t        Path(args.output).write_text(json.dumps(signal, indent=2) + \"\\n\", encoding=\"utf-8\")\n   374\t        return 0\n   375\t\n   376\t    if not required:\n   377\t        # Risk is KNOWN and below the Guardian threshold (low/medium, or a proven\n   378\t        # bookkeeping closeout evaluated as if low).\n   379\t        note = f\"Guardian approval not required for risk level {task_risk!r}\"\n   380\t        if exemption:\n   650\t        if not path.startswith(\".shiki/\"):\n   651\t            blocking.append(f\"{mode} must not change non-Shiki (implementation) file {path}\")\n   652\t            continue\n   653\t        if entry.status == \"D\":\n   654\t            blocking.append(f\"{mode} must not delete {path}\")\n   655\t            continue\n   656\t        if path.startswith(\".shiki/tasks/\") and path.endswith(\".json\"):\n   657\t            if entry.status != \"A\":\n   658\t                blocking.append(f\"{mode} may only ADD planned task files, not modify {path}\")\n   659\t                continue\n   660\t            data = load_json(target / path)\n   661\t            if not isinstance(data, dict):\n   662\t                blocking.append(f\"{mode} task file {path} is not a JSON object\")\n   663\t                continue\n   664\t            task_id = Path(path).stem\n   665\t            if str(data.get(\"id\") or \"\") != task_id:\n   666\t                blocking.append(f\"{mode} task filename {path} does not match its id {data.get('id')!r}\")\n   667\t            if str(data.get(\"goal_id\") or \"\") != goal_id:\n   668\t                blocking.append(f\"{mode} task {task_id} is not anchored to goal {goal_id}\")\n   669\t            if data.get(\"status\") != \"planned\":\n   670\t                blocking.append(f\"{mode} task {task_id} must be registered as status=planned, not {data.get('status')!r}\")\n   671\t            title = str(data.get(\"title\") or \"\").strip()\n   672\t            if title not in frozen_titles:\n   673\t                blocking.append(f\"{mode} task {task_id} title {title!r} is not in the goal's frozen plan\")\n   674\t            elif title in consumed_titles:\n   675\t                blocking.append(f\"{mode} task {task_id} re-registers frozen plan title {title!r} (already covered)\")\n   676\t            else:\n   677\t                consumed_titles.add(title)\n   678\t                # Bind to the FROZEN TASK DEFINITION, not just the title: a\n   679\t                # registered task must match every governance field the frozen\n   680\t                # plan declares (risk_level, locks, acceptance_checks, ...).\n   681\t                blocking.extend(_frozen_task_match_errors(task_id, data, frozen_tasks[title], mode))\n   682\t        elif path == dag_file:\n   683\t            # DAG restore is allowed; every node must resolve to a frozen-plan task\n   684\t            # anchored to this goal, and the DAG must cover the full frozen plan (a\n   685\t            # truncated DAG that drops frozen tasks would later let validate force\n   686\t            # premature goal-complete on the registered subset).\n   687\t            _check_frozen_dag_in_diff(target, goal_id, frozen_titles, path, blocking, mode)\n   688\t        elif path.startswith(\".shiki/dag/\") and path.endswith(\".json\"):\n   689\t            blocking.append(f\"{mode} must not change another goal's DAG {path}\")\n   690\t        elif path.startswith(\".shiki/ledger/\") and path.endswith(\".json\"):\n   691\t            if entry.status != \"A\":\n   692\t                blocking.append(f\"{mode} must append, not modify, ledger {path}\")\n   693\t                continue\n   694\t            led = load_json(target / path)\n   695\t            if not isinstance(led, dict) or str(led.get(\"goal_id\") or \"\") != goal_id:\n   696\t                blocking.append(f\"{mode} ledger {path} must be scoped to goal {goal_id}\")\n   697\t                continue\n   698\t            # The registration event must be a task-registered ledger, not any\n   699\t            # goal-scoped ledger, so the requirement proves an actual registration.\n   700\t            if led.get(\"type\") == \"task-registered\":\n   701\t                reconcile_ledger_seen = True\n   702\t        elif allow_goal_registration and path == goal_file:\n   703\t            # Contract mode only (SADR-0015): the Contract PR registers the goal\n   704\t            # for the first time. Validate it against its frozen plan; deny a\n   705\t            # modify/delete or a risk downgrade that would weaken the Guardian\n   706\t            # gate this contract exists to force.\n   707\t            _validate_contract_goal_registration(target, goal_id, entry, blocking, mode)\n   708\t        elif path.startswith(\".shiki/plans/\") and path.endswith(\".json\"):\n   709\t            # Contract flow only (SADR-0015): the Contract PR may carry the Goal's\n   710\t            # own spec-frozen source plan to the default branch, gated by the\n   711\t            # explicit ``allow_source_plan_carry`` opt-in. This is the sole plan\n   712\t            # mutation any frozen-registration PR may make, and it is deny-by-\n   713\t            # default: only ADDING (status A) the goal's source_plan, and only when\n   714\t            # its content is a spec-frozen plan. A MODIFY, a plan that is not the\n   715\t            # goal's source_plan, or an unfrozen plan is blocked \u2014 so a PR can never\n   716\t            # author or alter a spec_freeze block on any other plan through this\n   717\t            # relaxation. goal_reconcile never opts in (the frozen plan is the\n   718\t            # immutable base authority it validates against), so no plan mutation\n   719\t            # is allowed there at all.\n   720\t            _validate_contract_source_plan(\n   721\t                target=target,\n   722\t                source_plan_id=source_plan_id,\n   723\t                entry=entry,\n   724\t                blocking=blocking,\n   725\t                mode=mode,\n   726\t                allow_source_plan=allow_source_plan_carry,\n   727\t            )\n   728\t        else:\n   729\t            # Deny by default. In goal_reconcile mode the goal file itself is\n   730\t            # forbidden here: a reconcile must not move the goal's source_plan\n   731\t            # binding (it is validated against), change its status, or touch any\n   732\t            # other field. Also forbidden in both modes: memories, reports,\n   733\t            # guardian-policy, locks, repairs, runner, plans, and everything else.\n   734\t            blocking.append(\n   735\t                f\"{mode} must not change {path}; only frozen-plan task registration, \"\n   736\t                f\"DAG restore for {goal_id}, and a task-registered reconcile ledger are allowed\"\n   737\t            )\n   738\t\n   739\t    if not reconcile_ledger_seen:\n   740\t        blocking.append(f\"{mode} must include a goal-scoped task-registered reconcile ledger event for {goal_id}\")\n   741\t\n   742\t    # HEAD invariant + frozen-dependency edge binding (independent of whether the\n   743\t    # DAG file is in this diff): after the reconcile, the goal's DAG must cover\n   744\t    # every frozen-plan task and its edges/task.dependencies must match the frozen\n   745\t    # plan. Without this, a PR could register a subset of tasks and simply OMIT the\n   746\t    # DAG; a legacy DAG-less goal would then let validate force premature\n   747\t    # goal-complete on the registered subset, abandoning the unregistered frozen\n   748\t    # tasks. This makes \"the reconcile restores the goal's DAG\" a hard invariant.\n   749\t    _check_frozen_dag_head_invariant(target, goal_id, frozen_tasks, blocking, mode)\n   750\t\n   751\t\n   752\tdef enforce_goal_reconcile(\n   753\t    *,\n   754\t    target: Path,\n   755\t    goal_id: str,\n   756\t    changed_files_status: list[ChangedFile],\n   757\t    blocking: list[str],\n   758\t    warnings: list[str],\n   759\t) -> None:\n   760\t    \"\"\"Validate a goal_reconcile PR (drift recovery): register spec-frozen tasks\n  3420\t\n  3421\t            for dependency_id in task.get(\"dependencies\") or []:\n  3422\t                dependency = load_task(target, str(dependency_id))\n  3423\t                if dependency is None:\n  3424\t                    blocking.append(f\"Task dependency {dependency_id} has no task file\")\n  3425\t                elif dependency.get(\"status\") != \"done\":\n  3426\t                    blocking.append(f\"Task dependency {dependency_id} is not done: {dependency.get('status')!r}\")\n  3427\t\n  3428\t            files = changed_files(Path(args.changed_files))\n  3429\t            files_status = parse_changed_files_status(Path(args.changed_files_status), files)\n  3430\t            locks = [str(lock) for lock in task.get(\"locks\") or []]\n  3431\t            if files and not locks:\n  3432\t                blocking.append(f\"Task {resolved_task_id} has no locks but PR changes files\")\n  3433\t            # The task's declared (product) locks stay exactly what the plan froze;\n  3434\t            # the .shiki evidence the loop syncs onto the branch is covered by the\n  3435\t            # id-scoped mirror set derived from the task's own ids/ledgers at read\n  3436\t            # time. Union the two for the files-outside-locks gate only \u2014 lock\n  3437\t            # CONFLICT detection stays on the declared locks, since shared mirror\n  3438\t            # evidence is not a contended product resource.\n  3439\t            effective_locks = list(dict.fromkeys([*locks, *_derive_task_mirror_locks(target, task)]))\n  3440\t            # Judge the lock-scope gates against the WHOLE diff, both legs of every\n  3441\t            # rename included. Using the name-only `files` list would let a rename\n  3442\t            # move a file out of locked scope unseen (git shows only the new leg).\n  3443\t            scope_paths = lock_scope_paths(files_status)\n  3444\t            for path in files_outside_locks(scope_paths, effective_locks):\n  3445\t                blocking.append(f\"Changed file {path} is outside declared task locks\")\n  3446\t            blocking.extend(active_lock_conflicts(target, resolved_task_id, locks, scope_paths))\n  3447\t\n  3448\t            # SADR-0017: a proven bookkeeping closeout (task terminal state only, no\n  3449\t            # implementation) is evaluated as if low risk at the Guardian gate\n  3450\t            # below. Computed here (where the head task, base snapshot, diff and\n  3451\t            # merged-PR proof are all in hand); consulted only by\n  3452\t            # enforce_guardian_policy. Read-only \u2014 it never adds a blocking reason.\n  3453\t            bookkeeping_closeout = is_bookkeeping_closeout(\n  3454\t                target=target,\n  3455\t                task=task,\n  3456\t                task_id=resolved_task_id,\n  3457\t                goal_id=task_goal_id,\n  3458\t                base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n  3459\t                changed_files_status=files_status,\n  3460\t                merged_pr_numbers=merged_prs,\n  3461\t            )\n  3462\t            # The exemption is same-repository only and fail-closed: the live PR\n  3463\t            # snapshot must explicitly prove isCrossRepository=false.\n  3464\t            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\n  3465\t                bookkeeping_closeout = False\n  3466\t\n  3467\t            ledger_entries = load_ledger_entries(target, task, warnings, blocking)\n  3468\t            if pr:\n  3469\t                enforce_untrusted_shiki_mutations(\n  3470\t                    target=target,\n  3471\t                    manifest=manifest,\n  3472\t                    base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n  3473\t                    changed_files_status=files_status,\n  3474\t                    task=task,\n  3475\t                    goal_id=task_goal_id,\n  3476\t                    task_id=resolved_task_id,\n  3477\t                    pr=pr,\n  3478\t                    blocking=blocking,\n  3479\t                    warnings=warnings,\n  3480\t                )\ntests/test_migration_mode.py:21:``migration_decision`` / ``resolve_base_migration_registry``) directly, including\ntests/test_migration_mode.py:41:    resolve_base_migration_registry,\ntests/test_migration_mode.py:406:# and self-contained (no ``shiki_process`` import) so ``resolve_base_migration_registry``\ntests/test_migration_mode.py:479:            registry = resolve_base_migration_registry(repo, merge_base_oid)\ntests/test_migration_mode.py:510:                    \"mergeBaseOid\": merge_base_oid,\ntests/test_migration_mode.py:541:            any(\"immutable mergeBaseOid\" in reason for reason in blocking),\nscripts/mergegate_check.py:1286:        raise FileNotFoundError(f\"invalid immutable mergeBaseOid {merge_base_oid!r}\")\nscripts/mergegate_check.py:1297:        f\"could not read {repo_path} from immutable mergeBaseOid {merge_base_oid} ({detail})\"\nscripts/mergegate_check.py:1301:def resolve_base_migration_registry(target: Path, merge_base_oid: str) -> tuple[Any, ...]:\nscripts/mergegate_check.py:1379:    resolved from the base branch via ``resolve_base_migration_registry`` (the\nscripts/mergegate_check.py:1400:        merge_base_oid = str(pr.get(\"mergeBaseOid\") or \"\")\nscripts/mergegate_check.py:1403:                \"migration mode requires the workflow-resolved immutable mergeBaseOid \"\nscripts/mergegate_check.py:1408:            base_registry = resolve_base_migration_registry(target, merge_base_oid)\n\n2026-08-17T10:08:50.717923Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1,430p' tests/test_bookkeeping_closeout.py; sed -n '1,580p' tests/test_guardian_signal_closeout.py; rg -n 'mergegate_check\\\\.main|mock\\\\.patch|patch\\\\(|enforce_guardian_policy|bookkeeping_closeout' tests/test_policy_job_merged_prs.py tests/test_bookkeeping_closeout.py tests/test_guardian_signal_closeout.py tests/test_closeout.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n\"\"\"The bookkeeping-closeout Guardian exemption (SADR-0017).\n\nAfter a high/critical task's implementation PR merges under full Guardian\napproval, the autonomous loop opens a *closeout* PR that carries only the task's\nterminal state \u2014 ``task.status = done``, ``lock.state = released`` and (when the\ntask completes its goal) ``goal.status = complete`` plus exactly one scorecard\nreport. It contains zero lines of implementation, so it needed no review to\nproduce and inherits no new risk; yet MergeGate used to demand a *second*\nGuardian approval for it, because ``_guardian_risk_labels`` derived the\nrequirement from the same task's ``risk_level``. Across 31 PRs merged\n2026-07-29/30, 7 of 21 Guardian approval actions were exactly this.\n\n``mergegate_check.is_bookkeeping_closeout`` classifies such a PR, and the single\nGuardian decision point (``enforce_guardian_policy`` via ``_guardian_risk_labels``)\nevaluates a qualifying PR as if the task were low risk. Every other gate runs\nunchanged.\n\nThese tests pin the classifier against the positive case and every disqualifier\nIN ISOLATION, and assert the Guardian-requirement FALLBACK \u2014 that a disqualified\ncloseout re-inherits the task's real risk and requires Guardian approval \u2014 rather\nthan merely that the PR blocks, so a future change that blocks for an unrelated\nreason cannot mask a classifier regression. The exemption is proven only from the\ndiff, the base snapshot and live PR state (the merged-PR set); the classifier\nnever reads the PR at all, so a label, marker or PR-body claim can never classify\na non-qualifying PR.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport inspect\nimport json\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport mergegate_check\nfrom mergegate_check import (\n    ChangedFile,\n    _builtin_guardian_risk_required,\n    _guardian_risk_labels,\n    enforce_guardian_policy,\n    is_bookkeeping_closeout,\n)\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\nREPO_GUARDIAN_POLICY = REPO_ROOT / \".shiki\" / \"guardian-policy.json\"\n\nGOAL_ID = \"G-20260731T004111130646Z-0dbace30\"\nTASK_ID = \"T-20260731T004111132162Z-bfcf2c9e\"\nSIBLING_ID = \"T-20260731T004111132162Z-5ib11a60\"\nIMPL_PR = 300\nCLOSEOUT_PR = 301\nHEAD_SHA = \"a1b2c3d4a1b2c3d4a1b2c3d4a1b2c3d4a1b2c3d4\"\n\n# A real closeout is cut from the implementation branch onto ``<branch>-closeout``\n# (scripts/shiki_loop.py: ``branch = f\"shiki/{task_id.lower()}-closeout\"``), and the\n# goal loop rewrites the closeout task file's ``expected_branch`` to that new branch.\n# So on EVERY real closeout the base task's expected_branch (the implementation\n# branch) and the head task's expected_branch (the closeout branch) DIFFER. The\n# fixture reflects that real shape; a fixture that shared one expected_branch would\n# never exercise the field the loop always changes.\nIMPL_BRANCH = \"shiki/t-20260731t004111132162z-bfcf2c9e-derive-the-bookkeeping-closeout-exemption-in-mer\"\nCLOSEOUT_BRANCH = \"shiki/t-20260731t004111132162z-bfcf2c9e-closeout\"\n\nCOMPLETION_LEDGER = \"L-20260731T010000000000Z-c0mp0001\"\nPULL_LEDGER = \"L-20260731T010000000001Z-pu110001\"\nREPORT_ID = \"R-20260731T010000000002Z-5c0re001\"\nREPORT_REL = f\".shiki/reports/{REPORT_ID}.json\"\n\n# The frozen governance contract shared byte-for-byte by the base and head task.\n# expected_branch is deliberately NOT in here: the goal loop rewrites it to the\n# closeout branch on every closeout, so it is supplied per-task (base -> impl\n# branch, head -> closeout branch) and is EXPECTED to differ.\n_GOVERNANCE = {\n    \"title\": \"Derive the bookkeeping closeout exemption in MergeGate\",\n    \"scope\": \"Add is_bookkeeping_closeout and consult it at the Guardian decision point.\",\n    \"non_goals\": [\"No change to any other MergeGate gate.\"],\n    \"dependencies\": [],\n    \"locks\": [\n        \"path:scripts/mergegate_check.py\",\n        \"path:tests/test_bookkeeping_closeout.py\",\n    ],\n    \"assigned_runtime\": \"claude-code\",\n    \"risk_level\": \"high\",\n    \"required_skills\": [\"tdd\", \"code-review\"],\n    \"acceptance_checks\": [\"a bookkeeping closeout does not require Guardian approval\"],\n    \"test_command\": \"python3 -m unittest discover -s tests\",\n    \"github_issue\": 210,\n}\n\n\ndef cf(status: str, path: str, old_path: str | None = None) -> ChangedFile:\n    return ChangedFile(status, path, old_path=old_path)\n\n\ndef _write(path: Path, payload: dict) -> None:\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(payload, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n\nclass Closeout:\n    \"\"\"A mutable, on-disk bookkeeping-closeout scenario.\n\n    By default it is a VALID completing single-task closeout: the diff carries the\n    task file (review -> done), the lock (active -> released), the goal\n    (in-progress -> complete), one scorecard report, the goal-level completion\n    ledger and the task-scoped /pull ledger. Every test starts from ``build`` (or\n    ``build_noncompleting``) and mutates exactly one axis before calling\n    ``classify``.\n    \"\"\"\n\n    def __init__(self, root: Path) -> None:\n        self.root = root\n        self.target = root / \"repo\"\n        self.base = root / \"base\" / \".shiki\"\n        self.merged: set[int] = {IMPL_PR}\n        self.changed: list[ChangedFile] = []\n        self.head_task: dict = {}\n\n    # --- construction -----------------------------------------------------\n\n    def _task(self, *, status: str, expected_pr, ledger_evidence, expected_branch, closeout_pr=None) -> dict:\n        task = {\"id\": TASK_ID, \"goal_id\": GOAL_ID, **_GOVERNANCE,\n                \"status\": status, \"expected_pr\": expected_pr,\n                \"expected_branch\": expected_branch,\n                \"ledger_evidence\": list(ledger_evidence)}\n        if closeout_pr is not None:\n            task[\"closeout_pr\"] = closeout_pr\n        return task\n\n    def build(self, *, completes: bool = True) -> \"Closeout\":\n        # Base snapshot (origin/main): the merged impl PR left the task at review,\n        # naming the impl PR, on the implementation branch, its lock active and its\n        # goal in-progress.\n        _write(self.base / \"tasks\" / f\"{TASK_ID}.json\",\n               self._task(status=\"review\", expected_pr=IMPL_PR, expected_branch=IMPL_BRANCH,\n                          ledger_evidence=[\"L-base00001\"]))\n        _write(self.base / \"locks\" / f\"{TASK_ID}.json\",\n               {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"active\", \"owner\": \"shiki-run\"})\n        _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n               {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                \"ledger_evidence\": []})\n\n        # HEAD (closeout branch cut from origin/main): terminal state, with\n        # expected_branch rewritten to the closeout branch (as the goal loop does).\n        head_ledgers = (\n            [\"L-base00001\", COMPLETION_LEDGER, PULL_LEDGER]\n            if completes\n            else [\"L-base00001\", PULL_LEDGER]\n        )\n        self.head_task = self._task(status=\"done\", expected_pr=CLOSEOUT_PR, expected_branch=CLOSEOUT_BRANCH,\n                                    ledger_evidence=head_ledgers)\n        _write(self.target / \".shiki\" / \"tasks\" / f\"{TASK_ID}.json\", self.head_task)\n        _write(self.target / \".shiki\" / \"locks\" / f\"{TASK_ID}.json\",\n               {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"released\", \"owner\": \"shiki-run\"})\n        # Goal-level completion ledger (task_id null) cites the scorecard report;\n        # the /pull ledger is task-scoped. Both are listed in the head task's\n        # ledger_evidence so _derive_task_mirror_locks covers their paths + refs.\n        _write(self.target / \".shiki\" / \"ledger\" / f\"{COMPLETION_LEDGER}.json\",\n               {\"id\": COMPLETION_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": None,\n                \"type\": \"completion\", \"summary\": \"goal complete\", \"evidence\": [REPORT_REL]})\n        _write(self.target / \".shiki\" / \"ledger\" / f\"{PULL_LEDGER}.json\",\n               {\"id\": PULL_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"lock\",\n                \"summary\": \"closeout /pull\", \"evidence\": [\n                    f\".shiki/tasks/{TASK_ID}.json\",\n                    f\".shiki/locks/{TASK_ID}.json\",\n                ]})\n\n        if completes:\n            _write(self.target / \".shiki\" / \"dag\" / f\"{GOAL_ID}.json\",\n                   {\"goal_id\": GOAL_ID, \"nodes\": [TASK_ID], \"edges\": []})\n            _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n                   {\"id\": GOAL_ID, \"status\": \"complete\", \"title\": \"g\", \"risk_level\": \"high\",\n                    \"ledger_evidence\": [COMPLETION_LEDGER]})\n            _write(self.target / REPORT_REL, {\n                \"id\": REPORT_ID,\n                \"goal_id\": GOAL_ID,\n                \"status\": \"complete\",\n                \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"],\n                \"blocking_reasons\": [],\n                \"mergegate\": {\n                    \"dependencies\": \"pass\",\n                    \"locks\": \"pass\",\n                    \"checks\": \"pass\",\n                    \"review\": \"recorded\",\n                    \"ledger\": \"pass\",\n                    \"risk\": \"high\",\n                },\n                \"scorecard\": {\n                    \"goal_id\": GOAL_ID,\n                    \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1},\n                },\n            })\n            self.changed = [\n                cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n                cf(\"M\", f\".shiki/goals/{GOAL_ID}.json\"),\n                cf(\"A\", REPORT_REL),\n                cf(\"A\", f\".shiki/ledger/{COMPLETION_LEDGER}.json\"),\n                cf(\"A\", f\".shiki/ledger/{PULL_LEDGER}.json\"),\n            ]\n        else:\n            # Multi-task goal, this is NOT the last task: a sibling stays running,\n            # so the goal is untouched and no scorecard is added.\n            _write(self.target / \".shiki\" / \"dag\" / f\"{GOAL_ID}.json\",\n                   {\"goal_id\": GOAL_ID, \"nodes\": [TASK_ID, SIBLING_ID], \"edges\": []})\n            _write(self.target / \".shiki\" / \"tasks\" / f\"{SIBLING_ID}.json\",\n                   {\"id\": SIBLING_ID, \"goal_id\": GOAL_ID, \"status\": \"running\", \"title\": \"s\"})\n            _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n                   {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                    \"ledger_evidence\": []})\n            self.changed = [\n                cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n                cf(\"A\", f\".shiki/ledger/{PULL_LEDGER}.json\"),\n            ]\n        return self\n\n    def build_noncompleting(self) -> \"Closeout\":\n        return self.build(completes=False)\n\n    # --- mutation helpers -------------------------------------------------\n\n    def rewrite_head_task(self) -> None:\n        _write(self.target / \".shiki\" / \"tasks\" / f\"{TASK_ID}.json\", self.head_task)\n\n    def write_base_task_raw(self, text: str) -> None:\n        path = self.base / \"tasks\" / f\"{TASK_ID}.json\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text(text, encoding=\"utf-8\")\n\n    # --- evaluation -------------------------------------------------------\n\n    def classify(self, *, base: Path | None = \"__default__\") -> bool:\n        base_shiki = self.base if base == \"__default__\" else base\n        return is_bookkeeping_closeout(\n            target=self.target,\n            task=self.head_task,\n            task_id=TASK_ID,\n            goal_id=GOAL_ID,\n            base_shiki=base_shiki,\n            changed_files_status=self.changed,\n            merged_pr_numbers=self.merged,\n        )\n\n    def pr(self, labels: list[dict] | None = None, body: str = \"\") -> dict:\n        return {\"number\": CLOSEOUT_PR, \"headRefOid\": HEAD_SHA,\n                \"labels\": labels or [], \"reviews\": [], \"body\": body}\n\n    def guardian_required_decision(self, *, bookkeeping: bool, labels: list[dict] | None = None) -> bool:\n        \"\"\"The single Guardian decision point: does the task's risk force the gate?\"\"\"\n        risk_labels = _guardian_risk_labels(self.pr(labels), self.head_task, bookkeeping_closeout=bookkeeping)\n        return _builtin_guardian_risk_required(risk_labels)\n\n    def guardian_gate_blocks(self, *, bookkeeping: bool) -> bool:\n        \"\"\"The real gate over the repo Guardian policy with NO approval evidence:\n        an exempted PR sails through; a non-exempted high-risk PR blocks.\"\"\"\n        gha = self.target / \".shiki\" / \"gha\"\n        gha.mkdir(parents=True, exist_ok=True)\n        for name in (\"comments\", \"events\", \"timeline\"):\n            (gha / f\"{name}.json\").write_text(\"[]\", encoding=\"utf-8\")\n        blocking: list[str] = []\n        warnings: list[str] = []\n        enforce_guardian_policy(\n            pr=self.pr(),\n            task=self.head_task,\n            target=self.target,\n            guardian_policy=str(REPO_GUARDIAN_POLICY),\n            guardian_comments=str(gha / \"comments.json\"),\n            guardian_events=str(gha / \"events.json\"),\n            guardian_timeline=str(gha / \"timeline.json\"),\n            blocking=blocking,\n            warnings=warnings,\n            bookkeeping_closeout=bookkeeping,\n        )\n        return any(\"Guardian\" in reason for reason in blocking)\n\n\nclass BookkeepingCloseoutTest(unittest.TestCase):\n    def setUp(self) -> None:\n        self._tmp = tempfile.TemporaryDirectory()\n        self.root = Path(self._tmp.name)\n\n    def tearDown(self) -> None:\n        self._tmp.cleanup()\n\n    def new(self) -> Closeout:\n        return Closeout(self.root / f\"c{self.id().rsplit('.', 1)[-1]}\")\n\n    # --- positive ---------------------------------------------------------\n\n    def test_positive_completing_is_classified_and_needs_no_guardian(self) -> None:\n        c = self.new().build(completes=True)\n        self.assertTrue(c.classify())\n        # The single decision point: exempted -> guardian NOT required.\n        self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n        # The real gate confirms it: an exempted high-risk closeout does not block\n        # for missing Guardian approval.\n        self.assertFalse(c.guardian_gate_blocks(bookkeeping=True))\n        # Control: without the exemption the SAME high-risk PR would require it.\n        self.assertTrue(c.guardian_required_decision(bookkeeping=False))\n        self.assertTrue(c.guardian_gate_blocks(bookkeeping=False))\n\n    def test_positive_noncompleting_is_classified(self) -> None:\n        c = self.new().build_noncompleting()\n        self.assertTrue(c.classify())\n        self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n\n    def test_positive_expected_branch_differs_is_classified(self) -> None:\n        # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n        # implementation branch) and head.expected_branch (the closeout branch)\n        # DIFFER \u2014 the goal loop performs this rewrite on every closeout by\n        # construction. A closeout that moves ONLY the terminal-state fields plus\n        # expected_branch must still classify as a bookkeeping closeout. This case is\n        # the point of SADR-0017's whitelist fix: it FAILS if expected_branch is\n        # removed from _CLOSEOUT_MUTABLE_TASK_FIELDS, because condition 4 would then\n        # reject the branch rewrite before any other condition is reached.\n        c = self.new().build(completes=True)\n        base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n        self.assertEqual(base_task[\"expected_branch\"], IMPL_BRANCH)\n        self.assertEqual(c.head_task[\"expected_branch\"], CLOSEOUT_BRANCH)\n        self.assertNotEqual(base_task[\"expected_branch\"], c.head_task[\"expected_branch\"])\n        self.assertTrue(c.classify())\n        # The single decision point still exempts it (no Guardian required).\n        self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n\n    def test_strongest_goal_or_task_risk_matches_completion_report(self) -> None:\n        c = self.new().build(completes=True)\n        base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n        base_task[\"risk_level\"] = \"critical\"\n        _write(c.base / \"tasks\" / f\"{TASK_ID}.json\", base_task)\n        c.head_task[\"risk_level\"] = \"critical\"\n        c.rewrite_head_task()\n        for goal_path in (\n            c.base / \"goals\" / f\"{GOAL_ID}.json\",\n            c.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n        ):\n            goal = json.loads(goal_path.read_text())\n            goal[\"risk_level\"] = \"medium\"\n            _write(goal_path, goal)\n        report = json.loads((c.target / REPORT_REL).read_text())\n        report[\"mergegate\"][\"risk\"] = \"critical\"\n        _write(c.target / REPORT_REL, report)\n        self.assertTrue(c.classify())\n\n    # --- disqualifiers (each in isolation; assert the Guardian fallback) ---\n\n    def _assert_disqualified(self, c: Closeout) -> None:\n        # Classifier rejects it, so the code passes bookkeeping_closeout=False and\n        # the task's real (high) risk re-forces the Guardian gate. Assert the\n        # fallback itself, not merely that some gate blocks.\n        self.assertFalse(c.classify())\n        self.assertTrue(c.guardian_required_decision(bookkeeping=c.classify()))\n        self.assertTrue(c.guardian_gate_blocks(bookkeeping=c.classify()))\n\n    def test_non_shiki_file_disqualifies(self) -> None:\n        c = self.new().build()\n        c.changed.append(cf(\"M\", \"scripts/mergegate_check.py\"))  # covered by a lock, still not .shiki\n        self._assert_disqualified(c)\n\n    def test_deleted_shiki_path_disqualifies(self) -> None:\n        c = self.new().build()\n        c.changed.append(cf(\"D\", f\".shiki/ledger/{PULL_LEDGER}.json\"))\n        self._assert_disqualified(c)\n\n    def test_renamed_shiki_path_disqualifies(self) -> None:\n        c = self.new().build_noncompleting()\n        c.changed[0] = cf(\"R\", f\".shiki/tasks/{TASK_ID}.json\", \".shiki/tasks/T-0000.json\")\n        self._assert_disqualified(c)\n\n    def test_type_changed_shiki_path_disqualifies(self) -> None:\n        c = self.new().build_noncompleting()\n        c.changed[0] = cf(\"T\", f\".shiki/tasks/{TASK_ID}.json\")\n        self._assert_disqualified(c)\n\n    def test_symlinked_task_file_disqualifies(self) -> None:\n        c = self.new().build_noncompleting()\n        task_path = c.target / \".shiki\" / \"tasks\" / f\"{TASK_ID}.json\"\n        target_path = task_path.with_name(\"task-target.json\")\n        target_path.write_bytes(task_path.read_bytes())\n        task_path.unlink()\n        task_path.symlink_to(target_path.name)\n        self._assert_disqualified(c)\n\n    def test_coerced_expected_pr_disqualifies(self) -> None:\n        for value in (True, str(IMPL_PR)):\n            with self.subTest(value=value):\n                c = self.new().build_noncompleting()\n                base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n                base_task[\"expected_pr\"] = value\n                _write(c.base / \"tasks\" / f\"{TASK_ID}.json\", base_task)\n                self._assert_disqualified(c)\n\n    def test_worktree_record_disqualifies(self) -> None:\n        c = self.new().build_noncompleting()\n        c.changed.append(cf(\"A\", f\".shiki/worktrees/{TASK_ID}.json\"))\n        self._assert_disqualified(c)\n\n    def test_file_outside_effective_locks_disqualifies(self) -> None:\n        # A .shiki ledger that is NOT in the task's ledger_evidence is under .shiki\n        # (condition 1 passes) but outside the derived mirror locks (condition 3).\n        c = self.new().build()\n        stray = \"L-20260731T010000000009Z-5tray001\"\n        _write(c.target / \".shiki\" / \"ledger\" / f\"{stray}.json\",\n               {\"id\": stray, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"check\", \"evidence\": []})\n        c.changed.append(cf(\"A\", f\".shiki/ledger/{stray}.json\"))\n        self._assert_disqualified(c)\n\n    def test_governance_field_edit_disqualifies(self) -> None:\n        # Editing acceptance_checks (a governance field) diverges from the base\n        # snapshot; risk_level stays high so the fallback is unambiguous.\n        c = self.new().build()\n        c.head_task[\"acceptance_checks\"] = [\"a smuggled acceptance criterion\"]\n        c.rewrite_head_task()\n        self._assert_disqualified(c)\n\n    def test_every_governance_field_change_disqualifies(self) -> None:\n        # Guard against the whitelist drifting wider than the one entry SADR-0017\n        # adds. Two independent locks:\n        #   (a) the mutable set is EXACTLY these five entries \u2014 adding a sixth (or\n        #       dropping one) fails here directly; and\n        #   (b) every field of the frozen governance contract STILL disqualifies a\n        #       closeout when the head diverges from the base on it.\n        # (b) iterates the hardcoded _GOVERNANCE contract, NOT the complement of the\n        # mutable set \u2014 so promoting a governance field INTO the mutable set does not\n        # silently drop it from this guard: the tampered field would then be skipped\n        # by condition 4, the closeout would classify, and the assertFalse would fail.\n        self.assertEqual(\n\"\"\"The bookkeeping-closeout Guardian exemption on the CCA *signal* path (SADR-0017).\n\n``scripts/guardian_approval_signal.py`` writes ``.shiki/gha/guardian-approval.json``,\nthe deterministic Guardian-approval result the CCA judge reads for CCA-08. Before\nthis change it resolved risk only from the head task's ``risk_level`` and had no\nbase snapshot, no changed-files status and no merged-PR proof, so it could not\nclassify a closeout at all: a closeout PR for a high/critical task passed the\nMergeGate *metadata* check (which learned the SADR-0017 exemption) but the CCA\nsignal still returned ``needs_guardian``, leaving the exemption real but\nunreachable (PR #209).\n\nThe fix imports the SAME ``mergegate_check.is_bookkeeping_closeout`` classifier\nand feeds it the SAME inputs MergeGate feeds it. These tests prove:\n\n* a proven closeout for a *critical* task yields ``required=false``;\n* each SADR-0017 disqualifier IN ISOLATION re-requires Guardian approval;\n* each missing/unbuildable input fails closed to ``required=true``;\n* the emitted JSON records whether the exemption was applied; and\n* a PARITY property \u2014 for a representative matrix, the signal's exemption decision\n  equals MergeGate's for the same inputs, and the signal is NEVER more permissive\n  than MergeGate (it never drops Guardian approval that MergeGate would demand).\n\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport guardian_approval_signal as sig\nfrom mergegate_check import (\n    ChangedFile,\n    _builtin_guardian_risk_required,\n    _guardian_risk_labels,\n    is_bookkeeping_closeout,\n)\nfrom shiki_guardian import load_guardian_policy_file, risk_requires_guardian\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\nREPO_GUARDIAN_POLICY = REPO_ROOT / \".shiki\" / \"guardian-policy.json\"\n\nGOAL_ID = \"G-20260731T031357585622Z-8d094bd1\"\nTASK_ID = \"T-20260731T031357587095Z-2cb11839\"\nSIBLING_ID = \"T-20260731T031357587095Z-51b11a60\"\nIMPL_PR = 400\nCLOSEOUT_PR = 401\nHEAD_SHA = \"c\" * 40\n\nCOMPLETION_LEDGER = \"L-20260731T040000000000Z-c0mp0002\"\nPULL_LEDGER = \"L-20260731T040000000001Z-pu110002\"\nREPORT_ID = \"R-20260731T040000000002Z-5c0re003\"\nREPORT_REL = f\".shiki/reports/{REPORT_ID}.json\"\n\n# The frozen governance contract shared byte-for-byte by the base and head task.\n# risk_level is CRITICAL so the acceptance check \"a proven closeout for a critical\n# task yields required=false\" is exercised directly, and every disqualifier's\n# fallback re-forces the gate from a critical risk.\n_GOVERNANCE = {\n    \"title\": \"Give the CCA Guardian signal the bookkeeping closeout exemption\",\n    \"scope\": \"Import is_bookkeeping_closeout into the signal and plumb its inputs.\",\n    \"non_goals\": [\"No change to any other gate.\"],\n    \"dependencies\": [],\n    \"locks\": [\n        \"path:scripts/guardian_approval_signal.py\",\n        \"path:.github/workflows/shiki-cca-completion.yml\",\n        \"path:tests/test_guardian_signal_closeout.py\",\n    ],\n    \"assigned_runtime\": \"claude-code\",\n    \"risk_level\": \"critical\",\n    \"required_skills\": [\"tdd\", \"code-review\"],\n    \"acceptance_checks\": [\"a proven closeout does not require Guardian approval\"],\n    \"test_command\": \"python3 -m unittest discover -s tests\",\n    \"expected_branch\": \"shiki/t-20260731-signal-closeout\",\n    \"github_issue\": 211,\n}\n\n_SENTINEL = object()\n\n\ndef cf(status: str, path: str, old_path: str | None = None) -> ChangedFile:\n    return ChangedFile(status, path, old_path=old_path)\n\n\ndef _write(path: Path, payload: dict) -> None:\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(payload, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n\nclass Scenario:\n    \"\"\"A mutable, on-disk bookkeeping-closeout scenario, evaluated end-to-end\n    through ``guardian_approval_signal.main`` AND compared against MergeGate.\n\n    ``build`` lays out a VALID completing single-task closeout: the head checkout\n    (``self.target`` = the signal's ``--shiki-root``) carries the terminal state,\n    ``self.base`` is the base branch's ``.shiki`` snapshot, ``self.merged`` proves\n    the implementation PR merged, and ``self.changed`` is the diff. Tests mutate\n    exactly one axis, then call ``run_signal`` / ``classify``.\n    \"\"\"\n\n    def __init__(self, root: Path) -> None:\n        self.root = root\n        self.target = root / \"repo\"\n        self.base = root / \"base\" / \".shiki\"\n        self.merged: set[int] = {IMPL_PR}\n        self.changed: list[ChangedFile] = []\n        self.head_task: dict = {}\n        self.labels: list[str] = []\n        self.is_cross_repository = False\n\n    # --- construction -----------------------------------------------------\n\n    def _task(self, *, status: str, expected_pr, ledger_evidence, closeout_pr=None) -> dict:\n        task = {\"id\": TASK_ID, \"goal_id\": GOAL_ID, **_GOVERNANCE,\n                \"status\": status, \"expected_pr\": expected_pr,\n                \"ledger_evidence\": list(ledger_evidence)}\n        if closeout_pr is not None:\n            task[\"closeout_pr\"] = closeout_pr\n        return task\n\n    def build(self, *, completes: bool = True) -> \"Scenario\":\n        # Base snapshot (origin/main): the merged impl PR left the task at review,\n        # naming the impl PR, its lock active and its goal in-progress.\n        _write(self.base / \"tasks\" / f\"{TASK_ID}.json\",\n               self._task(status=\"review\", expected_pr=IMPL_PR, ledger_evidence=[\"L-base00002\"]))\n        _write(self.base / \"locks\" / f\"{TASK_ID}.json\",\n               {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"active\", \"owner\": \"shiki-run\"})\n        _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n               {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                \"ledger_evidence\": []})\n\n        # HEAD (closeout branch cut from origin/main): terminal state.\n        head_ledgers = (\n            [\"L-base00002\", COMPLETION_LEDGER, PULL_LEDGER]\n            if completes\n            else [\"L-base00002\", PULL_LEDGER]\n        )\n        self.head_task = self._task(status=\"done\", expected_pr=CLOSEOUT_PR,\n                                    ledger_evidence=head_ledgers)\n        self.rewrite_head_task()\n        _write(self.target / \".shiki\" / \"locks\" / f\"{TASK_ID}.json\",\n               {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"released\", \"owner\": \"shiki-run\"})\n        _write(self.target / \".shiki\" / \"ledger\" / f\"{COMPLETION_LEDGER}.json\",\n               {\"id\": COMPLETION_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": None,\n                \"type\": \"completion\", \"summary\": \"goal complete\", \"evidence\": [REPORT_REL]})\n        _write(self.target / \".shiki\" / \"ledger\" / f\"{PULL_LEDGER}.json\",\n               {\"id\": PULL_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"lock\",\n                \"summary\": \"closeout /pull\", \"evidence\": [\n                    f\".shiki/tasks/{TASK_ID}.json\",\n                    f\".shiki/locks/{TASK_ID}.json\",\n                ]})\n\n        if completes:\n            _write(self.target / \".shiki\" / \"dag\" / f\"{GOAL_ID}.json\",\n                   {\"goal_id\": GOAL_ID, \"nodes\": [TASK_ID], \"edges\": []})\n            _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n                   {\"id\": GOAL_ID, \"status\": \"complete\", \"title\": \"g\", \"risk_level\": \"critical\",\n                    \"ledger_evidence\": [COMPLETION_LEDGER]})\n            _write(self.target / REPORT_REL, {\n                \"id\": REPORT_ID,\n                \"goal_id\": GOAL_ID,\n                \"status\": \"complete\",\n                \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"],\n                \"blocking_reasons\": [],\n                \"mergegate\": {\n                    \"dependencies\": \"pass\",\n                    \"locks\": \"pass\",\n                    \"checks\": \"pass\",\n                    \"review\": \"recorded\",\n                    \"ledger\": \"pass\",\n                    \"risk\": \"critical\",\n                },\n                \"scorecard\": {\n                    \"goal_id\": GOAL_ID,\n                    \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1},\n                },\n            })\n            self.changed = [\n                cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n                cf(\"M\", f\".shiki/goals/{GOAL_ID}.json\"),\n                cf(\"A\", REPORT_REL),\n                cf(\"A\", f\".shiki/ledger/{COMPLETION_LEDGER}.json\"),\n                cf(\"A\", f\".shiki/ledger/{PULL_LEDGER}.json\"),\n            ]\n        else:\n            _write(self.target / \".shiki\" / \"dag\" / f\"{GOAL_ID}.json\",\n                   {\"goal_id\": GOAL_ID, \"nodes\": [TASK_ID, SIBLING_ID], \"edges\": []})\n            _write(self.target / \".shiki\" / \"tasks\" / f\"{SIBLING_ID}.json\",\n                   {\"id\": SIBLING_ID, \"goal_id\": GOAL_ID, \"status\": \"running\", \"title\": \"s\"})\n            _write(self.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n                   {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                    \"ledger_evidence\": []})\n            self.changed = [\n                cf(\"M\", f\".shiki/tasks/{TASK_ID}.json\"),\n                cf(\"M\", f\".shiki/locks/{TASK_ID}.json\"),\n                cf(\"A\", f\".shiki/ledger/{PULL_LEDGER}.json\"),\n            ]\n        return self\n\n    def build_noncompleting(self) -> \"Scenario\":\n        return self.build(completes=False)\n\n    def rewrite_head_task(self) -> None:\n        _write(self.target / \".shiki\" / \"tasks\" / f\"{TASK_ID}.json\", self.head_task)\n\n    # --- evaluation -------------------------------------------------------\n\n    def _write_status_file(self) -> Path:\n        lines: list[str] = []\n        for entry in self.changed:\n            if entry.status == \"R\" and entry.old_path:\n                lines.append(f\"R100\\t{entry.old_path}\\t{entry.path}\")\n            else:\n                lines.append(f\"{entry.status}\\t{entry.path}\")\n        path = self.target / \"changed-files-status.txt\"\n        path.parent.mkdir(parents=True, exist_ok=True)\n        path.write_text((\"\\n\".join(lines) + \"\\n\") if lines else \"\", encoding=\"utf-8\")\n        return path\n\n    def _write_pr(self, body) -> Path:\n        pr = {\n            \"number\": CLOSEOUT_PR,\n            \"headRefOid\": HEAD_SHA,\n            \"author\": {\"login\": \"mizutani-140\"},\n            \"labels\": [{\"name\": name} for name in self.labels],\n            \"reviews\": [],\n            \"body\": (f\"Closeout {TASK_ID} for goal {GOAL_ID}.\" if body is _SENTINEL else body),\n        }\n        if self.is_cross_repository is not _SENTINEL:\n            pr[\"isCrossRepository\"] = self.is_cross_repository\n        path = self.target / \"pr.json\"\n        path.write_text(json.dumps(pr), encoding=\"utf-8\")\n        return path\n\n    def run_signal(\n        self,\n        *,\n        base_shiki=_SENTINEL,\n        changed_files_status=_SENTINEL,\n        merged_prs=_SENTINEL,\n        body=_SENTINEL,\n        guardian_policy=_SENTINEL,\n    ) -> dict:\n        \"\"\"Run the real signal CLI and return the emitted JSON. Each input can be\n        overridden (e.g. to \"\" for the missing-input fail-closed cases).\"\"\"\n        status_path = self._write_status_file()\n        pr_path = self._write_pr(body)\n        base_arg = str(self.base) if base_shiki is _SENTINEL else base_shiki\n        status_arg = str(status_path) if changed_files_status is _SENTINEL else changed_files_status\n        merged_arg = (\n            \",\".join(str(n) for n in sorted(self.merged)) if merged_prs is _SENTINEL else merged_prs\n        )\n        policy_arg = str(REPO_GUARDIAN_POLICY) if guardian_policy is _SENTINEL else guardian_policy\n        out = self.target / \"guardian-approval.json\"\n        rc = sig.main_with_argv([\n            \"--pr-json\", str(pr_path),\n            \"--guardian-policy\", policy_arg,\n            \"--expected-repository\", \"mizutani-140/shiki\",\n            \"--shiki-root\", str(self.target),\n            \"--base-shiki\", base_arg,\n            \"--changed-files-status\", status_arg,\n            \"--merged-prs\", merged_arg,\n            \"--output\", str(out),\n        ])\n        assert rc == 0, f\"signal exited {rc}\"\n        return json.loads(out.read_text(encoding=\"utf-8\"))\n\n    def classify(self, *, base: Path | None = _SENTINEL, merged=_SENTINEL) -> bool:\n        \"\"\"MergeGate's exemption decision: the SAME classifier over the SAME inputs.\"\"\"\n        base_shiki = self.base if base is _SENTINEL else base\n        merged_prs = self.merged if merged is _SENTINEL else merged\n        return is_bookkeeping_closeout(\n            target=self.target,\n            task=self.head_task,\n            task_id=TASK_ID,\n            goal_id=GOAL_ID,\n            base_shiki=base_shiki,\n            changed_files_status=self.changed,\n            merged_pr_numbers=merged_prs,\n        )\n\n    def mergegate_required(self, *, exempt: bool) -> bool:\n        \"\"\"MergeGate's Guardian requirement for this PR, mirroring\n        ``enforce_guardian_policy`` (builtin high/critical OR policy-driven).\"\"\"\n        pr = {\"number\": CLOSEOUT_PR, \"headRefOid\": HEAD_SHA,\n              \"labels\": [{\"name\": name} for name in self.labels], \"reviews\": [], \"body\": \"\"}\n        risk_labels = _guardian_risk_labels(pr, self.head_task, bookkeeping_closeout=exempt)\n        if _builtin_guardian_risk_required(risk_labels):\n            return True\n        policy = load_guardian_policy_file(REPO_GUARDIAN_POLICY)\n        return risk_requires_guardian(risk_labels, policy)\n\n\nclass GuardianSignalCloseoutTest(unittest.TestCase):\n    def setUp(self) -> None:\n        self._tmp = tempfile.TemporaryDirectory()\n        self.root = Path(self._tmp.name)\n\n    def tearDown(self) -> None:\n        self._tmp.cleanup()\n\n    def new(self) -> Scenario:\n        return Scenario(self.root / f\"c{self.id().rsplit('.', 1)[-1]}\")\n\n    # --- positive: a proven closeout drops the requirement ----------------\n\n    def test_proven_completing_critical_closeout_not_required(self) -> None:\n        signal = self.new().build(completes=True).run_signal()\n        self.assertFalse(signal[\"required\"])\n        self.assertTrue(signal[\"approved\"])\n        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\n\n    def test_proven_noncompleting_closeout_not_required(self) -> None:\n        signal = self.new().build_noncompleting().run_signal()\n        self.assertFalse(signal[\"required\"])\n        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\n\n    # --- disqualifiers in isolation re-require Guardian approval -----------\n\n    def _assert_disqualified(self, s: Scenario) -> None:\n        signal = s.run_signal()\n        self.assertTrue(signal[\"required\"], \"disqualified closeout must require Guardian approval\")\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n        # The classifier agrees it is not a closeout (no exemption).\n        self.assertFalse(s.classify())\n\n    def test_non_shiki_path_requires_guardian(self) -> None:\n        s = self.new().build()\n        s.changed.append(cf(\"M\", \"scripts/guardian_approval_signal.py\"))\n        self._assert_disqualified(s)\n\n    def test_governance_field_edit_requires_guardian(self) -> None:\n        s = self.new().build()\n        s.head_task[\"acceptance_checks\"] = [\"a smuggled acceptance criterion\"]\n        s.rewrite_head_task()\n        self._assert_disqualified(s)\n\n    def test_deletion_requires_guardian(self) -> None:\n        s = self.new().build()\n        s.changed.append(cf(\"D\", f\".shiki/ledger/{PULL_LEDGER}.json\"))\n        self._assert_disqualified(s)\n\n    def test_non_terminal_status_requires_guardian(self) -> None:\n        s = self.new().build()\n        s.head_task[\"status\"] = \"review\"  # not review -> done\n        s.rewrite_head_task()\n        self._assert_disqualified(s)\n\n    def test_second_scorecard_requires_guardian(self) -> None:\n        s = self.new().build()\n        second = \"R-20260731T040000000003Z-5c0re004\"\n        _write(s.target / \".shiki\" / \"reports\" / f\"{second}.json\", {\"id\": second, \"goal_id\": GOAL_ID})\n        # Cover the extra report via the completion ledger so ONLY the second-report\n        # rule (condition 5), not files-outside-locks (condition 3), disqualifies.\n        _write(s.target / \".shiki\" / \"ledger\" / f\"{COMPLETION_LEDGER}.json\",\n               {\"id\": COMPLETION_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": None, \"type\": \"completion\",\n                \"summary\": \"goal complete\", \"evidence\": [REPORT_REL, f\".shiki/reports/{second}.json\"]})\n        s.changed.append(cf(\"A\", f\".shiki/reports/{second}.json\"))\n        self._assert_disqualified(s)\n\n    def test_unmerged_implementation_pr_requires_guardian(self) -> None:\n        s = self.new().build()\n        s.merged = set()  # the base expected_pr (impl PR) is not proven merged\n        self._assert_disqualified(s)\n\n    def test_cross_repository_pr_denies_exemption_and_uses_real_risk(self) -> None:\n        s = self.new().build()\n        self.assertTrue(s.classify(), \"the SADR-0017 shape itself should be proven\")\n        s.is_cross_repository = True\n        signal = s.run_signal()\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n        self.assertEqual(signal[\"risk_level\"], \"critical\")\n        self.assertEqual(\n            sig._carry_resolved_risk(\n                shiki_root=str(s.target),\n                base_shiki=str(s.base),\n                pr_body=f\"Closeout {TASK_ID} for goal {GOAL_ID}.\",\n                task_risk=\"critical\",\n                exemption=signal[\"bookkeeping_closeout_exemption\"],\n            ),\n            \"critical\",\n        )\n\n    def test_absent_cross_repository_field_denies_exemption_and_uses_real_risk(self) -> None:\n        s = self.new().build()\n        self.assertTrue(s.classify(), \"the SADR-0017 shape itself should be proven\")\n        s.is_cross_repository = _SENTINEL\n        signal = s.run_signal()\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n        self.assertEqual(signal[\"risk_level\"], \"critical\")\n\n    # --- missing / unbuildable inputs fail closed -------------------------\n\n    def test_missing_base_snapshot_requires_guardian(self) -> None:\n        s = self.new().build()\n        signal = s.run_signal(base_shiki=\"\")\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n\n    def test_nonexistent_base_snapshot_requires_guardian(self) -> None:\n        s = self.new().build()\n        signal = s.run_signal(base_shiki=str(s.root / \"no-such-base\" / \".shiki\"))\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n\n    def test_missing_changed_files_status_requires_guardian(self) -> None:\n        s = self.new().build()\n        signal = s.run_signal(changed_files_status=\"\")\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n\n    def test_nonexistent_changed_files_status_requires_guardian(self) -> None:\n        s = self.new().build()\n        signal = s.run_signal(changed_files_status=str(s.target / \"absent-status.txt\"))\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n\n    def test_unresolvable_implementation_pr_requires_guardian(self) -> None:\n        # The base snapshot names no implementation PR (expected_pr null), so the\n        # merge proof cannot be established -> no exemption.\n        s = self.new().build()\n        base_task = json.loads((s.base / \"tasks\" / f\"{TASK_ID}.json\").read_text(encoding=\"utf-8\"))\n        base_task[\"expected_pr\"] = None\n        _write(s.base / \"tasks\" / f\"{TASK_ID}.json\", base_task)\n        signal = s.run_signal()\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n\n    def test_unresolvable_task_id_requires_guardian(self) -> None:\n        # A PR body with no Shiki task id: the exemption cannot resolve a task, and\n        # (as before) risk is undeterminable -> fail closed.\n        s = self.new().build()\n        signal = s.run_signal(body=\"Closeout PR with no task id in the body.\")\n        self.assertTrue(signal[\"required\"])\n        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\n\n    # --- auditability -----------------------------------------------------\n\n    def test_signal_records_exemption_applied_flag(self) -> None:\n        # Positive records True; a disqualified closeout records False. The flag is\n        # ALWAYS present so the decision is auditable in the uploaded evidence.\n        proven = self.new().build().run_signal()\n        self.assertIn(\"bookkeeping_closeout_exemption\", proven)\n        self.assertTrue(proven[\"bookkeeping_closeout_exemption\"])\n\n        s = self.new().build()\n        s.changed.append(cf(\"M\", \"scripts/guardian_approval_signal.py\"))\n        disq = s.run_signal()\n        self.assertIn(\"bookkeeping_closeout_exemption\", disq)\n        self.assertFalse(disq[\"bookkeeping_closeout_exemption\"])\n\n    def test_maintainer_label_still_forces_gate_on_exempt_pr(self) -> None:\n        # The exemption downgrades only the TASK's risk; a maintainer-applied\n        # risk:critical PR label still forces Guardian approval \u2014 exactly as in\n        # MergeGate \u2014 so the exemption is not a bypass.\n        s = self.new().build()\n        s.labels = [\"risk:critical\"]\n        signal = s.run_signal()\n        self.assertTrue(signal[\"required\"])\n        # The exemption WAS applied to the task risk; the label is what re-forces it.\n        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\n        self.assertTrue(s.classify())\n\n    # --- parity: signal == MergeGate, and never more permissive ----------\n\n    def test_parity_signal_matches_mergegate_and_is_never_more_permissive(self) -> None:\n        # A representative matrix of (label-set, mutation) cases. For each, the\n        # signal's exemption decision must EQUAL MergeGate's classifier, the\n        # resulting Guardian requirement must match, and the signal must NEVER be\n        # more permissive (never required=false where MergeGate requires it).\n        def positive(labels):\n            s = self.new().build()\n            s.labels = list(labels)\n            return s\n\n        def positive_noncompleting(labels):\n            s = self.new().build_noncompleting()\n            s.labels = list(labels)\n            return s\n\n        def non_shiki(labels):\n            s = positive(labels)\n            s.changed.append(cf(\"M\", \"scripts/guardian_approval_signal.py\"))\n            return s\n\n        def governance_edit(labels):\n            s = positive(labels)\n            s.head_task[\"locks\"] = [\"path:scripts/other.py\"]\n            s.rewrite_head_task()\n            return s\n\n        def deletion(labels):\n            s = positive(labels)\n            s.changed.append(cf(\"D\", f\".shiki/ledger/{PULL_LEDGER}.json\"))\n            return s\n\n        def non_terminal(labels):\n            s = positive(labels)\n            s.head_task[\"status\"] = \"review\"\n            s.rewrite_head_task()\n            return s\n\n        def unmerged(labels):\n            s = positive(labels)\n            s.merged = set()\n            return s\n\n        builders = [\n            positive, positive_noncompleting, non_shiki,\n            governance_edit, deletion, non_terminal, unmerged,\n        ]\n        label_sets = [[], [\"risk:critical\"], [\"risk:low\"]]\n\n        checked = 0\n        for build in builders:\n            for labels in label_sets:\n                s = build(labels)\n                signal = s.run_signal()\n                exempt = s.classify()\n                mg_required = s.mergegate_required(exempt=exempt)\n\n                self.assertEqual(\n                    signal[\"bookkeeping_closeout_exemption\"], exempt,\n                    f\"{build.__name__} labels={labels}: exemption decision diverged\",\n                )\n                self.assertEqual(\n                    signal[\"required\"], mg_required,\n                    f\"{build.__name__} labels={labels}: required diverged from MergeGate\",\n                )\n                # Never more permissive: the signal must not drop a Guardian\n                # requirement that MergeGate would enforce.\n                self.assertFalse(\n                    signal[\"required\"] is False and mg_required is True,\n                    f\"{build.__name__} labels={labels}: signal MORE permissive than MergeGate\",\n                )\n                checked += 1\n        self.assertEqual(checked, len(builders) * len(label_sets))\n\n\nif __name__ == \"__main__\":  # pragma: no cover\n    unittest.main()\ntests/test_guardian_signal_closeout.py:12:The fix imports the SAME ``mergegate_check.is_bookkeeping_closeout`` classifier\ntests/test_guardian_signal_closeout.py:38:    is_bookkeeping_closeout,\ntests/test_guardian_signal_closeout.py:63:    \"scope\": \"Import is_bookkeeping_closeout into the signal and plumb its inputs.\",\ntests/test_guardian_signal_closeout.py:275:        return is_bookkeeping_closeout(\ntests/test_guardian_signal_closeout.py:287:        ``enforce_guardian_policy`` (builtin high/critical OR policy-driven).\"\"\"\ntests/test_guardian_signal_closeout.py:290:        risk_labels = _guardian_risk_labels(pr, self.head_task, bookkeeping_closeout=exempt)\ntests/test_guardian_signal_closeout.py:314:        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:319:        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:326:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:375:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:383:                exemption=signal[\"bookkeeping_closeout_exemption\"],\ntests/test_guardian_signal_closeout.py:394:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:403:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:409:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:415:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:421:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:432:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:440:        self.assertFalse(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:448:        self.assertIn(\"bookkeeping_closeout_exemption\", proven)\ntests/test_guardian_signal_closeout.py:449:        self.assertTrue(proven[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:454:        self.assertIn(\"bookkeeping_closeout_exemption\", disq)\ntests/test_guardian_signal_closeout.py:455:        self.assertFalse(disq[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:466:        self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\ntests/test_guardian_signal_closeout.py:528:                    signal[\"bookkeeping_closeout_exemption\"], exempt,\ntests/test_bookkeeping_closeout.py:13:``mergegate_check.is_bookkeeping_closeout`` classifies such a PR, and the single\ntests/test_bookkeeping_closeout.py:14:Guardian decision point (``enforce_guardian_policy`` via ``_guardian_risk_labels``)\ntests/test_bookkeeping_closeout.py:43:    enforce_guardian_policy,\ntests/test_bookkeeping_closeout.py:44:    is_bookkeeping_closeout,\ntests/test_bookkeeping_closeout.py:78:    \"scope\": \"Add is_bookkeeping_closeout and consult it at the Guardian decision point.\",\ntests/test_bookkeeping_closeout.py:83:        \"path:tests/test_bookkeeping_closeout.py\",\ntests/test_bookkeeping_closeout.py:238:        return is_bookkeeping_closeout(\ntests/test_bookkeeping_closeout.py:254:        risk_labels = _guardian_risk_labels(self.pr(labels), self.head_task, bookkeeping_closeout=bookkeeping)\ntests/test_bookkeeping_closeout.py:266:        enforce_guardian_policy(\ntests/test_bookkeeping_closeout.py:276:            bookkeeping_closeout=bookkeeping,\ntests/test_bookkeeping_closeout.py:351:        # Classifier rejects it, so the code passes bookkeeping_closeout=False and\ntests/test_bookkeeping_closeout.py:559:        params = set(inspect.signature(is_bookkeeping_closeout).parameters)\ntests/test_bookkeeping_closeout.py:570:            c.pr(labels=[{\"name\": \"bookkeeping\"}], body=\"<!-- shiki:bookkeeping_closeout -->\"),\ntests/test_bookkeeping_closeout.py:572:            bookkeeping_closeout=c.classify(),\n\n2026-08-17T10:08:57.364894Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nThe focused suite is green, and the core prohibited-surface checks are clean. I found one evidence gap in the pre-existing tests: the Guardian-signal fork behavior is exercised dynamically, while the MergeGate-site guard is currently asserted only as source text. I\u2019m comparing the downstream source tests and the platform\u2019s existing CLI harness to add a real MergeGate-path fixture without broadening production scope.\nexec\n/bin/zsh -lc \"rg -n -C 6 'isCrossRepository|mergeBaseOid|lockless|lock_terminal|strongest_goal|coerced_expected|symlinked_task' /Users/kio.mizutani/CubeShare/tests/test_bookkeeping_closeout.py /Users/kio.mizutani/CubeShare/tests/test_guardian_signal_closeout.py /Users/kio.mizutani/CubeShare/tests/test_paired_invariants.py /Users/kio.mizutani/CubeShare/tests/test_migration_mode.py 2>/dev/null\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n exited 1 in 0ms:\nexec\n/bin/zsh -lc \"rg -n 'main_with_argv|sys\\\\.argv|mergegate_check\\\\.main|with patch.*argv|mock\\\\.patch.*argv|patch\\\\.object.*enforce_guardian_policy|enforce_guardian_policy.*mock' tests | head -200\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ntests/test_workflow_invocation.py:12:   ``mergegate_check.main()`` with the metadata job's exact argv against a valid\ntests/test_workflow_invocation.py:249:    saved_argv = sys.argv\ntests/test_workflow_invocation.py:251:    sys.argv = [\"mergegate_check.py\", *argv]\ntests/test_workflow_invocation.py:254:            rc = mergegate_check.main()\ntests/test_workflow_invocation.py:256:        sys.argv = saved_argv\ntests/test_guardian_status.py:197:            rc = status.main_with_argv([\ntests/test_guardian_status.py:219:                status.main_with_argv([\ntests/test_guardian_carry_parity.py:226:        rc = sig.main_with_argv(argv)\ntests/test_guardian_signal_closeout.py:258:        rc = sig.main_with_argv([\ntests/test_guardian_signal.py:73:    rc = sig.main_with_argv([\ntests/test_guardian_signal.py:147:            rc = sig.main_with_argv([\n\nexec\n/bin/zsh -lc \"sed -n '3000,3185p' scripts/mergegate_check.py; sed -n '3480,3595p' scripts/mergegate_check.py; sed -n '3595,3680p' scripts/mergegate_check.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n                blocking.append(f\"Repair packet {repair_id} does not reference current PR #{pr_number}\")\n\n        if path.startswith(\".shiki/plans/\") and path.endswith(\".json\"):\n            state_class = classify_shiki_path(path, manifest or {})\n            plan_stem = Path(path).stem\n            if entry.status == \"D\":\n                blocking.append(f\"PR must not delete Shiki plan {path}; state_class={state_class}\")\n                continue\n            goal = load_goal(target, goal_id)\n            source_plan_id = str(goal.get(\"source_plan\") or \"\") if isinstance(goal, dict) else \"\"\n            if not source_plan_id or plan_stem != source_plan_id:\n                blocking.append(\n                    f\"PR changes plan {path} that is not goal {goal_id}'s source_plan; state_class={state_class}\"\n                )\n                continue\n            # Spec Freeze is operator-only (SADR-0009). No PR may author or alter a\n            # spec_freeze block \u2014 not even on its own goal's source plan, which\n            # goal scoping alone would permit: an implementation PR could grant\n            # itself a freeze or append amendments to the plan that governs it.\n            # Compared against the base snapshot so an unchanged, pre-existing\n            # freeze passes; base absence is fail-closed (a freeze present at head\n            # with no base to prove it was already there reads as newly authored).\n            try:\n                head_plan = load_json(target / path)\n            except (OSError, ValueError):\n                head_plan = None\n            head_freeze = head_plan.get(\"spec_freeze\") if isinstance(head_plan, dict) else None\n            base_plan = None\n            if base_shiki is not None and base_shiki.exists():\n                try:\n                    base_plan = load_json(base_shiki / \"plans\" / f\"{plan_stem}.json\")\n                except (OSError, ValueError):\n                    base_plan = None\n            base_freeze = base_plan.get(\"spec_freeze\") if isinstance(base_plan, dict) else None\n            if head_freeze != base_freeze:\n                blocking.append(\n                    f\"PR must not create or modify the spec_freeze block of {path}; \"\n                    f\"Spec Freeze is operator-only; state_class={state_class}\"\n                )\n            continue\n\n        for mirror_dir in _GENERIC_MIRROR_DIRS:\n            prefix = f\".shiki/{mirror_dir}/\"\n            if not (path.startswith(prefix) and path.endswith(\".json\")):\n                continue\n            state_class = classify_shiki_path(path, manifest or {})\n            if entry.status == \"D\":\n                blocking.append(f\"PR must not delete Shiki {mirror_dir} record {path}; state_class={state_class}\")\n            else:\n                try:\n                    record = load_json(target / path)\n                except (OSError, ValueError):\n                    record = None\n                if not _mirror_record_scoped(record, path, task_id=task_id, goal_id=goal_id):\n                    blocking.append(\n                        f\"PR changes {mirror_dir} record {path} not scoped to task {task_id} or goal {goal_id}; \"\n                        f\"state_class={state_class}\"\n                    )\n            break\n\n    if base_shiki is None:\n        warnings.append(\"No base .shiki snapshot provided; protected base-state comparison was skipped\")\n        return\n    if not base_shiki.exists():\n        warnings.append(f\"Base .shiki snapshot not found at {base_shiki}; protected base-state comparison was skipped\")\n        return\n\n    for base_path, source_path in protected_base_files(base_shiki, \"ledger\").items():\n        state_class = classify_shiki_path(base_path, manifest or {})\n        target_path = target / base_path\n        if not target_path.exists():\n            blocking.append(f\"PR must not delete base ledger file {base_path}; state_class={state_class}\")\n            continue\n        if file_bytes(source_path) != file_bytes(target_path):\n            blocking.append(f\"PR must not modify existing base ledger file {base_path}; state_class={state_class}\")\n\n    # Every tracked mirror directory's base records are delete-protected, not only\n    # tasks/goals/locks: a base record absent at HEAD is a deletion the PR must not\n    # make. The current task's OWN lock is the sole exception (a release may delete\n    # it). ledger is handled by its own byte-equality loop above (it is\n    # append-only-evidence, not a mirror class, so it is not in this set).\n    for subdir in _tracked_mirror_subdirs(manifest or {}):\n        for base_path in protected_base_files(base_shiki, subdir):\n            if subdir == \"locks\" and base_path == lock_file:\n                continue\n            if not (target / base_path).exists():\n                state_class = classify_shiki_path(base_path, manifest or {})\n                blocking.append(f\"PR must not delete base Shiki {subdir} record {base_path}; state_class={state_class}\")\n\n\ndef main() -> int:\n    parser = argparse.ArgumentParser(description=\"Check Shiki MergeGate readiness\")\n    parser.add_argument(\"--target\", default=\".\", help=\"Target repository path\")\n    parser.add_argument(\"--pr-json\", default=\".shiki/gha/pr.json\")\n    parser.add_argument(\"--cca-verdict\", default=\".shiki/gha/cca-verdict.json\")\n    parser.add_argument(\"--cca-evidence-manifest\", default=\"\")\n    parser.add_argument(\"--changed-files\", default=\".shiki/gha/changed-files.txt\")\n    parser.add_argument(\"--changed-files-status\", default=\".shiki/gha/changed-files-status.txt\")\n    parser.add_argument(\"--expected-repository\", default=\"\")\n    parser.add_argument(\"--expected-head-sha\")\n    parser.add_argument(\"--base-shiki\")\n    parser.add_argument(\n        \"--merged-prs\",\n        default=\"\",\n        help=\"Comma-separated PR numbers proven merged (used by post_merge_reconcile to verify the referenced PR actually merged).\",\n    )\n    parser.add_argument(\n        \"--contract-approval\",\n        default=\"\",\n        help=(\n            \"Path to the SADR-0015 Contract Approval registration proof (JSON). When present and valid, a \"\n            \"normal task PR whose contract was registered and Guardian-approved before dispatch satisfies the \"\n            \"Guardian requirement without a live approval of its own. Absent/missing/unreadable => no effect.\"\n        ),\n    )\n    parser.add_argument(\"--guardian-policy\", default=\".shiki/guardian-policy.json\")\n    parser.add_argument(\"--guardian-comments\", default=\".shiki/gha/live-guardian-comments.json\")\n    parser.add_argument(\"--guardian-events\", default=\".shiki/gha/live-guardian-events.json\")\n    parser.add_argument(\"--guardian-timeline\", default=\".shiki/gha/live-guardian-timeline.json\")\n    parser.add_argument(\"--result-file\", default=\".shiki/gha/mergegate-result.json\")\n    parser.add_argument(\"--allow-missing-cca\", action=\"store_true\")\n    # Base-sync carry (guardian_comment_carried). Inert unless BOTH are given: the\n    # flag is off by default and --default-branch defaults to empty, so an\n    # invocation that omits them is byte-for-byte the pre-carry gate.\n    parser.add_argument(\n        \"--base-sync-carry\",\n        action=\"store_true\",\n        help=\"Carry a Guardian comment approval across a proven pure base sync (guardian_comment_carried); requires --default-branch.\",\n    )\n    parser.add_argument(\n        \"--default-branch\",\n        default=\"\",\n        help=\"Default branch name; the base the carry proves a pure sync against. Empty disables the carry.\",\n    )\n    args = parser.parse_args()\n\n    target = Path(args.target).resolve()\n    blocking: list[str] = []\n    warnings: list[str] = []\n    resolved_task_id: str | None = None\n    resolved_goal_id: str | None = None\n    reconcile_mode = False\n    post_merge_mode = False\n    contract_mode = False\n    amendment_mode = False\n    migration_mode = False\n    # PR numbers proven merged (live PR state gathered by the workflow). Used by\n    # post_merge_reconcile to prove its referenced PR merged, and by the bookkeeping\n    # closeout classifier to prove the task's implementation PR merged (SADR-0017).\n    merged_prs = {\n        int(token.strip())\n        for token in str(args.merged_prs or \"\").split(\",\")\n        if token.strip().isdigit()\n    }\n    # Whether the (normal single-task) PR is a proven bookkeeping closeout, so the\n    # Guardian gate evaluates it as if the task were low risk (SADR-0017). Stays\n    # False for contract / goal_reconcile / post_merge_reconcile PRs.\n    bookkeeping_closeout = False\n    manifest: dict[str, Any] | None = None\n    try:\n        manifest = load_manifest(target)\n    except ManifestError as error:\n        blocking.append(str(error))\n\n    pr = load_json(Path(args.pr_json))\n    body = \"\"\n    if pr:\n        body = str(pr.get(\"body\") or \"\")\n        resolved_task_id = first_match(TASK_ID, body)\n        resolved_goal_id = first_match(GOAL_ID, body)\n        reconcile_mode, reconcile_error = goal_reconcile_decision(pr)\n        post_merge_mode, post_merge_error = post_merge_reconcile_decision(pr)\n        contract_mode, contract_error = contract_decision(pr)\n        amendment_mode, amendment_error = amendment_decision(pr)\n        migration_mode, migration_error = migration_decision(pr)\n        if reconcile_error:\n            blocking.append(reconcile_error)\n        if post_merge_error:\n            blocking.append(post_merge_error)\n        if contract_error:\n            blocking.append(contract_error)\n        if amendment_error:\n            blocking.append(amendment_error)\n        if migration_error:\n            blocking.append(migration_error)\n        if reconcile_mode and post_merge_mode:\n                )\n                # SADR-0015 Contract immutability on the NORMAL task-PR path: a PR\n                # that changes its own task file may move only bookkeeping \u2014 its\n                # frozen governance contract is bound to the base snapshot, and its\n                # risk_level may never resolve weaker than base. Only this branch\n                # calls it (contract / goal_reconcile / post_merge_reconcile use\n                # their own validators), so nothing is double-reported.\n                blocking.extend(\n                    normal_task_contract_immutability_reasons(\n                        base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                        task_id=resolved_task_id,\n                        head_task=task,\n                        changed_files_status=files_status,\n                    )\n                )\n            ledger_text = \"\\n\".join(ledger_entry_text(entry) for entry in ledger_entries)\n            for skill in task.get(\"required_skills\") or []:\n                skill_name = str(skill).strip().lower()\n                if skill_name and skill_name != \"none\" and skill_name not in ledger_text:\n                    blocking.append(f\"Required skill {skill} has no matching ledger evidence\")\n\n    cca = load_json(Path(args.cca_verdict))\n    if cca:\n        for error in validate_cca_contract(target, cca):\n            blocking.append(f\"CCA verdict schema violation: {error}\")\n        if pr and args.cca_evidence_manifest:\n            manifest_path = Path(args.cca_evidence_manifest)\n            if not manifest_path.is_absolute():\n                manifest_path = target / manifest_path\n            evidence_dir = manifest_path.parent if manifest_path.parent != Path(\"\") else Path(\".shiki/gha\")\n            for error in validate_cca_evidence_contract(\n                target=target,\n                manifest_path=manifest_path,\n                evidence_dir=evidence_dir,\n                pr=pr,\n                task_id=resolved_task_id,\n                goal_id=resolved_goal_id,\n                expected_repository=args.expected_repository,\n            ):\n                blocking.append(error)\n        elif pr and args.expected_repository:\n            blocking.append(\"CCA evidence manifest path is required when expected repository is provided\")\n        if cca.get(\"verdict\") != \"complete\":\n            blocking.append(f\"CCA verdict is not complete: {cca.get('verdict')!r}\")\n        if resolved_task_id and cca.get(\"task_id\") != resolved_task_id:\n            blocking.append(f\"CCA task_id {cca.get('task_id')!r} does not match PR task id {resolved_task_id}\")\n        if resolved_goal_id and cca.get(\"goal_id\") != resolved_goal_id:\n            blocking.append(f\"CCA goal_id {cca.get('goal_id')!r} does not match PR goal id {resolved_goal_id}\")\n        if task and cca.get(\"goal_id\") != task.get(\"goal_id\"):\n            blocking.append(f\"CCA goal_id {cca.get('goal_id')!r} does not match task goal_id {task.get('goal_id')!r}\")\n        if pr:\n            if cca.get(\"pr\") != pr.get(\"number\"):\n                blocking.append(f\"CCA pr {cca.get('pr')!r} does not match PR #{pr.get('number')}\")\n            if not cca.get(\"head_sha\"):\n                blocking.append(\"CCA head_sha is missing\")\n            elif pr.get(\"headRefOid\") and cca.get(\"head_sha\") != pr.get(\"headRefOid\"):\n                blocking.append(\"CCA head_sha does not match the current PR headRefOid\")\n        if cca.get(\"can_merge\") is not True:\n            blocking.append(\"CCA verdict did not set can_merge=true\")\n        failures = blocking_checklist_failures(cca)\n        if failures:\n            blocking.append(\"CCA verdict contains blocking failed checklist items: \" + \", \".join(failures))\n        if cca.get(\"verdict\") == \"complete\":\n            acceptance_failures = failing_acceptance_criteria(cca)\n            if acceptance_failures:\n                blocking.append(\n                    \"complete CCA verdict contains failing acceptance criteria: \" + \", \".join(acceptance_failures)\n                )\n        for reason in checklist_profile_coverage_failures(task, cca):\n            blocking.append(reason)\n        acceptance = cca.get(\"acceptance\")\n        if not isinstance(acceptance, list) or not acceptance:\n            blocking.append(\"CCA verdict acceptance evidence is empty\")\n        if task and task.get(\"status\") not in {\"review\", \"done\"}:\n            blocking.append(f\"Task status must be review or done after CCA verdict; got {task.get('status')!r}\")\n        if pr and ledger_entries:\n            pr_number = pr.get(\"number\")\n            pr_token = f\"/pull/{pr_number}\"\n            pr_text = \"\\n\".join(ledger_entry_text(entry) for entry in ledger_entries)\n            if pr_number is not None and pr_token not in pr_text and f\"pr #{pr_number}\" not in pr_text:\n                blocking.append(f\"Task ledger evidence does not reference PR #{pr_number}\")\n        if pr:\n            enforce_required_checks(pr, target, blocking, warnings)\n            enforce_review_policy(pr, target, blocking)\n            # Reconcile PRs carry no `task`, so the guardian gate would otherwise\n            # miss the reconciled work's risk. Derive a risk-bearing task so a\n            # high/critical merged task (post_merge) or goal (goal_reconcile)\n            # still forces Guardian evaluation for its reconcile.\n            guardian_task = task\n            if guardian_task is None and post_merge_mode and resolved_task_id:\n                guardian_task = load_task(target, resolved_task_id)\n            elif guardian_task is None and reconcile_mode and resolved_goal_id:\n                goal = load_goal(target, resolved_goal_id)\n                if isinstance(goal, dict) and goal.get(\"risk_level\"):\n                    guardian_task = {\"risk_level\": goal.get(\"risk_level\")}\n            elif task is not None and args.base_shiki and resolved_task_id:\n                # Normal path (SADR-0015): resolve the Guardian risk NEVER WEAKER\n                # than the base snapshot, so a PR that lowered its own task\n                # risk_level cannot dissolve its Guardian requirement \u2014 the gate\n                # still fires at the base risk. This is the independent floor the\n                # acceptance requires: the guardian outcome survives even if the\n                # immutability block above were bypassed. A no-op when the task\n                # file is unchanged (base risk == head risk) or on an upgrade.\n                base_shiki_dir = Path(args.base_shiki)\n                base_task_for_risk: dict[str, Any] | None = None\n                if base_shiki_dir.exists():\n                    try:\n                        base_task_for_risk = load_json(base_shiki_dir / \"tasks\" / f\"{resolved_task_id}.json\")\n                    except (OSError, ValueError):\n                        # An unreadable base leaves the head risk in force here; the\n                        # immutability gate above has already blocked the change.\n                        base_task_for_risk = None\n                resolved_risk = resolved_guardian_risk_never_weaker(base_task_for_risk, task)\n                if resolved_risk and resolved_risk != str(task.get(\"risk_level\") or \"\"):\n                    guardian_task = {**task, \"risk_level\": resolved_risk}\n            # SADR-0015 Contract Approval applies ONLY on the normal task path: a\n            # SADR-0015 Contract Approval applies ONLY on the normal task path: a\n            # Contract PR (contract mode) is the registration itself and must be\n            # approved live, and reconcile PRs carry no implementation task. When\n            # the flag is absent, this is None and the Guardian gate is unchanged.\n            contract_approval = None\n            if task is not None and not reconcile_mode and not post_merge_mode and not contract_mode:\n                contract_approval = contract_approval_for_pr(\n                    task_id=resolved_task_id,\n                    head_task=task,\n                    base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                    changed_files_status=files_status,\n                    registration=load_contract_approval_registration(args.contract_approval),\n                )\n            enforce_guardian_policy(\n                pr=pr,\n                task=guardian_task,\n                target=target,\n                guardian_policy=args.guardian_policy,\n                guardian_comments=args.guardian_comments,\n                guardian_events=args.guardian_events,\n                guardian_timeline=args.guardian_timeline,\n                blocking=blocking,\n                warnings=warnings,\n                expected_repository=args.expected_repository,\n                bookkeeping_closeout=bookkeeping_closeout,\n                contract_approval=contract_approval,\n                base_sync_carry=args.base_sync_carry,\n                default_branch=args.default_branch,\n            )\n    elif not args.allow_missing_cca:\n        blocking.append(f\"CCA verdict file not found at {args.cca_verdict}\")\n\n    changed_files_path = Path(args.changed_files)\n    if changed_files_path.exists():\n        files = changed_files(changed_files_path)\n        if any(path.startswith(\".github/workflows/\") for path in files):\n            warnings.append(\"workflow files changed; ci:infra or Guardian review may be required\")\n        if any(\"auth\" in path.lower() or \"secret\" in path.lower() for path in files):\n            warnings.append(\"auth/secret-like paths changed; ci:security or Guardian review may be required\")\n\n    result = {\n        \"mergegate\": \"blocked\" if blocking else \"ready\",\n        \"task_id\": resolved_task_id,\n        \"goal_id\": resolved_goal_id,\n        \"blocking_reasons\": blocking,\n        \"warnings\": warnings,\n    }\n    write_json(Path(args.result_file), result)\n    print(json.dumps(result, indent=2, sort_keys=True))\n    return 1 if blocking else 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n\nexec\n/bin/zsh -lc 'git -C /Users/kio.mizutani/CubeShare status --short -- tests/test_bookkeeping_closeout.py tests/test_guardian_signal_closeout.py tests/test_paired_invariants.py scripts/mergegate_check.py scripts/guardian_approval_signal.py 2>/dev/null && git -C /Users/kio.mizutani/CubeShare log -1 --oneline -- scripts/mergegate_check.py 2>/dev/null' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n23d04c67 chore(shiki): upgrade to 2026-08-07 platform\n\n2026-08-17T10:09:09.812712Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1,335p' tests/test_workflow_invocation.py; sed -n '335,520p' tests/test_workflow_invocation.py; rg -n 'def write_json|def load_manifest|ManifestError' tests/shiki_test_support.py scripts/mergegate_check.py | head -50\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n\"\"\"Workflows must invoke the scripts they claim to, with the flags those scripts\nneed \u2014 the contract asserts the *invocation*, not the mere presence of strings.\n\nThree gaps this pins:\n\n1. ``.github/workflows/shiki-mergegate.yml`` metadata check ran\n   ``mergegate_check.py`` WITHOUT ``--base-shiki``/``--merged-prs``, so a\n   ``post_merge_reconcile`` PR always blocked (\"requires a base .shiki snapshot\")\n   even though the CCA workflow already builds and passes exactly those inputs.\n   The metadata job now builds the base snapshot in \"Prepare PR context\" and\n   passes both flags, mirroring the CCA workflow. A test drives\n   ``mergegate_check.main()`` with the metadata job's exact argv against a valid\n   ``post_merge_reconcile`` fixture and proves it is ``ready``; the same argv with\n   ``--base-shiki`` removed still blocks (fail-closed behaviour unchanged).\n\n2. ``.github/workflows/shiki-orchestrator.yml`` commit-evidence step guarded with\n   ``git diff --quiet`` \u2014 which only sees TRACKED files \u2014 so newly created\n   (untracked) evidence reported \"no changes\" and the step exited 0 without\n   committing. It now stages ``.shiki`` first and guards with\n   ``git diff --cached --quiet``, so new-file-only evidence is committed and\n   pushed.\n\n3. The same workflow spliced comment- and input-derived GitHub expressions\n   (``${{ ... }}``) straight into ``run:`` bodies, an injection surface. Those\n   values now flow through ``env:`` and are referenced as quoted shell variables,\n   so a command-substitution payload in a comment reaches the plan-file argument\n   as a literal string.\n\n4. ``validate_shiki`` now asserts the invocation contract: the metadata job passes\n   the required flags, and the hardened workflows carry no ``${{ }}`` expression\n   inside any ``run:`` body. Removing a flag or reintroducing an expression fails\n   validation.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport contextlib\nimport io\nimport json\nimport os\nimport re\nimport shutil\nimport subprocess\nimport sys\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport mergegate_check\nimport validate_shiki\nfrom shiki_workflows import load_workflow_model, workflow_jobs\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\nWORKFLOWS = REPO_ROOT / \".github\" / \"workflows\"\nMERGEGATE_WF = WORKFLOWS / \"shiki-mergegate.yml\"\nORCHESTRATOR_WF = WORKFLOWS / \"shiki-orchestrator.yml\"\n# SADR-0018 base-sync carry: shiki-cca-completion.yml now has a top-level entry in\n# validate_shiki.WORKFLOW_INVOCATION_REQUIRED_FLAGS, so the staged root the contract\n# check runs against must include it or the loop FileNotFoundErrors (see _stage).\nCCA_WF = WORKFLOWS / \"shiki-cca-completion.yml\"\n\n\n# --- shared workflow-text helpers ----------------------------------------\n\n\ndef _step_run_body(workflow_path: Path, step_name: str) -> str:\n    \"\"\"The ``run:`` script of the ``- name: <step_name>`` step, raw from the file\n    (comments preserved), joined verbatim.\"\"\"\n    lines = workflow_path.read_text(encoding=\"utf-8\").splitlines()\n    start = next(\n        (i for i, line in enumerate(lines) if line.strip() == f\"- name: {step_name}\"),\n        None,\n    )\n    if start is None:\n        raise AssertionError(f\"step {step_name!r} not found in {workflow_path.name}\")\n    # Collect the block until the next `- name:` at the same or shallower indent.\n    block = lines[start + 1 :]\n    end = next((i for i, line in enumerate(block) if line.strip().startswith(\"- name:\")), len(block))\n    step_lines = [lines[start], *block[:end]]\n    # Keep only the run: body lines (everything after `run: |` up to the block's end).\n    text = \"\\n\".join(step_lines)\n    run_idx = text.find(\"run:\")\n    return text[run_idx:] if run_idx != -1 else text\n\n\ndef _job_step_runs(model: dict, job_id: str) -> list[str]:\n    job = workflow_jobs(model).get(job_id)\n    steps = job.get(\"steps\") if isinstance(job, dict) else None\n    if not isinstance(steps, list):\n        return []\n    return [s[\"run\"] for s in steps if isinstance(s, dict) and isinstance(s.get(\"run\"), str)]\n\n\ndef _mergegate_invocation() -> str:\n    \"\"\"The ``python3 scripts/mergegate_check.py ...`` command in the metadata job,\n    from the parsed model, sliced from the script token to the end of the step.\"\"\"\n    model = load_workflow_model(MERGEGATE_WF)\n    body = next((r for r in _job_step_runs(model, \"mergegate\") if \"scripts/mergegate_check.py\" in r), None)\n    if body is None:\n        raise AssertionError(\"metadata job has no mergegate_check.py invocation\")\n    return body[body.index(\"scripts/mergegate_check.py\") :]\n\n\n# --- AC1 / AC2: metadata job argv against a post_merge_reconcile fixture ---\n\nGOAL_ID = \"G-20260729T065622761503Z-4479b0d3\"\nTASK_ID = \"T-20260729T065622765968Z-234bf98b\"\nLEDGER_ID = \"L-20260729T070000000000Z-1234abcd\"\nHEAD_SHA = \"b\" * 40\nIMPL_PR = 4242          # the reconciled task's merged implementation PR\nRECONCILE_PR = 4243     # this post_merge_reconcile PR\n\n# Flag -> fixture value used to reconstruct the metadata job's argv. Path flags\n# keep the workflow's own relative paths (resolved under the fixture cwd);\n# env-var flags map to the fixture's concrete values.\n_FLAG_VALUES = {\n    \"--changed-files-status\": \".shiki/gha/changed-files-status.txt\",\n    \"--expected-head-sha\": HEAD_SHA,\n    \"--guardian-policy\": \".shiki/guardian-policy.json\",\n    \"--guardian-comments\": \".shiki/gha/live-guardian-comments.json\",\n    \"--guardian-events\": \".shiki/gha/live-guardian-events.json\",\n    \"--guardian-timeline\": \".shiki/gha/live-guardian-timeline.json\",\n    \"--base-shiki\": \".shiki/gha/base-shiki/.shiki\",\n    \"--merged-prs\": str(IMPL_PR),\n    # SADR-0015 Contract Approval carry: the metadata job now passes\n    # --contract-approval (built earlier in the step). The fixture does not write\n    # this file, so mergegate_check degrades to \"no proof\" (missing file -> None)\n    # and the medium-risk reconcile PR is unaffected \u2014 but the flag must resolve a\n    # value here or argv reconstruction KeyErrors the moment the workflow adds it.\n    \"--contract-approval\": \".shiki/gha/contract-approval.json\",\n    # SADR-0018 base-sync carry: the metadata job now also passes --default-branch.\n    # A concrete branch name resolves the argv; the carry is inert for this\n    # medium-risk reconcile PR (the carry refuses risk != high before any git), so\n    # the ready result is unchanged. --base-sync-carry is store_true (below).\n    \"--default-branch\": \"main\",\n}\n_STORE_TRUE = {\"--allow-missing-cca\", \"--base-sync-carry\"}\n\n\ndef _write(path: Path, payload: dict) -> None:\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(payload, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n\n# The immutable governance body shared byte-for-byte by base and head task. Only\n# status / expected_pr / ledger_evidence may move during a post_merge reconcile.\n_TASK_BODY = {\n    \"id\": TASK_ID,\n    \"goal_id\": GOAL_ID,\n    \"title\": \"Make workflows invoke the scripts they claim to\",\n    \"scope\": \"Wire base snapshot + merged-prs into the metadata check.\",\n    \"non_goals\": [],\n    \"dependencies\": [],\n    \"locks\": [\"path:.github/workflows/shiki-mergegate.yml\"],\n    \"assigned_runtime\": \"claude-code\",\n    \"risk_level\": \"medium\",\n    \"acceptance_checks\": [\"metadata check passes a post_merge_reconcile PR\"],\n    \"expected_branch\": \"shiki/t-20260729t065622765968z-234bf98b-make-wor\",\n}\n\nPR_BODY = f\"\"\"Reconcile the merged task residue.\n\n<!-- shiki:post_merge_reconcile -->\n\nTask: {TASK_ID}\n\n## Scope\nClear the residual lock and expected_pr for the merged task.\n\n## Acceptance\nTask lock released and expected_pr cleared.\n\n## Evidence\nReconcile ledger appended.\n\n## MergeGate\npost_merge_reconcile mode.\n\"\"\"\n\n\ndef _build_post_merge_fixture(target: Path) -> None:\n    \"\"\"A valid post_merge_reconcile fixture that a correctly-wired metadata check\n    must return ``ready`` for. Copies the repo's environmental artifacts (manifest,\n    config, guardian policy, workflows \u2014 for job-name/required-check validation)\n    and writes the reconcile diff.\"\"\"\n    shiki = target / \".shiki\"\n    shiki.mkdir(parents=True, exist_ok=True)\n    for rel in (\".shiki/manifest.json\", \".shiki/config.yaml\", \".shiki/guardian-policy.json\"):\n        dest = target / rel\n        dest.parent.mkdir(parents=True, exist_ok=True)\n        shutil.copyfile(REPO_ROOT / rel, dest)\n    shutil.copytree(WORKFLOWS, target / \".github\" / \"workflows\")\n\n    # HEAD task: reconciled residue \u2014 status back to review, expected_pr cleared.\n    head_task = {**_TASK_BODY, \"status\": \"review\", \"expected_pr\": None,\n                 \"ledger_evidence\": [LEDGER_ID]}\n    _write(shiki / \"tasks\" / f\"{TASK_ID}.json\", head_task)\n    _write(shiki / \"goals\" / f\"{GOAL_ID}.json\",\n           {\"id\": GOAL_ID, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"medium\"})\n    # Reconcile ledger (task-scoped lock event).\n    _write(shiki / \"ledger\" / f\"{LEDGER_ID}.json\",\n           {\"id\": LEDGER_ID, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"lock\",\n            \"actor\": \"shiki-loop\", \"timestamp\": \"2026-07-29T07:00:00+00:00\",\n            \"summary\": \"lock released post-merge\", \"evidence\": [f\".shiki/tasks/{TASK_ID}.json\"]})\n\n    # Base snapshot (origin/base): the merged impl PR left the task at review naming\n    # the impl PR, with its lock active \u2014 the residue being reconciled.\n    base = shiki / \"gha\" / \"base-shiki\" / \".shiki\"\n    _write(base / \"tasks\" / f\"{TASK_ID}.json\",\n           {**_TASK_BODY, \"status\": \"review\", \"expected_pr\": IMPL_PR, \"ledger_evidence\": [\"L-base00001\"]})\n\n    gha = shiki / \"gha\"\n    gha.mkdir(parents=True, exist_ok=True)\n    _write(gha / \"pr.json\",\n           {\"number\": RECONCILE_PR, \"body\": PR_BODY, \"headRefOid\": HEAD_SHA,\n            \"labels\": [{\"name\": \"mergegate:post_merge_reconcile\"}], \"reviews\": []})\n    # The diff: task file modified, lock deleted (released), reconcile ledger added.\n    (gha / \"changed-files-status.txt\").write_text(\n        f\"M\\t.shiki/tasks/{TASK_ID}.json\\n\"\n        f\"D\\t.shiki/locks/{TASK_ID}.json\\n\"\n        f\"A\\t.shiki/ledger/{LEDGER_ID}.json\\n\",\n        encoding=\"utf-8\",\n    )\n    for name in (\"live-guardian-comments\", \"live-guardian-events\", \"live-guardian-timeline\"):\n        (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n\n\ndef _metadata_argv(*, include_base_shiki: bool = True) -> list[str]:\n    \"\"\"Reconstruct the metadata job's argv from the workflow's own invocation, so a\n    flag added or dropped in the workflow changes what this test runs.\"\"\"\n    invocation = _mergegate_invocation()\n    flags = re.findall(r\"--[a-z][a-z-]+\", invocation)\n    if \"--base-shiki\" not in flags or \"--merged-prs\" not in flags:\n        raise AssertionError(f\"metadata invocation missing merge-proof flags: {flags}\")\n    argv: list[str] = []\n    for flag in flags:\n        if flag == \"--base-shiki\" and not include_base_shiki:\n            continue\n        argv.append(flag)\n        if flag not in _STORE_TRUE:\n            argv.append(_FLAG_VALUES[flag])\n    return argv\n\n\ndef _run_main(argv: list[str], cwd: Path) -> tuple[int, dict]:\n    saved_cwd = os.getcwd()\n    saved_argv = sys.argv\n    os.chdir(cwd)\n    sys.argv = [\"mergegate_check.py\", *argv]\n    try:\n        with contextlib.redirect_stdout(io.StringIO()):\n            rc = mergegate_check.main()\n    finally:\n        sys.argv = saved_argv\n        os.chdir(saved_cwd)\n    result = json.loads((cwd / \".shiki\" / \"gha\" / \"mergegate-result.json\").read_text())\n    return rc, result\n\n\nclass MetadataArgvPostMergeReconcile(unittest.TestCase):\n    def test_metadata_argv_returns_ready(self) -> None:\n        # AC1: driving main() with the metadata job's exact argv against a valid\n        # post_merge_reconcile fixture returns ready (it blocked before the wiring).\n        with tempfile.TemporaryDirectory() as tmp:\n            target = Path(tmp)\n            _build_post_merge_fixture(target)\n            rc, result = _run_main(_metadata_argv(), target)\n        self.assertEqual(result[\"blocking_reasons\"], [], result)\n        self.assertEqual(result[\"mergegate\"], \"ready\")\n        self.assertEqual(rc, 0)\n\n    def test_without_base_shiki_still_blocks(self) -> None:\n        # AC2: the same argv with --base-shiki removed still blocks \u2014 fail-closed\n        # behaviour is unchanged.\n        with tempfile.TemporaryDirectory() as tmp:\n            target = Path(tmp)\n            _build_post_merge_fixture(target)\n            rc, result = _run_main(_metadata_argv(include_base_shiki=False), target)\n        self.assertEqual(result[\"mergegate\"], \"blocked\")\n        self.assertEqual(rc, 1)\n        self.assertTrue(\n            any(\"base .shiki snapshot\" in reason for reason in result[\"blocking_reasons\"]),\n            result[\"blocking_reasons\"],\n        )\n\n\n# --- AC5: validate_shiki invocation contract ------------------------------\n\n\nclass ValidateWorkflowInvocationContract(unittest.TestCase):\n    def _stage(self, tmp: Path) -> Path:\n        \"\"\"A throwaway repo root holding the hardened workflows plus every workflow\n        named in the invocation contract so the contract check can run against a\n        mutated copy. shiki-cca-completion.yml is included because SADR-0018 added a\n        top-level entry for it to WORKFLOW_INVOCATION_REQUIRED_FLAGS; omitting it\n        would FileNotFoundError inside validate_workflow_invocation_contracts.\"\"\"\n        dest = tmp / \".github\" / \"workflows\"\n        dest.mkdir(parents=True, exist_ok=True)\n        for wf in (MERGEGATE_WF, ORCHESTRATOR_WF, CCA_WF):\n            shutil.copyfile(wf, dest / wf.name)\n        return tmp\n\n    def test_repo_workflows_satisfy_contract(self) -> None:\n        # The live repo workflows must pass the contract (proves it is wired in).\n        validate_shiki.validate_workflow_invocation_contracts(REPO_ROOT)\n\n    def test_missing_base_shiki_flag_fails(self) -> None:\n        with tempfile.TemporaryDirectory() as tmp:\n            root = self._stage(Path(tmp))\n            wf = root / \".github\" / \"workflows\" / MERGEGATE_WF.name\n            wf.write_text(\n                wf.read_text(encoding=\"utf-8\").replace(\n                    \"--base-shiki .shiki/gha/base-shiki/.shiki \\\\\\n\", \"\"\n                ),\n                encoding=\"utf-8\",\n            )\n            with self.assertRaises(validate_shiki.ValidationError):\n                validate_shiki.validate_workflow_invocation_contracts(root)\n\n    def test_missing_merged_prs_flag_fails(self) -> None:\n        with tempfile.TemporaryDirectory() as tmp:\n            root = self._stage(Path(tmp))\n            wf = root / \".github\" / \"workflows\" / MERGEGATE_WF.name\n            text = wf.read_text(encoding=\"utf-8\")\n            self.assertIn('--merged-prs \"$MERGED_PRS\"', text)\n            wf.write_text(text.replace('--merged-prs \"$MERGED_PRS\"', \"\"), encoding=\"utf-8\")\n            with self.assertRaises(validate_shiki.ValidationError):\n                validate_shiki.validate_workflow_invocation_contracts(root)\n\n    def test_expression_in_run_body_fails(self) -> None:\n        with tempfile.TemporaryDirectory() as tmp:\n            root = self._stage(Path(tmp))\n            wf = root / \".github\" / \"workflows\" / ORCHESTRATOR_WF.name\n            wf = root / \".github\" / \"workflows\" / ORCHESTRATOR_WF.name\n            text = wf.read_text(encoding=\"utf-8\")\n            self.assertIn('--plan-file \"$PLAN_PATH\"', text)\n            wf.write_text(\n                text.replace(\n                    '--plan-file \"$PLAN_PATH\"',\n                    '--plan-file \"${{ steps.command.outputs.plan_path }}\"',\n                    1,\n                ),\n                encoding=\"utf-8\",\n            )\n            with self.assertRaises(validate_shiki.ValidationError):\n                validate_shiki.validate_workflow_invocation_contracts(root)\n\n\n# --- AC3: commit-evidence stages before the change guard ------------------\n\n\nclass CommitEvidenceGuard(unittest.TestCase):\n    STEP = \"Execute Shiki plan and open evidence PR\"\n\n    def test_stages_before_guard(self) -> None:\n        run = _step_run_body(ORCHESTRATOR_WF, self.STEP)\n        self.assertIn(\"git add .shiki\", run)\n        self.assertIn(\"git diff --cached --quiet\", run)\n        self.assertLess(\n            run.index(\"git add .shiki\"),\n            run.index(\"git diff --cached --quiet\"),\n            \"staging must precede the change guard\",\n        )\n        # The buggy bare guard (which only sees tracked files) must be gone.\n        self.assertNotIn(\"if git diff --quiet\", run)\n\n    def test_new_files_only_are_committed_and_pushed(self) -> None:\n        # AC3: with only NEW (untracked) evidence, the fixed stage-then-guard\n        # sequence detects the change, commits and pushes \u2014 the bare guard did not.\n        with tempfile.TemporaryDirectory() as tmp:\n            root = Path(tmp)\n            origin = root / \"origin.git\"\n            work = root / \"work\"\n            self._git(\"init\", \"--bare\", str(origin))\n            self._git(\"init\", str(work))\n            self._git(\"-C\", str(work), \"config\", \"user.email\", \"t@example.com\")\n            self._git(\"-C\", str(work), \"config\", \"user.name\", \"Shiki Test\")\n            self._git(\"-C\", str(work), \"remote\", \"add\", \"origin\", str(origin))\n            (work / \"README.md\").write_text(\"seed\\n\", encoding=\"utf-8\")\n            self._git(\"-C\", str(work), \"add\", \"README.md\")\n            self._git(\"-C\", str(work), \"commit\", \"-m\", \"seed\")\n            self._git(\"-C\", str(work), \"branch\", \"-M\", \"main\")\n            self._git(\"-C\", str(work), \"push\", \"-u\", \"origin\", \"main\")\n\n            branch = \"shiki/orchestrator-evidence-1\"\n            self._git(\"-C\", str(work), \"switch\", \"-c\", branch)\n            # ONLY new untracked evidence \u2014 nothing tracked changes.\n            evidence = work / \".shiki\" / \"gha\" / \"evidence.json\"\n            evidence.parent.mkdir(parents=True, exist_ok=True)\n            evidence.write_text(\"{}\\n\", encoding=\"utf-8\")\n\n            # The old guard: `git diff --quiet` reports \"no changes\" (exit 0).\n            old = subprocess.run([\"git\", \"-C\", str(work), \"diff\", \"--quiet\"])\n            self.assertEqual(old.returncode, 0, \"bare git diff must miss untracked files\")\n\n            # The fixed guard: stage first, then diff the index.\n            self._git(\"-C\", str(work), \"add\", \".shiki\")\n            staged = subprocess.run([\"git\", \"-C\", str(work), \"diff\", \"--cached\", \"--quiet\"])\n            self.assertNotEqual(staged.returncode, 0, \"staged diff must see the new file\")\n\n            self._git(\"-C\", str(work), \"commit\", \"-m\", \"shiki: record orchestrator evidence\")\n            self._git(\"-C\", str(work), \"push\", \"-u\", \"origin\", branch)\n\n            remote_log = subprocess.run(\n                [\"git\", \"-C\", str(origin), \"log\", \"--oneline\", branch],\n                capture_output=True, text=True, check=True,\n            )\n        self.assertIn(\"record orchestrator evidence\", remote_log.stdout)\n\n    @staticmethod\n    def _git(*args: str) -> None:\n        subprocess.run([\"git\", *args], check=True, capture_output=True, text=True)\n\n\n# --- AC4: comment payload reaches plan-file as a literal string -----------\n\n\nclass PlanFileInjection(unittest.TestCase):\n    SHIKI = REPO_ROOT / \"scripts\" / \"shiki.py\"\n\n    def test_plan_path_flows_through_env_not_expression(self) -> None:\n        run = _step_run_body(ORCHESTRATOR_WF, \"Run Shiki plan\")\n        self.assertIn('--plan-file \"$PLAN_PATH\"', run)\n        self.assertNotIn(\"${{ steps.command.outputs.plan_path }}\", run)\n\n    def test_command_substitution_payload_is_literal(self) -> None:\n        # AC4: a $(...) payload in the (comment-derived) plan path reaches shiki.py\n        # as a literal string \u2014 it is NOT executed \u2014 and the script fails with its\n        # own missing-file error, exactly as the env-var `\"$PLAN_PATH\"` handoff does.\n        # This runs the workflow's actual `smoke live ... --plan-file \"$PLAN_PATH\"`\n        # command; a stub `gh` on PATH satisfies smoke live's auth/repo checks so\n        # the plan read (read_json -> \"missing file\") is what fires, hermetically.\n        with tempfile.TemporaryDirectory() as tmp:\n            root = Path(tmp)\n            target = root / \"target\"\n            (target / \".shiki\").mkdir(parents=True)\n            subprocess.run([\"git\", \"init\", str(target)], check=True, capture_output=True)\n            subprocess.run(\n                [\"git\", \"-C\", str(target), \"remote\", \"add\", \"origin\",\n                 \"https://github.com/example/shiki-injection-test.git\"],\n                check=True, capture_output=True,\n            )\n            fake_bin = root / \"bin\"\n            fake_bin.mkdir()\n            gh_stub = fake_bin / \"gh\"\n            gh_stub.write_text(\"#!/usr/bin/env bash\\nexit 0\\n\", encoding=\"utf-8\")\n            gh_stub.chmod(0o755)\n\n            sentinel = root / \"pwned\"\n            payload = f\"$(touch {sentinel})\"\n            script = 'python3 \"$SHIKI\" smoke live --target \"$TARGET\" --plan-file \"$PLAN_PATH\" --dry-run'\n            env = {\n                **os.environ,\n                \"PATH\": f\"{fake_bin}{os.pathsep}{os.environ.get('PATH', '')}\",\n                \"SHIKI\": str(self.SHIKI),\n                \"TARGET\": str(target),\n                \"PLAN_PATH\": payload,\n            }\n            result = subprocess.run(\n                [\"bash\", \"-c\", script], cwd=str(target), env=env, capture_output=True, text=True\n            )\n            self.assertNotEqual(result.returncode, 0)\n            self.assertFalse(sentinel.exists(), \"command substitution payload was executed\")\n            combined = result.stdout + result.stderr\n            self.assertIn(\"missing file\", combined)\n            self.assertIn(payload, combined)\n\n\nif __name__ == \"__main__\":  # pragma: no cover\n    unittest.main()\nscripts/mergegate_check.py:24:from shiki_manifest import ManifestError, load_manifest\nscripts/mergegate_check.py:64:def write_json(path: Path, data: dict[str, Any]) -> None:\nscripts/mergegate_check.py:3161:    except ManifestError as error:\n\n2026-08-17T10:09:18.691701Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -n -C 5 '_path_proven_absent|_path_is_regular_file' scripts/mergegate_check.py; sed -n '1260,1425p' scripts/mergegate_check.py; sed -n '2280,2355p' scripts/mergegate_check.py; sed -n '1380,1430p' scripts/shiki_tasks.py; rg -n -C 3 'MG-25|MG-26|mergegate:migration|immutable merge base|lockless' docs/agents/checklists.md docs/agents/decision-control.md docs/agents/triage-labels.md docs/agents/implementation-policy.md docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n1975-# ``expected_branch`` to a branch it is not on, so admitting the field here cannot\n1976-# let a closeout impersonate another branch. Do not weaken that binding.\n1977-_CLOSEOUT_MUTABLE_TASK_FIELDS = frozenset({\"status\", \"expected_pr\", \"closeout_pr\", \"ledger_evidence\", \"expected_branch\"})\n1978-\n1979-\n1980:def _path_proven_absent(path: Path) -> bool:\n1981-    \"\"\"Prove repository-path absence without following a dangling symlink.\"\"\"\n1982-    try:\n1983-        path.lstat()\n1984-    except FileNotFoundError:\n1985-        try:\n--\n1989-    except OSError:\n1990-        return False\n1991-    return False\n1992-\n1993-\n1994:def _path_is_regular_file(path: Path) -> bool:\n1995-    \"\"\"Require a regular repository file without following symlinks.\"\"\"\n1996-    try:\n1997-        return stat.S_ISREG(path.lstat().st_mode)\n1998-    except OSError:\n1999-        return False\n--\n2074-        ):\n2075-            return False\n2076-        task_file = f\".shiki/tasks/{task_id}.json\"\n2077-        base_task_path = base_shiki / \"tasks\" / f\"{task_id}.json\"\n2078-        head_task_path = target / task_file\n2079:        if not (_path_is_regular_file(base_task_path) and _path_is_regular_file(head_task_path)):\n2080-            return False\n2081-        base_task = load_json(base_task_path)\n2082-        head_task = load_json(head_task_path)\n2083-        if not isinstance(base_task, dict) or not isinstance(head_task, dict) or head_task != task:\n2084-            return False\n--\n2150-                # Condition 5 (lock leg): active -> released, proven against base.\n2151-                base_lock_path = base_shiki / \"locks\" / f\"{task_id}.json\"\n2152-                head_lock_path = target / path\n2153-                if (\n2154-                    entry.status != \"M\"\n2155:                    or not _path_is_regular_file(base_lock_path)\n2156:                    or not _path_is_regular_file(head_lock_path)\n2157-                ):\n2158-                    return False\n2159-                base_lock = load_json(base_lock_path)\n2160-                head_lock = load_json(head_lock_path)\n2161-                if not (isinstance(base_lock, dict) and base_lock.get(\"state\") == \"active\"):\n--\n2177-                base_goal_path = base_shiki / \"goals\" / f\"{goal_id}.json\"\n2178-                head_goal_path = target / path\n2179-                if (\n2180-                    entry.status != \"M\"\n2181-                    or not completes_goal\n2182:                    or not _path_is_regular_file(base_goal_path)\n2183:                    or not _path_is_regular_file(head_goal_path)\n2184-                ):\n2185-                    return False\n2186-                base_goal = load_json(base_goal_path)\n2187-                head_goal = load_json(head_goal_path)\n2188-                if not (\n--\n2204-                head_goal_for_closeout = head_goal\n2205-                goal_completed = True\n2206-            elif path.startswith(\".shiki/reports/\") and path.endswith(\".json\"):\n2207-                # A scorecard is ADDED by goal completion; a modified report is not\n2208-                # part of the terminal set.\n2209:                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n2210-                    return False\n2211-                reports_added += 1\n2212-                completion_report_path = path\n2213-            elif path == worktree_file:\n2214-                return False\n2215-            elif path.startswith(\".shiki/ledger/\") and path.endswith(\".json\"):\n2216-                # Ledgers are append-only mirror bookkeeping (condition 3 and the\n2217-                # untrusted-mutation gate police them); an ADD carries no transition,\n2218-                # but a modify is never part of a closeout's terminal set.\n2219:                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n2220-                    return False\n2221-                changed_ledger_paths.append(path)\n2222-            else:\n2223-                # Any other .shiki path (dag, memories, guardian-policy, another\n2224-                # task/goal/lock, a plan, \u2026) is not part of the terminal set.\n--\n2324-            added_goal_ledger_ids = head_goal_ledger[len(base_goal_ledger) :]\n2325-            if len(added_goal_ledger_ids) != 1 or not isinstance(added_goal_ledger_ids[0], str):\n2326-                return False\n2327-            completion_ledger_id = added_goal_ledger_ids[0]\n2328-            completion_ledger_path = target / \".shiki\" / \"ledger\" / f\"{completion_ledger_id}.json\"\n2329:            if not _path_is_regular_file(completion_ledger_path):\n2330-                return False\n2331-            completion_ledger = load_json(completion_ledger_path)\n2332-            completion_evidence = completion_ledger.get(\"evidence\") if isinstance(completion_ledger, dict) else None\n2333-            if not (\n2334-                isinstance(completion_ledger, dict)\n--\n2361-            if not isinstance(added_ledger_id, str):\n2362-                return False\n2363-            if added_ledger_id == completion_ledger_id:\n2364-                continue\n2365-            added_ledger_path = target / \".shiki\" / \"ledger\" / f\"{added_ledger_id}.json\"\n2366:            if not _path_is_regular_file(added_ledger_path):\n2367-                return False\n2368-            added_ledger = load_json(added_ledger_path)\n2369-            self_reference_evidence = added_ledger.get(\"evidence\") if isinstance(added_ledger, dict) else None\n2370-            required_self_reference_evidence = {task_file, lock_file}\n2371-            if not (\n\n    The same two-factor shape ``contract_decision`` implements, with the same\n    message when the label is missing: a marker without the maintainer-applied\n    label fails closed so untrusted PR text cannot self-grant the mode. The marker\n    is matched only outside code spans (as ``amendment_decision`` does) so a quoted\n    marker in reproduced task scope is documentation, not a declaration.\n    \"\"\"\n    body = str(pr.get(\"body\") or \"\")\n    marker = bool(MIGRATION_MARKER.search(_strip_markdown_code(body)))\n    label = MIGRATION_LABEL in pr_label_names(pr)\n    if marker and not label:\n        return False, (\n            f\"migration mode requires the {MIGRATION_LABEL} label (a maintainer-applied \"\n            \"second factor) in addition to the body marker\"\n        )\n    return (marker and label), None\n\n\ndef declared_migration_ids(body: str) -> list[str]:\n    \"\"\"Every id declared on its own ``Migration: <id>`` line in the PR body.\"\"\"\n    return [match.group(1) for match in _MIGRATION_ID_LINE.finditer(body)]\n\n\ndef _git_show_merge_base(target: Path, merge_base_oid: str, repo_path: str) -> str:\n    \"\"\"Read ``repo_path`` from one immutable, workflow-resolved merge base.\"\"\"\n    if not re.fullmatch(r\"[0-9a-fA-F]{40,64}\", merge_base_oid):\n        raise FileNotFoundError(f\"invalid immutable mergeBaseOid {merge_base_oid!r}\")\n    result = subprocess.run(\n        [\"git\", \"show\", f\"{merge_base_oid}:{repo_path}\"],\n        cwd=str(target),\n        capture_output=True,\n        text=True,\n    )\n    if result.returncode == 0:\n        return result.stdout\n    detail = result.stderr.strip() or \"not found\"\n    raise FileNotFoundError(\n        f\"could not read {repo_path} from immutable mergeBaseOid {merge_base_oid} ({detail})\"\n    )\n\n\ndef resolve_base_migration_registry(target: Path, merge_base_oid: str) -> tuple[Any, ...]:\n    \"\"\"Resolve the migration registry from the immutable merge base's\n    ``scripts/shiki_migrations.py`` \u2014 NEVER the PR head's.\n\n    THIS IS THE SECURITY PROPERTY OF MIGRATION MODE. Do NOT simplify this to import\n    the head registry (``from shiki_migrations import migration_registry``): the\n    migration function whose output the PR is measured against MUST come from code\n    the base branch already merged. If it came from the PR head, a PR could define a\n    migration that emits exactly the diff it wants and the re-run comparison would\n    pass \u2014 proving nothing. Reading from base means the gate executes trusted code\n    the PR cannot alter.\n\n    The base ``scripts/`` is deliberately read from git rather than a snapshot: the\n    ``--base-shiki`` archive the workflow builds is ``.shiki``-only, and staging\n    base ``scripts/`` would need a new workflow flag and archive step (out of this\n    change's scope). So the base source is read with ``git show`` (using the fetch\n    the MergeGate workflow already performs) and executed in an isolated namespace;\n    its only external dependency (``shiki_process``) resolves from ``scripts/`` on\n    ``sys.path``, infrastructure that carries no migration logic.\n    \"\"\"\n    source = _git_show_merge_base(target, merge_base_oid, \"scripts/shiki_migrations.py\")\n    # Execute the base source as a REAL module object registered in sys.modules:\n    # dataclass field resolution (Python 3.12+) looks the class's module up there,\n    # so a plain-dict namespace would fail on the module's ``@dataclass Migration``.\n    # ``shiki_process`` \u2014 the module's only external dependency \u2014 resolves from\n    # ``scripts/`` on sys.path, infrastructure that carries no migration logic.\n    module = types.ModuleType(\"shiki_migrations_base\")\n    module.__file__ = \"<base>/scripts/shiki_migrations.py\"\n    sys.modules[module.__name__] = module\n    exec(compile(source, module.__file__, \"exec\"), module.__dict__)\n    return tuple(module.migration_registry())\n\n\ndef _read_shiki_tree(shiki_dir: Path) -> dict[str, bytes]:\n    \"\"\"Every file under a ``.shiki`` directory, keyed by its repo-relative\n    (``.shiki/...``) path and mapped to its raw bytes. Order-independent.\"\"\"\n    files: dict[str, bytes] = {}\n    if not shiki_dir.exists():\n        return files\n    for path in shiki_dir.rglob(\"*\"):\n        if path.is_file():\n            rel = \".shiki/\" + path.relative_to(shiki_dir).as_posix()\n            files[rel] = path.read_bytes()\n    return files\n\n\ndef _base_applied_migration_ids(base_shiki: Path) -> set[str]:\n    \"\"\"Migration ids recorded ``applied`` in the base ``.shiki/migrations/state.json``.\n    A missing/unreadable/malformed state file yields the empty set (nothing proven\n    applied), so the already-applied block never fires on absent state.\"\"\"\n    try:\n        state = load_json(base_shiki / \"migrations\" / \"state.json\")\n    except (OSError, ValueError):\n        return set()\n    if not isinstance(state, dict) or not isinstance(state.get(\"applied\"), list):\n        return set()\n    return {\n        str(record.get(\"id\"))\n        for record in state[\"applied\"]\n        if isinstance(record, dict) and record.get(\"id\")\n    }\n\n\ndef enforce_migration(\n    *,\n    target: Path,\n    pr: dict[str, Any],\n    base_shiki: Path | None,\n    changed_files_status: list[ChangedFile],\n    blocking: list[str],\n    warnings: list[str],\n    base_registry: tuple[Any, ...] | None = None,\n) -> None:\n    \"\"\"Validate a Migration PR: the .shiki diff must EQUAL the re-run output of a\n    BASE-registered migration. Deny by default.\n\n    ``base_registry`` is injected only by tests to exercise the comparison and\n    precondition logic directly; in production it is None and the registry is\n    resolved from the base branch via ``resolve_base_migration_registry`` (the\n    security property \u2014 the migration function is read from base, never the PR\n    head). Every other input \u2014 the base snapshot applied against, the already-applied\n    state, the base registry \u2014 is reconstructed from the base branch the PR head\n    cannot forge; the PR head is only ever the thing COMPARED.\n    \"\"\"\n    if base_shiki is None or not base_shiki.exists():\n        blocking.append(\"migration mode requires a base .shiki snapshot to re-run the declared migration\")\n        return\n\n    body = str(pr.get(\"body\") or \"\")\n    ids = declared_migration_ids(body)\n    if not ids:\n        blocking.append(\"migration PR body must declare the migration id on its own line as 'Migration: <id>'\")\n        return\n    if len(ids) > 1:\n        blocking.append(f\"migration PR body must declare exactly one 'Migration:' line; found {sorted(ids)}\")\n        return\n    migration_id = ids[0]\n\n    if base_registry is None:\n        merge_base_oid = str(pr.get(\"mergeBaseOid\") or \"\")\n        if not merge_base_oid:\n            blocking.append(\n                \"migration mode requires the workflow-resolved immutable mergeBaseOid \"\n                \"to resolve the trusted migration registry\"\n            )\n            return\n        try:\n            base_registry = resolve_base_migration_registry(target, merge_base_oid)\n        except Exception as error:  # noqa: BLE001 - resolving base code can fail many ways; fail closed with a named reason, never crash the gate\n            blocking.append(f\"migration mode could not resolve the immutable merge-base migration registry: {error}\")\n            return\n\n    by_id = {str(migration.id): migration for migration in base_registry}\n    migration = by_id.get(migration_id)\n    if migration is None:\n        blocking.append(\n            f\"migration {migration_id} is not registered in the base branch migration registry; \"\n            \"a PR cannot define the migration whose output it is measured against\"\n        )\n        return\n    if getattr(migration, \"destructive\", False):\n        blocking.append(f\"migration {migration_id} is destructive; migration mode refuses destructive migrations\")\n        return\n    if migration_id in _base_applied_migration_ids(base_shiki):\n        blocking.append(\n                and report.get(\"id\") == Path(completion_report_path).stem\n                and report.get(\"goal_id\") == goal_id\n                and report.get(\"status\") == \"complete\"\n                and report_evidence == expected_task_paths\n                and report.get(\"blocking_reasons\") == []\n                and isinstance(report_mergegate, dict)\n                and report_mergegate.get(\"dependencies\") == \"pass\"\n                and report_mergegate.get(\"locks\") == \"pass\"\n                and report_mergegate.get(\"checks\") == \"pass\"\n                and report_mergegate.get(\"review\") == \"recorded\"\n                and report_mergegate.get(\"ledger\") == \"pass\"\n                and report_mergegate.get(\"risk\") == effective_risk\n            ):\n                return False\n            scorecard_complete = bool(\n                isinstance(scorecard, dict)\n                and scorecard.get(\"goal_id\") == goal_id\n                and isinstance(scorecard_tasks, dict)\n                and type(scorecard_tasks.get(\"total\")) is int\n                and type(scorecard_tasks.get(\"completed\")) is int\n                and type(scorecard_tasks.get(\"failed\")) is int\n                and scorecard_tasks.get(\"total\") == len(goal_task_ids)\n                and scorecard_tasks.get(\"completed\") == len(goal_task_ids)\n                and scorecard_tasks.get(\"failed\") == 0\n            )\n            degraded_warnings = scorecard.get(\"warnings\") if isinstance(scorecard, dict) else None\n            scorecard_degraded = bool(\n                isinstance(scorecard, dict)\n                and set(scorecard) == {\"goal_id\", \"warnings\"}\n                and scorecard.get(\"goal_id\") == goal_id\n                and isinstance(degraded_warnings, list)\n                and len(degraded_warnings) == 1\n                and isinstance(degraded_warnings[0], str)\n                and degraded_warnings[0].startswith(\"scorecard generation failed:\")\n            )\n            if not (scorecard_complete or scorecard_degraded):\n                return False\n\n            base_goal_ledger = base_goal_for_closeout.get(\"ledger_evidence\")\n            head_goal_ledger = head_goal_for_closeout.get(\"ledger_evidence\")\n            if not isinstance(base_goal_ledger, list) or not isinstance(head_goal_ledger, list):\n                return False\n            if head_goal_ledger[: len(base_goal_ledger)] != base_goal_ledger:\n                return False\n            added_goal_ledger_ids = head_goal_ledger[len(base_goal_ledger) :]\n            if len(added_goal_ledger_ids) != 1 or not isinstance(added_goal_ledger_ids[0], str):\n                return False\n            completion_ledger_id = added_goal_ledger_ids[0]\n            completion_ledger_path = target / \".shiki\" / \"ledger\" / f\"{completion_ledger_id}.json\"\n            if not _path_is_regular_file(completion_ledger_path):\n                return False\n            completion_ledger = load_json(completion_ledger_path)\n            completion_evidence = completion_ledger.get(\"evidence\") if isinstance(completion_ledger, dict) else None\n            if not (\n                isinstance(completion_ledger, dict)\n                and completion_ledger.get(\"id\") == completion_ledger_id\n                and completion_ledger.get(\"goal_id\") == goal_id\n                and completion_ledger.get(\"task_id\") is None\n                and completion_ledger.get(\"type\") == \"completion\"\n                and completion_evidence == [completion_report_path]\n            ):\n                return False\n        else:\n            # A non-completing closeout must carry NO goal transition and NO report.\n            if goal_completed or reports_added != 0:\n                return False\n\n        base_task_ledger = base_task.get(\"ledger_evidence\")\n        head_task_ledger = task.get(\"ledger_evidence\")\n        if not isinstance(base_task_ledger, list) or not isinstance(head_task_ledger, list):\n            return False\n        if head_task_ledger[: len(base_task_ledger)] != base_task_ledger:\n            return False\n        added_task_ledger_ids = head_task_ledger[len(base_task_ledger) :]\n        if (\n            not all(isinstance(value, str) for value in added_task_ledger_ids)\n\n\ndef cmd_goal_complete(args: argparse.Namespace) -> int:\n    target = target_path(args.target)\n    require_github_first_target(target)\n    ensure_control_dirs(target)\n    goal = load_goal(target, args.goal_id)\n    tasks = tasks_for_goal(target, args.goal_id)\n    incomplete = [task[\"id\"] for task in tasks if task.get(\"status\") != \"done\"]\n    blocking: list[str] = []\n    if not tasks:\n        blocking.append(\"goal has no tasks\")\n    if incomplete:\n        blocking.append(f\"incomplete tasks: {', '.join(incomplete)}\")\n\n    status = \"blocked\" if blocking else \"complete\"\n    effective_risk = str(goal.get(\"risk_level\") or \"\")\n    for task in tasks:\n        task_risk = str(task.get(\"risk_level\") or \"\")\n        if _RISK_ORDER.get(task_risk, -1) > _RISK_ORDER.get(effective_risk, -1):\n            effective_risk = task_risk\n    if not effective_risk:\n        effective_risk = \"low\"\n    report_id = next_control_id(target, \"R\")\n    report = {\n        \"id\": report_id,\n        \"goal_id\": args.goal_id,\n        \"status\": status,\n        \"summary\": args.summary or f\"Goal {args.goal_id} {status}: {goal['title']}\",\n        \"evidence\": [f\".shiki/tasks/{task['id']}.json\" for task in tasks],\n        \"blocking_reasons\": blocking,\n        \"mergegate\": {\n            \"dependencies\": \"pass\" if not blocking else \"blocked\",\n            \"locks\": \"pass\",\n            \"checks\": \"pass\" if not blocking else \"blocked\",\n            \"review\": \"recorded\",\n            \"ledger\": \"pass\",\n            \"risk\": effective_risk,\n        },\n        \"created_at\": utc_now(),\n    }\n    # Scorecard (proposal 3.6) lives INSIDE the report file, never on stdout, so\n    # the goal-complete stdout stays json_get_last compatible. Generation is\n    # failure tolerant: a scorecard error must not block goal completion.\n    try:\n        from shiki_memory import compute_scorecard\n\n        report[\"scorecard\"] = compute_scorecard(target, args.goal_id, tasks=tasks)\n    except Exception as error:  # noqa: BLE001 - scorecard is failure tolerant (3.6)\n        report[\"scorecard\"] = {\"goal_id\": args.goal_id, \"warnings\": [f\"scorecard generation failed: {error}\"]}\n    report_file = shiki_path(target, \"reports\", f\"{report_id}.json\")\ndocs/agents/triage-labels.md-29-- `mergegate:waiting` - merge evidence is incomplete.\ndocs/agents/triage-labels.md-30-- `mergegate:blocked` - check, lock, dependency, risk, or review blocks merge.\ndocs/agents/triage-labels.md-31-- `mergegate:ready` - MergeGate conditions are satisfied.\ndocs/agents/triage-labels.md:32:- `mergegate:migration` - a configured maintainer selected the repository-wide\ndocs/agents/triage-labels.md-33-  mirror migration route. The PR must still bind to a Task, CCA, review, and\ndocs/agents/triage-labels.md-34-  Guardian policy; its `.shiki/**` diff must equal one migration registered on\ndocs/agents/triage-labels.md:35:  the immutable merge base, path-for-path and byte-for-byte.\ndocs/agents/triage-labels.md-36-- `repair:needed` - a bounded Repair Loop is required.\ndocs/agents/triage-labels.md-37-\ndocs/agents/triage-labels.md-38-## Skill Gate\n--\ndocs/agents/decision-control.md-15-| Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\ndocs/agents/decision-control.md-16-| Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\ndocs/agents/decision-control.md-17-| Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndocs/agents/decision-control.md:18:| Can a repository-wide mirror migration run? | Planner proposes; maintainer selects mode; MergeGate proves output | After the migration code and reviewed old-value inventory are already on the immutable merge base | Task/Goal contract, migration ID, immutable merge-base snapshot, exact `.shiki/**` diff | migration ready / blocked | `mergegate:migration`, merge-base registry re-run, CCA, review, Guardian policy |\ndocs/agents/decision-control.md-19-| Can work be split into issues? | Planner | After PRD or settled Goal | Acceptance criteria, non-goals, dependencies | Vertical-slice issues | `to-issues`, issue templates |\ndocs/agents/decision-control.md-20-| Is an issue AFK-ready or HITL? | Triager | Issue triage | Scope, decisions remaining, testability, risk | `ready-for-agent` or HITL/blocker label | triage labels, dispatch check |\ndocs/agents/decision-control.md-21-| Which runtime should execute? | Runtime Router | After issue is AFK-ready | Task type, risk, skills, repo constraints | Runtime assignment | task metadata, handoff schema |\n--\ndocs/agents/checklists.md-227-| MG-19 | Runtime registry names, config role assignments, task `assigned_runtime` values, docs, and install/staging coverage remain consistent. | blocking |\ndocs/agents/checklists.md-228-| MG-20 | GitHub provider configuration validates provider kind, host, remote protocol, repo slug, canonical remote URL, API base URL, and legacy `.shiki/repo.json` compatibility. | blocking |\ndocs/agents/checklists.md-229-| MG-21 | `.shiki` state classes classify changed paths, block unknown/runtime/cache/local-only paths, and preserve append-only-evidence rules. | blocking |\ndocs/agents/checklists.md:230:| MG-25 | Repository branch protection or a ruleset requires Shiki checks; otherwise the absent hard enforcement is recorded as a rollout blocker and the Goal is not claimed enforceable. | blocking for governance rollout completion |\ndocs/agents/checklists.md:231:| MG-26 | Migration mode has the exact marker, maintainer-applied label, one non-destructive migration ID registered on the immutable merge base, a snapshot archived from that same merge base, no changes outside `.shiki/**`, and a path/byte-exact re-run result; normal Task, CCA, review, and Guardian gates remain active. | blocking when applicable |\ndocs/agents/checklists.md-232-\ndocs/agents/checklists.md-233-## GC \u2014 Goal Completion Checklist\ndocs/agents/checklists.md-234-\n\n2026-08-17T10:09:29.306155Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -n -C 6 '_path_proven_absent|_path_is_regular_file|def is_bookkeeping_closeout|lock_terminal|lockless' /Users/kio.mizutani/CubeShare/scripts/mergegate_check.py | head -240\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n1993-            return None\n1994-        if str(node_task.get(\"status\")) != \"done\":\n1995-            return False\n1996-    return True\n1997-\n1998-\n1999:def is_bookkeeping_closeout(\n2000-    *,\n2001-    target: Path,\n2002-    task: dict[str, Any] | None,\n2003-    task_id: str,\n2004-    goal_id: str,\n2005-    base_shiki: Path | None,\n\n2026-08-17T10:09:36.390710Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1930,2265p' /Users/kio.mizutani/CubeShare/scripts/mergegate_check.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n    path = Path(value)\n    return path if path.is_absolute() else target / path\n\n\ndef _guardian_risk_labels(\n    pr: dict[str, Any], task: dict[str, Any] | None, *, bookkeeping_closeout: bool = False\n) -> list[str]:\n    labels = list(pr_label_names(pr))\n    if task:\n        risk = str(task.get(\"risk_level\") or \"\").strip().lower()\n        if bookkeeping_closeout:\n            # SADR-0017: a PROVEN bookkeeping closeout carries no implementation and\n            # inherits no new risk, so the TASK's risk is evaluated as if low at this\n            # single Guardian decision point. PR labels (an independent\n            # maintainer-applied signal) are left untouched, so a maintainer can\n            # still force the gate by labelling the PR.\n            risk = \"low\"\n        if risk:\n            labels.append(risk)\n            labels.append(f\"risk:{risk}\")\n    return labels\n\n\ndef _builtin_guardian_risk_required(risk_labels: list[str]) -> bool:\n    normalized = {label.strip().lower().removeprefix(\"risk:\") for label in risk_labels if label.strip()}\n    return bool(normalized.intersection({\"high\", \"critical\"}))\n\n\n# Task-file keys a bookkeeping closeout may legitimately move between the base\n# snapshot and the PR head. Every OTHER key is the frozen governance contract\n# (scope, non_goals, required_skills, risk_level, locks, acceptance_checks,\n# test_command, title, dependencies, \u2026) and must be byte-identical, so the\n# exemption can never ride a smuggled contract edit.\n#\n# ``expected_branch`` belongs here \u2014 it is NOT a governance field. The goal loop\n# cuts the closeout onto ``shiki/<task_id>-closeout`` and rewrites the closeout\n# task file's ``expected_branch`` to that new branch on EVERY closeout by\n# construction (scripts/shiki_loop.py), because MergeGate independently requires a\n# PR's task ``expected_branch`` to equal the PR's actual head ref: the metadata gate\n# in ``main`` blocks when ``expected_branch != headRefName``.\n# So every real closeout differs from its base on ``expected_branch``, and without\n# this entry condition 4 would reject every closeout before any other condition is\n# reached \u2014 the exact defect SADR-0017 exists to remove. That independent head-ref\n# binding is load-bearing for THIS whitelist entry: a PR cannot set\n# ``expected_branch`` to a branch it is not on, so admitting the field here cannot\n# let a closeout impersonate another branch. Do not weaken that binding.\n_CLOSEOUT_MUTABLE_TASK_FIELDS = frozenset({\"status\", \"expected_pr\", \"closeout_pr\", \"ledger_evidence\", \"expected_branch\"})\n\n\ndef _closeout_completes_goal(target: Path, goal_id: str) -> bool | None:\n    \"\"\"Whether this closeout completes its goal: every task node of the goal's DAG\n    is ``done`` at HEAD (this task set ``done`` by the closeout). Returns None when\n    it cannot be established (missing/empty DAG, or an unresolvable node) so the\n    caller fails closed. The DAG is read at HEAD, but a bookkeeping closeout may\n    not change the DAG (``is_bookkeeping_closeout`` rejects any ``.shiki/dag`` path\n    in the diff), so the DAG this reads is the authoritative base DAG.\"\"\"\n    dag = load_dag(target, goal_id)\n    nodes = dag.get(\"nodes\") if isinstance(dag, dict) else None\n    if not isinstance(nodes, list) or not nodes:\n        return None\n    for node in nodes:\n        node_task = load_task(target, str(node))\n        if not isinstance(node_task, dict):\n            return None\n        if str(node_task.get(\"status\")) != \"done\":\n            return False\n    return True\n\n\ndef is_bookkeeping_closeout(\n    *,\n    target: Path,\n    task: dict[str, Any] | None,\n    task_id: str,\n    goal_id: str,\n    base_shiki: Path | None,\n    changed_files_status: list[ChangedFile],\n    merged_pr_numbers: set[int],\n) -> bool:\n    \"\"\"Classify whether a PR is a pure bookkeeping closeout (SADR-0017).\n\n    A bookkeeping closeout carries ONLY the terminal state that a merged task's\n    completion writes to ``main``: the task moves ``review -> done``, its lock\n    ``active -> released``, and \u2014 ONLY when this task completes its goal \u2014 the goal\n    moves ``-> complete`` with exactly one added scorecard report. It contains zero\n    implementation, so it needed no code review to produce and inherits no new\n    risk; a qualifying PR is therefore evaluated as if the task were low risk at\n    the single Guardian decision point (``enforce_guardian_policy``), while every\n    OTHER gate runs unchanged.\n\n    Every condition is proven from the diff, the base snapshot, and live PR state\n    (``merged_pr_numbers``) \u2014 NEVER from the PR body, a label, a marker, or any\n    task field the PR head can write (note: this function never reads the PR at\n    all). It FAILS CLOSED on every axis: any condition that cannot be established\n    (missing base snapshot, unreadable task file, unresolvable implementation PR,\n    missing DAG, \u2026) returns False, and the PR inherits the task's real risk exactly\n    as before.\n\n    The six SADR-0017 conditions:\n      1. every changed path is under ``.shiki/`` (one byte outside disqualifies);\n      2. no path is deleted;\n      3. every changed path is inside the task's declared locks unioned with its\n         derived id-scoped mirror locks (the existing files-outside-locks rule);\n      4. the task file's governance fields are byte-identical to the base snapshot\n         \u2014 only the ``_CLOSEOUT_MUTABLE_TASK_FIELDS`` (status, expected_pr,\n         closeout_pr, ledger_evidence and expected_branch \u2014 see that definition for\n         why the branch rewrite is safe) may differ;\n      5. the transitions are exactly the terminal set: task ``review -> done``,\n         lock ``active -> released``, and ONLY when this task completes its goal,\n         goal ``-> complete`` with exactly one added scorecard report;\n      6. the task's implementation PR (named by the BASE snapshot's expected_pr,\n         which the PR head cannot write) is proven merged.\n    \"\"\"\n    try:\n        if base_shiki is None or not base_shiki.exists():\n            return False\n        if not isinstance(task, dict) or str(task.get(\"id\") or \"\") != task_id or not goal_id:\n            return False\n        base_task = load_json(base_shiki / \"tasks\" / f\"{task_id}.json\")\n        if not isinstance(base_task, dict):\n            return False\n\n        # Condition 4: every task-file key outside the mutable set is the frozen\n        # governance contract and must equal the base snapshot exactly.\n        for key in set(base_task) | set(task):\n            if key in _CLOSEOUT_MUTABLE_TASK_FIELDS:\n                continue\n            if base_task.get(key) != task.get(key):\n                return False\n\n        # Condition 5 (task leg): status transitions review -> done, exactly.\n        if str(base_task.get(\"status\")) != \"review\" or str(task.get(\"status\")) != \"done\":\n            return False\n\n        # Condition 6: the implementation PR is named by the BASE expected_pr (a\n        # field the PR head cannot write) and must be proven merged.\n        try:\n            impl_pr_num = int(base_task.get(\"expected_pr\"))\n        except (TypeError, ValueError):\n            return False\n        if impl_pr_num not in (merged_pr_numbers or set()):\n            return False\n\n        # Whether this closeout completes its goal, judged from the HEAD DAG.\n        completes_goal = _closeout_completes_goal(target, goal_id)\n        if completes_goal is None:\n            return False\n\n        task_file = f\".shiki/tasks/{task_id}.json\"\n        lock_file = f\".shiki/locks/{task_id}.json\"\n        goal_file = f\".shiki/goals/{goal_id}.json\"\n        worktree_file = f\".shiki/worktrees/{task_id}.json\"\n\n        task_changed = False\n        lock_released = False\n        goal_completed = False\n        reports_added = 0\n        changed_paths: list[str] = []\n\n        for entry in changed_files_status:\n            path = normalize_repo_path(entry.path)\n            old_path = normalize_repo_path(entry.old_path or \"\")\n            # Condition 1: every changed path (both legs of a rename) under .shiki/.\n            for candidate in (candidate for candidate in (path, old_path) if candidate):\n                if not candidate.startswith(\".shiki/\"):\n                    return False\n                changed_paths.append(candidate)\n            # Condition 2: no deletion (a rename splits into a delete leg too).\n            if entry.status == \"D\":\n                return False\n\n            if path == task_file:\n                task_changed = True\n            elif path == lock_file:\n                # Condition 5 (lock leg): active -> released, proven against base.\n                base_lock = load_json(base_shiki / \"locks\" / f\"{task_id}.json\")\n                head_lock = load_json(target / path)\n                if not (isinstance(base_lock, dict) and base_lock.get(\"state\") == \"active\"):\n                    return False\n                if not (isinstance(head_lock, dict) and head_lock.get(\"state\") == \"released\"):\n                    return False\n                lock_released = True\n            elif path == goal_file:\n                # Condition 5 (goal leg): a goal transition is allowed ONLY when\n                # this task completes its goal, and must be -> complete.\n                if not completes_goal:\n                    return False\n                base_goal = load_json(base_shiki / \"goals\" / f\"{goal_id}.json\")\n                head_goal = load_json(target / path)\n                if not (isinstance(head_goal, dict) and str(head_goal.get(\"status\")) == \"complete\"):\n                    return False\n                if isinstance(base_goal, dict) and str(base_goal.get(\"status\")) == \"complete\":\n                    # Already complete on base: not a real completion transition.\n                    return False\n                goal_completed = True\n            elif path.startswith(\".shiki/reports/\") and path.endswith(\".json\"):\n                # A scorecard is ADDED by goal completion; a modified report is not\n                # part of the terminal set.\n                if entry.status != \"A\":\n                    return False\n                reports_added += 1\n            elif path == worktree_file:\n                # This task's own worktree record is benign id-scoped mirror state\n                # (no governance transition); condition 3 still covers it.\n                pass\n            elif path.startswith(\".shiki/ledger/\") and path.endswith(\".json\"):\n                # Ledgers are append-only mirror bookkeeping (condition 3 and the\n                # untrusted-mutation gate police them); an ADD carries no transition,\n                # but a modify is never part of a closeout's terminal set.\n                if entry.status != \"A\":\n                    return False\n            else:\n                # Any other .shiki path (dag, memories, guardian-policy, another\n                # task/goal/lock, a plan, \u2026) is not part of the terminal set.\n                return False\n\n        # Condition 3: every changed path inside the task's declared locks unioned\n        # with its derived id-scoped mirror locks. HEAD locks equal base locks\n        # (frozen by condition 4), so a PR cannot widen them to sneak a file in.\n        declared_locks = [str(lock) for lock in task.get(\"locks\") or []]\n        effective_locks = list(dict.fromkeys([*declared_locks, *_derive_task_mirror_locks(target, task)]))\n        if files_outside_locks(changed_paths, effective_locks):\n            return False\n\n        # The task's review -> done transition is the closeout; its lock release is\n        # a merged task's residue. Both are mandatory.\n        if not task_changed or not lock_released:\n            return False\n\n        if completes_goal:\n            # A completing closeout MUST carry the goal -> complete transition and\n            # EXACTLY ONE added scorecard report.\n            if not goal_completed or reports_added != 1:\n                return False\n        else:\n            # A non-completing closeout must carry NO goal transition and NO report.\n            if goal_completed or reports_added != 0:\n                return False\n\n        return True\n    except (OSError, ValueError, TypeError):\n        # Unreadable/malformed evidence is fail-closed: NOT a bookkeeping closeout.\n        return False\n\n\ndef _load_guardian_evidence(\n    *,\n    path: str,\n    target: Path,\n    required: bool,\n    blocking: list[str],\n    warnings: list[str],\n    description: str,\n) -> list[dict[str, Any]]:\n    evidence_path = Path(path)\n    if not evidence_path.is_absolute():\n        evidence_path = target / evidence_path\n    if not evidence_path.exists():\n        message = f\"Guardian {description} evidence file is missing at {evidence_path}\"\n        if required:\n            blocking.append(message)\n        else:\n            warnings.append(message)\n        return []\n    try:\n        return load_json_list(evidence_path)\n    except (OSError, json.JSONDecodeError, ValueError) as error:\n        message = f\"Guardian {description} evidence file is invalid: {error}\"\n        if required:\n            blocking.append(message)\n        else:\n            warnings.append(message)\n        return []\n\n\ndef enforce_guardian_policy(\n    *,\n    pr: dict[str, Any],\n    task: dict[str, Any] | None,\n    target: Path,\n    guardian_policy: str,\n    guardian_comments: str,\n    guardian_events: str,\n    guardian_timeline: str,\n    blocking: list[str],\n    warnings: list[str],\n    expected_repository: str = \"\",\n    bookkeeping_closeout: bool = False,\n    contract_approval: Any = None,\n    base_sync_carry: bool = False,\n    default_branch: str = \"\",\n) -> None:\n    risk_labels = _guardian_risk_labels(pr, task, bookkeeping_closeout=bookkeeping_closeout)\n    requires_guardian = _builtin_guardian_risk_required(risk_labels)\n    try:\n        policy = load_guardian_policy_file(_guardian_policy_path(target, guardian_policy))\n    except GuardianPolicyError as error:\n        if requires_guardian:\n            blocking.append(f\"Guardian policy is required for high/critical risk PRs: {error}\")\n        else:\n            warnings.append(f\"Guardian policy could not be loaded; skipping Guardian check for non-high-risk PR: {error}\")\n        return\n    policy_errors = validate_guardian_policy(policy)\n    if policy_errors:\n        if requires_guardian:\n            blocking.extend(f\"Guardian policy validation failed: {error}\" for error in policy_errors)\n        else:\n            warnings.extend(f\"Guardian policy validation warning: {error}\" for error in policy_errors)\n        return\n    requires_guardian = requires_guardian or risk_requires_guardian(risk_labels, policy)\n    if not requires_guardian:\n        return\n\n    # SADR-0015 Contract Approval OR-branch (\"Evaluation placement\"). The Guardian\n    # requirement is satisfied by EITHER a live PR approval (the\n    # ``evaluate_guardian_approval`` path below) OR a proven Contract Approval: a\n    # normal implementation PR whose task contract was registered to the default\n    # branch and Guardian-approved BEFORE dispatch. ``evaluate_guardian_approval``\n    # is never consulted or modified here, so a defect in the contract path\n    # degrades to \"does not apply\" and the live-approval path below runs\n    # byte-for-byte as today. When the carry applies, live-approval evidence is\n    # not required at all \u2014 the approval was recorded on the merged Contract PR.\n    if contract_approval is not None and getattr(contract_approval, \"applies\", False):\n        warnings.append(\n            \"Guardian approval satisfied by \"\n            + \", \".join(contract_approval.sources)\n            + \" (SADR-0015 Contract Approval: the task contract was registered and Guardian-approved before dispatch)\"\n        )\n        return\n\n    blocker_count_before_evidence = len(blocking)\n    comments = _load_guardian_evidence(\n        path=guardian_comments,\n        target=target,\n        required=True,\n        blocking=blocking,\n\n2026-08-17T10:09:39.895989Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -l '_path_proven_absent' /Users/kio.mizutani/CubeShare /Users/kio.mizutani/shiki/.claude/worktrees 2>/dev/null | head -40\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n2026-08-17T10:09:44.097410Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -uu -l '_path_proven_absent' /Users/kio.mizutani/CubeShare /Users/kio.mizutani/shiki/.claude/worktrees 2>/dev/null | head -60\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n2026-08-17T10:09:50.118069Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 2192ms:\n/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/handoffs/T-20260817T063758155247Z-0b11207b-task.md\n/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/inbox/P-REFLUX1-draft.json\n/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/runner/EXEC-20260817T072857479689Z-624cbea9.json\n/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/plans/P-20260817T063752520648Z-230f8707.json\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/.shiki/plans/P-20260817T063752520648Z-230f8707.json\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/mergegate_check.py\n\ncodex\nThe audit found a concrete hardening omission: `_path_proven_absent` was ported but never used, so an \u201cadded\u201d closeout artifact was not independently proven absent from the frozen base. The handoff explicitly requires both path-proof helpers to participate. I\u2019m adding a failing base-symlink fixture first, then I\u2019ll wire the minimal fail-closed proof into the classifier.\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..2ba4a3b5303bec753c451c1a8950f4efacac1e95\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -384,6 +384,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..2ba4a3b5303bec753c451c1a8950f4efacac1e95\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -384,6 +384,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\nexec\n/bin/zsh -lc 'python3 tests/test_bookkeeping_closeout.py BookkeepingCloseoutTest.test_added_ledger_symlink_on_base_disqualifies' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n exited 1 in 0ms:\nF\n======================================================================\nFAIL: test_added_ledger_symlink_on_base_disqualifies (__main__.BookkeepingCloseoutTest.test_added_ledger_symlink_on_base_disqualifies)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\", line 392, in test_added_ledger_symlink_on_base_disqualifies\n    self._assert_disqualified(c)\n    ~~~~~~~~~~~~~~~~~~~~~~~~~^^^\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\", line 354, in _assert_disqualified\n    self.assertFalse(c.classify())\n    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^\nAssertionError: True is not false\n\n----------------------------------------------------------------------\nRan 1 test in 0.005s\n\nFAILED (failures=1)\n\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..2ba4a3b5303bec753c451c1a8950f4efacac1e95\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -384,6 +384,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/mergegate_check.py\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\n2026-08-17T10:10:41.136329Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'python3 tests/test_bookkeeping_closeout.py BookkeepingCloseoutTest.test_added_ledger_symlink_on_base_disqualifies && python3 -m unittest discover -s tests -p test_bookkeeping_closeout.py -v' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\n2026-08-17T10:10:45.268880Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 245ms:\nok\ntest_base_task_status_not_review_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_base_task_status_not_review_disqualifies) ... ok\ntest_classifier_never_reads_the_pr (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_classifier_never_reads_the_pr) ... ok\ntest_coerced_expected_pr_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_coerced_expected_pr_disqualifies) ... ok\ntest_contract_shaped_registration_is_not_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_contract_shaped_registration_is_not_classified) ... ok\ntest_deleted_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_deleted_shiki_path_disqualifies) ... ok\ntest_every_governance_field_change_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_every_governance_field_change_disqualifies) ... ok\ntest_file_outside_effective_locks_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_file_outside_effective_locks_disqualifies) ... ok\ntest_goal_status_not_complete_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_goal_status_not_complete_disqualifies) ... ok\ntest_goal_transition_without_completion_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_goal_transition_without_completion_disqualifies) ... ok\ntest_governance_field_edit_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_governance_field_edit_disqualifies) ... ok\ntest_impl_pr_not_merged_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_impl_pr_not_merged_disqualifies) ... ok\ntest_impl_pr_unresolvable_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_impl_pr_unresolvable_disqualifies) ... ok\ntest_label_or_marker_does_not_classify_non_qualifying_pr (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_label_or_marker_does_not_classify_non_qualifying_pr) ... ok\ntest_lock_not_released_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_lock_not_released_disqualifies) ... ok\ntest_missing_base_snapshot_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_missing_base_snapshot_disqualifies) ... ok\ntest_missing_dag_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_missing_dag_disqualifies) ... ok\ntest_non_shiki_file_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_non_shiki_file_disqualifies) ... ok\ntest_positive_completing_is_classified_and_needs_no_guardian (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_completing_is_classified_and_needs_no_guardian) ... ok\ntest_positive_expected_branch_differs_is_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_expected_branch_differs_is_classified) ... ok\ntest_positive_noncompleting_is_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_noncompleting_is_classified) ... ok\ntest_renamed_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_renamed_shiki_path_disqualifies) ... ok\ntest_report_without_completion_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_report_without_completion_disqualifies) ... ok\ntest_risk_level_downgrade_is_not_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_risk_level_downgrade_is_not_classified) ... ok\ntest_second_scorecard_report_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_second_scorecard_report_disqualifies) ... ok\ntest_strongest_goal_or_task_risk_matches_completion_report (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_strongest_goal_or_task_risk_matches_completion_report) ... ok\ntest_symlinked_task_file_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_symlinked_task_file_disqualifies) ... ok\ntest_task_head_status_not_done_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_task_head_status_not_done_disqualifies) ... ok\ntest_type_changed_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_type_changed_shiki_path_disqualifies) ... ok\ntest_unreadable_base_task_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_unreadable_base_task_disqualifies) ... ok\ntest_worktree_record_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_worktree_record_disqualifies) ... ok\n\n----------------------------------------------------------------------\nRan 32 tests in 0.144s\n\nOK\n\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests -p test_guardian_signal_closeout.py -v' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\n2026-08-17T10:10:48.800418Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 122ms:\nFAIL\ntest_unmerged_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unmerged_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_task_id_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_task_id_requires_guardian) ... ok\n\n======================================================================\nFAIL: test_absent_cross_repository_field_denies_exemption_and_uses_real_risk (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_absent_cross_repository_field_denies_exemption_and_uses_real_risk)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 390, in test_absent_cross_repository_field_denies_exemption_and_uses_real_risk\n    self.assertTrue(s.classify(), \"the SADR-0017 shape itself should be proven\")\n    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\nAssertionError: False is not true : the SADR-0017 shape itself should be proven\n\n======================================================================\nFAIL: test_cross_repository_pr_denies_exemption_and_uses_real_risk (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_cross_repository_pr_denies_exemption_and_uses_real_risk)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 371, in test_cross_repository_pr_denies_exemption_and_uses_real_risk\n    self.assertTrue(s.classify(), \"the SADR-0017 shape itself should be proven\")\n    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\nAssertionError: False is not true : the SADR-0017 shape itself should be proven\n\n======================================================================\nFAIL: test_maintainer_label_still_forces_gate_on_exempt_pr (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_maintainer_label_still_forces_gate_on_exempt_pr)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 466, in test_maintainer_label_still_forces_gate_on_exempt_pr\n    self.assertTrue(signal[\"bookkeeping_closeout_exemption\"])\n    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\nAssertionError: False is not true\n\n======================================================================\nFAIL: test_proven_completing_critical_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_completing_critical_closeout_not_required)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 312, in test_proven_completing_critical_closeout_not_required\n    self.assertFalse(signal[\"required\"])\n    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^\nAssertionError: True is not false\n\n======================================================================\nFAIL: test_proven_noncompleting_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_noncompleting_closeout_not_required)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 318, in test_proven_noncompleting_closeout_not_required\n    self.assertFalse(signal[\"required\"])\n    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^\nAssertionError: True is not false\n\n======================================================================\nFAIL: test_signal_records_exemption_applied_flag (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_signal_records_exemption_applied_flag)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\", line 449, in test_signal_records_exemption_applied_flag\n    self.assertTrue(proven[\"bookkeeping_closeout_exemption\"])\n    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\nAssertionError: False is not true\n\n----------------------------------------------------------------------\nRan 19 tests in 0.099s\n\nFAILED (failures=6)\n\n2026-08-17T10:10:53.241295Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py:\n        _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n               {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                \"ledger_evidence\": []})\n\n        head_ledgers = (\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\n2026-08-17T10:10:53.334369Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -n -C 8 'self\\\\.base / \\\"goals\\\"|head_ledgers' tests/test_guardian_signal_closeout.py | head -80\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n122-\n123-    def build(self, *, completes: bool = True) -> \"Scenario\":\n124-        # Base snapshot (origin/main): the merged impl PR left the task at review,\n125-        # naming the impl PR, its lock active and its goal in-progress.\n126-        _write(self.base / \"tasks\" / f\"{TASK_ID}.json\",\n127-               self._task(status=\"review\", expected_pr=IMPL_PR, ledger_evidence=[\"L-base00002\"]))\n128-        _write(self.base / \"locks\" / f\"{TASK_ID}.json\",\n129-               {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"active\", \"owner\": \"shiki-run\"})\n130:        _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n131-               {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n132-                \"ledger_evidence\": []})\n133-\n134-        # HEAD (closeout branch cut from origin/main): terminal state.\n135:        head_ledgers = (\n136-            [\"L-base00002\", COMPLETION_LEDGER, PULL_LEDGER]\n137-            if completes\n138-            else [\"L-base00002\", PULL_LEDGER]\n139-        )\n140-        self.head_task = self._task(status=\"done\", expected_pr=CLOSEOUT_PR,\n141:                                    ledger_evidence=head_ledgers)\n142-        self.rewrite_head_task()\n143-        _write(self.target / \".shiki\" / \"locks\" / f\"{TASK_ID}.json\",\n144-               {\"task_id\": TASK_ID, \"goal_id\": GOAL_ID, \"state\": \"released\", \"owner\": \"shiki-run\"})\n145-        _write(self.target / \".shiki\" / \"ledger\" / f\"{COMPLETION_LEDGER}.json\",\n146-               {\"id\": COMPLETION_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": None,\n147-                \"type\": \"completion\", \"summary\": \"goal complete\", \"evidence\": [REPORT_REL]})\n148-        _write(self.target / \".shiki\" / \"ledger\" / f\"{PULL_LEDGER}.json\",\n149-               {\"id\": PULL_LEDGER, \"goal_id\": GOAL_ID, \"task_id\": TASK_ID, \"type\": \"lock\",\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\n\n2026-08-17T10:11:01.147719Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_guardian_signal_closeout.py\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:11:06.361205Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests -p test_guardian_signal_closeout.py -v' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:11:10.109802Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 143ms:\nok\ntest_proven_completing_critical_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_completing_critical_closeout_not_required) ... ok\ntest_proven_noncompleting_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_noncompleting_closeout_not_required) ... ok\ntest_second_scorecard_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_second_scorecard_requires_guardian) ... ok\ntest_signal_records_exemption_applied_flag (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_signal_records_exemption_applied_flag) ... ok\ntest_unmerged_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unmerged_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_task_id_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_task_id_requires_guardian) ... ok\n\n----------------------------------------------------------------------\nRan 19 tests in 0.121s\n\nOK\n\nexec\n/bin/zsh -lc \"sed -n '3150,3555p' scripts/mergegate_check.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n    migration_mode = False\n    # PR numbers proven merged (live PR state gathered by the workflow). Used by\n    # post_merge_reconcile to prove its referenced PR merged, and by the bookkeeping\n    # closeout classifier to prove the task's implementation PR merged (SADR-0017).\n    merged_prs = {\n        int(token.strip())\n        for token in str(args.merged_prs or \"\").split(\",\")\n        if token.strip().isdigit()\n    }\n    # Whether the (normal single-task) PR is a proven bookkeeping closeout, so the\n    # Guardian gate evaluates it as if the task were low risk (SADR-0017). Stays\n    # False for contract / goal_reconcile / post_merge_reconcile PRs.\n    bookkeeping_closeout = False\n    manifest: dict[str, Any] | None = None\n    try:\n        manifest = load_manifest(target)\n    except ManifestError as error:\n        blocking.append(str(error))\n\n    pr = load_json(Path(args.pr_json))\n    body = \"\"\n    if pr:\n        body = str(pr.get(\"body\") or \"\")\n        resolved_task_id = first_match(TASK_ID, body)\n        resolved_goal_id = first_match(GOAL_ID, body)\n        reconcile_mode, reconcile_error = goal_reconcile_decision(pr)\n        post_merge_mode, post_merge_error = post_merge_reconcile_decision(pr)\n        contract_mode, contract_error = contract_decision(pr)\n        amendment_mode, amendment_error = amendment_decision(pr)\n        migration_mode, migration_error = migration_decision(pr)\n        if reconcile_error:\n            blocking.append(reconcile_error)\n        if post_merge_error:\n            blocking.append(post_merge_error)\n        if contract_error:\n            blocking.append(contract_error)\n        if amendment_error:\n            blocking.append(amendment_error)\n        if migration_error:\n            blocking.append(migration_error)\n        if reconcile_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both goal_reconcile and post_merge_reconcile\")\n        if contract_mode and reconcile_mode:\n            blocking.append(\"a PR cannot be both contract and goal_reconcile\")\n        if contract_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both contract and post_merge_reconcile\")\n        if amendment_mode and reconcile_mode:\n            blocking.append(\"a PR cannot be both amendment and goal_reconcile\")\n        if amendment_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both amendment and post_merge_reconcile\")\n        if amendment_mode and contract_mode:\n            blocking.append(\"a PR cannot be both amendment and contract\")\n        if migration_mode and reconcile_mode:\n            blocking.append(\"a PR cannot be both migration and goal_reconcile\")\n        if migration_mode and post_merge_mode:\n            blocking.append(\"a PR cannot be both migration and post_merge_reconcile\")\n        if migration_mode and contract_mode:\n            blocking.append(\"a PR cannot be both migration and contract\")\n        if migration_mode and amendment_mode:\n            blocking.append(\"a PR cannot be both migration and amendment\")\n        if args.expected_head_sha:\n            pr_head = str(pr.get(\"headRefOid\") or \"\")\n            if not pr_head:\n                blocking.append(\"PR headRefOid is missing\")\n            elif pr_head != args.expected_head_sha:\n                blocking.append(f\"PR headRefOid {pr_head} does not match expected checked-out HEAD {args.expected_head_sha}\")\n        if reconcile_mode:\n            # A goal_reconcile PR is goal-scoped (frozen-plan task registration);\n            # it does not carry a single implementation task id.\n            if not resolved_goal_id:\n                blocking.append(\"goal_reconcile PR body does not contain a Shiki goal id like G-0001\")\n        elif contract_mode:\n            # A Contract PR is goal-scoped (frozen-plan task-contract registration);\n            # it carries no single implementation task id (SADR-0015).\n            if not resolved_goal_id:\n                blocking.append(\"contract PR body does not contain a Shiki goal id like G-0001\")\n        elif amendment_mode:\n            # An Amendment PR is goal-scoped (amends the goal's spec-frozen source\n            # plan and re-opens this goal's tasks); it carries no single\n            # implementation task id.\n            if not resolved_goal_id:\n                blocking.append(\"amendment PR body does not contain a Shiki goal id like G-0001\")\n        elif post_merge_mode:\n            # A post_merge_reconcile PR is task-scoped (reconcile a merged task).\n            if not resolved_task_id:\n                blocking.append(\"post_merge_reconcile PR body does not contain a Shiki task id like T-0001\")\n        elif migration_mode:\n            # A Migration PR is repository-wide (a mirror-scoped re-run proof); it\n            # references NEITHER a task nor a goal id \u2014 it declares a `Migration: <id>`\n            # line, validated in enforce_migration.\n            pass\n        else:\n            if not resolved_task_id:\n                blocking.append(\"PR body does not contain a Shiki task id like T-0001\")\n            if not resolved_goal_id:\n                blocking.append(\"PR body does not contain a Shiki goal id like G-0001\")\n        for heading in [\"Scope\", \"Acceptance\", \"Evidence\", \"MergeGate\"]:\n            if heading.lower() not in body.lower() and not has_heading(body, heading):\n                blocking.append(f\"PR body is missing {heading} section\")\n    else:\n        warnings.append(f\"PR JSON not found at {args.pr_json}; skipping PR metadata checks\")\n    enforce_required_check_definitions(target, blocking)\n\n    task: dict[str, Any] | None = None\n    ledger_entries: list[dict[str, Any]] = []\n    if reconcile_mode and pr:\n        # goal_reconcile is a goal-scoped, frozen-plan registration PR; it uses a\n        # dedicated deny-by-default validator instead of the single-task readiness\n        # flow (no implementation task, CCA, locks, or guardian gate).\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_goal_reconcile(\n            target=target,\n            goal_id=resolved_goal_id or \"\",\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n        )\n    elif contract_mode and pr:\n        # Contract mode (SADR-0015): a goal-scoped, spec-frozen task-contract\n        # registration PR. It uses the generalized deny-by-default validator and\n        # forces the Guardian gate from the Goal's frozen-plan risk BEFORE any\n        # implementation is dispatched. The Guardian gate here is INDEPENDENT of a\n        # CCA verdict: a Contract PR carries no implementation for CCA to judge, so\n        # requiring a verdict to reach the gate (as the single-task flow does)\n        # would let a high-risk contract merge unapproved when CCA is absent.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_contract(\n            target=target,\n            goal_id=resolved_goal_id or \"\",\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n            # The contract flow is the one path that carries a Goal's frozen plan\n            # to the default branch (SADR-0015 plan-reachability gap); enable it.\n            allow_source_plan_carry=True,\n        )\n        if resolved_goal_id:\n            contract_risk = contract_guardian_risk(target, resolved_goal_id)\n            if contract_risk not in _RISK_ORDER:\n                # Fail closed: if the Guardian risk cannot be resolved from the\n                # goal's frozen plan, the gate cannot be evaluated, so the contract\n                # must not slip through as if it were unguarded. (enforce_contract\n                # already blocks a risk-less goal registration; this guards the\n                # guardian input independently in case the goal is on base.)\n                blocking.append(\n                    f\"contract PR Guardian risk for goal {resolved_goal_id} could not be resolved \"\n                    f\"from its frozen plan (got {contract_risk!r}); the Guardian gate cannot be evaluated\"\n                )\n            else:\n                enforce_guardian_policy(\n                    pr=pr,\n                    task={\"risk_level\": contract_risk},\n                    target=target,\n                    guardian_policy=args.guardian_policy,\n                    guardian_comments=args.guardian_comments,\n                    guardian_events=args.guardian_events,\n                    guardian_timeline=args.guardian_timeline,\n                    blocking=blocking,\n                    warnings=warnings,\n                    expected_repository=args.expected_repository,\n                    base_sync_carry=args.base_sync_carry,\n                    default_branch=args.default_branch,\n                )\n    elif amendment_mode and pr:\n        # Amendment mode (SADR-0009/SADR-0015): a goal-scoped PR that corrects a\n        # spec-frozen Goal's contract on a protected branch \u2014 an append to the\n        # source plan's spec_freeze.amendments plus the task files bound to the\n        # amended plan. Deny-by-default validator; Guardian gate forced from the\n        # goal's frozen plan and INDEPENDENT of a CCA verdict (an amendment carries\n        # no implementation for CCA to judge). Like contract mode, the Guardian\n        # requirement runs here rather than in the single-task CCA flow.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        base_shiki_amend = Path(args.base_shiki) if args.base_shiki else None\n        enforce_amendment(\n            target=target,\n            goal_id=resolved_goal_id or \"\",\n            base_shiki=base_shiki_amend,\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n        )\n        if resolved_goal_id:\n            # Reuse contract_guardian_risk on BOTH the head and the base snapshot\n            # and take the stronger, so an amendment that WEAKENS its own plan's\n            # risk_level can never lower its own Guardian gate below what the\n            # pre-amendment (base) contract required. No second resolution \u2014 the\n            # existing resolver runs against each root and _max_risk combines them.\n            head_risk = contract_guardian_risk(target, resolved_goal_id) or \"\"\n            base_risk = \"\"\n            if base_shiki_amend is not None and base_shiki_amend.exists():\n                base_risk = contract_guardian_risk(base_shiki_amend.parent, resolved_goal_id) or \"\"\n            amendment_risk = _max_risk(head_risk, base_risk) or None\n            if amendment_risk not in _RISK_ORDER:\n                # Fail closed: an unresolvable amendment risk means the Guardian gate\n                # cannot be evaluated, so the amendment must not slip through as if\n                # it were unguarded.\n                blocking.append(\n                    f\"amendment PR Guardian risk for goal {resolved_goal_id} could not be resolved \"\n                    f\"from its frozen plan (got {amendment_risk!r}); the Guardian gate cannot be evaluated\"\n                )\n            else:\n                enforce_guardian_policy(\n                    pr=pr,\n                    task={\"risk_level\": amendment_risk},\n                    target=target,\n                    guardian_policy=args.guardian_policy,\n                    guardian_comments=args.guardian_comments,\n                    guardian_events=args.guardian_events,\n                    guardian_timeline=args.guardian_timeline,\n                    blocking=blocking,\n                    warnings=warnings,\n                    expected_repository=args.expected_repository,\n                )\n    elif migration_mode and pr:\n        # Migration mode: a repository-wide mirror change whose sole proof\n        # obligation is that the .shiki diff EQUALS the re-run output of a migration\n        # registered on the BASE branch (see enforce_migration). It carries no\n        # implementation task, CCA verdict, or Guardian gate \u2014 the maintainer label\n        # is the authorization and the base-registered re-run is the proof.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_migration(\n            target=target,\n            pr=pr,\n            base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n        )\n    elif post_merge_mode and pr:\n        # post_merge_reconcile is a task-scoped reconcile of a merged task's\n        # residual lock / status; a dedicated deny-by-default validator.\n        files_status = parse_changed_files_status(\n            Path(args.changed_files_status), changed_files(Path(args.changed_files))\n        )\n        enforce_post_merge_reconcile(\n            target=target,\n            task_id=resolved_task_id or \"\",\n            base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n            changed_files_status=files_status,\n            blocking=blocking,\n            warnings=warnings,\n            merged_pr_numbers=merged_prs,\n            pr_body=body,\n        )\n    elif resolved_task_id:\n        task = load_task(target, resolved_task_id)\n        if task is None:\n            blocking.append(f\"Task file not found for {resolved_task_id}\")\n        else:\n            task_goal_id = str(task.get(\"goal_id\") or \"\")\n            if resolved_goal_id and task_goal_id != resolved_goal_id:\n                blocking.append(f\"PR goal id {resolved_goal_id} does not match task goal_id {task_goal_id}\")\n            if not load_goal(target, task_goal_id):\n                blocking.append(f\"Goal file not found for {task_goal_id}\")\n\n            if pr:\n                pr_number = pr.get(\"number\")\n                expected_pr = task.get(\"expected_pr\")\n                if expected_pr is not None and pr_number is not None and expected_pr != pr_number:\n                    blocking.append(f\"Task expected_pr {expected_pr} does not match PR #{pr_number}\")\n                elif expected_pr is None:\n                    warnings.append(f\"Task {resolved_task_id} has no expected_pr recorded\")\n\n                head_ref = pr.get(\"headRefName\")\n                expected_branch = task.get(\"expected_branch\")\n                if expected_branch and head_ref and expected_branch != head_ref:\n                    blocking.append(f\"Task expected_branch {expected_branch!r} does not match PR head {head_ref!r}\")\n\n            for dependency_id in task.get(\"dependencies\") or []:\n                dependency = load_task(target, str(dependency_id))\n                if dependency is None:\n                    blocking.append(f\"Task dependency {dependency_id} has no task file\")\n                elif dependency.get(\"status\") != \"done\":\n                    blocking.append(f\"Task dependency {dependency_id} is not done: {dependency.get('status')!r}\")\n\n            files = changed_files(Path(args.changed_files))\n            files_status = parse_changed_files_status(Path(args.changed_files_status), files)\n            locks = [str(lock) for lock in task.get(\"locks\") or []]\n            if files and not locks:\n                blocking.append(f\"Task {resolved_task_id} has no locks but PR changes files\")\n            # The task's declared (product) locks stay exactly what the plan froze;\n            # the .shiki evidence the loop syncs onto the branch is covered by the\n            # id-scoped mirror set derived from the task's own ids/ledgers at read\n            # time. Union the two for the files-outside-locks gate only \u2014 lock\n            # CONFLICT detection stays on the declared locks, since shared mirror\n            # evidence is not a contended product resource.\n            effective_locks = list(dict.fromkeys([*locks, *_derive_task_mirror_locks(target, task)]))\n            # Judge the lock-scope gates against the WHOLE diff, both legs of every\n            # rename included. Using the name-only `files` list would let a rename\n            # move a file out of locked scope unseen (git shows only the new leg).\n            scope_paths = lock_scope_paths(files_status)\n            for path in files_outside_locks(scope_paths, effective_locks):\n                blocking.append(f\"Changed file {path} is outside declared task locks\")\n            blocking.extend(active_lock_conflicts(target, resolved_task_id, locks, scope_paths))\n\n            # SADR-0017: a proven bookkeeping closeout (task terminal state only, no\n            # implementation) is evaluated as if low risk at the Guardian gate\n            # below. Computed here (where the head task, base snapshot, diff and\n            # merged-PR proof are all in hand); consulted only by\n            # enforce_guardian_policy. Read-only \u2014 it never adds a blocking reason.\n            bookkeeping_closeout = is_bookkeeping_closeout(\n                target=target,\n                task=task,\n                task_id=resolved_task_id,\n                goal_id=task_goal_id,\n                base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                changed_files_status=files_status,\n                merged_pr_numbers=merged_prs,\n            )\n            # The exemption is same-repository only and fail-closed: the live PR\n            # snapshot must explicitly prove isCrossRepository=false.\n            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\n                bookkeeping_closeout = False\n\n            ledger_entries = load_ledger_entries(target, task, warnings, blocking)\n            if pr:\n                enforce_untrusted_shiki_mutations(\n                    target=target,\n                    manifest=manifest,\n                    base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                    changed_files_status=files_status,\n                    task=task,\n                    goal_id=task_goal_id,\n                    task_id=resolved_task_id,\n                    pr=pr,\n                    blocking=blocking,\n                    warnings=warnings,\n                )\n                # SADR-0015 Contract immutability on the NORMAL task-PR path: a PR\n                # that changes its own task file may move only bookkeeping \u2014 its\n                # frozen governance contract is bound to the base snapshot, and its\n                # risk_level may never resolve weaker than base. Only this branch\n                # calls it (contract / goal_reconcile / post_merge_reconcile use\n                # their own validators), so nothing is double-reported.\n                blocking.extend(\n                    normal_task_contract_immutability_reasons(\n                        base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n                        task_id=resolved_task_id,\n                        head_task=task,\n                        changed_files_status=files_status,\n                    )\n                )\n            ledger_text = \"\\n\".join(ledger_entry_text(entry) for entry in ledger_entries)\n            for skill in task.get(\"required_skills\") or []:\n                skill_name = str(skill).strip().lower()\n                if skill_name and skill_name != \"none\" and skill_name not in ledger_text:\n                    blocking.append(f\"Required skill {skill} has no matching ledger evidence\")\n\n    cca = load_json(Path(args.cca_verdict))\n    if cca:\n        for error in validate_cca_contract(target, cca):\n            blocking.append(f\"CCA verdict schema violation: {error}\")\n        if pr and args.cca_evidence_manifest:\n            manifest_path = Path(args.cca_evidence_manifest)\n            if not manifest_path.is_absolute():\n                manifest_path = target / manifest_path\n            evidence_dir = manifest_path.parent if manifest_path.parent != Path(\"\") else Path(\".shiki/gha\")\n            for error in validate_cca_evidence_contract(\n                target=target,\n                manifest_path=manifest_path,\n                evidence_dir=evidence_dir,\n                pr=pr,\n                task_id=resolved_task_id,\n                goal_id=resolved_goal_id,\n                expected_repository=args.expected_repository,\n            ):\n                blocking.append(error)\n        elif pr and args.expected_repository:\n            blocking.append(\"CCA evidence manifest path is required when expected repository is provided\")\n        if cca.get(\"verdict\") != \"complete\":\n            blocking.append(f\"CCA verdict is not complete: {cca.get('verdict')!r}\")\n        if resolved_task_id and cca.get(\"task_id\") != resolved_task_id:\n            blocking.append(f\"CCA task_id {cca.get('task_id')!r} does not match PR task id {resolved_task_id}\")\n        if resolved_goal_id and cca.get(\"goal_id\") != resolved_goal_id:\n            blocking.append(f\"CCA goal_id {cca.get('goal_id')!r} does not match PR goal id {resolved_goal_id}\")\n        if task and cca.get(\"goal_id\") != task.get(\"goal_id\"):\n            blocking.append(f\"CCA goal_id {cca.get('goal_id')!r} does not match task goal_id {task.get('goal_id')!r}\")\n        if pr:\n            if cca.get(\"pr\") != pr.get(\"number\"):\n                blocking.append(f\"CCA pr {cca.get('pr')!r} does not match PR #{pr.get('number')}\")\n            if not cca.get(\"head_sha\"):\n                blocking.append(\"CCA head_sha is missing\")\n            elif pr.get(\"headRefOid\") and cca.get(\"head_sha\") != pr.get(\"headRefOid\"):\n                blocking.append(\"CCA head_sha does not match the current PR headRefOid\")\n        if cca.get(\"can_merge\") is not True:\n            blocking.append(\"CCA verdict did not set can_merge=true\")\n        failures = blocking_checklist_failures(cca)\n        if failures:\n            blocking.append(\"CCA verdict contains blocking failed checklist items: \" + \", \".join(failures))\n        if cca.get(\"verdict\") == \"complete\":\n            acceptance_failures = failing_acceptance_criteria(cca)\n            if acceptance_failures:\n                blocking.append(\n                    \"complete CCA verdict contains failing acceptance criteria: \" + \", \".join(acceptance_failures)\n                )\n        for reason in checklist_profile_coverage_failures(task, cca):\n            blocking.append(reason)\n        acceptance = cca.get(\"acceptance\")\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..5d3b8e17cb663cc84cc7d1a3e025900880ec9013\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -142,6 +142,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -384,6 +386,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:11:21.001840Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..d657e90711a28bccaf66a4b62dc4403e832b4f73\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,17 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +477,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..d657e90711a28bccaf66a4b62dc4403e832b4f73\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,17 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +477,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:05.269644Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'python3 tests/test_bookkeeping_closeout.py BookkeepingCloseoutTest.test_mergegate_fork_and_absent_field_deny_exemption' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n.\n----------------------------------------------------------------------\nRan 1 test in 0.010s\n\nOK\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..d657e90711a28bccaf66a4b62dc4403e832b4f73\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,17 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +477,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:09.367989Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/tests/test_bookkeeping_closeout.py\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:18.041668Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'python3 tests/test_bookkeeping_closeout.py BookkeepingCloseoutTest.test_mergegate_fork_and_absent_field_deny_exemption' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n.\n----------------------------------------------------------------------\nRan 1 test in 0.031s\n\nOK\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:22.478600Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nThe RED\u2192GREEN slice is complete: the new base-symlink proof fails before the classifier change and passes after it. I also added a real `mergegate_check.main()` fixture showing forked and field-absent snapshots re-enable the high-risk Guardian gate, with a same-repository control proving the blocker is specifically caused by the new guard. I\u2019m moving to full repository verification now.\nThe pre-existing patch was audited; one missing base-absence proof was fixed via a failing test, and both fork enforcement points now have executable coverage.\n  \u2713 Audit pre-existing patch, task contract, source decisions, and locked-file boundaries\n  \u2713 Run focused tests to establish RED failures, then implement each vertical behavior slice within locks\n  \u2192 Run targeted and full acceptance suites, inspect immutable/fork/closeout invariants\n  \u2022 Perform the required code-review gate manually and report durable evidence/blockers\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:28.418518Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:32.620796Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:12:45.468493Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:13:05.472399Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:13:19.109021Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:13:39.112634Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:13:51.492985Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:02.586012Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 91065ms:\n...........................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................\n----------------------------------------------------------------------\nRan 1170 tests in 90.972s\n\nOK\nGuardian approval status for PR #130 (mizutani-140/shiki)\nHead SHA: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\nResult: NOT APPROVED\n\nSatisfied approval sources:\n  - guardian_label (by mizutani-140)\n\nMissing / what the gate is waiting for:\n  - Guardian approval requires a configured Guardian review or current-head Guardian comment\n\nHead SHA stability:\n  - clean: safe to request approval against this head SHA\n\nPaste-ready Guardian approval comment (a configured Guardian pastes this verbatim):\n----------8<----------\nGuardian approval granted\n\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\n---------->8----------\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpc3y1heim/repo/.shiki/handoffs/RP-20260817T101324693953Z-9a453926-repair.md\",\n  \"ledger_id\": \"L-20260817T101324707465Z-eb310587\",\n  \"repair_id\": \"RP-20260817T101324693953Z-9a453926\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp1f2ryjvg/repo/.shiki/handoffs/RP-20260817T101326613468Z-624e5841-repair.md\",\n  \"ledger_id\": \"L-20260817T101326626939Z-aad6a123\",\n  \"repair_id\": \"RP-20260817T101326613468Z-624e5841\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpdigm_8e2/repo/.shiki/handoffs/RP-20260817T101328008841Z-3fd40073-repair.md\",\n  \"ledger_id\": \"L-20260817T101328023180Z-94ac2e98\",\n  \"repair_id\": \"RP-20260817T101328008841Z-3fd40073\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpr1b3vhsg/repo/.shiki/handoffs/RP-20260817T101340248721Z-b161a3b6-repair.md\",\n  \"ledger_id\": \"L-20260817T101340262504Z-447362d0\",\n  \"repair_id\": \"RP-20260817T101340248721Z-b161a3b6\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp_s4yf24n/repo/.shiki/handoffs/RP-20260817T101341423861Z-8504f2d0-repair.md\",\n  \"ledger_id\": \"L-20260817T101341436551Z-852564cf\",\n  \"repair_id\": \"RP-20260817T101341423861Z-8504f2d0\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp2xqc0t4s/repo/.shiki/handoffs/RP-20260817T101341960782Z-07594c4d-repair.md\",\n  \"ledger_id\": \"L-20260817T101341975430Z-5d3899dc\",\n  \"repair_id\": \"RP-20260817T101341960782Z-07594c4d\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpq45xr_o_/repo/.shiki/handoffs/RP-20260817T101342562837Z-dd144762-repair.md\",\n  \"ledger_id\": \"L-20260817T101342577735Z-c408a286\",\n  \"repair_id\": \"RP-20260817T101342562837Z-dd144762\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpnzatkwip/repo/.shiki/handoffs/RP-20260817T101343175167Z-392f7080-repair.md\",\n  \"ledger_id\": \"L-20260817T101343188047Z-6b591d3e\",\n  \"repair_id\": \"RP-20260817T101343175167Z-392f7080\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpz8bpjj0u/repo/.shiki/handoffs/RP-20260817T101343540212Z-9801cad9-repair.md\",\n  \"ledger_id\": \"L-20260817T101343553624Z-508de8db\",\n  \"repair_id\": \"RP-20260817T101343540212Z-9801cad9\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpm7b513us/repo/.shiki/handoffs/RP-20260817T101343985534Z-bbd9ca9f-repair.md\",\n  \"ledger_id\": \"L-20260817T101343999527Z-d0ade2a9\",\n  \"repair_id\": \"RP-20260817T101343985534Z-bbd9ca9f\"\n}\n[shiki] token verified: ok\n[shiki] token verified: ok\n{\n  \"repo\": \"owner/name\",\n  \"secret\": \"CLAUDE_CODE_OAUTH_TOKEN\",\n  \"source\": \"env:SHIKI_TEST_TOK\",\n  \"verified\": true\n}\n\ncodex\nThe full Python suite passes: 1,170 tests, including the new fork/absent CLI fixture and symlink-base proof. Next I\u2019m running repository validation and then every `scripts/test_shiki_*.sh`; after that I\u2019ll do the final diff/lock and review audit.\nexec\n/bin/zsh -lc 'python3 scripts/validate_shiki.py --target .' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:07.370267Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 291ms:\nShiki validation passed\n\nexec\n/bin/zsh -lc 'for s in scripts/test_shiki_*.sh; do bash \"$s\" || exit 1; done' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:11.878547Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 3083ms:\nTraceback (most recent call last):\n  File \"<stdin>\", line 973, in <module>\n  File \"<stdin>\", line 967, in expect_exempted\nAssertionError: bookkeeping exemption did not fire; Guardian block present:\n{\n  \"blocking_reasons\": [\n    \"PR changes reports record .shiki/reports/R-20260731T010000000002Z-c105e5cd.json not scoped to task T-20260731T004111132162Z-c105e001 or goal G-20260731T004111130646Z-c105e0a1; state_class=mirror\",\n    \"Guardian policy is required for high/critical risk PRs: .shiki/guardian-policy.json: Guardian policy file is missing\"\n  ],\n  \"goal_id\": \"G-20260731T004111130646Z-c105e0a1\",\n  \"mergegate\": \"blocked\",\n  \"task_id\": \"T-20260731T004111132162Z-c105e001\",\n  \"warnings\": []\n}\n\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:14.596334Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nThe shell acceptance suite exposed the expected fixture drift from the stricter base-absence proof: a bookkeeping-closeout shell fixture did not create the frozen base\u2019s empty ledger/report directories, so the new fail-closed classifier correctly refused the exemption. This is a test-fixture repair within the declared shell-test locks, not a production relaxation; I\u2019m updating that fixture and rerunning the specific script before the whole shell suite.\nexec\n/bin/zsh -lc \"rg -n -C 8 'expect_exempted|R-20260731T010000000002Z-c105e5cd|c105e001' scripts/test_shiki_*.sh\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\nscripts/test_shiki_governance_evidence.sh-751-# Each smuggling case below is a CRITICAL-risk closeout with ONE defect that must\nscripts/test_shiki_governance_evidence.sh-752-# disqualify it, so the task re-inherits its risk and \u2014 with NO approval evidence\nscripts/test_shiki_governance_evidence.sh-753-# present \u2014 the Guardian approval requirement fires. We assert the SPECIFIC\nscripts/test_shiki_governance_evidence.sh-754-# Guardian approval fallback, never merely that the PR blocks, so an unrelated\nscripts/test_shiki_governance_evidence.sh-755-# future block cannot mask a classifier regression. The clean control proves the\nscripts/test_shiki_governance_evidence.sh-756-# classifier CAN exempt (the same critical PR, with no approval, does NOT block on\nscripts/test_shiki_governance_evidence.sh-757-# Guardian).\nscripts/test_shiki_governance_evidence.sh-758-# ============================================================================\nscripts/test_shiki_governance_evidence.sh:759:CLOSE_TASK = \"T-20260731T004111132162Z-c105e001\"\nscripts/test_shiki_governance_evidence.sh-760-CLOSE_GOAL = \"G-20260731T004111130646Z-c105e0a1\"\nscripts/test_shiki_governance_evidence.sh-761-CLOSE_IMPL_PR = 940\nscripts/test_shiki_governance_evidence.sh-762-CLOSE_PR = 941\nscripts/test_shiki_governance_evidence.sh-763-CLOSE_HEAD = \"c\" * 40\nscripts/test_shiki_governance_evidence.sh:764:CLOSE_BRANCH = \"shiki/t-c105e001-closeout\"\nscripts/test_shiki_governance_evidence.sh-765-CLOSE_COMP = \"L-20260731T010000000000Z-c105ec3d\"\nscripts/test_shiki_governance_evidence.sh-766-CLOSE_PULL = \"L-20260731T010000000001Z-c105e91d\"\nscripts/test_shiki_governance_evidence.sh:767:CLOSE_REPORT_ID = \"R-20260731T010000000002Z-c105e5cd\"\nscripts/test_shiki_governance_evidence.sh-768-CLOSE_REPORT_REL = f\".shiki/reports/{CLOSE_REPORT_ID}.json\"\nscripts/test_shiki_governance_evidence.sh-769-CLOSE_URL = f\"https://github.com/mizutani-140/shiki/pull/{CLOSE_PR}\"\nscripts/test_shiki_governance_evidence.sh-770-GUARDIAN_APPROVAL_FALLBACK = \"Guardian approval requires a configured Guardian review or current-head Guardian comment\"\nscripts/test_shiki_governance_evidence.sh-771-\nscripts/test_shiki_governance_evidence.sh-772-CLOSE_GOVERNANCE = {\nscripts/test_shiki_governance_evidence.sh-773-    \"title\": \"Closeout smuggling fixture\",\nscripts/test_shiki_governance_evidence.sh-774-    \"scope\": \"Push the merged task's terminal state to main.\",\nscripts/test_shiki_governance_evidence.sh-775-    \"non_goals\": [\"No code change.\"],\n--\nscripts/test_shiki_governance_evidence.sh-915-\nscripts/test_shiki_governance_evidence.sh-916-def expect_guardian_fallback(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> None:\nscripts/test_shiki_governance_evidence.sh-917-    result = close_run(target, merged=merged)\nscripts/test_shiki_governance_evidence.sh-918-    if result.returncode == 0:\nscripts/test_shiki_governance_evidence.sh-919-        raise AssertionError(f\"expected the Guardian approval fallback to block, got success:\\n{result.stdout}\")\nscripts/test_shiki_governance_evidence.sh-920-    assert_contains(result.stdout, GUARDIAN_APPROVAL_FALLBACK)\nscripts/test_shiki_governance_evidence.sh-921-\nscripts/test_shiki_governance_evidence.sh-922-\nscripts/test_shiki_governance_evidence.sh:923:def expect_exempted(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> None:\nscripts/test_shiki_governance_evidence.sh-924-    result = close_run(target, merged=merged)\nscripts/test_shiki_governance_evidence.sh-925-    if GUARDIAN_APPROVAL_FALLBACK in result.stdout:\nscripts/test_shiki_governance_evidence.sh-926-        raise AssertionError(f\"bookkeeping exemption did not fire; Guardian block present:\\n{result.stdout}\")\nscripts/test_shiki_governance_evidence.sh-927-\nscripts/test_shiki_governance_evidence.sh-928-\nscripts/test_shiki_governance_evidence.sh-929-# Clean control: a proven completing closeout of a CRITICAL task with NO Guardian\nscripts/test_shiki_governance_evidence.sh-930-# approval evidence is exempted, so the approval requirement does NOT fire.\nscripts/test_shiki_governance_evidence.sh:931:expect_exempted(build_closeout(\"close-clean-completing\"))\nscripts/test_shiki_governance_evidence.sh:932:expect_exempted(build_closeout(\"close-clean-noncompleting\", completes=False))\nscripts/test_shiki_governance_evidence.sh-933-\nscripts/test_shiki_governance_evidence.sh-934-# Smuggling 1: one non-.shiki file (covered by a declared lock, so ONLY the\nscripts/test_shiki_governance_evidence.sh-935-# under-.shiki rule can reject it).\nscripts/test_shiki_governance_evidence.sh-936-expect_guardian_fallback(\nscripts/test_shiki_governance_evidence.sh-937-    build_closeout(\"close-smuggle-nonshiki\", extra_changed=[(\"M\", \"scripts/close_fixture_placeholder.py\")])\nscripts/test_shiki_governance_evidence.sh-938-)\nscripts/test_shiki_governance_evidence.sh-939-# Smuggling 2: editing a governance field (acceptance_checks) of the task contract.\nscripts/test_shiki_governance_evidence.sh-940-expect_guardian_fallback(\n--\nscripts/test_shiki_adversarial_state.sh-813-# evaluated as if the task were low risk at the single Guardian decision point.\nscripts/test_shiki_adversarial_state.sh-814-# Each smuggling case below is a CRITICAL-risk closeout with ONE defect that must\nscripts/test_shiki_adversarial_state.sh-815-# disqualify it, so the task re-inherits its risk and the Guardian requirement\nscripts/test_shiki_adversarial_state.sh-816-# fires. We assert the SPECIFIC Guardian fallback block (this suite passes no\nscripts/test_shiki_adversarial_state.sh-817-# guardian policy, so a required high/critical PR blocks on the missing policy),\nscripts/test_shiki_adversarial_state.sh-818-# never merely that the PR blocks, so an unrelated future block cannot mask a\nscripts/test_shiki_adversarial_state.sh-819-# classifier regression. The clean control proves the classifier CAN exempt.\nscripts/test_shiki_adversarial_state.sh-820-# ============================================================================\nscripts/test_shiki_adversarial_state.sh:821:CLOSE_TASK = \"T-20260731T004111132162Z-c105e001\"\nscripts/test_shiki_adversarial_state.sh-822-CLOSE_GOAL = \"G-20260731T004111130646Z-c105e0a1\"\nscripts/test_shiki_adversarial_state.sh-823-CLOSE_IMPL_PR = 940\nscripts/test_shiki_adversarial_state.sh-824-CLOSE_PR = 941\nscripts/test_shiki_adversarial_state.sh-825-CLOSE_HEAD = \"c\" * 40\nscripts/test_shiki_adversarial_state.sh:826:CLOSE_BRANCH = \"shiki/t-c105e001-closeout\"\nscripts/test_shiki_adversarial_state.sh-827-CLOSE_COMP = \"L-20260731T010000000000Z-c105ec3d\"\nscripts/test_shiki_adversarial_state.sh-828-CLOSE_PULL = \"L-20260731T010000000001Z-c105e91d\"\nscripts/test_shiki_adversarial_state.sh:829:CLOSE_REPORT_ID = \"R-20260731T010000000002Z-c105e5cd\"\nscripts/test_shiki_adversarial_state.sh-830-CLOSE_REPORT_REL = f\".shiki/reports/{CLOSE_REPORT_ID}.json\"\nscripts/test_shiki_adversarial_state.sh-831-CLOSE_URL = f\"https://github.com/mizutani-140/shiki/pull/{CLOSE_PR}\"\nscripts/test_shiki_adversarial_state.sh-832-GUARDIAN_POLICY_FALLBACK = \"Guardian policy is required for high/critical risk PRs\"\nscripts/test_shiki_adversarial_state.sh-833-\nscripts/test_shiki_adversarial_state.sh-834-CLOSE_GOVERNANCE = {\nscripts/test_shiki_adversarial_state.sh-835-    \"title\": \"Closeout smuggling fixture\",\nscripts/test_shiki_adversarial_state.sh-836-    \"scope\": \"Push the merged task's terminal state to main.\",\nscripts/test_shiki_adversarial_state.sh-837-    \"non_goals\": [\"No code change.\"],\n--\nscripts/test_shiki_adversarial_state.sh-971-\nscripts/test_shiki_adversarial_state.sh-972-def expect_guardian_fallback(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> None:\nscripts/test_shiki_adversarial_state.sh-973-    result = run_mergegate(target, allow_missing_cca=False, expected_head=CLOSE_HEAD, merged_prs=merged)\nscripts/test_shiki_adversarial_state.sh-974-    if result.returncode == 0:\nscripts/test_shiki_adversarial_state.sh-975-        raise AssertionError(f\"expected the Guardian fallback to block, got success:\\n{result.stdout}\")\nscripts/test_shiki_adversarial_state.sh-976-    assert_contains(result.stdout, GUARDIAN_POLICY_FALLBACK)\nscripts/test_shiki_adversarial_state.sh-977-\nscripts/test_shiki_adversarial_state.sh-978-\nscripts/test_shiki_adversarial_state.sh:979:def expect_exempted(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> None:\nscripts/test_shiki_adversarial_state.sh-980-    result = run_mergegate(target, allow_missing_cca=False, expected_head=CLOSE_HEAD, merged_prs=merged)\nscripts/test_shiki_adversarial_state.sh-981-    if GUARDIAN_POLICY_FALLBACK in result.stdout:\nscripts/test_shiki_adversarial_state.sh-982-        raise AssertionError(f\"bookkeeping exemption did not fire; Guardian block present:\\n{result.stdout}\")\nscripts/test_shiki_adversarial_state.sh-983-\nscripts/test_shiki_adversarial_state.sh-984-\nscripts/test_shiki_adversarial_state.sh-985-# Clean control: a proven completing closeout of a CRITICAL task is exempted, so\nscripts/test_shiki_adversarial_state.sh-986-# the Guardian requirement does NOT fire (this suite passes no guardian policy, so\nscripts/test_shiki_adversarial_state.sh-987-# a non-exempted critical PR would block on the missing policy \u2014 it does not).\nscripts/test_shiki_adversarial_state.sh:988:expect_exempted(build_closeout(\"close-clean-completing\"))\nscripts/test_shiki_adversarial_state.sh-989-# A proven non-completing closeout (a sibling still runs) is also exempted.\nscripts/test_shiki_adversarial_state.sh:990:expect_exempted(build_closeout(\"close-clean-noncompleting\", completes=False))\nscripts/test_shiki_adversarial_state.sh-991-\nscripts/test_shiki_adversarial_state.sh-992-# Smuggling 1: one non-.shiki file (covered by a declared lock, so ONLY the\nscripts/test_shiki_adversarial_state.sh-993-# under-.shiki rule can reject it).\nscripts/test_shiki_adversarial_state.sh-994-expect_guardian_fallback(\nscripts/test_shiki_adversarial_state.sh-995-    build_closeout(\"close-smuggle-nonshiki\", extra_changed=[(\"M\", \"scripts/close_fixture_placeholder.py\")])\nscripts/test_shiki_adversarial_state.sh-996-)\nscripts/test_shiki_adversarial_state.sh-997-# Smuggling 2: editing a governance field (acceptance_checks) of the task contract.\nscripts/test_shiki_adversarial_state.sh-998-expect_guardian_fallback(\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:19.682196Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '790,920p' scripts/test_shiki_governance_evidence.sh; sed -n '850,975p' scripts/test_shiki_adversarial_state.sh\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n        \"title\": \"Closeout smuggling fixture\",\n        \"body\": \"\\n\".join([\n            \"## Scope\", f\"Task {CLOSE_TASK} for Goal {CLOSE_GOAL}.\",\n            \"## Acceptance\", \"Terminal state pushed to main.\",\n            \"## Evidence\", f\"Closeout PR #{CLOSE_PR}; {CLOSE_URL}.\",\n            \"## MergeGate\", \"Ready when checks pass.\",\n        ]),\n        \"author\": {\"login\": \"mizutani-140\"},\n        \"headRefName\": CLOSE_BRANCH,\n        \"baseRefName\": \"main\",\n        \"headRefOid\": CLOSE_HEAD,\n        \"labels\": [],\n        \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n        \"reviewDecision\": \"APPROVED\",\n        \"statusCheckRollup\": [\n            {\"name\": \"Validate Shiki mirror\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n            {\"name\": \"MergeGate metadata check\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n        ],\n    }\n\n\ndef close_cca() -> dict:\n    return {\n        \"verdict\": \"complete\", \"summary\": \"closeout complete\",\n        \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"pr\": CLOSE_PR, \"head_sha\": CLOSE_HEAD,\n        \"can_merge\": True,\n        \"checklist\": [{\"id\": \"CCA-01\", \"status\": \"pass\", \"blocking\": True, \"evidence\": \"closeout\"}],\n        \"acceptance\": [{\"criterion\": \"terminal state\", \"status\": \"pass\", \"evidence\": [\"closeout\"]}],\n        \"mergegate\": {\"closeout\": \"pass\"}, \"confidence\": 1.0, \"repair_packet\": None,\n    }\n\n\ndef build_closeout(\n    name: str,\n    *,\n    completes: bool = True,\n    head_mutate=None,\n    goal_head_status: str = \"complete\",\n    lock_head_state: str = \"released\",\n    base_lock_state: str = \"active\",\n    extra_changed: list[tuple[str, str]] | None = None,\n    comp_evidence: list[str] | None = None,\n    extra_target_files: dict[str, dict] | None = None,\n) -> pathlib.Path:\n    target = tmp_root / name\n    if target.exists():\n        shutil.rmtree(target)\n    copy_base_support(target)\n    S = target / \".shiki\"\n    B = S / \"gha\" / \"base-shiki\" / \".shiki\"\n\n    write_json(B / \"tasks\" / f\"{CLOSE_TASK}.json\",\n               {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n                \"status\": \"review\", \"expected_pr\": CLOSE_IMPL_PR, \"ledger_evidence\": [\"L-close-base\"]})\n    write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n               {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n    write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n\n    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n    head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n                 \"ledger_evidence\": head_ledgers}\n    if head_mutate:\n        head_mutate(head_task)\n    write_json(S / \"tasks\" / f\"{CLOSE_TASK}.json\", head_task)\n    write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n               {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n    write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n    write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n               {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n\n    if completes:\n        write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n        write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                   {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                    \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n        changed = [\n            (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n            (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n            (\"M\", f\".shiki/goals/{CLOSE_GOAL}.json\"),\n            (\"A\", CLOSE_REPORT_REL),\n            (\"A\", f\".shiki/ledger/{CLOSE_COMP}.json\"),\n            (\"A\", f\".shiki/ledger/{CLOSE_PULL}.json\"),\n        ]\n    else:\n        write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\",\n                   {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK, f\"{CLOSE_TASK}-sib\"], \"edges\": []})\n        write_json(S / \"tasks\" / f\"{CLOSE_TASK}-sib.json\",\n                   {\"id\": f\"{CLOSE_TASK}-sib\", \"goal_id\": CLOSE_GOAL, \"status\": \"running\", \"title\": \"s\"})\n        changed = [\n            (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n            (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n            (\"A\", f\".shiki/ledger/{CLOSE_PULL}.json\"),\n        ]\n\n    if extra_target_files:\n        for rel, payload in extra_target_files.items():\n            write_json(S / rel, payload)\n    if extra_changed:\n        changed = changed + extra_changed\n\n    write_json(S / \"gha\" / \"pr.json\", close_pr())\n    write_json(S / \"gha\" / \"cca-verdict.json\", close_cca())\n    # No Guardian approval evidence: a non-exempted critical PR must fall back to\n    # REQUIRING approval and block for its absence; an exempted one skips the gate.\n    for gname in (\"live-guardian-comments.json\", \"live-guardian-events.json\", \"live-guardian-timeline.json\"):\n        write_json(S / \"gha\" / gname, [])\n    files = [path for _, path in changed]\n    (S / \"gha\" / \"changed-files.txt\").write_text(\"\\n\".join(files) + \"\\n\", encoding=\"utf-8\")\n    (S / \"gha\" / \"changed-files-status.txt\").write_text(\"\\n\".join(f\"{s}\\t{p}\" for s, p in changed) + \"\\n\", encoding=\"utf-8\")\n    return target\n\n\ndef close_run(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> subprocess.CompletedProcess:\n    return run_mergegate(\n        target,\n        expected_head=CLOSE_HEAD,\n        base_shiki=str(target / \".shiki\" / \"gha\" / \"base-shiki\" / \".shiki\"),\n        merged_prs=merged,\n    )\n\n\ndef expect_guardian_fallback(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> None:\n    result = close_run(target, merged=merged)\n    if result.returncode == 0:\n        raise AssertionError(f\"expected the Guardian approval fallback to block, got success:\\n{result.stdout}\")\n    assert_contains(result.stdout, GUARDIAN_APPROVAL_FALLBACK)\n    pr = {\n        \"number\": CLOSE_PR,\n        \"title\": \"Closeout smuggling fixture\",\n        \"body\": \"\\n\".join([\n            \"## Scope\", f\"Task {CLOSE_TASK} for Goal {CLOSE_GOAL}.\",\n            \"## Acceptance\", \"Terminal state pushed to main.\",\n            \"## Evidence\", f\"Closeout PR #{CLOSE_PR}; {CLOSE_URL}.\",\n            \"## MergeGate\", \"Ready when checks pass.\",\n        ]),\n        \"author\": {\"login\": \"mizutani-140\"},\n        \"headRefName\": CLOSE_BRANCH,\n        \"baseRefName\": \"main\",\n        \"headRefOid\": CLOSE_HEAD,\n        \"labels\": [],\n        \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n        \"reviewDecision\": \"APPROVED\",\n        \"statusCheckRollup\": [\n            {\"name\": \"Validate Shiki mirror\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n            {\"name\": \"MergeGate metadata check\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n        ],\n    }\n    pr.update(over)\n    return pr\n\n\ndef close_cca(**over) -> dict:\n    cca = {\n        \"verdict\": \"complete\", \"summary\": \"closeout complete\",\n        \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"pr\": CLOSE_PR, \"head_sha\": CLOSE_HEAD,\n        \"can_merge\": True,\n        \"checklist\": [{\"id\": \"CCA-01\", \"status\": \"pass\", \"blocking\": True, \"evidence\": \"closeout\"}],\n        \"acceptance\": [{\"criterion\": \"terminal state\", \"status\": \"pass\", \"evidence\": [\"closeout\"]}],\n        \"mergegate\": {\"closeout\": \"pass\"}, \"confidence\": 1.0, \"repair_packet\": None,\n    }\n    cca.update(over)\n    return cca\n\n\ndef build_closeout(\n    name: str,\n    *,\n    completes: bool = True,\n    head_mutate=None,\n    base_mutate=None,\n    goal_head_status: str = \"complete\",\n    lock_head_state: str = \"released\",\n    base_lock_state: str = \"active\",\n    extra_changed: list[tuple[str, str]] | None = None,\n    comp_evidence: list[str] | None = None,\n    extra_target_files: dict[str, dict] | None = None,\n) -> pathlib.Path:\n    target = tmp_root / name\n    if target.exists():\n        shutil.rmtree(target)\n    copy_base_support(target)\n    S = target / \".shiki\"\n    B = S / \"gha\" / \"base-shiki\" / \".shiki\"\n\n    base_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n                 \"status\": \"review\", \"expected_pr\": CLOSE_IMPL_PR, \"ledger_evidence\": [\"L-close-base\"]}\n    if base_mutate:\n        base_mutate(base_task)\n    write_json(B / \"tasks\" / f\"{CLOSE_TASK}.json\", base_task)\n    write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n               {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n    write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n\n    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n    head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n                 \"ledger_evidence\": head_ledgers}\n    if head_mutate:\n        head_mutate(head_task)\n    write_json(S / \"tasks\" / f\"{CLOSE_TASK}.json\", head_task)\n    write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n               {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n    write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n    write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n               {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n\n    if completes:\n        write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n        write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                   {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                    \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n        changed = [\n            (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n            (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n            (\"M\", f\".shiki/goals/{CLOSE_GOAL}.json\"),\n            (\"A\", CLOSE_REPORT_REL),\n            (\"A\", f\".shiki/ledger/{CLOSE_COMP}.json\"),\n            (\"A\", f\".shiki/ledger/{CLOSE_PULL}.json\"),\n        ]\n    else:\n        write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\",\n                   {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK, f\"{CLOSE_TASK}-sib\"], \"edges\": []})\n        write_json(S / \"tasks\" / f\"{CLOSE_TASK}-sib.json\",\n                   {\"id\": f\"{CLOSE_TASK}-sib\", \"goal_id\": CLOSE_GOAL, \"status\": \"running\", \"title\": \"s\"})\n        changed = [\n            (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n            (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n            (\"A\", f\".shiki/ledger/{CLOSE_PULL}.json\"),\n        ]\n\n    if extra_target_files:\n        for rel, payload in extra_target_files.items():\n            write_json(S / rel, payload)\n    if extra_changed:\n        changed = changed + extra_changed\n\n    write_json(S / \"gha\" / \"pr.json\", close_pr())\n    write_json(S / \"gha\" / \"cca-verdict.json\", close_cca())\n    files = [path for _, path in changed]\n    (S / \"gha\" / \"changed-files.txt\").write_text(\"\\n\".join(files) + \"\\n\", encoding=\"utf-8\")\n    (S / \"gha\" / \"changed-files-status.txt\").write_text(\"\\n\".join(f\"{s}\\t{p}\" for s, p in changed) + \"\\n\", encoding=\"utf-8\")\n    return target\n\n\ndef expect_guardian_fallback(target: pathlib.Path, *, merged: str = str(CLOSE_IMPL_PR)) -> None:\n    result = run_mergegate(target, allow_missing_cca=False, expected_head=CLOSE_HEAD, merged_prs=merged)\n    if result.returncode == 0:\n        raise AssertionError(f\"expected the Guardian fallback to block, got success:\\n{result.stdout}\")\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:23.164872Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '750,810p' scripts/test_shiki_governance_evidence.sh; sed -n '820,870p' scripts/test_shiki_adversarial_state.sh; sed -n '930,1045p' scripts/test_shiki_governance_evidence.sh; sed -n '985,1110p' scripts/test_shiki_adversarial_state.sh\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n# evaluated as if the task were low risk at the single Guardian decision point.\n# Each smuggling case below is a CRITICAL-risk closeout with ONE defect that must\n# disqualify it, so the task re-inherits its risk and \u2014 with NO approval evidence\n# present \u2014 the Guardian approval requirement fires. We assert the SPECIFIC\n# Guardian approval fallback, never merely that the PR blocks, so an unrelated\n# future block cannot mask a classifier regression. The clean control proves the\n# classifier CAN exempt (the same critical PR, with no approval, does NOT block on\n# Guardian).\n# ============================================================================\nCLOSE_TASK = \"T-20260731T004111132162Z-c105e001\"\nCLOSE_GOAL = \"G-20260731T004111130646Z-c105e0a1\"\nCLOSE_IMPL_PR = 940\nCLOSE_PR = 941\nCLOSE_HEAD = \"c\" * 40\nCLOSE_BRANCH = \"shiki/t-c105e001-closeout\"\nCLOSE_COMP = \"L-20260731T010000000000Z-c105ec3d\"\nCLOSE_PULL = \"L-20260731T010000000001Z-c105e91d\"\nCLOSE_REPORT_ID = \"R-20260731T010000000002Z-c105e5cd\"\nCLOSE_REPORT_REL = f\".shiki/reports/{CLOSE_REPORT_ID}.json\"\nCLOSE_URL = f\"https://github.com/mizutani-140/shiki/pull/{CLOSE_PR}\"\nGUARDIAN_APPROVAL_FALLBACK = \"Guardian approval requires a configured Guardian review or current-head Guardian comment\"\n\nCLOSE_GOVERNANCE = {\n    \"title\": \"Closeout smuggling fixture\",\n    \"scope\": \"Push the merged task's terminal state to main.\",\n    \"non_goals\": [\"No code change.\"],\n    \"dependencies\": [],\n    \"locks\": [\"path:scripts/close_fixture_placeholder.py\"],\n    \"assigned_runtime\": \"claude-code\",\n    \"risk_level\": \"critical\",\n    \"required_skills\": [],\n    \"acceptance_checks\": [\"task done, lock released, goal complete\"],\n    \"test_command\": \"python3 -m unittest discover -s tests\",\n    \"expected_branch\": CLOSE_BRANCH,\n}\n\n\ndef close_pr() -> dict:\n    return {\n        \"number\": CLOSE_PR,\n        \"title\": \"Closeout smuggling fixture\",\n        \"body\": \"\\n\".join([\n            \"## Scope\", f\"Task {CLOSE_TASK} for Goal {CLOSE_GOAL}.\",\n            \"## Acceptance\", \"Terminal state pushed to main.\",\n            \"## Evidence\", f\"Closeout PR #{CLOSE_PR}; {CLOSE_URL}.\",\n            \"## MergeGate\", \"Ready when checks pass.\",\n        ]),\n        \"author\": {\"login\": \"mizutani-140\"},\n        \"headRefName\": CLOSE_BRANCH,\n        \"baseRefName\": \"main\",\n        \"headRefOid\": CLOSE_HEAD,\n        \"labels\": [],\n        \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n        \"reviewDecision\": \"APPROVED\",\n        \"statusCheckRollup\": [\n            {\"name\": \"Validate Shiki mirror\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n            {\"name\": \"MergeGate metadata check\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n        ],\n    }\n\n\n# ============================================================================\nCLOSE_TASK = \"T-20260731T004111132162Z-c105e001\"\nCLOSE_GOAL = \"G-20260731T004111130646Z-c105e0a1\"\nCLOSE_IMPL_PR = 940\nCLOSE_PR = 941\nCLOSE_HEAD = \"c\" * 40\nCLOSE_BRANCH = \"shiki/t-c105e001-closeout\"\nCLOSE_COMP = \"L-20260731T010000000000Z-c105ec3d\"\nCLOSE_PULL = \"L-20260731T010000000001Z-c105e91d\"\nCLOSE_REPORT_ID = \"R-20260731T010000000002Z-c105e5cd\"\nCLOSE_REPORT_REL = f\".shiki/reports/{CLOSE_REPORT_ID}.json\"\nCLOSE_URL = f\"https://github.com/mizutani-140/shiki/pull/{CLOSE_PR}\"\nGUARDIAN_POLICY_FALLBACK = \"Guardian policy is required for high/critical risk PRs\"\n\nCLOSE_GOVERNANCE = {\n    \"title\": \"Closeout smuggling fixture\",\n    \"scope\": \"Push the merged task's terminal state to main.\",\n    \"non_goals\": [\"No code change.\"],\n    \"dependencies\": [],\n    \"locks\": [\"path:scripts/close_fixture_placeholder.py\"],\n    \"assigned_runtime\": \"claude-code\",\n    \"risk_level\": \"critical\",\n    \"required_skills\": [],\n    \"acceptance_checks\": [\"task done, lock released, goal complete\"],\n    \"test_command\": \"python3 -m unittest discover -s tests\",\n    \"expected_branch\": CLOSE_BRANCH,\n}\n\n\ndef close_pr(**over) -> dict:\n    pr = {\n        \"number\": CLOSE_PR,\n        \"title\": \"Closeout smuggling fixture\",\n        \"body\": \"\\n\".join([\n            \"## Scope\", f\"Task {CLOSE_TASK} for Goal {CLOSE_GOAL}.\",\n            \"## Acceptance\", \"Terminal state pushed to main.\",\n            \"## Evidence\", f\"Closeout PR #{CLOSE_PR}; {CLOSE_URL}.\",\n            \"## MergeGate\", \"Ready when checks pass.\",\n        ]),\n        \"author\": {\"login\": \"mizutani-140\"},\n        \"headRefName\": CLOSE_BRANCH,\n        \"baseRefName\": \"main\",\n        \"headRefOid\": CLOSE_HEAD,\n        \"labels\": [],\n        \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n        \"reviewDecision\": \"APPROVED\",\n        \"statusCheckRollup\": [\n            {\"name\": \"Validate Shiki mirror\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n            {\"name\": \"MergeGate metadata check\", \"status\": \"COMPLETED\", \"conclusion\": \"SUCCESS\", \"headSha\": CLOSE_HEAD},\n        ],\n    }\n# approval evidence is exempted, so the approval requirement does NOT fire.\nexpect_exempted(build_closeout(\"close-clean-completing\"))\nexpect_exempted(build_closeout(\"close-clean-noncompleting\", completes=False))\n\n# Smuggling 1: one non-.shiki file (covered by a declared lock, so ONLY the\n# under-.shiki rule can reject it).\nexpect_guardian_fallback(\n    build_closeout(\"close-smuggle-nonshiki\", extra_changed=[(\"M\", \"scripts/close_fixture_placeholder.py\")])\n)\n# Smuggling 2: editing a governance field (acceptance_checks) of the task contract.\nexpect_guardian_fallback(\n    build_closeout(\"close-smuggle-governance\",\n                   head_mutate=lambda t: t.__setitem__(\"acceptance_checks\", [\"a smuggled criterion\"]))\n)\n# Smuggling 3: deleting a mirror record.\nexpect_guardian_fallback(\n    build_closeout(\"close-smuggle-delete\", extra_changed=[(\"D\", f\".shiki/worktrees/{CLOSE_TASK}.json\")])\n)\n# Smuggling 4: setting a status outside the terminal set (goal -> blocked).\nexpect_guardian_fallback(build_closeout(\"close-smuggle-status\", goal_head_status=\"blocked\"))\n# Smuggling 5: adding a second scorecard report (covered by the completion ledger\n# so ONLY the one-report rule can reject it).\n_second_report = \".shiki/reports/R-20260731T010000000003Z-c105e5ce.json\"\nexpect_guardian_fallback(\n    build_closeout(\n        \"close-smuggle-second-report\",\n        comp_evidence=[CLOSE_REPORT_REL, _second_report],\n        extra_target_files={_second_report.split(\".shiki/\", 1)[1]: {\"id\": \"R-20260731T010000000003Z-c105e5ce\", \"goal_id\": CLOSE_GOAL}},\n        extra_changed=[(\"A\", _second_report)],\n    )\n)\n# Smuggling 6: the task's implementation PR is not proven merged.\nexpect_guardian_fallback(build_closeout(\"close-smuggle-unmerged\"), merged=\"\")\n\n\n# ============================================================================\n# Amendment mode (SADR-0009/SADR-0015): a Spec Amendment PR cannot lower its OWN\n# Guardian gate. The gate is forced from the STRONGER of the goal's base-plan and\n# head-plan risk, so an amendment that WEAKENS its own plan (critical -> low)\n# still resolves at the base (critical) risk and blocks for missing approval.\n# End-to-end through the real gate: a well-formed, weakened amendment WITHOUT\n# guardian approval blocks; the SAME amendment WITH approval is ready \u2014 proving\n# the only thing gating the fires-case is the Guardian requirement the amendment\n# tried to shed, not a malformed contract.\n# ============================================================================\nAMEND_GOAL = \"G-20260804T111111111111Z-0000a0d1\"\nAMEND_PLAN = \"P-20260804T111111111111Z-0000a0d2\"\nAMEND_T_A = \"T-20260804T111111111111Z-0000a0a1\"\nAMEND_T_B = \"T-20260804T111111111111Z-0000a0b2\"\nAMEND_LEDGER = \"L-20260804T111111111111Z-0000a0c3\"\nAMEND_MARKER = \"<!-- shiki:amendment -->\"\nNARROW_A = [\"path:scripts/amend_a.py\"]\nWIDE_A = [\"path:scripts/amend_a.py\", \"path:scripts/amend_a2.py\"]\n_A1 = {\"id\": \"AMD-0001\", \"reason\": \"initial freeze\"}\n_A2 = {\"id\": \"AMD-0002\", \"reason\": \"widen T_A locks\"}\n\n\ndef _amend_plan(*, amendments, risk, a_locks):\n    return {\n        \"id\": AMEND_PLAN,\n        \"title\": \"amendment plan\",\n        \"outcome\": \"corrected contract\",\n        \"risk_level\": risk,\n        \"spec_freeze\": {\"status\": \"frozen\", \"approved_by\": \"operator\", \"prd\": \"PRD-1\", \"source\": \"grill\", \"amendments\": amendments},\n        \"tasks\": [\n            {\"title\": \"Widen the narrow lock\", \"scope\": \"scope-a\", \"non_goals\": [\"ng-a\"],\n             \"required_skills\": [\"tdd\", \"code-review\"], \"risk_level\": \"high\", \"locks\": a_locks,\n             \"acceptance_checks\": [\"a\"], \"runtime\": \"claude-code\"},\n            {\"title\": \"Downstream slice\", \"scope\": \"scope-b\", \"non_goals\": [\"ng-b\"],\n             \"required_skills\": [\"tdd\", \"code-review\"], \"risk_level\": \"high\", \"locks\": [\"path:scripts/amend_b.py\"],\n             \"acceptance_checks\": [\"b\"], \"runtime\": \"claude-code\", \"dependencies\": [\"Widen the narrow lock\"]},\n        ],\n    }\n\n\ndef _amend_task_a(*, locks, status):\n    return {\n        \"id\": AMEND_T_A, \"goal_id\": AMEND_GOAL, \"title\": \"Widen the narrow lock\", \"status\": status,\n        \"scope\": \"scope-a\", \"non_goals\": [\"ng-a\"], \"required_skills\": [\"tdd\", \"code-review\"],\n        \"risk_level\": \"high\", \"locks\": locks, \"acceptance_checks\": [\"a\"], \"assigned_runtime\": \"claude-code\",\n        \"dependencies\": [], \"expected_pr\": 100, \"closeout_pr\": None, \"expected_branch\": \"shiki/amend-a\",\n        \"ledger_evidence\": [\"L-20260804T111111111111Z-0000ba51\"],\n    }\n\n\ndef _amend_task_b():\n    return {\n        \"id\": AMEND_T_B, \"goal_id\": AMEND_GOAL, \"title\": \"Downstream slice\", \"status\": \"planned\",\n        \"scope\": \"scope-b\", \"non_goals\": [\"ng-b\"], \"required_skills\": [\"tdd\", \"code-review\"],\n        \"risk_level\": \"high\", \"locks\": [\"path:scripts/amend_b.py\"], \"acceptance_checks\": [\"b\"],\n        \"assigned_runtime\": \"claude-code\", \"dependencies\": [AMEND_T_A], \"expected_pr\": 101,\n        \"closeout_pr\": None, \"expected_branch\": \"shiki/amend-b\",\n        \"ledger_evidence\": [\"L-20260804T111111111111Z-0000ba52\"],\n    }\n\n\ndef _amend_dag():\n    return {\"goal_id\": AMEND_GOAL, \"nodes\": [AMEND_T_A, AMEND_T_B],\n            \"edges\": [{\"from\": AMEND_T_A, \"to\": AMEND_T_B, \"reason\": \"declared plan dependency\"}]}\n\n\ndef _amend_goal():\n    return {\"id\": AMEND_GOAL, \"status\": \"planned\", \"source_plan\": AMEND_PLAN, \"risk_level\": \"low\", \"title\": \"g\"}\n\n\ndef make_amendment_fixture(name: str, *, approved: bool) -> pathlib.Path:\n    \"\"\"A well-formed amendment that WEAKENS its own plan risk (base critical ->\n    head low). The Guardian evidence is the only knob: ``approved`` toggles a\n    current-head approval vs a non-approving comment.\"\"\"\n    target = tmp_root / name\n    if target.exists():\n        shutil.rmtree(target)\n    copy_base_support(target)\n    # Head (the amendment): plan weakened to low, amendments appended, T_A locks widened + re-opened.\n    write_json(target / \".shiki\" / \"goals\" / f\"{AMEND_GOAL}.json\", _amend_goal())\n    write_json(target / \".shiki\" / \"plans\" / f\"{AMEND_PLAN}.json\", _amend_plan(amendments=[_A1, _A2], risk=\"low\", a_locks=WIDE_A))\n# Clean control: a proven completing closeout of a CRITICAL task is exempted, so\n# the Guardian requirement does NOT fire (this suite passes no guardian policy, so\n# a non-exempted critical PR would block on the missing policy \u2014 it does not).\nexpect_exempted(build_closeout(\"close-clean-completing\"))\n# A proven non-completing closeout (a sibling still runs) is also exempted.\nexpect_exempted(build_closeout(\"close-clean-noncompleting\", completes=False))\n\n# Smuggling 1: one non-.shiki file (covered by a declared lock, so ONLY the\n# under-.shiki rule can reject it).\nexpect_guardian_fallback(\n    build_closeout(\"close-smuggle-nonshiki\", extra_changed=[(\"M\", \"scripts/close_fixture_placeholder.py\")])\n)\n# Smuggling 2: editing a governance field (acceptance_checks) of the task contract.\nexpect_guardian_fallback(\n    build_closeout(\"close-smuggle-governance\",\n                   head_mutate=lambda t: t.__setitem__(\"acceptance_checks\", [\"a smuggled criterion\"]))\n)\n# Smuggling 3: deleting a mirror record.\nexpect_guardian_fallback(\n    build_closeout(\"close-smuggle-delete\", extra_changed=[(\"D\", f\".shiki/worktrees/{CLOSE_TASK}.json\")])\n)\n# Smuggling 4: setting a status outside the terminal set (goal -> blocked).\nexpect_guardian_fallback(build_closeout(\"close-smuggle-status\", goal_head_status=\"blocked\"))\n# Smuggling 5: adding a second scorecard report (covered by the completion ledger\n# so ONLY the one-report rule can reject it).\n_second_report = \".shiki/reports/R-20260731T010000000003Z-c105e5ce.json\"\nexpect_guardian_fallback(\n    build_closeout(\n        \"close-smuggle-second-report\",\n        comp_evidence=[CLOSE_REPORT_REL, _second_report],\n        extra_target_files={_second_report.split(\".shiki/\", 1)[1]: {\"id\": \"R-20260731T010000000003Z-c105e5ce\", \"goal_id\": CLOSE_GOAL}},\n        extra_changed=[(\"A\", _second_report)],\n    )\n)\n# Smuggling 6: the task's implementation PR is not proven merged.\nexpect_guardian_fallback(build_closeout(\"close-smuggle-unmerged\"), merged=\"\")\n\n\n# ============================================================================\n# T-20260729T065622763684Z-ee01f3c4: MergeGate scope enforcement sees the WHOLE\n# diff. Renames are judged through both legs, the eleven generic mirror\n# directories block foreign/deleted records, the runtime-evidence match is exact\n# (so the CCA schema is mergeable), and no PR may author a spec_freeze block.\n# ============================================================================\n\n# (a) A rename that moves a file OUT of locked scope is seen through its source\n# (delete) leg, identically to the explicit delete+add form.\nexpect_block(\n    make_fixture(\n        \"mg-rename-outside-locks\",\n        locks=[\"path:docs/**\"],\n        changed=[\"docs/moved.py\"],\n        status_entries=[\"R100\\ttests/moved.py\\tdocs/moved.py\"],\n    ),\n    \"Changed file tests/moved.py is outside declared task locks\",\n)\nexpect_block(\n    make_fixture(\n        \"mg-delete-add-outside-locks\",\n        locks=[\"path:docs/**\"],\n        changed=[\"docs/moved.py\", \"tests/moved.py\"],\n        status_entries=[\"A\\tdocs/moved.py\", \"D\\ttests/moved.py\"],\n    ),\n    \"Changed file tests/moved.py is outside declared task locks\",\n)\n# A rename entirely inside the declared locks is clean (no false positive).\nexpect_ready(\n    make_fixture(\n        \"mg-rename-inside-locks\",\n        locks=[\"path:docs/**\"],\n        changed=[\"docs/b.py\"],\n        status_entries=[\"R100\\tdocs/a.py\\tdocs/b.py\"],\n    )\n)\n\n# (b) The generic mirror-class rule. A broad lock (path:.shiki/**) satisfies\n# files-outside-locks, so ONLY the mirror-class rule can reject these.\n# Deleting a foreign goal's DAG blocks (base snapshot absent here \u2014 the head-diff\n# rule alone must catch it).\nexpect_block(\n    make_fixture(\n        \"mg-delete-foreign-dag\",\n        locks=[\"path:.shiki/**\"],\n        changed=[\".shiki/dag/G-9999.json\"],\n        status_entries=[\"D\\t.shiki/dag/G-9999.json\"],\n    ),\n    \"PR must not delete Shiki dag record .shiki/dag/G-9999.json\",\n)\n# And with a base snapshot present too.\nexpect_block(\n    make_fixture(\n        \"mg-delete-foreign-dag-based\",\n        locks=[\"path:.shiki/**\"],\n        changed=[\".shiki/dag/G-9999.json\"],\n        status_entries=[\"D\\t.shiki/dag/G-9999.json\"],\n        base={\"dag\": {\"G-9999\": {\"goal_id\": \"G-9999\", \"nodes\": [], \"edges\": []}}},\n    ),\n    \"PR must not delete Shiki dag record .shiki/dag/G-9999.json\",\n)\n# Modifying a foreign report/worktree/runner record blocks (not own-scoped).\nmg_foreign_report = make_fixture(\n    \"mg-foreign-report-modify\",\n    locks=[\"path:.shiki/**\"],\n    changed=[\".shiki/reports/R-foreign.json\"],\n    status_entries=[\"M\\t.shiki/reports/R-foreign.json\"],\n)\nwrite_json(mg_foreign_report / \".shiki\" / \"reports\" / \"R-foreign.json\", {\"id\": \"R-foreign\", \"goal_id\": \"G-9999\"})\nexpect_block(mg_foreign_report, f\"not scoped to task {TASK_ID} or goal {GOAL_ID}\")\nexpect_block(\n    make_fixture(\n        \"mg-foreign-worktree-delete\",\n        locks=[\"path:.shiki/**\"],\n        changed=[\".shiki/worktrees/T-9999.json\"],\n        status_entries=[\"D\\t.shiki/worktrees/T-9999.json\"],\n    ),\n    \"PR must not delete Shiki worktrees record .shiki/worktrees/T-9999.json\",\n)\n# The base-snapshot loop now delete-protects every mirror directory: a base\n# runner record absent at head blocks even with no diff entry naming it.\nexpect_block(\n    make_fixture(\n        \"mg-base-runner-delete\",\n        locks=[\"path:scripts/test_shiki_adversarial_state.sh\"],\n        changed=[\"scripts/test_shiki_adversarial_state.sh\"],\n        base={\"runner\": {\"EXEC-BASE\": {\"id\": \"EXEC-BASE\", \"goal_id\": GOAL_ID, \"task_id\": TASK_ID}}},\n    ),\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:33.356657Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -n 'close_pr\\\\(' scripts/test_shiki_adversarial_state.sh scripts/test_shiki_governance_evidence.sh | head -40; rg -n 'base_mutate|coerce|symlink|rename|type-change|type changed|lockless|lock_terminal' scripts/test_shiki_adversarial_state.sh scripts/test_shiki_governance_evidence.sh | tail -80\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\nscripts/test_shiki_governance_evidence.sh:787:def close_pr() -> dict:\nscripts/test_shiki_governance_evidence.sh:895:    write_json(S / \"gha\" / \"pr.json\", close_pr())\nscripts/test_shiki_adversarial_state.sh:849:def close_pr(**over) -> dict:\nscripts/test_shiki_adversarial_state.sh:964:    write_json(S / \"gha\" / \"pr.json\", close_pr())\nscripts/test_shiki_adversarial_state.sh:893:    base_mutate=None,\nscripts/test_shiki_adversarial_state.sh:910:    if base_mutate:\nscripts/test_shiki_adversarial_state.sh:911:        base_mutate(base_task)\nscripts/test_shiki_adversarial_state.sh:1030:# (a) A rename that moves a file OUT of locked scope is seen through its source\nscripts/test_shiki_adversarial_state.sh:1034:        \"mg-rename-outside-locks\",\nscripts/test_shiki_adversarial_state.sh:1050:# A rename entirely inside the declared locks is clean (no false positive).\nscripts/test_shiki_adversarial_state.sh:1053:        \"mg-rename-inside-locks\",\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:14:45.570538Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/test_shiki_adversarial_state.sh\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/test_shiki_governance_evidence.sh\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..0c6e55b45a13e2e62e2998b79937f3a2e76abad1\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +929,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..0ca5860fc75ec1cfe5228f477ab2e705c4a9993a\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +860,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..0c6e55b45a13e2e62e2998b79937f3a2e76abad1\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +929,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..0ca5860fc75ec1cfe5228f477ab2e705c4a9993a\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +860,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:15:13.247684Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_governance_evidence.sh' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..0c6e55b45a13e2e62e2998b79937f3a2e76abad1\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +929,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..0ca5860fc75ec1cfe5228f477ab2e705c4a9993a\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +860,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:15:19.905255Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 3291ms:\nTraceback (most recent call last):\n  File \"<stdin>\", line 930, in <module>\n  File \"<stdin>\", line 925, in expect_exempted\nAssertionError: bookkeeping exemption did not fire; Guardian block present:\n{\n  \"blocking_reasons\": [\n    \"Task ledger evidence L-close-base is missing at /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-governance-evidence-test-14702/close-clean-completing/.shiki/ledger/L-close-base.json\",\n    \"PR changes reports record .shiki/reports/R-20260731T010000000002Z-c105e5cd.json not scoped to task T-20260731T004111132162Z-c105e001 or goal G-20260731T004111130646Z-c105e0a1; state_class=mirror\",\n    \"Guardian label 'guardian:approved' is missing\",\n    \"Guardian approval requires a configured Guardian review or current-head Guardian comment\"\n  ],\n  \"goal_id\": \"G-20260731T004111130646Z-c105e0a1\",\n  \"mergegate\": \"blocked\",\n  \"task_id\": \"T-20260731T004111132162Z-c105e001\",\n  \"warnings\": [\n    \"github-actions Review Bridge approval is not Guardian approval\"\n  ]\n}\n\n\nexec\n/bin/zsh -lc \"rg -n -A45 'def _mirror_record_scoped' scripts/mergegate_check.py; rg -n -A80 'def _derive_task_mirror_locks' scripts/mergegate_check.py; rg -n 'L-close-base|closeout' scripts/test_shiki_governance_evidence.sh | head -20\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n2703:def _mirror_record_scoped(data: Any, path: str, *, task_id: str, goal_id: str) -> bool:\n2704-    \"\"\"Whether a mirror record is scoped to the PR's own task or goal.\n2705-\n2706-    A record is own-scoped when its filename stem is this task's or goal's id\n2707-    (id-named records such as ``dag/<goal>.json`` and ``worktrees/<task>.json``),\n2708-    or when its JSON content is task/goal scoped by the same rule the ledger gate\n2709-    uses (``ledger_entry_allowed_for_task``): its ``task_id`` equals this task, or\n2710-    it carries no ``task_id`` and its ``goal_id`` equals this goal. A record naming\n2711-    a DIFFERENT task \u2014 even one of the same goal \u2014 is NOT own-scoped\n2712-    (anti-padding). Unreadable/malformed content (``data`` not a dict) is\n2713-    fail-closed: not scoped.\n2714-    \"\"\"\n2715-    stem = Path(path).stem\n2716-    if stem and (stem == task_id or stem == goal_id):\n2717-        return True\n2718-    if isinstance(data, dict):\n2719-        return ledger_entry_allowed_for_task(data, task_id=task_id, goal_id=goal_id)\n2720-    return False\n2721-\n2722-\n2723-# The governance contract a NORMAL task PR must not rewrite in its OWN task file\n2724-# (SADR-0015 Contract immutability, normal path). When the task file is present in\n2725-# the base snapshot the base is authority over these fields, so a head that\n2726-# differs on any of them is blocked. Every OTHER task field may legitimately move\n2727-# between the base registration and the implementation-PR head \u2014 the\n2728-# ``_CLOSEOUT_MUTABLE_TASK_FIELDS`` (status/expected_pr/closeout_pr/\n2729-# expected_branch/ledger_evidence) AND loop-written bookkeeping such as\n2730-# ``pre_pr_code_review`` / ``cca_rerun_count`` that ``shiki_loop`` writes onto the\n2731-# task file and syncs to the branch. Freezing the COMPLEMENT of the mutable set\n2732-# would therefore wrongly block real PRs; this is the explicit frozen set instead.\n2733-# It is DISJOINT from ``_CLOSEOUT_MUTABLE_TASK_FIELDS`` (bound by a consistency\n2734-# test) so the frozen contract and the may-move set cannot drift into overlap, and\n2735-# the mutable set stays the single SADR-0017 constant, never re-copied here.\n2736-_NORMAL_PATH_GOVERNANCE_FIELDS: tuple[str, ...] = (\n2737-    \"scope\",\n2738-    \"non_goals\",\n2739-    \"required_skills\",\n2740-    \"risk_level\",\n2741-    \"locks\",\n2742-    \"acceptance_checks\",\n2743-    \"test_command\",\n2744-)\n2745-\n2746-\n2747-def resolved_guardian_risk_never_weaker(\n2748-    base_task: dict[str, Any] | None, head_task: dict[str, Any]\n186:def _derive_task_mirror_locks(target: Path, task: dict[str, Any]) -> list[str]:\n187-    \"\"\"The task's id-scoped .shiki mirror lock set, recomputed at judgment time.\n188-\n189-    A registered task's stored ``locks`` are EXACTLY what the plan declared \u2014\n190-    nothing is injected. But the goal loop syncs the task's own .shiki evidence\n191-    onto its branch (shiki_loop._evidence_relatives_for_task): its task file,\n192-    worktree record and lock record (task-id scoped); its goal file and DAG\n193-    (goal-id scoped); the ledger entries it owns; AND every ``.shiki``-prefixed\n194-    path those ledgers' ``evidence`` arrays reference (a ``type:check`` gate ledger\n195-    -> ``.shiki/runner/EXEC-*.json``; a completion ledger ->\n196-    ``.shiki/reports/R-*.json``). MergeGate's files-outside-locks gate must accept\n197-    those mutations, so this recomputes the covering set from the task's own ids\n198-    and unions it into the effective locks. Deriving at read time (not injecting at\n199-    registration) is the point: the loop appends ledgers and writes runner/EXEC\n200-    records AFTER dispatch, so any coverage written into the stored locks would be\n201-    stale by the time MergeGate reads it.\n202-\n203-    The derivation is BOUNDED so a PR-authored task file cannot widen its own\n204-    effective locks by citing someone else's evidence: a ledger in\n205-    ``ledger_evidence`` is expanded ONLY when its own ``goal_id`` matches this\n206-    task's goal AND its ``task_id`` is either this task's id or empty. An empty\n207-    ``task_id`` means a goal-level ledger of this goal \u2014 e.g. the completion\n208-    ledger ``cmd_goal_complete`` writes for a closeout, whose evidence cites\n209-    ``.shiki/reports/R-*.json`` \u2014 which the loop syncs onto the branch. Any other\n210-    reference \u2014 a foreign goal's ledger, one owned by a DIFFERENT task of this\n211-    goal, a missing/malformed one, or an evidence ref that escapes the ``.shiki``\n212-    subtree \u2014 contributes nothing. Every lock is a concrete single-file path\n213-    (never a directory glob over a shared mirror namespace). Order-stable,\n214-    de-duplicated.\n215-    \"\"\"\n216-    task_id = str(task.get(\"id\") or \"\")\n217-    goal_id = str(task.get(\"goal_id\") or \"\")\n218-    locks: list[str] = []\n219-    if task_id:\n220-        for subtree in _MIRROR_TASK_SUBTREES:\n221-            locks.append(f\"path:.shiki/{subtree}/{task_id}.json\")\n222-    if goal_id:\n223-        for subtree in _MIRROR_GOAL_SUBTREES:\n224-            locks.append(f\"path:.shiki/{subtree}/{goal_id}.json\")\n225-\n226-    shiki_root = (target / \".shiki\").resolve()\n227-    for ledger_id in task.get(\"ledger_evidence\") or []:\n228-        ledger_id = str(ledger_id)\n229-        if not ledger_id:\n230-            continue\n231-        try:\n232-            entry = load_json(target / \".shiki\" / \"ledger\" / f\"{ledger_id}.json\")\n233-        except (OSError, ValueError):\n234-            # Missing/malformed ledger: contribute nothing (fail open on read,\n235-            # matching the loop's own sync, and never widening).\n236-            continue\n237-        if not isinstance(entry, dict):\n238-            continue\n239-        # Bound to the task's OWN goal: a foreign goal_id is ignored so a\n240-        # PR-authored task file cannot pad ledger_evidence with someone else's\n241-        # ledger to inherit the .shiki paths its evidence references. A ledger\n242-        # explicitly owned by a DIFFERENT task of this goal is also ignored (the\n243-        # anti-padding property). But a ledger with an EMPTY task_id is a\n244-        # goal-level ledger of this goal \u2014 e.g. cmd_goal_complete's type:completion\n245-        # entry, whose evidence cites .shiki/reports/R-*.json \u2014 and the loop syncs\n246-        # it onto the closeout branch, so it is accepted.\n247-        entry_goal = str(entry.get(\"goal_id\") or \"\")\n248-        entry_task = str(entry.get(\"task_id\") or \"\")\n249-        if entry_goal != goal_id:\n250-            continue\n251-        if entry_task and entry_task != task_id:\n252-            continue\n253-        locks.append(f\"path:.shiki/ledger/{ledger_id}.json\")\n254-        for ref in entry.get(\"evidence\") or []:\n255-            ref = str(ref)\n256-            if not ref.startswith(\".shiki/\"):\n257-                continue\n258-            # Containment: a prefix check alone does not stop traversal\n259-            # ('.shiki/../x' passes startswith but resolves outside). Resolve and\n260-            # require the path to stay within .shiki, exactly as the loop's sync does.\n261-            candidate = (target / ref).resolve()\n262-            try:\n263-                candidate.relative_to(shiki_root)\n264-            except ValueError:\n265-                continue\n266-            locks.append(f\"path:{ref}\")\n745:# SADR-0017: bookkeeping-closeout Guardian exemption smuggling cases.\n747:# A closeout PR carries only a merged task's terminal state (task done, lock\n749:# scorecard) with zero implementation, so a PROVEN bookkeeping closeout is\n751:# Each smuggling case below is a CRITICAL-risk closeout with ONE defect that must\n764:CLOSE_BRANCH = \"shiki/t-c105e001-closeout\"\n814:        \"verdict\": \"complete\", \"summary\": \"closeout complete\",\n817:        \"checklist\": [{\"id\": \"CCA-01\", \"status\": \"pass\", \"blocking\": True, \"evidence\": \"closeout\"}],\n818:        \"acceptance\": [{\"criterion\": \"terminal state\", \"status\": \"pass\", \"evidence\": [\"closeout\"]}],\n819:        \"mergegate\": {\"closeout\": \"pass\"}, \"confidence\": 1.0, \"repair_packet\": None,\n823:def build_closeout(\n844:                \"status\": \"review\", \"expected_pr\": CLOSE_IMPL_PR, \"ledger_evidence\": [\"L-close-base\"]})\n853:    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n867:                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n943:# Clean control: a proven completing closeout of a CRITICAL task with NO Guardian\n945:expect_exempted(build_closeout(\"close-clean-completing\"))\n946:expect_exempted(build_closeout(\"close-clean-noncompleting\", completes=False))\n951:    build_closeout(\"close-smuggle-nonshiki\", extra_changed=[(\"M\", \"scripts/close_fixture_placeholder.py\")])\n955:    build_closeout(\"close-smuggle-governance\",\n960:    build_closeout(\"close-smuggle-delete\", extra_changed=[(\"D\", f\".shiki/worktrees/{CLOSE_TASK}.json\")])\n963:expect_guardian_fallback(build_closeout(\"close-smuggle-status\", goal_head_status=\"blocked\"))\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..0c6e55b45a13e2e62e2998b79937f3a2e76abad1\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +929,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..0ca5860fc75ec1cfe5228f477ab2e705c4a9993a\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +860,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:15:28.003641Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"rg -n -A35 'def ledger_entry_allowed_for_task' scripts/mergegate_check.py; sed -n '220,275p' scripts/mergegate_check.py\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n2650:def ledger_entry_allowed_for_task(entry: dict[str, Any], *, task_id: str, goal_id: str) -> bool:\n2651-    entry_task = str(entry.get(\"task_id\") or \"\").strip()\n2652-    entry_goal = str(entry.get(\"goal_id\") or \"\").strip()\n2653-    if entry_task == task_id:\n2654-        return True\n2655-    return not entry_task and entry_goal == goal_id\n2656-\n2657-\n2658-# The .shiki mirror directories governed by the GENERIC mirror-class rule: every\n2659-# tracked mirror directory that lacks a dedicated per-record rule of its own.\n2660-# tasks/goals/locks/ledger/repairs are handled by their own branches above, and\n2661-# plans has the extra spec_freeze guard below, so all six are excluded here. For\n2662-# these the rule is uniform: a PR may never delete a record, and every added or\n2663-# modified record must be scoped to the PR's own task or goal. Without it, a PR\n2664-# with a broad lock (path:.shiki/**) could delete a foreign goal's DAG or rewrite\n2665-# another goal's report and files-outside-locks would wave it through.\n2666-_GENERIC_MIRROR_DIRS: tuple[str, ...] = (\n2667-    \"dag\",\n2668-    \"reports\",\n2669-    \"memories\",\n2670-    \"runs\",\n2671-    \"worktrees\",\n2672-    \"handoffs\",\n2673-    \"runner\",\n2674-    \"smoke\",\n2675-    \"starts\",\n2676-    \"inbox\",\n2677-)\n2678-\n2679-\n2680-def _tracked_mirror_subdirs(manifest: dict[str, Any]) -> list[str]:\n2681-    \"\"\"The last path component of every tracked ``state_class: mirror`` directory\n2682-    in the manifest (e.g. ``.shiki/reports`` -> ``reports``). Drives the\n2683-    base-snapshot delete-protection loop so it covers every mirror directory, not\n2684-    only tasks/goals/locks. Order-stable.\n2685-\n        for subtree in _MIRROR_TASK_SUBTREES:\n            locks.append(f\"path:.shiki/{subtree}/{task_id}.json\")\n    if goal_id:\n        for subtree in _MIRROR_GOAL_SUBTREES:\n            locks.append(f\"path:.shiki/{subtree}/{goal_id}.json\")\n\n    shiki_root = (target / \".shiki\").resolve()\n    for ledger_id in task.get(\"ledger_evidence\") or []:\n        ledger_id = str(ledger_id)\n        if not ledger_id:\n            continue\n        try:\n            entry = load_json(target / \".shiki\" / \"ledger\" / f\"{ledger_id}.json\")\n        except (OSError, ValueError):\n            # Missing/malformed ledger: contribute nothing (fail open on read,\n            # matching the loop's own sync, and never widening).\n            continue\n        if not isinstance(entry, dict):\n            continue\n        # Bound to the task's OWN goal: a foreign goal_id is ignored so a\n        # PR-authored task file cannot pad ledger_evidence with someone else's\n        # ledger to inherit the .shiki paths its evidence references. A ledger\n        # explicitly owned by a DIFFERENT task of this goal is also ignored (the\n        # anti-padding property). But a ledger with an EMPTY task_id is a\n        # goal-level ledger of this goal \u2014 e.g. cmd_goal_complete's type:completion\n        # entry, whose evidence cites .shiki/reports/R-*.json \u2014 and the loop syncs\n        # it onto the closeout branch, so it is accepted.\n        entry_goal = str(entry.get(\"goal_id\") or \"\")\n        entry_task = str(entry.get(\"task_id\") or \"\")\n        if entry_goal != goal_id:\n            continue\n        if entry_task and entry_task != task_id:\n            continue\n        locks.append(f\"path:.shiki/ledger/{ledger_id}.json\")\n        for ref in entry.get(\"evidence\") or []:\n            ref = str(ref)\n            if not ref.startswith(\".shiki/\"):\n                continue\n            # Containment: a prefix check alone does not stop traversal\n            # ('.shiki/../x' passes startswith but resolves outside). Resolve and\n            # require the path to stay within .shiki, exactly as the loop's sync does.\n            candidate = (target / ref).resolve()\n            try:\n                candidate.relative_to(shiki_root)\n            except ValueError:\n                continue\n            locks.append(f\"path:{ref}\")\n    return list(dict.fromkeys(locks))\n\n\ndef goal_reconcile_decision(pr: dict[str, Any]) -> tuple[bool, str | None]:\n    \"\"\"Decide whether a PR runs in goal_reconcile mode.\n\n    Requires BOTH the body marker (declares intent) AND a maintainer-applied\n    label (authorizes the relaxed scope). A marker without the label fails closed\n    with an error so untrusted PR text cannot self-grant the relaxed mode.\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..0c6e55b45a13e2e62e2998b79937f3a2e76abad1\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +929,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..0ca5860fc75ec1cfe5228f477ab2e705c4a9993a\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,14 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +860,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(S / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:15:31.871767Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/test_shiki_adversarial_state.sh\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/test_shiki_governance_evidence.sh\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..9909ba16a3f8a314577db1ce1414c6533270a800\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..9909ba16a3f8a314577db1ce1414c6533270a800\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:15:46.776165Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_governance_evidence.sh' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..9909ba16a3f8a314577db1ce1414c6533270a800\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:15:53.850839Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 3947ms:\ngovernance evidence adversarial fixtures passed\n\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_adversarial_state.sh' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..9909ba16a3f8a314577db1ce1414c6533270a800\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:16:04.674090Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 4984ms:\nTraceback (most recent call last):\n  File \"<stdin>\", line 1508, in <module>\n  File \"<stdin>\", line 20, in assert_contains\nAssertionError: expected 'migration M-20260801-0002-attacker is not registered in the base branch' in:\nmigration mode requires the workflow-resolved immutable mergeBaseOid to resolve the trusted migration registry\n\ncodex\nThe governance shell fixture is now green. The adversarial suite moved to the next intended contract change: its migration-mode fixture still omits `mergeBaseOid`, so it now fails earlier with the new immutable-base requirement instead of reaching the attacker-registry assertion. I\u2019m pinning that fixture to its actual base commit so the test continues exercising the intended self-registration denial.\nexec\n/bin/zsh -lc \"rg -n -C 20 'attacker is not registered|attacker|mergeBaseOid|migration mode requires' scripts/test_shiki_adversarial_state.sh | tail -160\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n1409-    shiki_loop._dispatch = _orig_dispatch\n1410-\n1411-\n1412-# ============================================================================\n1413-# Migration mode (repository-wide re-run proof). Both cases run against a REAL\n1414-# git repository, so the BASE-registry resolution \u2014 the security property, that\n1415-# the migration function is read from the base branch and NEVER the PR head \u2014\n1416-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n1417-#\n1418-#   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n1419-#      and declares it must NOT pass, because the id is absent from the base\n1420-#      registry (a PR cannot author the migration whose output it is measured\n1421-#      against).\n1422-#   2. One-byte deviation: a conforming PR whose .shiki output differs from the\n1423-#      re-run by a single byte in one file is blocked with a reason naming that\n1424-#      file.\n1425-# ============================================================================\n1426-import mergegate_check\n1427-\n1428-MIG_ID = \"M-20260801-0001-cca-profile-backfill\"\n1429:ATTACK_ID = \"M-20260801-0002-attacker\"\n1430-MIG_TASKS = (\"T-20260801T000000000000Z-0000a001\", \"T-20260801T000000000000Z-0000a002\")\n1431-MIG_STATE = {\n1432-    \"version\": 1,\n1433-    \"source_of_truth\": \"x\",\n1434-    \"applied\": [{\"id\": \"M-20260604-0001-baseline\", \"status\": \"applied\", \"applied_at\": \"t\", \"actor\": \"a\", \"summary\": \"s\"}],\n1435-}\n1436-\n1437-\n1438-def _mig_git(cwd: pathlib.Path, *args: str) -> None:\n1439-    subprocess.run([\"git\", *args], cwd=str(cwd), check=True, capture_output=True, text=True)\n1440-\n1441-\n1442-def _mig_init(repo: pathlib.Path) -> None:\n1443-    repo.mkdir(parents=True)\n1444-    _mig_git(repo, \"init\", \"-q\", \"-b\", \"main\")\n1445-    _mig_git(repo, \"config\", \"user.email\", \"t@t\")\n1446-    _mig_git(repo, \"config\", \"user.name\", \"t\")\n1447-\n1448-\n1449-def _mig_module_src(ids) -> str:\n--\n1480-    return (__BODY__,)\n1481-'''\n1482-    return template.replace(\"__BODY__\", body)\n1483-\n1484-\n1485-def _mig_seed_base_shiki(root: pathlib.Path) -> None:\n1486-    for task_id in MIG_TASKS:\n1487-        write_json(root / \".shiki\" / \"tasks\" / f\"{task_id}.json\", {\"id\": task_id, \"goal_id\": \"G-mig\", \"title\": task_id, \"status\": \"planned\"})\n1488-    write_json(root / \".shiki\" / \"migrations\" / \"state.json\", MIG_STATE)\n1489-\n1490-\n1491-def _mig_changes():\n1492-    return [mergegate_check.ChangedFile(\"M\", f\".shiki/tasks/{task_id}.json\") for task_id in MIG_TASKS]\n1493-\n1494-\n1495-# Case 1: the self-defining-migration attempt.\n1496-with tempfile.TemporaryDirectory(dir=tmp_root) as _mig_tmp:\n1497-    repo = pathlib.Path(_mig_tmp) / \"repo\"\n1498-    _mig_init(repo)\n1499-    (repo / \"scripts\").mkdir()\n1500:    # Base branch registers ONLY the backfill migration, not the attacker one.\n1501-    (repo / \"scripts\" / \"shiki_migrations.py\").write_text(_mig_module_src([MIG_ID]), encoding=\"utf-8\")\n1502-    _mig_seed_base_shiki(repo)\n1503-    _mig_git(repo, \"add\", \"-A\")\n1504-    _mig_git(repo, \"commit\", \"-qm\", \"base\")\n1505-    # The PR head defines a brand-new migration and delivers a .shiki change,\n1506-    # declaring the id it just invented.\n1507-    _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n1508-    (repo / \"scripts\" / \"shiki_migrations.py\").write_text(_mig_module_src([MIG_ID, ATTACK_ID]), encoding=\"utf-8\")\n1509-    for task_id in MIG_TASKS:\n1510-        _path = repo / \".shiki\" / \"tasks\" / f\"{task_id}.json\"\n1511-        _data = read_json(_path)\n1512-        _data[\"cca_checklist_profile\"] = []\n1513-        write_json(_path, _data)\n1514-    _blocking = []\n1515-    mergegate_check.enforce_migration(\n1516-        target=repo,\n1517-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n1518-        base_shiki=repo / \".shiki\",\n1519-        changed_files_status=_mig_changes(),\n1520-        blocking=_blocking,\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..9909ba16a3f8a314577db1ce1414c6533270a800\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:16:10.910897Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1490,1585p' scripts/test_shiki_adversarial_state.sh\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n\ndef _mig_changes():\n    return [mergegate_check.ChangedFile(\"M\", f\".shiki/tasks/{task_id}.json\") for task_id in MIG_TASKS]\n\n\n# Case 1: the self-defining-migration attempt.\nwith tempfile.TemporaryDirectory(dir=tmp_root) as _mig_tmp:\n    repo = pathlib.Path(_mig_tmp) / \"repo\"\n    _mig_init(repo)\n    (repo / \"scripts\").mkdir()\n    # Base branch registers ONLY the backfill migration, not the attacker one.\n    (repo / \"scripts\" / \"shiki_migrations.py\").write_text(_mig_module_src([MIG_ID]), encoding=\"utf-8\")\n    _mig_seed_base_shiki(repo)\n    _mig_git(repo, \"add\", \"-A\")\n    _mig_git(repo, \"commit\", \"-qm\", \"base\")\n    # The PR head defines a brand-new migration and delivers a .shiki change,\n    # declaring the id it just invented.\n    _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n    (repo / \"scripts\" / \"shiki_migrations.py\").write_text(_mig_module_src([MIG_ID, ATTACK_ID]), encoding=\"utf-8\")\n    for task_id in MIG_TASKS:\n        _path = repo / \".shiki\" / \"tasks\" / f\"{task_id}.json\"\n        _data = read_json(_path)\n        _data[\"cca_checklist_profile\"] = []\n        write_json(_path, _data)\n    _blocking = []\n    mergegate_check.enforce_migration(\n        target=repo,\n        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n        base_shiki=repo / \".shiki\",\n        changed_files_status=_mig_changes(),\n        blocking=_blocking,\n        warnings=[],\n    )\n    assert_contains(\"\\n\".join(_blocking), f\"migration {ATTACK_ID} is not registered in the base branch\")\n\n# Case 2: a one-byte deviation from the re-run output.\nwith tempfile.TemporaryDirectory(dir=tmp_root) as _mig_tmp:\n    _base = pathlib.Path(_mig_tmp)\n    repo = _base / \"repo\"\n    _mig_init(repo)\n    (repo / \"scripts\").mkdir()\n    (repo / \"scripts\" / \"shiki_migrations.py\").write_text(_mig_module_src([MIG_ID]), encoding=\"utf-8\")\n    _mig_seed_base_shiki(repo)\n    _mig_git(repo, \"add\", \"-A\")\n    _mig_git(repo, \"commit\", \"-qm\", \"base\")\n    # The unmodified base .shiki snapshot the gate re-runs against.\n    base_snap = _base / \"base-shiki\"\n    shutil.copytree(repo / \".shiki\", base_snap)\n    # The PR head applies the (base-registered) migration, then flips ONE byte in\n    # exactly one produced file.\n    _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n    for task_id in MIG_TASKS:\n        _path = repo / \".shiki\" / \"tasks\" / f\"{task_id}.json\"\n        _data = read_json(_path)\n        _data[\"cca_checklist_profile\"] = []\n        write_json(_path, _data)\n    _deviant = repo / \".shiki\" / \"tasks\" / f\"{MIG_TASKS[1]}.json\"\n    _bytes = _deviant.read_bytes()\n    _deviant.write_bytes(_bytes[:-2] + b\"X\" + _bytes[-1:])\n    _blocking = []\n    mergegate_check.enforce_migration(\n        target=repo,\n        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n        base_shiki=base_snap,\n        changed_files_status=_mig_changes(),\n        blocking=_blocking,\n        warnings=[],\n    )\n    _joined = \"\\n\".join(_blocking)\n    assert_contains(_joined, \"byte-for-byte\")\n    assert_contains(_joined, f\".shiki/tasks/{MIG_TASKS[1]}.json\")\n    # The conforming file must NOT be flagged: exactly the deviant file is named.\n    if f\".shiki/tasks/{MIG_TASKS[0]}.json\" in _joined:\n        raise AssertionError(f\"the conforming file must not be flagged: {_blocking}\")\n\n\nprint(\"adversarial state/evidence/lock regression suite passed\")\nPY\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..9909ba16a3f8a314577db1ce1414c6533270a800\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:16:14.859855Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/scripts/test_shiki_adversarial_state.sh\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_adversarial_state.sh' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n succeeded in 5063ms:\nadversarial state/evidence/lock regression suite passed\n\nexec\n/bin/zsh -lc 'for s in scripts/test_shiki_*.sh; do bash \"$s\" || exit 1; done' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:17:55.683529Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:18:15.686777Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:18:30.427652Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:18:50.431054Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:19:02.598869Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ncodex\nThe two repaired shell fixtures now pass individually, and the complete shell sweep is still running cleanly through the control-plane, daemon, goal-loop, governance, Guardian, and branch-protection suites. I\u2019m letting the remaining scripts finish before starting the review gate.\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:19:56.133504Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:20:08.326751Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:20:28.330556Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:20:40.995922Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:21:01.000589Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:21:04.499261Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 267094ms:\nadversarial state/evidence/lock regression suite passed\nShiki validation passed\nShiki validation passed\nshiki code-review gate tests passed\nShiki validation passed\nShiki validation passed\nShiki validation passed\nshiki control-plane tests passed\nShiki validation passed\nShiki validation passed\nshiki daemon runner tests passed\nshiki doctor tests passed\nevidence manifest and ledger helper fixtures passed\nshiki evidence integrity tests passed\nShiki validation passed\nhint: Using 'master' as the name for the initial branch. This default branch name\nhint: will change to \"main\" in Git 3.0. To configure the initial branch name\nhint: to use in all of your new repositories, which will suppress this warning,\nhint: call:\nhint:\nhint: \tgit config --global init.defaultBranch <name>\nhint:\nhint: Names commonly chosen instead of 'master' are 'main', 'trunk' and\nhint: 'development'. The just-created branch can be renamed via this command:\nhint:\nhint: \tgit branch -m <name>\nhint:\nhint: Disable this message with \"git config set advice.defaultBranchName false\"\nshiki goal loop tests passed\ngovernance evidence adversarial fixtures passed\nguardian policy evaluator fixtures passed\ncontract approval end-to-end carry fixtures passed\nguardian status base-sync carry fixtures passed\nshiki guardian policy tests passed\nShiki validation passed\nShiki validation passed\n[shiki] configured branch protection for main\nshould_skip leases-filter assertions passed\ninstall-and-verify passed: fresh target passes its own required checks\nshiki init tests passed\nShiki validation passed\nhint: Using 'master' as the name for the initial branch. This default branch name\nhint: will change to \"main\" in Git 3.0. To configure the initial branch name\nhint: to use in all of your new repositories, which will suppress this warning,\nhint: call:\nhint:\nhint: \tgit config --global init.defaultBranch <name>\nhint:\nhint: Names commonly chosen instead of 'master' are 'main', 'trunk' and\nhint: 'development'. The just-created branch can be renamed via this command:\nhint:\nhint: \tgit branch -m <name>\nhint:\nhint: Disable this message with \"git config set advice.defaultBranchName false\"\ne2e: loop-owned T3 code-review THEN T2 tdd-evidence ledgers, in the implemented order\nshiki loop e2e: integrated self-drive (code-review -> TDD -> commit/push -> auto-merge) + post-PR repair verified\nloop-lock-guard: ok\nShiki manifest tests passed\nShiki validation passed\nShiki validation passed\nShiki validation passed\nShiki validation passed\nvalidator poisoned-memory rejection checks passed\nshiki memory loop tests passed\nshiki migration tests passed\nmodule import and staging smoke passed\nshiki module boundary tests passed\nP2.3.8 branch protection failure handling passed\nP2.3.7 required check name drift detection passed\nP2.3.4 fork PR limitation handling passed\nP2.3.3 malicious protected-path PR rejection passed\nP2.3.5 missing CCA token handling passed\nrepair packet content satisfies schema\nP2.3.9 repair attempt-limit and packet content passed\nP2.3.6 CCA schema path drift detection passed\nprovider config basics passed\nshiki provider config tests passed\nShiki validation passed\nShiki validation passed\nshiki run orchestrator tests passed\nShiki validation passed\nshiki runner claude tests passed\nShiki validation passed\nshiki runner codex tests passed\nShiki validation passed\nshiki runtime auth tests passed\nshiki runtime registry tests passed\nShiki shellcheck passed\nShiki validation passed\nShiki validation passed\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/bin/shiki\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/AGENTS.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/CLAUDE.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/CONTEXT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/SYSTEM_PROMPT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.gitignore\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.claude/commands/shiki.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.codex/skills/shiki/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/migrations/state.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/guardian-policy.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/config.yaml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/runner-record.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/report.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/dispatch-guard.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/external-ai-guardian-review-packet.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/run.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/worktree-record.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/cca-evidence-manifest.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/cca-verdict.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/plan.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/inbox.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/memory-entry.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/repair-packet.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/smoke.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/start.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/ledger.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/task.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/dag.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/schemas/goal.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/README.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/manifest.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/LEDGER_ENTRY.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/CCA_VERDICT.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/GOAL.template.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/REPORT.template.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/DAG.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/worktree-record.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/REPAIR_PACKET.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/templates/TASK.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.shiki/policy.example.yaml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/ISSUE_TEMPLATE/shiki_goal.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/ISSUE_TEMPLATE/shiki_task.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/CODEOWNERS\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/prompts/cca-completion-check.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/prompts/codex-repair-handoff.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/prompts/codex-task-handoff.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/workflows/shiki-validate.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/workflows/shiki-claude-review.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/workflows/shiki-cca-completion.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/workflows/shiki-mergegate.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/.github/workflows/shiki-orchestrator.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/node24-workflow-compatibility.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/checklists.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/codex-handoff.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/bootstrap-command.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/guardian-policy.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/autonomous-loop-e2e-acceptance.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/runtime-auth-model.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/shiki-doctor.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/control-commands.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/memory.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/shiki-migrations.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/decision-control.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/domain.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/skill-gate.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/github-provider-config.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/state-classes.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/completion-check-agent.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/runtime-registry.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/implementation-policy.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/triage-labels.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/shiki-cli-architecture.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/issue-tracker.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/execution-control.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/branch-protection-smoke.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/ledger-schema.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/agents/evidence-integrity.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/template.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0016-locks-bound-product-paths.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/README.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0007-packaging-and-release.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0010-external-ai-guardian-review.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/shiki/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/setup-matt-pocock-skills/domain.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/setup-matt-pocock-skills/triage-labels.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/setup-matt-pocock-skills/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/code-review/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/improve-codebase-architecture/LANGUAGE.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/improve-codebase-architecture/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/improve-codebase-architecture/DEEPENING.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/triage/OUT-OF-SCOPE.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/triage/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/triage/AGENT-BRIEF.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/prototype/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/prototype/LOGIC.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/prototype/UI.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/diagnose/scripts/hitl-loop.template.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/diagnose/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/to-issues/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/README.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/zoom-out/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/grill-with-docs/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/grill-with-docs/ADR-FORMAT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/tdd/mocking.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/tdd/deep-modules.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/tdd/tests.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/tdd/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/tdd/interface-design.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/tdd/refactoring.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/skills/engineering/to-prd/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_lock_release.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_verdict_usable.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_id_allocation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_mirror_scope.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_config_loading.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_verdict_nondegenerate.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_reopened_task.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_required_checks_derivation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_install_completeness.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_policy_job_merged_prs.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_rerun_economy.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_adr_references.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_session_lease.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_signal.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_verdict_consistency.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_head_sha_injection.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_sync_proof.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_carry.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_memory_scorecard.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_workflow_permissions_payload.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_contract_mode.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_memory_consult.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_workflow_invocation_carry.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_lock_conflict_stop.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_migrations_target.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_branch_protection_payload.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_branch_plumbing.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_contract_immutability.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_smoke.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_worktree_base.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_secret_set_claude.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_completeness.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_paired_invariants.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/__init__.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_bookkeeping_closeout.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_contract_open.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_signal_closeout.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_cca_evidence_manifest_taskless.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_codeowners_wiring.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_review.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/shiki_test_support.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_approval_binding.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_completion_lock_scope.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_memory_promotion.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_code_review.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_verdict_gating.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_lock_lifecycle.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_memory_capture.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_tdd_evidence.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_migration_mode.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_dispatch_lock_precondition.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_dispatch_state_sync.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_goal_reconcile.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_runner_adapters.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_lock_matching.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_contract_approval.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_memory_hooks.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_branch_behind_sync.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_install_force_semantics.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_contract_plan_registration.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_sadr_install_migration.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_engine.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_repair_delivery.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_handoff_consult.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_smoke3.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_contract_amendment.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_bootstrap_safety.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_carry_parity.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_evidence.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_smoke2.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_lock_guard.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_manifest_staging.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_loop_lock_validator_hint.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_guardian_status.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_workflow_invocation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_closeout.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_mirror_schema_validation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/tests/test_repair_reasons.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_schema.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/validate_shiki.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_contracts.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_jsonschema.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_evidence.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_locks.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_loop.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_memory.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_manifest.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_migrations.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_provider.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_workflows.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/enforce_cca_verdict.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/cca_verdict_usable.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/build_cca_evidence_manifest.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/guardian_approval_signal.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/mergegate_check.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_sync_proof.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_bootstrap.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_cli.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_config.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_contract_approval.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_doctor.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_git.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_github.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_guardian.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_guardian_review.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_guardian_status.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_installer.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_process.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_runtime.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_runtime_adapters.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_runtime_registry.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_session_lease.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_state_classes.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_tasks.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/shiki_state.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_init.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_control_plane.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_run_orchestrator.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_daemon_runner.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_runner_codex.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_runner_claude.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_goal_loop.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_memory_loop.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_code_review_gate.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_start.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_runtime_auth.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_runtime_registry.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_state_classes.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_provider_config.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_guardian_policy.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_evidence_integrity.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_governance_evidence.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_doctor.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_migrations.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_module_boundaries.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_shellcheck.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_validator_hardening.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-protect/scripts/test_shiki_workflow_lint.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/bin/shiki\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/AGENTS.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/CLAUDE.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/CONTEXT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/SYSTEM_PROMPT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.gitignore\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.claude/commands/shiki.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.codex/skills/shiki/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/migrations/state.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/guardian-policy.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/config.yaml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/runner-record.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/report.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/dispatch-guard.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/external-ai-guardian-review-packet.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/run.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/worktree-record.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/cca-evidence-manifest.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/cca-verdict.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/plan.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/inbox.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/memory-entry.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/repair-packet.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/smoke.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/start.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/ledger.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/task.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/dag.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/schemas/goal.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/README.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/manifest.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/LEDGER_ENTRY.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/CCA_VERDICT.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/GOAL.template.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/REPORT.template.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/DAG.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/worktree-record.schema.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/REPAIR_PACKET.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/templates/TASK.template.json\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.shiki/policy.example.yaml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/ISSUE_TEMPLATE/shiki_goal.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/ISSUE_TEMPLATE/shiki_task.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/CODEOWNERS\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/prompts/cca-completion-check.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/prompts/codex-repair-handoff.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/prompts/codex-task-handoff.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/workflows/shiki-validate.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/workflows/shiki-claude-review.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/workflows/shiki-cca-completion.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/workflows/shiki-mergegate.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/.github/workflows/shiki-orchestrator.yml\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/node24-workflow-compatibility.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/checklists.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/codex-handoff.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/bootstrap-command.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/guardian-policy.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/autonomous-loop-e2e-acceptance.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/runtime-auth-model.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/shiki-doctor.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/control-commands.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/memory.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/shiki-migrations.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/decision-control.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/domain.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/skill-gate.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/github-provider-config.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/state-classes.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/completion-check-agent.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/runtime-registry.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/implementation-policy.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/triage-labels.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/shiki-cli-architecture.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/issue-tracker.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/execution-control.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/branch-protection-smoke.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/ledger-schema.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/agents/evidence-integrity.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/template.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0016-locks-bound-product-paths.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/README.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0007-packaging-and-release.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0010-external-ai-guardian-review.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/shiki/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/setup-matt-pocock-skills/domain.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/setup-matt-pocock-skills/triage-labels.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/setup-matt-pocock-skills/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/code-review/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/improve-codebase-architecture/LANGUAGE.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/improve-codebase-architecture/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/improve-codebase-architecture/DEEPENING.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/triage/OUT-OF-SCOPE.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/triage/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/triage/AGENT-BRIEF.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/prototype/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/prototype/LOGIC.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/prototype/UI.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/diagnose/scripts/hitl-loop.template.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/diagnose/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/to-issues/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/README.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/zoom-out/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/grill-with-docs/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/grill-with-docs/ADR-FORMAT.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/tdd/mocking.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/tdd/deep-modules.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/tdd/tests.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/tdd/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/tdd/interface-design.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/tdd/refactoring.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/skills/engineering/to-prd/SKILL.md\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_lock_release.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_verdict_usable.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_id_allocation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_mirror_scope.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_config_loading.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_verdict_nondegenerate.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_reopened_task.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_required_checks_derivation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_install_completeness.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_policy_job_merged_prs.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_rerun_economy.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_adr_references.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_session_lease.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_signal.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_verdict_consistency.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_head_sha_injection.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_sync_proof.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_carry.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_memory_scorecard.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_workflow_permissions_payload.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_contract_mode.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_memory_consult.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_workflow_invocation_carry.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_lock_conflict_stop.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_migrations_target.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_branch_protection_payload.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_branch_plumbing.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_contract_immutability.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_smoke.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_worktree_base.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_secret_set_claude.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_completeness.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_paired_invariants.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/__init__.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_bookkeeping_closeout.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_contract_open.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_signal_closeout.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_cca_evidence_manifest_taskless.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_codeowners_wiring.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_review.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/shiki_test_support.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_approval_binding.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_completion_lock_scope.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_memory_promotion.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_code_review.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_verdict_gating.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_lock_lifecycle.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_memory_capture.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_tdd_evidence.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_migration_mode.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_dispatch_lock_precondition.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_dispatch_state_sync.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_goal_reconcile.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_runner_adapters.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_lock_matching.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_contract_approval.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_memory_hooks.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_branch_behind_sync.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_install_force_semantics.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_contract_plan_registration.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_sadr_install_migration.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_engine.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_repair_delivery.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_handoff_consult.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_smoke3.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_contract_amendment.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_bootstrap_safety.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_carry_parity.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_evidence.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_smoke2.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_lock_guard.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_manifest_staging.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_loop_lock_validator_hint.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_guardian_status.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_workflow_invocation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_closeout.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_mirror_schema_validation.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/tests/test_repair_reasons.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_schema.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/validate_shiki.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_contracts.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_jsonschema.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_evidence.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_locks.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_loop.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_memory.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_manifest.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_migrations.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_provider.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_workflows.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/enforce_cca_verdict.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/cca_verdict_usable.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/build_cca_evidence_manifest.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/guardian_approval_signal.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/mergegate_check.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_sync_proof.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_bootstrap.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_cli.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_config.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_contract_approval.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_doctor.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_git.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_github.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_guardian.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_guardian_review.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_guardian_status.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_installer.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_process.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_runtime.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_runtime_adapters.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_runtime_registry.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_session_lease.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_state_classes.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_tasks.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/shiki_state.py\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_init.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_control_plane.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_run_orchestrator.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_daemon_runner.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_runner_codex.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_runner_claude.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_goal_loop.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_memory_loop.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_code_review_gate.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_start.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_runtime_auth.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_runtime_registry.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_state_classes.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_provider_config.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_guardian_policy.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_evidence_integrity.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_governance_evidence.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_doctor.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_migrations.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_module_boundaries.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_shellcheck.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_validator_hardening.sh\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-51320/retry-secret/scripts/test_shiki_workflow_lint.sh\nshiki start tests passed\nshiki state class tests passed\nShiki validator hardening tests passed\nShiki workflow actionlint passed\n\ncodex\nAll `scripts/test_shiki_*.sh` now pass end to end. The install tests also revealed the repository-local required `code-review` skill at `skills/engineering/code-review/SKILL.md`; I\u2019m loading and applying that actual gate now (superseding my earlier fallback assumption), then I\u2019ll rerun the final validation set against the reviewed diff.\nexec\n/bin/zsh -lc \"sed -n '1,320p' skills/engineering/code-review/SKILL.md\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n---\nname: code-review\ndescription: Mandatory pre-PR self-review gate for Shiki implementers. Run after the TDD loop is green and before creating or updating a PR. Reviews the task diff for correctness bugs and reuse/simplification/efficiency cleanups, applies fixes on the task branch, and records durable evidence.\n---\n\n# Code Review (Pre-PR Implementer Gate)\n\nThis is the Shiki wrapper for the implementer's native code-review capability.\nIn Claude Code it is satisfied by the built-in `/code-review` skill; another\nAgent Runtime may satisfy it with an equivalent diff review that produces the\nsame evidence.\n\n## Position In The Loop\n\n```\ntdd (red-green-refactor, all checks green)\n  -> code-review (this gate: review the full task diff, apply fixes)\n  -> re-run verification\n  -> PR creation or update\n```\n\nThe gate runs in the **Implementer** role on the implementer's own task\nbranch, before any PR exists or before pushing new commits to an existing PR.\nApplying fixes here does not violate the Reviewer rule (\"reviewers do not\nmutate the implementation branch\") because no review handoff has happened yet.\n\n## How To Run\n\n1. Confirm the TDD loop is complete and verification commands pass.\n2. Review the entire task diff against the task contract (scope, non-goals,\n   acceptance checks, locks). In Claude Code, invoke the native `/code-review`\n   skill at an effort matching task risk: low/medium risk \u2192 medium effort;\n   high/critical risk \u2192 high or max effort. The cloud multi-agent deep review\n   (ultra) is operator-triggered and not required by this gate.\n3. Triage findings:\n   - Correctness bugs, contract violations, scope drift: fix now.\n   - Quality cleanups (reuse, simplification, efficiency): fix when bounded\n     to the task scope; otherwise record as a follow-up note.\n   - Findings you reject: record why.\n4. Re-run the task's verification commands after fixes.\n5. Record evidence (below). Only then create or update the PR.\n\n## Required Evidence\n\nBoth of these are required; missing evidence blocks MergeGate readiness:\n\n- A Ledger entry whose summary names this skill (`code-review`) and states\n  findings count, applied fixes, and rejected findings with reasons. List the\n  entry in the task's `ledger_evidence`.\n- A `## Pre-PR code review` section in the PR body summarizing findings and\n  resolutions (or stating \"no findings\" when the review was clean).\n\n## Exceptions\n\nEvidence-only tasks, pure `.shiki` state reconciliation, and trivial\ndocumentation-only changes may skip the gate when the skip is stated in the\nPR body and recorded in the ledger. CCA may treat an unjustified skip as\n`insufficient_evidence`.\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:21:13.847609Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260817T100648130920Z-5568a6ab.json 2>/dev/null || true; sed -n '1,140p' .shiki/tasks/T-20260817T063758155247Z-0b11207b.json\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n{\n  \"acceptance_checks\": [\n    \"A fork PR snapshot (isCrossRepository: true) denies the SADR-0017 exemption at BOTH enforcement points.\",\n    \"A snapshot with isCrossRepository ABSENT also denies the exemption at both points \\u2014 fail-closed.\",\n    \"Neither enforcement point contains an `and pr` short-circuit before the field test.\",\n    \"With the exemption denied, _bookkeeping_closeout_exemption resolves the task's real risk_level, not \\\"low\\\".\",\n    \"Every gh pr view --json field list in shiki-mergegate.yml and shiki-cca-completion.yml contains isCrossRepository.\",\n    \"grep -c \\\"lockless\\\\|lock_terminal\\\" scripts/mergegate_check.py == 0, and `if not task_changed or not lock_released:` is present verbatim.\",\n    \"Symlinked, renamed, type-changed and coerced-expected_pr closeout fixtures all return False.\",\n    \"A goal/task risk pair (medium/critical) produces report[\\\"mergegate\\\"][\\\"risk\\\"] == \\\"critical\\\" and the closeout classifier's equality check passes on it.\",\n    \"A gate that resolves a base ref uses the immutable mergeBaseOid, and a failed resolution is not swallowed by `|| true`.\",\n    \"tests/test_workflow_invocation_carry.py still asserts count(\\\"git fetch origin\\\") == 2 in the CCA workflow, == 1 in the mergegate workflow, and total == 4.\",\n    \"docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md is unchanged, and SADR-0012 step 3 is not published.\",\n    \"python3 -m unittest discover -s tests, validate_shiki, and every scripts/test_shiki_*.sh pass.\",\n    \"No file outside the declared locks is changed.\"\n  ],\n  \"assigned_runtime\": \"codex\",\n  \"dependencies\": [],\n  \"expected_branch\": \"shiki/t-20260817t063758155247z-0b11207b-deny-the-sadr-0017-exemption-to-fork-prs-and-pin\",\n  \"expected_pr\": null,\n  \"github_issue\": null,\n  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n  \"id\": \"T-20260817T063758155247Z-0b11207b\",\n  \"ledger_evidence\": [\n    \"L-20260817T063758155305Z-197baec8\",\n    \"L-20260817T100648130920Z-5568a6ab\"\n  ],\n  \"locks\": [\n    \"path:scripts/mergegate_check.py\",\n    \"path:scripts/guardian_approval_signal.py\",\n    \"path:scripts/shiki_tasks.py\",\n    \"path:.github/workflows/shiki-mergegate.yml\",\n    \"path:.github/workflows/shiki-cca-completion.yml\",\n    \"path:tests/test_bookkeeping_closeout.py\",\n    \"path:tests/test_closeout.py\",\n    \"path:tests/test_guardian_signal_closeout.py\",\n    \"path:tests/test_paired_invariants.py\",\n    \"path:tests/test_memory_scorecard.py\",\n    \"path:tests/test_policy_job_merged_prs.py\",\n    \"path:tests/test_migration_mode.py\",\n    \"path:docs/agents/checklists.md\",\n    \"path:docs/agents/decision-control.md\",\n    \"path:docs/agents/triage-labels.md\",\n    \"path:docs/agents/implementation-policy.md\",\n    \"path:docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\",\n    \"path:scripts/test_shiki_governance_evidence.sh\",\n    \"path:scripts/test_shiki_adversarial_state.sh\"\n  ],\n  \"non_goals\": [\n    \"Refluxing the `and pr` short-circuit\",\n    \"Adding lockless/lock_terminal, or weakening `if not task_changed or not lock_released:`\",\n    \"Guarding the contract_approval carry or the SADR-0018 base-sync carry\",\n    \"SADR-0012 step 3, or any change to SADR-0017 itself\",\n    \"exact_head_only, normal_frozen_spec_errors, exact_pr_link, spec_amendment, delivery_only\",\n    \"Binding _review_source to a head SHA\",\n    \"Adding a second git fetch origin, or weakening the 2/1 fetch counts or total == 4\",\n    \"Touching any file outside the declared locks\"\n  ],\n  \"required_skills\": [\n    \"tdd\",\n    \"code-review\"\n  ],\n  \"risk_level\": \"high\",\n  \"scope\": \"Port five capabilities from mizutani-140/CubeShare into this platform repository, one of them CORRECTED rather than copied.\\n\\n(a) FORK GUARD \\u2014 the reason this task exists. Measured on origin/main: `git grep -c isCrossRepository -- scripts/ .github/ tests/` returns NOTHING (0 occurrences repo-wide; the same grep returns 63 files for `bookkeeping_closeout`, so the search works). And `mergegate_check.py` computes `carry_resolved_risk = \\\"low\\\" if bookkeeping_closeout else str((task or {}).get(\\\"risk_level\\\") or \\\"\\\")`. A PR shaped as a SADR-0017 bookkeeping closeout therefore resolves to `low` and skips the Guardian gate at any real risk level. This repository is PUBLIC with `allow_forking: true`, so a fork PR is an unauthenticated path to that shape.\\n\\nAdd the guard at BOTH enforcement points, in the `guardian_approval_signal.py` form:\\n\\n    if exemption and pr.get(\\\"isCrossRepository\\\") is not False:\\n        exemption = False\\n\\nDO NOT copy CubeShare's `mergegate_check.py:3600`, which reads `if bookkeeping_closeout and pr and pr.get(...) is not False:`. That `and pr` short-circuits the guard on a falsy `pr`. On the Guardian leg the branch is unreachable, but `bookkeeping_closeout` also flows to `_frozen_spec_mutable_task_fields(...)` at `cs:3608`, under `if not migration_mode:` and OUTSIDE any `pr` guard. Use the un-short-circuited form at both sites.\\n\\nThe guard is fail-closed by design: `is not False` means absent/null/anything-but-`False` collapses the exemption, restoring the task's real risk. That is strictly stricter, never a bypass \\u2014 but it means EVERY `gh pr view --json` field list that produces a PR snapshot must request `isCrossRepository`, or the exemption silently stops working on that path. The Python and every workflow field list MUST land in ONE commit.\\n\\nGuard scope, so it is not overclaimed: SADR-0017 bookkeeping exemption ONLY. The contract_approval carry and the SADR-0018 base-sync carry are out of scope.\\n\\n(b) IMMUTABLE MERGE-BASE PINNING. Resolve `mergeBaseOid` once in each gate workflow from `baseRefOid` plus `git merge-base`, inject it into the PR snapshot with jq, consume it in `mergegate_check.py` (a `_git_show_base`-style reader), and read the frozen tree with `git archive \\\"$merge_base\\\" .shiki`. Remove the `|| true` that swallows a failed resolution. Python and both workflows must land together or every migration PR blocks on the missing field.\\n\\n(c) CLOSEOUT CLASSIFIER HARDENING, MINUS THE RELAXATION. Port CubeShare's `is_bookkeeping_closeout` hardening: symlink-safe `lstat`/`S_ISREG` path proofs; `entry.status in {\\\"A\\\",\\\"M\\\"}` and `not old_path` so rename/copy/type-change legs are rejected (main rejects only `D`); the task's own worktree record returning False instead of passing; `type(...) is not int` for `expected_pr` so `True` and `\\\"12\\\"` no longer coerce; base goal status restricted to `{planned, ready, blocked}`; field-identity loops over base/head goal and lock; a completion-report content proof; a scorecard shape proof; a ledger-evidence proof; and a closing `set(changed_paths) != permitted_changed_paths`.\\n\\nEXCLUDE the lockless arm. CubeShare has `lock_terminal = lock_released or (lockless and not lock_path_in_diff)`; main has `if not task_changed or not lock_released: return False`. KEEP MAIN'S. CubeShare widened its own SADR-0017 exemption without amending SADR-0017 (the ADR is byte-identical on both sides). After this task, `grep -c \\\"lockless\\\\|lock_terminal\\\" scripts/mergegate_check.py` must be 0. The `_path_proven_absent` / `_path_is_regular_file` helpers still port \\u2014 the symlink hardening uses them independently.\\n\\n(d) COMPLETION-REPORT RISK. Write the report's risk as the strongest of the goal's and every task's, across the `shiki_tasks.py` writer and the closeout classifier's equality check. Both legs in one commit, or the classifier fails on every outranked closeout.\\n\\n(e) DOCS. MG-25, MG-26 and the migration decision-control row; SADR-0012 STEP 5 ONLY. Step 3 documents the lockless closeout \\u2014 main does not implement it, and publishing it would make CubeShare-local behaviour read as platform doctrine. MG-26 asserts registration on the immutable merge base, so it lands with or after (b), never before.\\n\\nMUST NOT: reflux `and pr`; add `lockless`/`lock_terminal`; touch SADR-0017 itself; port `exact_head_only`, `normal_frozen_spec_errors`, `exact_pr_link`, or any `spec_amendment`/`delivery_only` surface; add a second `git fetch origin`; weaken `tests/test_workflow_invocation_carry.py`'s `2`/`1` fetch counts or its `total == 4`; bind `_review_source` to a head SHA (a real but separate defect).\\n\\nTESTS. Fork fixtures at both enforcement points: `isCrossRepository: true` denies the exemption, and a snapshot with the field ABSENT also denies it. Assert `_bookkeeping_closeout_exemption` then resolves the task's real risk, not `low`. Assert every `gh pr view --json` list in both workflows contains `isCrossRepository`. Assert `grep -c \\\"lockless\\\\|lock_terminal\\\"` is 0 and that `if not task_changed or not lock_released:` is present verbatim. Symlinked, renamed, type-changed and coerced-`expected_pr` closeout fixtures all return False. A goal/task risk pair (`medium`/`critical`) produces `report[\\\"mergegate\\\"][\\\"risk\\\"] == \\\"critical\\\"` and the classifier's equality check passes on it. Keep the fetch-count invariants exactly as they are.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: on a PUBLIC repository, anyone may fork, open a PR shaped as a SADR-0017 bookkeeping closeout, and have `carry_resolved_risk` computed as `low`, bypassing the Guardian gate for a high- or critical-risk task.\\n  Measured 2026-08-17: `isCrossRepository` 0 occurrences across scripts/, .github/ and tests/ on origin/main; `visibility: public`, `allow_forking: true`; 0 cross-repository PRs ever opened, so nothing has exploited it.\\n  Root cause: the exemption never asks whether the PR came from a fork.\\n  Correction this task makes over its source: CubeShare's `mergegate_check.py:3600` carries an `and pr` short-circuit its sibling at `guardian_approval_signal.py:336` does not; both sites here use the un-short-circuited form.\\n  Reproducible when: a fork-PR fixture and a field-absent fixture both deny the exemption at both sites, `lockless`/`lock_terminal` appear nowhere, and the fetch-count invariants are untouched.\",\n  \"status\": \"running\",\n  \"test_command\": \"python3 -m unittest discover -s tests && python3 scripts/validate_shiki.py --target . && for s in scripts/test_shiki_*.sh; do bash \\\"$s\\\" || exit 1; done\",\n  \"title\": \"Deny the SADR-0017 exemption to fork PRs and pin the judged base\"\n}\n\nexec\n/bin/zsh -lc \"git diff --numstat && git diff -- .github/workflows/shiki-mergegate.yml .github/workflows/shiki-cca-completion.yml scripts/mergegate_check.py scripts/guardian_approval_signal.py scripts/shiki_tasks.py tests/test_bookkeeping_closeout.py tests/test_guardian_signal_closeout.py tests/test_paired_invariants.py tests/test_memory_scorecard.py tests/test_migration_mode.py scripts/test_shiki_governance_evidence.sh scripts/test_shiki_adversarial_state.sh | sed -n '1,260p'\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n35\t14\t.github/workflows/shiki-cca-completion.yml\n13\t6\t.github/workflows/shiki-mergegate.yml\n6\t3\t.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n1\t1\tdocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n2\t0\tdocs/agents/checklists.md\n1\t0\tdocs/agents/decision-control.md\n7\t0\tdocs/agents/implementation-policy.md\n4\t0\tdocs/agents/triage-labels.md\n4\t0\tscripts/guardian_approval_signal.py\n300\t54\tscripts/mergegate_check.py\n10\t1\tscripts/shiki_tasks.py\n36\t10\tscripts/test_shiki_adversarial_state.sh\n25\t7\tscripts/test_shiki_governance_evidence.sh\n196\t8\ttests/test_bookkeeping_closeout.py\n68\t8\ttests/test_guardian_signal_closeout.py\n32\t0\ttests/test_memory_scorecard.py\n35\t2\ttests/test_migration_mode.py\n50\t0\ttests/test_paired_invariants.py\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 57081e3..04719fe 100644\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -52,13 +52,21 @@ jobs:\n           REPOSITORY: ${{ github.repository }}\n           DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n         run: |\n+          set -euo pipefail\n           rm -rf .shiki/gha\n           mkdir -p .shiki/gha\n-          gh pr view \"$PR_NUMBER\" --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n+          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n+          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n           git fetch origin \"$base_ref\"\n-          git diff --name-only \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files.txt\n-          git diff --name-status \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files-status.txt\n+          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n+          printf '%s\\n' \"$merge_base\" > .shiki/gha/merge-base-oid.txt\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n+          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n+          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n \n           # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n           # mergegate job builds them below. The Guardian-approval signal must\n@@ -70,7 +78,7 @@ jobs:\n           # the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n           # Implementation-PR merge proof: resolve the task id with the SAME\n           # semantics mergegate_check.py uses (first T-<4+ digits> OR\n@@ -85,7 +93,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\n@@ -218,7 +226,7 @@ jobs:\n           while true; do\n             gh pr view \"$PR_NUMBER\" \\\n               --repo \"$REPOSITORY\" \\\n-              --json headRefOid,statusCheckRollup \\\n+              --json headRefOid,statusCheckRollup,isCrossRepository \\\n               > .shiki/gha/pr-settling.json\n \n             observed_head=\"$(jq -r '.headRefOid // \"\"' .shiki/gha/pr-settling.json)\"\n@@ -267,9 +275,13 @@ jobs:\n \n           # The judge must consume the settled rollup, not the snapshot gathered at\n           # the beginning of this concurrently-triggered workflow run.\n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n           gh pr view \"$PR_NUMBER\" \\\n             --repo \"$REPOSITORY\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n \n       - name: Prepare CCA transport schema\n         id: schema\n@@ -706,10 +718,14 @@ jobs:\n             exit 1\n           fi\n \n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n           gh pr view \"$PR_NUMBER\" \\\n             --repo \"$REPOSITORY\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n-            > .shiki/gha/pr.json\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n+            > .shiki/gha/pr-refresh.json\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr-refresh.json > .shiki/gha/pr.json\n+          rm .shiki/gha/pr-refresh.json\n \n           echo \"::endgroup::\"\n \n@@ -782,7 +798,7 @@ jobs:\n           mkdir -p .shiki/gha/base-shiki\n \n           gh pr view \"$PR_NUMBER\" \\\n-            --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n+            --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup \\\n             > .shiki/gha/live-pr.json\n \n           gh api --paginate \"repos/${{ github.repository }}/issues/${PR_NUMBER}/comments\" \\\n@@ -797,16 +813,21 @@ jobs:\n \n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/live-pr.json)\"\n           head_oid=\"$(jq -r '.headRefOid' .shiki/gha/live-pr.json)\"\n+          merge_base=\"$(cat .shiki/gha/merge-base-oid.txt)\"\n \n           git fetch origin \"$base_ref\"\n \n-          git diff --name-only \"origin/${base_ref}...HEAD\" \\\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/live-pr.json > .shiki/gha/live-pr-with-merge-base.json\n+          mv .shiki/gha/live-pr-with-merge-base.json .shiki/gha/live-pr.json\n+\n+          git diff --name-only \"${merge_base}...HEAD\" \\\n             > .shiki/gha/live-changed-files.txt\n \n-          git diff --name-status \"origin/${base_ref}...HEAD\" \\\n+          git diff --name-status \"${merge_base}...HEAD\" \\\n             > .shiki/gha/live-changed-files-status.txt\n \n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n           current_head=\"$(git rev-parse HEAD)\"\n           if [ \"$head_oid\" != \"$current_head\" ]; then\n@@ -849,7 +870,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\ndiff --git a/.github/workflows/shiki-mergegate.yml b/.github/workflows/shiki-mergegate.yml\nindex 9ee3d28..128e912 100644\n--- a/.github/workflows/shiki-mergegate.yml\n+++ b/.github/workflows/shiki-mergegate.yml\n@@ -49,13 +49,20 @@ jobs:\n           PR_NUMBER: ${{ github.event.pull_request.number }}\n           REPOSITORY: ${{ github.repository }}\n         run: |\n+          set -euo pipefail\n           rm -rf .shiki/gha\n           mkdir -p .shiki/gha\n-          gh pr view \"$PR_NUMBER\" --json number,title,body,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n+          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n           base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n+          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n+          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n           git fetch origin \"$base_ref\"\n-          git diff --name-only \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files.txt\n-          git diff --name-status \"origin/${base_ref}...HEAD\" > .shiki/gha/changed-files-status.txt\n+          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n+          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n+            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n+          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n+          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n+          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n \n           # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n           # Guardian gate from the Goal's frozen-plan risk in THIS metadata job,\n@@ -69,7 +76,7 @@ jobs:\n           gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/events\" > .shiki/gha/live-guardian-events.json || echo \"[]\" > .shiki/gha/live-guardian-events.json\n           gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/timeline\" -H \"Accept: application/vnd.github+json\" > .shiki/gha/live-guardian-timeline.json || echo \"[]\" > .shiki/gha/live-guardian-timeline.json\n \n-          # Base .shiki snapshot from origin/<base_ref>, built the SAME way\n+          # Base .shiki snapshot from the PR's immutable merge base, built the SAME way\n           # shiki-cca-completion.yml builds it. mergegate_check.py needs it to\n           # verify task-field immutability for a post_merge_reconcile PR and to\n           # resolve the SADR-0017 bookkeeping-closeout exemption; without it a\n@@ -78,7 +85,7 @@ jobs:\n           # would go shallow.\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n-          git archive \"origin/${base_ref}\" .shiki | tar -x -C .shiki/gha/base-shiki || true\n+          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n \n       - name: MergeGate metadata check\n         env:\n@@ -103,7 +110,7 @@ jobs:\n           if [ -n \"$task_id\" ] && [ -f \"$base_task\" ]; then\n             base_pr=\"$(jq -r '.expected_pr // empty' \"$base_task\")\"\n             if [ -n \"$base_pr\" ]; then\n-              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state -q .state 2>/dev/null || echo UNKNOWN)\"\n+              state=\"$(gh pr view \"$base_pr\" --repo \"$REPOSITORY\" --json state,isCrossRepository -q .state 2>/dev/null || echo UNKNOWN)\"\n               if [ \"$state\" = \"MERGED\" ]; then\n                 MERGED_PRS=\"$base_pr\"\n               fi\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex 1692489..c5236ac 100644\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -330,6 +330,10 @@ def main(argv: list[str] | None = None) -> int:\n         changed_files_status=args.changed_files_status,\n         merged_prs=args.merged_prs,\n     )\n+    # A fork PR is never eligible for the SADR-0017 exemption. The snapshot must\n+    # prove the negative; absent/null/anything-but-False fails closed.\n+    if exemption and pr.get(\"isCrossRepository\") is not False:\n+        exemption = False\n     # The risk that actually gates Guardian approval: \"low\" for a proven closeout,\n     # otherwise the real task risk. task_risk is still reported as risk_level for\n     # audit. PR labels may only ESCALATE the effective risk (a maintainer can force\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 3fecaa8..21d3e29 100755\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -8,6 +8,7 @@ from dataclasses import dataclass\n import json\n import re\n import shutil\n+import stat\n import subprocess\n import sys\n import tempfile\n@@ -1279,29 +1280,26 @@ def declared_migration_ids(body: str) -> list[str]:\n     return [match.group(1) for match in _MIGRATION_ID_LINE.finditer(body)]\n \n \n-def _git_show_base(target: Path, base_ref: str, repo_path: str) -> str:\n-    \"\"\"Read ``repo_path`` from the base branch via a read-only ``git show``.\n-\n-    Tries ``origin/<base_ref>`` first \u2014 the MergeGate workflow fetches it with\n-    ``fetch-depth: 0`` (shiki-mergegate.yml) \u2014 then the bare ``<base_ref>`` as a\n-    fallback for a local base branch. Raises ``FileNotFoundError`` naming every\n-    ref it tried when neither resolves the path.\"\"\"\n-    errors: list[str] = []\n-    for ref in (f\"origin/{base_ref}\", base_ref):\n-        result = subprocess.run(\n-            [\"git\", \"show\", f\"{ref}:{repo_path}\"],\n-            cwd=str(target),\n-            capture_output=True,\n-            text=True,\n-        )\n-        if result.returncode == 0:\n-            return result.stdout\n-        errors.append(f\"{ref}: {result.stderr.strip() or 'not found'}\")\n-    raise FileNotFoundError(f\"could not read {repo_path} from the base branch ({'; '.join(errors)})\")\n+def _git_show_merge_base(target: Path, merge_base_oid: str, repo_path: str) -> str:\n+    \"\"\"Read ``repo_path`` from one immutable, workflow-resolved merge base.\"\"\"\n+    if not re.fullmatch(r\"[0-9a-fA-F]{40,64}\", merge_base_oid):\n+        raise FileNotFoundError(f\"invalid immutable mergeBaseOid {merge_base_oid!r}\")\n+    result = subprocess.run(\n+        [\"git\", \"show\", f\"{merge_base_oid}:{repo_path}\"],\n+        cwd=str(target),\n+        capture_output=True,\n+        text=True,\n+    )\n+    if result.returncode == 0:\n+        return result.stdout\n+    detail = result.stderr.strip() or \"not found\"\n+    raise FileNotFoundError(\n+        f\"could not read {repo_path} from immutable mergeBaseOid {merge_base_oid} ({detail})\"\n+    )\n \n \n-def resolve_base_migration_registry(target: Path, base_ref: str) -> tuple[Any, ...]:\n-    \"\"\"Resolve the migration registry from the BASE branch's\n+def resolve_base_migration_registry(target: Path, merge_base_oid: str) -> tuple[Any, ...]:\n+    \"\"\"Resolve the migration registry from the immutable merge base's\n     ``scripts/shiki_migrations.py`` \u2014 NEVER the PR head's.\n \n\nexec\n/bin/zsh -lc 'git status --short && git diff --stat && git diff --name-only' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\nexec\n/bin/zsh -lc \"git diff --check && git diff -- docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md && git diff | rg -n 'lockless|lock_terminal|exact_head_only|normal_frozen_spec_errors|exact_pr_link|spec_amendment|delivery_only|bookkeeping_closeout and pr and|exemption and pr and' || true\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n M .github/workflows/shiki-cca-completion.yml\n M .github/workflows/shiki-mergegate.yml\n M .shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n M docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n M docs/agents/checklists.md\n M docs/agents/decision-control.md\n M docs/agents/implementation-policy.md\n M docs/agents/triage-labels.md\n M scripts/guardian_approval_signal.py\n M scripts/mergegate_check.py\n M scripts/shiki_tasks.py\n M scripts/test_shiki_adversarial_state.sh\n M scripts/test_shiki_governance_evidence.sh\n M tests/test_bookkeeping_closeout.py\n M tests/test_guardian_signal_closeout.py\n M tests/test_memory_scorecard.py\n M tests/test_migration_mode.py\n M tests/test_paired_invariants.py\n?? .shiki/locks/T-20260817T063758155247Z-0b11207b.json\n .github/workflows/shiki-cca-completion.yml         |  49 ++-\n .github/workflows/shiki-mergegate.yml              |  19 +-\n .../tasks/T-20260817T063758155247Z-0b11207b.json   |   9 +-\n .../SADR-0012-autonomous-loop-closeout-to-main.md  |   2 +-\n docs/agents/checklists.md                          |   2 +\n docs/agents/decision-control.md                    |   1 +\n docs/agents/implementation-policy.md               |   7 +\n docs/agents/triage-labels.md                       |   4 +\n scripts/guardian_approval_signal.py                |   4 +\n scripts/mergegate_check.py                         | 354 +++++++++++++++++----\n scripts/shiki_tasks.py                             |  11 +-\n scripts/test_shiki_adversarial_state.sh            |  46 ++-\n scripts/test_shiki_governance_evidence.sh          |  32 +-\n tests/test_bookkeeping_closeout.py                 | 204 +++++++++++-\n tests/test_guardian_signal_closeout.py             |  76 ++++-\n tests/test_memory_scorecard.py                     |  32 ++\n tests/test_migration_mode.py                       |  37 ++-\n tests/test_paired_invariants.py                    |  50 +++\n 18 files changed, 825 insertions(+), 114 deletions(-)\n.github/workflows/shiki-cca-completion.yml\n.github/workflows/shiki-mergegate.yml\n.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/agents/checklists.md\ndocs/agents/decision-control.md\ndocs/agents/implementation-policy.md\ndocs/agents/triage-labels.md\nscripts/guardian_approval_signal.py\nscripts/mergegate_check.py\nscripts/shiki_tasks.py\nscripts/test_shiki_adversarial_state.sh\nscripts/test_shiki_governance_evidence.sh\ntests/test_bookkeeping_closeout.py\ntests/test_guardian_signal_closeout.py\ntests/test_memory_scorecard.py\ntests/test_migration_mode.py\ntests/test_paired_invariants.py\n\n succeeded in 0ms:\n215:   \"scope\": \"Port five capabilities from mizutani-140/CubeShare into this platform repository, one of them CORRECTED rather than copied.\\n\\n(a) FORK GUARD \\u2014 the reason this task exists. Measured on origin/main: `git grep -c isCrossRepository -- scripts/ .github/ tests/` returns NOTHING (0 occurrences repo-wide; the same grep returns 63 files for `bookkeeping_closeout`, so the search works). And `mergegate_check.py` computes `carry_resolved_risk = \\\"low\\\" if bookkeeping_closeout else str((task or {}).get(\\\"risk_level\\\") or \\\"\\\")`. A PR shaped as a SADR-0017 bookkeeping closeout therefore resolves to `low` and skips the Guardian gate at any real risk level. This repository is PUBLIC with `allow_forking: true`, so a fork PR is an unauthenticated path to that shape.\\n\\nAdd the guard at BOTH enforcement points, in the `guardian_approval_signal.py` form:\\n\\n    if exemption and pr.get(\\\"isCrossRepository\\\") is not False:\\n        exemption = False\\n\\nDO NOT copy CubeShare's `mergegate_check.py:3600`, which reads `if bookkeeping_closeout and pr and pr.get(...) is not False:`. That `and pr` short-circuits the guard on a falsy `pr`. On the Guardian leg the branch is unreachable, but `bookkeeping_closeout` also flows to `_frozen_spec_mutable_task_fields(...)` at `cs:3608`, under `if not migration_mode:` and OUTSIDE any `pr` guard. Use the un-short-circuited form at both sites.\\n\\nThe guard is fail-closed by design: `is not False` means absent/null/anything-but-`False` collapses the exemption, restoring the task's real risk. That is strictly stricter, never a bypass \\u2014 but it means EVERY `gh pr view --json` field list that produces a PR snapshot must request `isCrossRepository`, or the exemption silently stops working on that path. The Python and every workflow field list MUST land in ONE commit.\\n\\nGuard scope, so it is not overclaimed: SADR-0017 bookkeeping exemption ONLY. The contract_approval carry and the SADR-0018 base-sync carry are out of scope.\\n\\n(b) IMMUTABLE MERGE-BASE PINNING. Resolve `mergeBaseOid` once in each gate workflow from `baseRefOid` plus `git merge-base`, inject it into the PR snapshot with jq, consume it in `mergegate_check.py` (a `_git_show_base`-style reader), and read the frozen tree with `git archive \\\"$merge_base\\\" .shiki`. Remove the `|| true` that swallows a failed resolution. Python and both workflows must land together or every migration PR blocks on the missing field.\\n\\n(c) CLOSEOUT CLASSIFIER HARDENING, MINUS THE RELAXATION. Port CubeShare's `is_bookkeeping_closeout` hardening: symlink-safe `lstat`/`S_ISREG` path proofs; `entry.status in {\\\"A\\\",\\\"M\\\"}` and `not old_path` so rename/copy/type-change legs are rejected (main rejects only `D`); the task's own worktree record returning False instead of passing; `type(...) is not int` for `expected_pr` so `True` and `\\\"12\\\"` no longer coerce; base goal status restricted to `{planned, ready, blocked}`; field-identity loops over base/head goal and lock; a completion-report content proof; a scorecard shape proof; a ledger-evidence proof; and a closing `set(changed_paths) != permitted_changed_paths`.\\n\\nEXCLUDE the lockless arm. CubeShare has `lock_terminal = lock_released or (lockless and not lock_path_in_diff)`; main has `if not task_changed or not lock_released: return False`. KEEP MAIN'S. CubeShare widened its own SADR-0017 exemption without amending SADR-0017 (the ADR is byte-identical on both sides). After this task, `grep -c \\\"lockless\\\\|lock_terminal\\\" scripts/mergegate_check.py` must be 0. The `_path_proven_absent` / `_path_is_regular_file` helpers still port \\u2014 the symlink hardening uses them independently.\\n\\n(d) COMPLETION-REPORT RISK. Write the report's risk as the strongest of the goal's and every task's, across the `shiki_tasks.py` writer and the closeout classifier's equality check. Both legs in one commit, or the classifier fails on every outranked closeout.\\n\\n(e) DOCS. MG-25, MG-26 and the migration decision-control row; SADR-0012 STEP 5 ONLY. Step 3 documents the lockless closeout \\u2014 main does not implement it, and publishing it would make CubeShare-local behaviour read as platform doctrine. MG-26 asserts registration on the immutable merge base, so it lands with or after (b), never before.\\n\\nMUST NOT: reflux `and pr`; add `lockless`/`lock_terminal`; touch SADR-0017 itself; port `exact_head_only`, `normal_frozen_spec_errors`, `exact_pr_link`, or any `spec_amendment`/`delivery_only` surface; add a second `git fetch origin`; weaken `tests/test_workflow_invocation_carry.py`'s `2`/`1` fetch counts or its `total == 4`; bind `_review_source` to a head SHA (a real but separate defect).\\n\\nTESTS. Fork fixtures at both enforcement points: `isCrossRepository: true` denies the exemption, and a snapshot with the field ABSENT also denies it. Assert `_bookkeeping_closeout_exemption` then resolves the task's real risk, not `low`. Assert every `gh pr view --json` list in both workflows contains `isCrossRepository`. Assert `grep -c \\\"lockless\\\\|lock_terminal\\\"` is 0 and that `if not task_changed or not lock_released:` is present verbatim. Symlinked, renamed, type-changed and coerced-`expected_pr` closeout fixtures all return False. A goal/task risk pair (`medium`/`critical`) produces `report[\\\"mergegate\\\"][\\\"risk\\\"] == \\\"critical\\\"` and the classifier's equality check passes on it. Keep the fetch-count invariants exactly as they are.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: on a PUBLIC repository, anyone may fork, open a PR shaped as a SADR-0017 bookkeeping closeout, and have `carry_resolved_risk` computed as `low`, bypassing the Guardian gate for a high- or critical-risk task.\\n  Measured 2026-08-17: `isCrossRepository` 0 occurrences across scripts/, .github/ and tests/ on origin/main; `visibility: public`, `allow_forking: true`; 0 cross-repository PRs ever opened, so nothing has exploited it.\\n  Root cause: the exemption never asks whether the PR came from a fork.\\n  Correction this task makes over its source: CubeShare's `mergegate_check.py:3600` carries an `and pr` short-circuit its sibling at `guardian_approval_signal.py:336` does not; both sites here use the un-short-circuited form.\\n  Reproducible when: a fork-PR fixture and a field-absent fixture both deny the exemption at both sites, `lockless`/`lock_terminal` appear nowhere, and the fetch-count invariants are untouched.\",\n1577:+        self.assertNotIn(\"bookkeeping_closeout and pr and pr.get\", mergegate)\n1582:+        self.assertNotIn(\"exemption and pr and pr.get\", signal)\n1586:+        self.assertIsNone(re.search(r\"lockless|lock_terminal\", mergegate))\n\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:21:24.588524Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nThe high-effort diff review found one bounded quality issue: the CCA workflow\u2019s snapshot comment still described `origin/<base_ref>` after the implementation switched the actual archive to the pinned merge base. I\u2019m correcting that stale explanation; no behavioral review finding remains so far. The Shiki-owned task/lock metadata changes were present before my edits and remain untouched.\napply patch\npatch: completed\n/Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the/.github/workflows/shiki-cca-completion.yml\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:21:46.715976Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"git diff -- scripts/mergegate_check.py scripts/guardian_approval_signal.py scripts/shiki_tasks.py .github/workflows/shiki-mergegate.yml .github/workflows/shiki-cca-completion.yml docs/agents/checklists.md docs/agents/decision-control.md docs/agents/triage-labels.md docs/agents/implementation-policy.md docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md | rg -n 'exact_head_only|normal_frozen_spec_errors|exact_pr_link|spec_amendment|delivery_only|_review_source' || true\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"rg -n 'git fetch origin' .github/workflows/shiki-cca-completion.yml .github/workflows/shiki-mergegate.yml tests/test_workflow_invocation_carry.py; rg -n 'if not task_changed or not lock_released:|bookkeeping_closeout and pr\\\\.get\\\\(\\\"isCrossRepository\\\"\\\\)|exemption and pr\\\\.get\\\\(\\\"isCrossRepository\\\"\\\\)|git archive \\\"\\\\\"'$merge_base\" '\"\\\\.shiki|git merge-base \\\"\\\\\"'$base_oid\" \"'\"\\\\\"'$head_oid\"'\"' scripts/mergegate_check.py scripts/guardian_approval_signal.py .github/workflows/shiki-cca-completion.yml .github/workflows/shiki-mergegate.yml; if rg -n 'lockless|lock_terminal' scripts/mergegate_check.py; then exit 1; fi\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\ntests/test_workflow_invocation_carry.py:214:        self.assertEqual(CCA_WF.read_text(encoding=\"utf-8\").count(\"git fetch origin\"), 2)\ntests/test_workflow_invocation_carry.py:215:        self.assertEqual(MERGEGATE_WF.read_text(encoding=\"utf-8\").count(\"git fetch origin\"), 1)\n.github/workflows/shiki-mergegate.yml:59:          git fetch origin \"$base_ref\"\n.github/workflows/shiki-cca-completion.yml:62:          git fetch origin \"$base_ref\"\n.github/workflows/shiki-cca-completion.yml:818:          git fetch origin \"$base_ref\"\n.github/workflows/shiki-mergegate.yml:60:          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n.github/workflows/shiki-mergegate.yml:88:          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\nscripts/guardian_approval_signal.py:335:    if exemption and pr.get(\"isCrossRepository\") is not False:\n.github/workflows/shiki-cca-completion.yml:63:          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n.github/workflows/shiki-cca-completion.yml:81:          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n.github/workflows/shiki-cca-completion.yml:830:          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\nscripts/mergegate_check.py:2242:        if not task_changed or not lock_released:\nscripts/mergegate_check.py:3469:            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\n\nexec\n/bin/zsh -lc 'git diff --check && test \"$(git diff -- docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md | wc -c | tr -d '\"' ')\\\" = 0 && test \\\"\"'$(git diff -- docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md | rg '\"'\"'^[-+]3'\"\\\\.' | wc -l | tr -d ' ')\\\" = 0\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests -p test_paired_invariants.py -v && python3 -m unittest discover -s tests -p test_workflow_invocation_carry.py -v && python3 -m unittest discover -s tests -p test_bookkeeping_closeout.py -v && python3 -m unittest discover -s tests -p test_guardian_signal_closeout.py -v && python3 -m unittest discover -s tests -p test_migration_mode.py -v && python3 -m unittest discover -s tests -p test_memory_scorecard.py -v' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:22:08.552983Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 957ms:\nok\ntest_enumerates_exactly_four_and_all_carry_both_flags (test_workflow_invocation_carry.EveryInvocationCarriesBothFlags.test_enumerates_exactly_four_and_all_carry_both_flags) ... ok\ntest_metadata_run_body_uses_shell_var_not_expression (test_workflow_invocation_carry.MergegateRunBodiesStayExpressionFree.test_metadata_run_body_uses_shell_var_not_expression) ... ok\ntest_no_base_fetch_carries_depth (test_workflow_invocation_carry.NoShallowOrSecondBaseFetch.test_no_base_fetch_carries_depth) ... ok\ntest_no_second_base_fetch_added (test_workflow_invocation_carry.NoShallowOrSecondBaseFetch.test_no_second_base_fetch_added) ... ok\ntest_dropping_either_flag_from_any_invocation_fails (test_workflow_invocation_carry.ValidateShikiGuardsEveryInvocation.test_dropping_either_flag_from_any_invocation_fails) ... ok\ntest_repo_workflows_satisfy_contract (test_workflow_invocation_carry.ValidateShikiGuardsEveryInvocation.test_repo_workflows_satisfy_contract) ... ok\n\n----------------------------------------------------------------------\nRan 7 tests in 0.033s\n\nOK\ntest_added_ledger_symlink_on_base_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_added_ledger_symlink_on_base_disqualifies) ... ok\ntest_base_lock_not_active_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_base_lock_not_active_disqualifies) ... ok\ntest_base_task_status_not_review_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_base_task_status_not_review_disqualifies) ... ok\ntest_classifier_never_reads_the_pr (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_classifier_never_reads_the_pr) ... ok\ntest_coerced_expected_pr_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_coerced_expected_pr_disqualifies) ... ok\ntest_contract_shaped_registration_is_not_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_contract_shaped_registration_is_not_classified) ... ok\ntest_deleted_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_deleted_shiki_path_disqualifies) ... ok\ntest_every_governance_field_change_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_every_governance_field_change_disqualifies) ... ok\ntest_file_outside_effective_locks_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_file_outside_effective_locks_disqualifies) ... ok\ntest_goal_status_not_complete_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_goal_status_not_complete_disqualifies) ... ok\ntest_goal_transition_without_completion_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_goal_transition_without_completion_disqualifies) ... ok\ntest_governance_field_edit_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_governance_field_edit_disqualifies) ... ok\ntest_impl_pr_not_merged_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_impl_pr_not_merged_disqualifies) ... ok\ntest_impl_pr_unresolvable_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_impl_pr_unresolvable_disqualifies) ... ok\ntest_label_or_marker_does_not_classify_non_qualifying_pr (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_label_or_marker_does_not_classify_non_qualifying_pr) ... ok\ntest_lock_not_released_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_lock_not_released_disqualifies) ... ok\ntest_mergegate_fork_and_absent_field_deny_exemption (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_mergegate_fork_and_absent_field_deny_exemption) ... ok\ntest_missing_base_snapshot_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_missing_base_snapshot_disqualifies) ... ok\ntest_missing_dag_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_missing_dag_disqualifies) ... ok\ntest_non_shiki_file_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_non_shiki_file_disqualifies) ... ok\ntest_positive_completing_is_classified_and_needs_no_guardian (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_completing_is_classified_and_needs_no_guardian) ... ok\ntest_positive_expected_branch_differs_is_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_expected_branch_differs_is_classified) ... ok\ntest_positive_noncompleting_is_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_positive_noncompleting_is_classified) ... ok\ntest_renamed_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_renamed_shiki_path_disqualifies) ... ok\ntest_report_without_completion_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_report_without_completion_disqualifies) ... ok\ntest_risk_level_downgrade_is_not_classified (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_risk_level_downgrade_is_not_classified) ... ok\ntest_second_scorecard_report_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_second_scorecard_report_disqualifies) ... ok\ntest_strongest_goal_or_task_risk_matches_completion_report (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_strongest_goal_or_task_risk_matches_completion_report) ... ok\ntest_symlinked_task_file_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_symlinked_task_file_disqualifies) ... ok\ntest_task_head_status_not_done_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_task_head_status_not_done_disqualifies) ... ok\ntest_type_changed_shiki_path_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_type_changed_shiki_path_disqualifies) ... ok\ntest_unreadable_base_task_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_unreadable_base_task_disqualifies) ... ok\ntest_worktree_record_disqualifies (test_bookkeeping_closeout.BookkeepingCloseoutTest.test_worktree_record_disqualifies) ... ok\n\n----------------------------------------------------------------------\nRan 33 tests in 0.150s\n\nOK\ntest_absent_cross_repository_field_denies_exemption_and_uses_real_risk (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_absent_cross_repository_field_denies_exemption_and_uses_real_risk) ... ok\ntest_cross_repository_pr_denies_exemption_and_uses_real_risk (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_cross_repository_pr_denies_exemption_and_uses_real_risk) ... ok\ntest_deletion_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_deletion_requires_guardian) ... ok\ntest_governance_field_edit_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_governance_field_edit_requires_guardian) ... ok\ntest_maintainer_label_still_forces_gate_on_exempt_pr (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_maintainer_label_still_forces_gate_on_exempt_pr) ... ok\ntest_missing_base_snapshot_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_missing_base_snapshot_requires_guardian) ... ok\ntest_missing_changed_files_status_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_missing_changed_files_status_requires_guardian) ... ok\ntest_non_shiki_path_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_non_shiki_path_requires_guardian) ... ok\ntest_non_terminal_status_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_non_terminal_status_requires_guardian) ... ok\ntest_nonexistent_base_snapshot_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_nonexistent_base_snapshot_requires_guardian) ... ok\ntest_nonexistent_changed_files_status_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_nonexistent_changed_files_status_requires_guardian) ... ok\ntest_parity_signal_matches_mergegate_and_is_never_more_permissive (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_parity_signal_matches_mergegate_and_is_never_more_permissive) ... ok\ntest_proven_completing_critical_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_completing_critical_closeout_not_required) ... ok\ntest_proven_noncompleting_closeout_not_required (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_proven_noncompleting_closeout_not_required) ... ok\ntest_second_scorecard_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_second_scorecard_requires_guardian) ... ok\ntest_signal_records_exemption_applied_flag (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_signal_records_exemption_applied_flag) ... ok\ntest_unmerged_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unmerged_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_implementation_pr_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_implementation_pr_requires_guardian) ... ok\ntest_unresolvable_task_id_requires_guardian (test_guardian_signal_closeout.GuardianSignalCloseoutTest.test_unresolvable_task_id_requires_guardian) ... ok\n\n----------------------------------------------------------------------\nRan 19 tests in 0.118s\n\nOK\ntest_missing_immutable_merge_base_oid_blocks_registry_resolution (test_migration_mode.BaseRegistryResolutionTests.test_missing_immutable_merge_base_oid_blocks_registry_resolution) ... ok\ntest_registry_is_read_from_base_not_head (test_migration_mode.BaseRegistryResolutionTests.test_registry_is_read_from_base_not_head) ... ok\ntest_self_defining_migration_pr_blocks_because_id_absent_from_base (test_migration_mode.BaseRegistryResolutionTests.test_self_defining_migration_pr_blocks_because_id_absent_from_base) ... ok\ntest_absent_line_yields_nothing (test_migration_mode.DeclaredMigrationIdTests.test_absent_line_yields_nothing) ... ok\ntest_mid_sentence_mention_is_not_a_declaration (test_migration_mode.DeclaredMigrationIdTests.test_mid_sentence_mention_is_not_a_declaration) ... ok\ntest_single_line_is_parsed (test_migration_mode.DeclaredMigrationIdTests.test_single_line_is_parsed) ... ok\ntest_two_lines_are_both_returned (test_migration_mode.DeclaredMigrationIdTests.test_two_lines_are_both_returned) ... ok\ntest_label_without_marker_does_not_enter_mode (test_migration_mode.MigrationDecisionTests.test_label_without_marker_does_not_enter_mode) ... ok\ntest_marker_and_label_enables_mode (test_migration_mode.MigrationDecisionTests.test_marker_and_label_enables_mode) ... ok\ntest_marker_quoted_in_code_span_is_not_a_directive (test_migration_mode.MigrationDecisionTests.test_marker_quoted_in_code_span_is_not_a_directive) ... ok\ntest_marker_without_label_blocks_with_the_label_message (test_migration_mode.MigrationDecisionTests.test_marker_without_label_blocks_with_the_label_message) ... ok\ntest_mid_sentence_marker_in_prose_is_not_a_directive (test_migration_mode.MigrationDecisionTests.test_mid_sentence_marker_in_prose_is_not_a_directive) ... ok\ntest_this_tasks_own_impl_pr_body_does_not_enter_or_error (test_migration_mode.MigrationDecisionTests.test_this_tasks_own_impl_pr_body_does_not_enter_or_error) ... ok\ntest_already_applied_migration_blocks (test_migration_mode.MigrationGateTests.test_already_applied_migration_blocks) ... ok\ntest_conforming_pr_passes (test_migration_mode.MigrationGateTests.test_conforming_pr_passes) ... ok\ntest_declared_id_absent_from_base_registry_blocks (test_migration_mode.MigrationGateTests.test_declared_id_absent_from_base_registry_blocks) ... ok\ntest_destructive_migration_blocks (test_migration_mode.MigrationGateTests.test_destructive_migration_blocks) ... ok\ntest_duplicated_migration_line_blocks (test_migration_mode.MigrationGateTests.test_duplicated_migration_line_blocks) ... ok\ntest_extra_changed_file_blocks_naming_the_path (test_migration_mode.MigrationGateTests.test_extra_changed_file_blocks_naming_the_path) ... ok\ntest_migration_whose_apply_raises_fails_closed (test_migration_mode.MigrationGateTests.test_migration_whose_apply_raises_fails_closed) ... ok\ntest_missing_base_snapshot_blocks (test_migration_mode.MigrationGateTests.test_missing_base_snapshot_blocks) ... ok\ntest_missing_migration_line_blocks (test_migration_mode.MigrationGateTests.test_missing_migration_line_blocks) ... ok\ntest_missing_produced_file_blocks_naming_the_path (test_migration_mode.MigrationGateTests.test_missing_produced_file_blocks_naming_the_path) ... ok\ntest_non_shiki_file_blocks (test_migration_mode.MigrationGateTests.test_non_shiki_file_blocks) ... ok\ntest_single_changed_byte_blocks_naming_the_path (test_migration_mode.MigrationGateTests.test_single_changed_byte_blocks_naming_the_path) ... ok\n\n----------------------------------------------------------------------\nRan 25 tests in 0.239s\n\nOK\ntest_report_uses_strongest_goal_or_task_risk (test_memory_scorecard.GoalCompleteContractTests.test_report_uses_strongest_goal_or_task_risk) ... ok\ntest_scorecard_in_report_file_and_not_on_stdout (test_memory_scorecard.GoalCompleteContractTests.test_scorecard_in_report_file_and_not_on_stdout) ... ok\ntest_counts_from_ledger_and_task_state_only (test_memory_scorecard.ScorecardTests.test_counts_from_ledger_and_task_state_only) ... ok\ntest_missing_categories_are_zero_with_warnings (test_memory_scorecard.ScorecardTests.test_missing_categories_are_zero_with_warnings) ... ok\ntest_raw_memory_is_not_a_counting_source (test_memory_scorecard.ScorecardTests.test_raw_memory_is_not_a_counting_source) ... ok\ntest_single_memory_does_not_yield_suggestion (test_memory_scorecard.SuggestionTests.test_single_memory_does_not_yield_suggestion) ... ok\ntest_suggestion_does_not_change_memory_status (test_memory_scorecard.SuggestionTests.test_suggestion_does_not_change_memory_status) ... ok\ntest_suggestion_from_recurring_raw_memories_includes_mem_ids (test_memory_scorecard.SuggestionTests.test_suggestion_from_recurring_raw_memories_includes_mem_ids) ... ok\n\n----------------------------------------------------------------------\nRan 8 tests in 0.093s\n\nOK\n\ncodex\nThe review gate is clean after two applied findings: the missing base-absence proof and the stale merge-base comment. No findings were rejected, and the prohibited relaxations/out-of-scope surfaces remain absent. I\u2019m now rerunning the handoff\u2019s exact combined verification command after those fixes, as required by the code-review skill.\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests && python3 scripts/validate_shiki.py --target . && for s in scripts/test_shiki_*.sh; do bash \"$s\" || exit 1; done' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:22:14.382296Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:24:04.493739Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:24:24.495625Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:24:37.538748Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:24:45.838851Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 152560ms:\n........................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................\n----------------------------------------------------------------------\nRan 1170 tests in 91.431s\n\nOK\nGuardian approval status for PR #130 (mizutani-140/shiki)\nHead SHA: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\nResult: NOT APPROVED\n\nSatisfied approval sources:\n  - guardian_label (by mizutani-140)\n\nMissing / what the gate is waiting for:\n  - Guardian approval requires a configured Guardian review or current-head Guardian comment\n\nHead SHA stability:\n  - clean: safe to request approval against this head SHA\n\nPaste-ready Guardian approval comment (a configured Guardian pastes this verbatim):\n----------8<----------\nGuardian approval granted\n\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\n---------->8----------\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpo5ywp3o6/repo/.shiki/handoffs/RP-20260817T102306628062Z-be37cd2d-repair.md\",\n  \"ledger_id\": \"L-20260817T102306642507Z-cf02fb56\",\n  \"repair_id\": \"RP-20260817T102306628062Z-be37cd2d\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmppg3jb49d/repo/.shiki/handoffs/RP-20260817T102308551515Z-c6fc5e3c-repair.md\",\n  \"ledger_id\": \"L-20260817T102308565880Z-014d0e66\",\n  \"repair_id\": \"RP-20260817T102308551515Z-c6fc5e3c\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp91hio9s6/repo/.shiki/handoffs/RP-20260817T102309955241Z-8b626e19-repair.md\",\n  \"ledger_id\": \"L-20260817T102309969492Z-45e1cf58\",\n  \"repair_id\": \"RP-20260817T102309955241Z-8b626e19\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpjx_hn3vh/repo/.shiki/handoffs/RP-20260817T102322297979Z-267084a3-repair.md\",\n  \"ledger_id\": \"L-20260817T102322312730Z-155cb11b\",\n  \"repair_id\": \"RP-20260817T102322297979Z-267084a3\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpgscuu2p1/repo/.shiki/handoffs/RP-20260817T102323528047Z-b5c6fb4a-repair.md\",\n  \"ledger_id\": \"L-20260817T102323542123Z-df308d3e\",\n  \"repair_id\": \"RP-20260817T102323528047Z-b5c6fb4a\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp7s8ukdty/repo/.shiki/handoffs/RP-20260817T102324063258Z-6bdf8a7f-repair.md\",\n  \"ledger_id\": \"L-20260817T102324077393Z-ea74f3c6\",\n  \"repair_id\": \"RP-20260817T102324063258Z-6bdf8a7f\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpkvgxyool/repo/.shiki/handoffs/RP-20260817T102324698018Z-8e7202df-repair.md\",\n  \"ledger_id\": \"L-20260817T102324712860Z-d9eb8fc0\",\n  \"repair_id\": \"RP-20260817T102324698018Z-8e7202df\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpmlog0fgq/repo/.shiki/handoffs/RP-20260817T102325326214Z-1afabfce-repair.md\",\n  \"ledger_id\": \"L-20260817T102325341499Z-63331922\",\n  \"repair_id\": \"RP-20260817T102325326214Z-1afabfce\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpmc2mmna8/repo/.shiki/handoffs/RP-20260817T102325690072Z-e0aa5885-repair.md\",\n  \"ledger_id\": \"L-20260817T102325704287Z-81343202\",\n  \"repair_id\": \"RP-20260817T102325690072Z-e0aa5885\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp7f8ge7w1/repo/.shiki/handoffs/RP-20260817T102326170487Z-83108b7f-repair.md\",\n  \"ledger_id\": \"L-20260817T102326185737Z-17638f89\",\n  \"repair_id\": \"RP-20260817T102326170487Z-83108b7f\"\n}\n[shiki] token verified: ok\n[shiki] token verified: ok\n{\n  \"repo\": \"owner/name\",\n  \"secret\": \"CLAUDE_CODE_OAUTH_TOKEN\",\n  \"source\": \"env:SHIKI_TEST_TOK\",\n  \"verified\": true\n}\nShiki validation passed\nadversarial state/evidence/lock regression suite passed\nShiki validation passed\nShiki validation passed\nshiki code-review gate tests passed\nShiki validation passed\nShiki validation passed\nShiki validation passed\nshiki control-plane tests passed\nShiki validation passed\nShiki validation passed\nshiki daemon runner tests passed\nshiki doctor tests passed\nevidence manifest and ledger helper fixtures passed\nshiki evidence integrity tests passed\nShiki validation passed\nhint: Using 'master' as the name for the initial branch. This default branch name\nhint: will change to \"main\" in Git 3.0. To configure the initial branch name\nhint: to use in all of your new repositories, which will suppress this warning,\nhint: call:\nhint:\nhint: \tgit config --global init.defaultBranch <name>\nhint:\nhint: Names commonly chosen instead of 'master' are 'main', 'trunk' and\nhint: 'development'. The just-created branch can be renamed via this command:\nhint:\nhint: \tgit branch -m <name>\nhint:\nhint: Disable this message with \"git config set advice.defaultBranchName false\"\nTraceback (most recent call last):\n  File \"<stdin>\", line 5, in <module>\nFileNotFoundError: [Errno 2] No such file or directory: '/tmp/shiki-goal-loop-result.json'\n\ncodex\nThe exact rerun hit a new failure in `test_shiki_goal_loop.sh`: `/tmp/shiki-goal-loop-result.json` was missing. This script passed in the immediately preceding full sweep, and none of the reviewed changes touch goal-loop code, so I\u2019m invoking the required diagnosis skill to distinguish a flaky test-harness race from a regression before making any edit.\nexec\n/bin/zsh -lc \"sed -n '1,320p' /Users/kio.mizutani/skills/skills/engineering/diagnose/SKILL.md\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n---\nname: diagnose\ndescription: Disciplined diagnosis loop for hard bugs and performance regressions. Reproduce \u2192 minimise \u2192 hypothesise \u2192 instrument \u2192 fix \u2192 regression-test. Use when user says \"diagnose this\" / \"debug this\", reports a bug, says something is broken/throwing/failing, or describes a performance regression.\n---\n\n# Diagnose\n\nA discipline for hard bugs. Skip phases only when explicitly justified.\n\nWhen exploring the codebase, use the project's domain glossary to get a clear mental model of the relevant modules, and check ADRs in the area you're touching.\n\n## Phase 1 \u2014 Build a feedback loop\n\n**This is the skill.** Everything else is mechanical. If you have a fast, deterministic, agent-runnable pass/fail signal for the bug, you will find the cause \u2014 bisection, hypothesis-testing, and instrumentation all just consume that signal. If you don't have one, no amount of staring at code will save you.\n\nSpend disproportionate effort here. **Be aggressive. Be creative. Refuse to give up.**\n\n### Ways to construct one \u2014 try them in roughly this order\n\n1. **Failing test** at whatever seam reaches the bug \u2014 unit, integration, e2e.\n2. **Curl / HTTP script** against a running dev server.\n3. **CLI invocation** with a fixture input, diffing stdout against a known-good snapshot.\n4. **Headless browser script** (Playwright / Puppeteer) \u2014 drives the UI, asserts on DOM/console/network.\n5. **Replay a captured trace.** Save a real network request / payload / event log to disk; replay it through the code path in isolation.\n6. **Throwaway harness.** Spin up a minimal subset of the system (one service, mocked deps) that exercises the bug code path with a single function call.\n7. **Property / fuzz loop.** If the bug is \"sometimes wrong output\", run 1000 random inputs and look for the failure mode.\n8. **Bisection harness.** If the bug appeared between two known states (commit, dataset, version), automate \"boot at state X, check, repeat\" so you can `git bisect run` it.\n9. **Differential loop.** Run the same input through old-version vs new-version (or two configs) and diff outputs.\n10. **HITL bash script.** Last resort. If a human must click, drive _them_ with `scripts/hitl-loop.template.sh` so the loop is still structured. Captured output feeds back to you.\n\nBuild the right feedback loop, and the bug is 90% fixed.\n\n### Iterate on the loop itself\n\nTreat the loop as a product. Once you have _a_ loop, ask:\n\n- Can I make it faster? (Cache setup, skip unrelated init, narrow the test scope.)\n- Can I make the signal sharper? (Assert on the specific symptom, not \"didn't crash\".)\n- Can I make it more deterministic? (Pin time, seed RNG, isolate filesystem, freeze network.)\n\nA 30-second flaky loop is barely better than no loop. A 2-second deterministic loop is a debugging superpower.\n\n### Non-deterministic bugs\n\nThe goal is not a clean repro but a **higher reproduction rate**. Loop the trigger 100\u00d7, parallelise, add stress, narrow timing windows, inject sleeps. A 50%-flake bug is debuggable; 1% is not \u2014 keep raising the rate until it's debuggable.\n\n### When you genuinely cannot build a loop\n\nStop and say so explicitly. List what you tried. Ask the user for: (a) access to whatever environment reproduces it, (b) a captured artifact (HAR file, log dump, core dump, screen recording with timestamps), or (c) permission to add temporary production instrumentation. Do **not** proceed to hypothesise without a loop.\n\nDo not proceed to Phase 2 until you have a loop you believe in.\n\n## Phase 2 \u2014 Reproduce\n\nRun the loop. Watch the bug appear.\n\nConfirm:\n\n- [ ] The loop produces the failure mode the **user** described \u2014 not a different failure that happens to be nearby. Wrong bug = wrong fix.\n- [ ] The failure is reproducible across multiple runs (or, for non-deterministic bugs, reproducible at a high enough rate to debug against).\n- [ ] You have captured the exact symptom (error message, wrong output, slow timing) so later phases can verify the fix actually addresses it.\n\nDo not proceed until you reproduce the bug.\n\n## Phase 3 \u2014 Hypothesise\n\nGenerate **3\u20135 ranked hypotheses** before testing any of them. Single-hypothesis generation anchors on the first plausible idea.\n\nEach hypothesis must be **falsifiable**: state the prediction it makes.\n\n> Format: \"If <X> is the cause, then <changing Y> will make the bug disappear / <changing Z> will make it worse.\"\n\nIf you cannot state the prediction, the hypothesis is a vibe \u2014 discard or sharpen it.\n\n**Show the ranked list to the user before testing.** They often have domain knowledge that re-ranks instantly (\"we just deployed a change to #3\"), or know hypotheses they've already ruled out. Cheap checkpoint, big time saver. Don't block on it \u2014 proceed with your ranking if the user is AFK.\n\n## Phase 4 \u2014 Instrument\n\nEach probe must map to a specific prediction from Phase 3. **Change one variable at a time.**\n\nTool preference:\n\n1. **Debugger / REPL inspection** if the env supports it. One breakpoint beats ten logs.\n2. **Targeted logs** at the boundaries that distinguish hypotheses.\n3. Never \"log everything and grep\".\n\n**Tag every debug log** with a unique prefix, e.g. `[DEBUG-a4f2]`. Cleanup at the end becomes a single grep. Untagged logs survive; tagged logs die.\n\n**Perf branch.** For performance regressions, logs are usually wrong. Instead: establish a baseline measurement (timing harness, `performance.now()`, profiler, query plan), then bisect. Measure first, fix second.\n\n## Phase 5 \u2014 Fix + regression test\n\nWrite the regression test **before the fix** \u2014 but only if there is a **correct seam** for it.\n\nA correct seam is one where the test exercises the **real bug pattern** as it occurs at the call site. If the only available seam is too shallow (single-caller test when the bug needs multiple callers, unit test that can't replicate the chain that triggered the bug), a regression test there gives false confidence.\n\n**If no correct seam exists, that itself is the finding.** Note it. The codebase architecture is preventing the bug from being locked down. Flag this for the next phase.\n\nIf a correct seam exists:\n\n1. Turn the minimised repro into a failing test at that seam.\n2. Watch it fail.\n3. Apply the fix.\n4. Watch it pass.\n5. Re-run the Phase 1 feedback loop against the original (un-minimised) scenario.\n\n## Phase 6 \u2014 Cleanup + post-mortem\n\nRequired before declaring done:\n\n- [ ] Original repro no longer reproduces (re-run the Phase 1 loop)\n- [ ] Regression test passes (or absence of seam is documented)\n- [ ] All `[DEBUG-...]` instrumentation removed (`grep` the prefix)\n- [ ] Throwaway prototypes deleted (or moved to a clearly-marked debug location)\n- [ ] The hypothesis that turned out correct is stated in the commit / PR message \u2014 so the next debugger learns\n\n**Then ask: what would have prevented this bug?** If the answer involves architectural change (no good test seam, tangled callers, hidden coupling) hand off to the `/improve-codebase-architecture` skill with the specifics. Make the recommendation **after** the fix is in, not before \u2014 you have more information now than when you started.\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:24:51.107815Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nRanked hypotheses for the missing goal-loop result:\n\n1. The shell fixture uses a fixed `/tmp/shiki-goal-loop-result.json`, and an asynchronous/cleanup path races its creation or removal; rerunning the isolated script should reproduce intermittently.\n2. A prior shell script leaves environment/process state that changes the goal-loop invocation; isolated runs pass, sequence-local runs fail.\n3. The combined command\u2019s long runtime/resource pressure causes the loop subprocess to terminate before writing its result.\n4. A real deterministic regression exists in the current diff; the isolated script should fail consistently.\n\nI\u2019ll test the isolated script repeatedly first, then inspect the exact producer/cleanup path.\nexec\n/bin/zsh -lc 'git diff -- scripts/test_shiki_goal_loop.sh scripts/shiki_loop.py scripts/shiki.py scripts/shiki_cli.py' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"rg -n -C 12 'shiki-goal-loop-result|goal-loop-result|result.json' scripts/test_shiki_goal_loop.sh\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n212-  ]\n213-}\n214-JSON\n215-\n216-python3 \"$ROOT/scripts/shiki.py\" plan ingest --target \"$TARGET\" --plan-file \"$TMP_ROOT/plan.json\" >/tmp/shiki-goal-loop-plan.json\n217-PLAN_ID=\"$(json_get /tmp/shiki-goal-loop-plan.json plan_id)\"\n218-python3 \"$ROOT/scripts/shiki.py\" run --target \"$TARGET\" --plan \"$PLAN_ID\" >/tmp/shiki-goal-loop-run.json\n219-GOAL_ID=\"$(json_get /tmp/shiki-goal-loop-run.json goal_id)\"\n220-\n221-# The loop drives dispatch -> impl PR -> green checks -> auto-merge -> a closeout\n222-# PR that pushes task=done + goal=complete to main (SADR-0012) -> auto-merge the\n223-# closeout -> goal completion, with no operator input. Two merges: impl + closeout.\n224:python3 \"$ROOT/scripts/shiki.py\" loop run --target \"$TARGET\" --goal-id \"$GOAL_ID\" --max-cycles 14 --interval 0 >/tmp/shiki-goal-loop-result.json\n225:test \"$(json_get_last /tmp/shiki-goal-loop-result.json outcome)\" = \"complete\"\n226-grep '\"status\": \"complete\"' \"$TARGET/.shiki/goals/$GOAL_ID.json\" >/dev/null\n227-# The loop opened a closeout PR (SADR-0012): its history records create_closeout_pr.\n228:grep '\"action\": \"create_closeout_pr\"' /tmp/shiki-goal-loop-result.json >/dev/null\n229-# Impl PR + closeout PR both auto-merged.\n230-test \"$(grep -c merged \"$GH_STATE/gh-log\")\" = \"2\"\n231-\n232-# High/critical risk merges autonomously when all required checks are green:\n233-# the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n234-# guardian-policy.json \u2014 human approval OR external AI guardian review,\n235-# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n236-# green checks, simulating that the Guardian gate passed.\n237-python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n238-GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\n239-python3 \"$ROOT/scripts/shiki.py\" issue plan --target \"$TARGET\" --goal-id \"$GGOAL\" \\\n240-  --title \"High risk slice\" --scope \"Needs guardian\" --risk-level high \\\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:24:58.323760Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc 'for i in 1 2 3; do bash scripts/test_shiki_goal_loop.sh || exit 1; done' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:04.360459Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n exited 1 in 4541ms:\nShiki validation passed\nhint: Using 'master' as the name for the initial branch. This default branch name\nhint: will change to \"main\" in Git 3.0. To configure the initial branch name\nhint: to use in all of your new repositories, which will suppress this warning,\nhint: call:\nhint:\nhint: \tgit config --global init.defaultBranch <name>\nhint:\nhint: Names commonly chosen instead of 'master' are 'main', 'trunk' and\nhint: 'development'. The just-created branch can be renamed via this command:\nhint:\nhint: \tgit branch -m <name>\nhint:\nhint: Disable this message with \"git config set advice.defaultBranchName false\"\nTraceback (most recent call last):\n  File \"<stdin>\", line 5, in <module>\nFileNotFoundError: [Errno 2] No such file or directory: '/tmp/shiki-goal-loop-repair.json'\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:08.063178Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -n '1,270p' scripts/test_shiki_goal_loop.sh; rg -n 'trap|rm .*shiki-goal-loop|&\"'$|background|wait'\"' scripts/test_shiki_goal_loop.sh\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n#!/usr/bin/env bash\nset -euo pipefail\n\nROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)\"\nTMP_ROOT=\"${TMPDIR:-/tmp}/shiki-goal-loop-test-$$\"\nTARGET=\"$TMP_ROOT/target\"\nFAKE_BIN=\"$TMP_ROOT/bin\"\nexport GH_STATE=\"$TMP_ROOT/gh-state\"\nexport ORIGIN_GIT=\"$TMP_ROOT/origin.git\"\n\ncleanup() {\n  rm -rf \"$TMP_ROOT\"\n}\ntrap cleanup EXIT\n\njson_get() {\n  python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))[sys.argv[2]])' \"$1\" \"$2\"\n}\n\n# Loop outputs stream one JSON document per executed control-plane command;\n# the loop's own result is always the last document.\njson_get_last() {\n  python3 - \"$1\" \"$2\" <<'PY'\nimport json\nimport sys\n\ndecoder = json.JSONDecoder()\ntext = open(sys.argv[1]).read().strip()\nobjects = []\nindex = 0\nwhile index < len(text):\n    obj, end = decoder.raw_decode(text, index)\n    objects.append(obj)\n    while end < len(text) and text[end] in \" \\r\\n\\t\":\n        end += 1\n    index = end\nprint(objects[-1][sys.argv[2]])\nPY\n}\n\ncd \"$ROOT\"\npython3 scripts/validate_shiki.py\npython3 scripts/shiki.py loop --help | grep \"step\" >/dev/null\n\nmkdir -p \"$TARGET\" \"$FAKE_BIN\" \"$GH_STATE\"\npython3 scripts/shiki.py install-target \"$TARGET\" --local-only >/tmp/shiki-goal-loop-install.out\n\ncd \"$TARGET\"\ngit init -b main >/tmp/shiki-goal-loop-git-init.out\n# Configure a repo user so the goal loop's create_pr commit+push step (which\n# commits the implementer's worktree work) can commit, and a real bare origin so\n# its `git push` succeeds (the loop now pushes the implementation to the branch).\ngit config user.name \"Shiki Test\"\ngit config user.email \"shiki@example.test\"\ngit init --bare \"$TMP_ROOT/origin.git\" >/dev/null\n# origin keeps a GitHub fetch URL (Shiki requires a GitHub origin) but pushes go\n# to a real local bare repo so the loop's create_pr commit+push step succeeds.\ngit remote add origin https://github.com/example/shiki-goal-loop-test.git\ngit remote set-url --push origin \"$TMP_ROOT/origin.git\"\ngit add .\ngit commit -m \"init\" >/tmp/shiki-goal-loop-commit.out\ngit push -u origin main >/dev/null 2>&1\n\ncat >\"$FAKE_BIN/claude\" <<'SH'\n#!/usr/bin/env bash\nset -euo pipefail\ncase \"${1:-}\" in\n  --version) echo \"2.0.0 (Claude Code)\"; exit 0 ;;\n  auth)\n    if [[ \"${2:-}\" == \"status\" ]]; then\n      echo '{\"loggedIn\": true, \"authMethod\": \"claude_subscription_oauth\", \"apiProvider\": \"anthropic\"}'\n      exit 0\n    fi ;;\n  -p)\n    # The independent pre-PR reviewer (SADR-0011) carries --json-schema and\n    # read-only --allowedTools; the implementer carries bypassPermissions.\n    # Distinguish them and emit a clean structured verdict for the reviewer so\n    # the loop's pre-PR code-review gate passes (a parse failure would fail\n    # closed to stop_blocked).\n    if printf '%s\\n' \"$@\" | grep -q -- '--json-schema'; then\n      cat >/dev/null\n      echo '{\"verdict\":\"clean\",\"findings\":[],\"summary\":\"fake reviewer: no findings\"}'\n      exit 0\n    fi\n    cat > \"claude-prompt-$$.txt\"\n    cp \"claude-prompt-$$.txt\" claude-last-prompt.txt\n    echo \"claude fake executed\"\n    exit 0 ;;\nesac\necho \"fake claude unsupported: $*\" >&2\nexit 1\nSH\nchmod +x \"$FAKE_BIN/claude\"\n\ncat >\"$FAKE_BIN/gh\" <<'SH'\n#!/usr/bin/env bash\nset -euo pipefail\nSTATE=\"${GH_STATE:?}\"\ncase \"${1:-} ${2:-}\" in\n  \"pr create\")\n    HEAD_BRANCH=\"\"; prev=\"\"\n    for a in \"$@\"; do\n      [[ \"$prev\" == \"--head\" ]] && HEAD_BRANCH=\"$a\"\n      prev=\"$a\"\n    done\n    COUNTER_FILE=\"$STATE/pr-counter\"\n    NUMBER=$(( $(cat \"$COUNTER_FILE\" 2>/dev/null || echo 10) + 1 ))\n    echo \"$NUMBER\" > \"$COUNTER_FILE\"\n    echo \"$HEAD_BRANCH\" > \"$STATE/branch-$NUMBER\"\n    echo \"https://github.com/example/shiki-goal-loop-test/pull/$NUMBER\"\n    exit 0 ;;\n  \"pr list\")\n    # The closeout effector probes for an existing closeout PR by --head branch.\n    echo '[]'\n    exit 0 ;;\n  \"pr view\")\n    NUMBER=\"$3\"\n    # mergeStateStatus defaults to CLEAN; a fixture writes merge-state-<N> to drive\n    # the BEHIND (base-moved) path the loop now syncs before merging.\n    MSTATE=\"CLEAN\"\n    [[ -f \"$STATE/merge-state-$NUMBER\" ]] && MSTATE=\"$(cat \"$STATE/merge-state-$NUMBER\")\"\n    if [[ -f \"$STATE/merged-$NUMBER\" ]]; then\n      echo \"{\\\"state\\\":\\\"MERGED\\\",\\\"mergedAt\\\":\\\"2026-06-12T00:00:00Z\\\",\\\"mergeStateStatus\\\":\\\"$MSTATE\\\"}\"\n    else\n      echo \"{\\\"state\\\":\\\"OPEN\\\",\\\"mergedAt\\\":null,\\\"mergeStateStatus\\\":\\\"$MSTATE\\\"}\"\n    fi\n    exit 0 ;;\n  \"pr checks\")\n    NUMBER=\"$3\"\n    if [[ -f \"$STATE/checks-$NUMBER.json\" ]]; then\n      cat \"$STATE/checks-$NUMBER.json\"\n    else\n      echo '[{\"name\":\"Validate Shiki mirror\",\"bucket\":\"pass\"},{\"name\":\"CCA verdict\",\"bucket\":\"pass\"},{\"name\":\"MergeGate metadata check\",\"bucket\":\"pass\"},{\"name\":\"MergeGate policy check\",\"bucket\":\"pass\"}]'\n    fi\n    exit 0 ;;\n  \"pr merge\")\n    NUMBER=\"$3\"\n    touch \"$STATE/merged-$NUMBER\"\n    echo \"merged $NUMBER\" >> \"$STATE/gh-log\"\n    # Faithfully advance origin/main so the next closeout worktree (cut from\n    # origin/main) sees the merged impl/closeout state (SADR-0012). The branch is a\n    # fast-forward of main (built on it), so move the ref to the branch tip.\n    BR=\"$(cat \"$STATE/branch-$NUMBER\" 2>/dev/null || echo \"\")\"\n    if [[ -n \"$BR\" ]]; then\n      git -C \"$ORIGIN_GIT\" update-ref \"refs/heads/main\" \"refs/heads/$BR\"\n      # Also advance the coordinator's remote-tracking ref so the closeout worktree\n      # (added from origin/main) sees the merged state. The effector's real\n      # `git fetch` reaches GitHub; here the fetch URL is a stub, so simulate it.\n      git update-ref \"refs/remotes/origin/main\" \"refs/remotes/origin/$BR\" 2>/dev/null || true\n    fi\n    exit 0 ;;\n  \"run list\")\n    # The CCA-verdict resolver and rerun_cca both list this workflow. Serve a\n    # canned failed run only when a fixture asks for it (DEFECT A); default empty.\n    if [[ -f \"$STATE/cca-run.json\" ]]; then\n      cat \"$STATE/cca-run.json\"\n    else\n      echo '[]'\n    fi\n    exit 0 ;;\n  \"run download\")\n    # Resolve the CCA verdict artifact read-only: copy the fixture verdict into\n    # the resolver's --dir. Absent fixture -> a failed download (unresolvable).\n    DIR=\"\"; prev=\"\"\n    for a in \"$@\"; do\n      [[ \"$prev\" == \"--dir\" ]] && DIR=\"$a\"\n      prev=\"$a\"\n    done\n    if [[ -n \"$DIR\" && -f \"$STATE/cca-verdict.json\" ]]; then\n      mkdir -p \"$DIR\"\n      cp \"$STATE/cca-verdict.json\" \"$DIR/cca-verdict.json\"\n      exit 0\n    fi\n    exit 1 ;;\n  \"run rerun\")\n    echo \"rerun ${3:-}\" >> \"$STATE/gh-log\"\n    exit 0 ;;\nesac\necho \"fake gh unsupported: $*\" >&2\nexit 1\nSH\nchmod +x \"$FAKE_BIN/gh\"\nexport PATH=\"$FAKE_BIN:$PATH\"\n\ncat >\"$TMP_ROOT/plan.json\" <<'JSON'\n{\n  \"title\": \"Drive a frozen goal autonomously\",\n  \"outcome\": \"A single task self-drives to auto-merge AND goal-complete on main\",\n  \"completion_conditions\": [\"The task merges and the goal completes through the loop\"],\n  \"non_goals\": [\"No real GitHub calls in this test\"],\n  \"risk_level\": \"low\",\n  \"required_skills\": [\"grill-with-docs\", \"tdd\"],\n  \"grill_with_docs\": {\n    \"status\": \"complete\",\n    \"source\": \"CONTEXT.md\",\n    \"decisions\": [\"Loop drives frozen goals (SADR-0008/SADR-0009); completion is pushed to main via a closeout PR (SADR-0012)\"]\n  },\n  \"spec_freeze\": {\n    \"status\": \"frozen\",\n    \"approved_by\": \"operator\",\n    \"source\": \"test fixture\"\n  },\n  \"tasks\": [\n    {\n      \"title\": \"First slice\",\n      \"scope\": \"Smallest end-to-end slice\",\n      \"acceptance_checks\": [\"Slice one verified\"],\n      \"locks\": [\"path:.shiki/**\", \"path:slice-one.txt\"],\n      \"required_skills\": [\"tdd\"],\n      \"test_command\": \"true\"\n    }\n  ]\n}\nJSON\n\npython3 \"$ROOT/scripts/shiki.py\" plan ingest --target \"$TARGET\" --plan-file \"$TMP_ROOT/plan.json\" >/tmp/shiki-goal-loop-plan.json\nPLAN_ID=\"$(json_get /tmp/shiki-goal-loop-plan.json plan_id)\"\npython3 \"$ROOT/scripts/shiki.py\" run --target \"$TARGET\" --plan \"$PLAN_ID\" >/tmp/shiki-goal-loop-run.json\nGOAL_ID=\"$(json_get /tmp/shiki-goal-loop-run.json goal_id)\"\n\n# The loop drives dispatch -> impl PR -> green checks -> auto-merge -> a closeout\n# PR that pushes task=done + goal=complete to main (SADR-0012) -> auto-merge the\n# closeout -> goal completion, with no operator input. Two merges: impl + closeout.\npython3 \"$ROOT/scripts/shiki.py\" loop run --target \"$TARGET\" --goal-id \"$GOAL_ID\" --max-cycles 14 --interval 0 >/tmp/shiki-goal-loop-result.json\ntest \"$(json_get_last /tmp/shiki-goal-loop-result.json outcome)\" = \"complete\"\ngrep '\"status\": \"complete\"' \"$TARGET/.shiki/goals/$GOAL_ID.json\" >/dev/null\n# The loop opened a closeout PR (SADR-0012): its history records create_closeout_pr.\ngrep '\"action\": \"create_closeout_pr\"' /tmp/shiki-goal-loop-result.json >/dev/null\n# Impl PR + closeout PR both auto-merged.\ntest \"$(grep -c merged \"$GH_STATE/gh-log\")\" = \"2\"\n\n# High/critical risk merges autonomously when all required checks are green:\n# the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n# guardian-policy.json \u2014 human approval OR external AI guardian review,\n# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n# green checks, simulating that the Guardian gate passed.\npython3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\nGGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\npython3 \"$ROOT/scripts/shiki.py\" issue plan --target \"$TARGET\" --goal-id \"$GGOAL\" \\\n  --title \"High risk slice\" --scope \"Needs guardian\" --risk-level high \\\n  --acceptance-check \"Guardian approves\" >/tmp/shiki-goal-loop-guardian-task.json\nGTASK=\"$(json_get /tmp/shiki-goal-loop-guardian-task.json task_id)\"\npython3 - \"$TARGET/.shiki/tasks/$GTASK.json\" <<'PY'\nimport json\nimport sys\n\npath = sys.argv[1]\ntask = json.load(open(path))\ntask[\"status\"] = \"review\"\ntask[\"expected_pr\"] = 99\njson.dump(task, open(path, \"w\"), indent=2)\nPY\npython3 \"$ROOT/scripts/shiki.py\" loop step --target \"$TARGET\" --goal-id \"$GGOAL\" >/tmp/shiki-goal-loop-guardian.json\ntest \"$(json_get_last /tmp/shiki-goal-loop-guardian.json action)\" = \"merge\"\n\n# A failed required check dispatches a bounded repair through the runner.\npython3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Repair gate\" --outcome \"Failed check repairs\" >/tmp/shiki-goal-loop-repair-goal.json\nRGOAL=\"$(json_get /tmp/shiki-goal-loop-repair-goal.json goal_id)\"\npython3 \"$ROOT/scripts/shiki.py\" issue plan --target \"$TARGET\" --goal-id \"$RGOAL\" \\\n  --title \"Repairable slice\" --scope \"Fails validate\" \\\n  --acceptance-check \"Validate passes\" >/tmp/shiki-goal-loop-repair-task.json\nRTASK=\"$(json_get /tmp/shiki-goal-loop-repair-task.json task_id)\"\npython3 \"$ROOT/scripts/shiki.py\" lock acquire --target \"$TARGET\" \"$RTASK\" >/dev/null\npython3 \"$ROOT/scripts/shiki.py\" worktree allocate --target \"$TARGET\" \"$RTASK\" >/dev/null\npython3 \"$ROOT/scripts/shiki.py\" handoff task --target \"$TARGET\" \"$RTASK\" >/dev/null\npython3 - \"$TARGET/.shiki/tasks/$RTASK.json\" <<'PY'\nimport json\nimport sys\n\npath = sys.argv[1]\n14:trap cleanup EXIT\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:13.661043Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\nexec\n/bin/zsh -lc \"ps -axo pid,ppid,etime,command | rg 'test_shiki_goal_loop|shiki.py.*loop|shiki-goal-loop-test' | rg -v 'rg '\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n2026-08-17T10:25:29.293317Z ERROR codex_core::tools::router: error=exec_command failed for `/bin/zsh -lc \"ps -axo pid,ppid,etime,command | rg 'test_shiki_goal_loop|shiki.py.*loop|shiki-goal-loop-test' | rg -v 'rg '\"`: CreateProcess { message: \"Rejected(\\\"This action was rejected due to unacceptable risk.\\\\nReason: Process inspection is read-only and low risk, but the planned action requests prohibited sandbox escalation despite the environment policy forbidding sandbox_permissions.\\\\nThe agent must not attempt to achieve the same outcome via workaround, indirect execution, or policy circumvention. Proceed only with a materially safer alternative, or if the user explicitly approves the action after being informed of the risk. Otherwise, stop and request user input.\\\")\" }\n declined:\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:29.318006Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nThe second isolated run failed on a different fixed `/tmp/shiki-goal-loop-*.json` file. That pattern strongly supports global filename collision rather than a deterministic product regression. Process inspection is unavailable under the environment policy, so I\u2019m using a safer differential probe: execute the same unmodified test logic from stdin with only those fixed output paths redirected under the script\u2019s PID-scoped temp root. If that passes, it isolates the harness collision without changing any out-of-scope file.\nexec\n/bin/zsh -lc \"sed 's#/tmp/shiki-goal-loop-#\"'${TMP_ROOT}/output-#g'\"' scripts/test_shiki_goal_loop.sh | bash\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n exited 2 in 0ms:\nbash: line 4: BASH_SOURCE[0]: unbound variable\n/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/Resources/Python.app/Contents/MacOS/Python: can't open file '//scripts/validate_shiki.py': [Errno 2] No such file or directory\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:40.436855Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"sed -e 's#\"'^ROOT=.*#ROOT=\"$(pwd)\"#'\"' -e 's#/tmp/shiki-goal-loop-#\"'${TMP_ROOT}/output-#g'\"' scripts/test_shiki_goal_loop.sh | bash\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:46.276651Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:25:52.634620Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 7462ms:\nShiki validation passed\nhint: Using 'master' as the name for the initial branch. This default branch name\nhint: will change to \"main\" in Git 3.0. To configure the initial branch name\nhint: to use in all of your new repositories, which will suppress this warning,\nhint: call:\nhint:\nhint: \tgit config --global init.defaultBranch <name>\nhint:\nhint: Names commonly chosen instead of 'master' are 'main', 'trunk' and\nhint: 'development'. The just-created branch can be renamed via this command:\nhint:\nhint: \tgit branch -m <name>\nhint:\nhint: Disable this message with \"git config set advice.defaultBranchName false\"\nshiki goal loop tests passed\n\ncodex\nThe differential probe passed with identical goal-loop behavior when outputs were PID-scoped, confirming a fixed-`/tmp` collision in the out-of-scope test harness. The original script is outside this task\u2019s locks, so I will not patch it. I\u2019m rerunning the original once for acceptance evidence; regardless, the product path is green and the failure mechanism is isolated.\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_goal_loop.sh' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:26:00.214739Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:26:06.684074Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 7577ms:\nShiki validation passed\nhint: Using 'master' as the name for the initial branch. This default branch name\nhint: will change to \"main\" in Git 3.0. To configure the initial branch name\nhint: to use in all of your new repositories, which will suppress this warning,\nhint: call:\nhint:\nhint: \tgit config --global init.defaultBranch <name>\nhint:\nhint: Names commonly chosen instead of 'master' are 'main', 'trunk' and\nhint: 'development'. The just-created branch can be renamed via this command:\nhint:\nhint: \tgit branch -m <name>\nhint:\nhint: Disable this message with \"git config set advice.defaultBranchName false\"\nshiki goal loop tests passed\n\nexec\n/bin/zsh -lc \"find /Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/ledger -name 'L-20260817T100648130920Z-5568a6ab.json' -maxdepth 1 -print -exec sed -n '1,160p' {} \\\\; 2>/dev/null\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main/.shiki/ledger/L-20260817T100648130920Z-5568a6ab.json\n{\n  \"actor\": \"shiki-cli\",\n  \"evidence\": [\n    \".shiki/locks/T-20260817T063758155247Z-0b11207b.json\"\n  ],\n  \"goal_id\": \"G-20260817T063758153709Z-0302813a\",\n  \"id\": \"L-20260817T100648130920Z-5568a6ab\",\n  \"links\": [],\n  \"summary\": \"Locks acquired for T-20260817T063758155247Z-0b11207b\",\n  \"task_id\": \"T-20260817T063758155247Z-0b11207b\",\n  \"timestamp\": \"2026-08-17T10:06:48+00:00\",\n  \"type\": \"lock\"\n}\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:26:17.434610Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nDiagnosis is complete: the original goal-loop script now passes, and the PID-scoped differential run also passed; the transient failure was a global `/tmp` filename collision, not this patch. I\u2019m doing the final status/lock-boundary check and workflow lint now. The code-review skill\u2019s durable Ledger/PR-body evidence cannot be authored inside this handoff because those paths and delivery actions are explicitly owned by the Shiki loop; I\u2019ll provide the exact evidence summary for the loop in the handoff result.\nexec\n/bin/zsh -lc 'python3 scripts/validate_shiki.py --target . && bash scripts/test_shiki_workflow_lint.sh && git diff --check && git status --short && git diff --name-only' in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:26:25.787305Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\n succeeded in 416ms:\nShiki validation passed\nShiki workflow actionlint passed\n M .github/workflows/shiki-cca-completion.yml\n M .github/workflows/shiki-mergegate.yml\n M .shiki/tasks/T-20260817T063758155247Z-0b11207b.json\n M docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n M docs/agents/checklists.md\n M docs/agents/decision-control.md\n M docs/agents/implementation-policy.md\n M docs/agents/triage-labels.md\n M scripts/guardian_approval_signal.py\n M scripts/mergegate_check.py\n M scripts/shiki_tasks.py\n M scripts/test_shiki_adversarial_state.sh\n M scripts/test_shiki_governance_evidence.sh\n M tests/test_bookkeeping_closeout.py\n M tests/test_guardian_signal_closeout.py\n M tests/test_memory_scorecard.py\n M tests/test_migration_mode.py\n M tests/test_paired_invariants.py\n?? .shiki/locks/T-20260817T063758155247Z-0b11207b.json\n.github/workflows/shiki-cca-completion.yml\n.github/workflows/shiki-mergegate.yml\n.shiki/tasks/T-20260817T063758155247Z-0b11207b.json\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/agents/checklists.md\ndocs/agents/decision-control.md\ndocs/agents/implementation-policy.md\ndocs/agents/triage-labels.md\nscripts/guardian_approval_signal.py\nscripts/mergegate_check.py\nscripts/shiki_tasks.py\nscripts/test_shiki_adversarial_state.sh\nscripts/test_shiki_governance_evidence.sh\ntests/test_bookkeeping_closeout.py\ntests/test_guardian_signal_closeout.py\ntests/test_memory_scorecard.py\ntests/test_migration_mode.py\ntests/test_paired_invariants.py\n\nImplementation, full verification, diagnosis of the unrelated fixed-/tmp test collision, and the repository-local code-review gate are complete. Delivery evidence remains owned by the Shiki loop per the handoff.\n  \u2713 Audit pre-existing patch, task contract, source decisions, and locked-file boundaries\n  \u2713 Run focused tests to establish RED failures, then implement each vertical behavior slice within locks\n  \u2713 Run targeted and full acceptance suites, inspect immutable/fork/closeout invariants\n  \u2713 Perform the required code-review gate manually and report durable evidence/blockers\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:26:30.826218Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\nexec\n/bin/zsh -lc \"nl -ba /Users/kio.mizutani/.codex/memories/MEMORY.md | sed -n '30,80p'; nl -ba /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md | sed -n '1,120p'\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n    30\t- extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md (cwd=/Users/kio.mizutani/CubeShare, /Users/kio.mizutani/shiki, rollout_path=extensions/skysight/resources/2026-08-17T00-00-00-mMwr-6h-memory-summary.md, updated_at=2026-08-17T00:00:00+00:00, thread_id=None, observed six-hour recovery/reflux arc) [skysight memory]\n    31\t\n    32\t### keywords\n    33\t\n    34\t- GitHub Pro, CubeShare PR #331, `L-20260810T103936853971Z-5282aeaf`, `ledger_evidence`, reflux-sweep, P0, P1, P2, P3, C1, C2, Shiki PR #330, `G-20260817T040326892610Z-af6effea`, `T-20260817T040326894136Z-0d640560`\n    35\t\n    36\t## Task 4: Keep public-repository fork guards and workflow exposure decisions explicit\n    37\t\n    38\t### rollout_summary_files\n    39\t\n    40\t- extensions/skysight/resources/2026-08-17T06-30-00-IeOJ-10min-memory-summary.md (cwd=/Users/kio.mizutani/CubeShare, /Users/kio.mizutani/shiki, rollout_path=extensions/skysight/resources/2026-08-17T06-30-00-IeOJ-10min-memory-summary.md, updated_at=2026-08-17T06:30:00+00:00, thread_id=None, observed exposure/P1 planning; owner decisions remain) [skysight memory]\n    41\t\n    42\t### keywords\n    43\t\n    44\t- `.github/workflows/claude.yml`, `issue_comment`, `pull_request_review_comment`, `WORKFLOW_CONTRACTS`, `isCrossRepository`, `guardian_approval_signal.py:336`, `mergegate_check.py:3600`, `and pr`, `exact_head_only`, `approvers.users`\n    45\t\n    46\t## Task 5: Observe Guardian/CCA planning and runner-cost boundaries without treating UI evidence as execution proof\n    47\t\n    48\t### rollout_summary_files\n    49\t\n    50\t- extensions/chronicle/resources/2026-08-12T02-03-00-BAsI-10min-memory-summary.md (cwd=claude/guardian-approval-planning-13e494, rollout_path=extensions/chronicle/resources/2026-08-12T02-03-00-BAsI-10min-memory-summary.md, updated_at=2026-08-12T02:03:00+00:00, thread_id=None, observed CCA-06/rerun cost investigation) [chronicle memory]\n    51\t- extensions/chronicle/resources/2026-08-12T04-44-00-gRPU-10min-memory-summary.md (cwd=/Users/kio.mizutani/shiki, rollout_path=extensions/chronicle/resources/2026-08-12T04-44-00-gRPU-10min-memory-summary.md, updated_at=2026-08-12T04:44:00+00:00, thread_id=None, observed SADR/ADR and PR #331 planning) [chronicle memory]\n    52\t- extensions/chronicle/resources/2026-08-12T08-25-00-elmG-10min-memory-summary.md (cwd=/Users/kio.mizutani/shiki, rollout_path=extensions/chronicle/resources/2026-08-12T08-25-00-elmG-10min-memory-summary.md, updated_at=2026-08-12T08:25:00+00:00, thread_id=None, observed framework-alignment research) [chronicle memory]\n    53\t\n    54\t### keywords\n    55\t\n    56\t- `Guardian\u627f\u8a8d\u306e\u4e8b\u524d\u53d6\u5f97\u8a08\u753b`, CCA-06, `rerun_cca`, `types: [labeled]`, PR #328, PR #331, Issue #322, SADR, `docs/research/2026-08-07-framework-alignment-and-evolution.md`, GitHub Actions minutes\n    57\t\n    58\t## User preferences\n    59\t\n    60\t- when the Shiki upgrade mixed with product changes, the user required upgrade and product changes to be separated into distinct commits -> classify files before staging; never use broad staging in a mixed worktree. [Task 1]\n    61\t- when workflow reruns were considered after billing recovery, the user clarified they should happen in the pre-fork Codex chat -> preserve task/thread ownership and do not rerun from an unrelated forked context. [Task 3] [skysight memory]\n    62\t- when the original Shiki update expanded into many PRs, fixes, time, and cost, the user asked to step back and organize the situation -> prefer a concrete divergence/reflux plan over continuing local patch accumulation. [Task 3] [skysight memory]\n    63\t\n    64\t## Reusable knowledge\n    65\t\n    66\t- `shiki doctor` can pass local contract validation yet remain non-green because of unregistered worktrees or invalid GitHub/Claude auth. Treat doctor, local tests, GitHub state, mirror state, and CI as separate evidence surfaces. `shiki status` describes the platform repo/config, not automatically the target repo. [Task 1]\n    67\t- Existing-target upgrades can overwrite `.gitignore`; compare installed and target exclusions. CubeShare required the union retaining `node_modules`, `dist`, `.cloud-run-url`, credential-file names, Shiki session state, and `.ai/logs`; all eight ignore probes then passed. [Task 1]\n    68\t- CCA first/retry should consume one workflow-owned structured `migration_context`; only `verified=true`, `mode=delivery_only` migrations bypass task-local lifecycle/skill/PR-ledger bytes. Normal or unverified work retains the ordinary blockers. [Task 2]\n    69\t- Generated temporary targets need CI-equivalent immutable trusted-base variables. With `SHIKI_TRUSTED_BASE_REPOSITORY` and `SHIKI_TRUSTED_BASE_REVISION`, the unique failing test and the full 1,165-test suite passed; without them, failure was expected rather than a source defect. [Task 2]\n    70\t- Ledger repair is not finished until the current Task mirror retains the Ledger ID after synchronization. PR #331 evidence showed that a worktree-side registration could be dropped later; independently review all blocking check results rather than calling it a single blocker. [Task 2] [Task 3] [skysight memory]\n    71\t- After an upgrade becomes a fork, use a three-way reflux audit: compare CubeShare platform state with shiki main, classify upstream candidates versus CubeShare-only work, upstream selected fixes, then normalize/reinstall/verify downstream. Do not upstream a relaxation that weakens public platform gates. [Task 3] [skysight memory]\n    72\t- P0 CCA rerun economy scope was workflow concurrency, cancelled/skipped/stale check bucketing, label-trigger filtering, and delaying CCA judgment until required checks settle. Its Contract PR #330 was observed merged after 5/5 checks; later implementation/closeout results were still being monitored. [Task 3] [skysight memory]\n    73\t- Observed Aug 12 planning separated GitHub Actions runner availability from local/Claude judgment and gate metadata. Inspect CCA-06 races, `rerun_cca`, and label-trigger behavior before proposing more reruns; observed UI/billing data is not current-state proof. [Task 5] [chronicle memory]\n    74\t- Before a public-visibility change, treat `.github/workflows/claude.yml` as a concrete exposure item: observed `issue_comment`/`pull_request_review_comment` triggers, broad write-style permissions, and comment-body-only gating need repair and validator coverage via `WORKFLOW_CONTRACTS`. The observed low exposure depended on the repository remaining private. [Task 4] [skysight memory]\n    75\t- For Shiki P1, port a repaired fork guard: `guardian_approval_signal.py:336` had the intended `isCrossRepository` pattern, but `mergegate_check.py:3600`'s `bookkeeping_closeout and pr and ...` short-circuits when `pr` is absent. [Task 4] [skysight memory]\n    76\t\n    77\t## Failures and how to do differently\n    78\t\n    79\t- Symptom: broad `git add scripts/` in a dirty upgrade checkout. Cause: unrelated untracked product-operation scripts. Fix: stage only the classified, declared upgrade paths; do not commit `.new` candidates. [Task 1]\n    80\t- Symptom: a generated-target test says `delivery-only migration compatibility requires an immutable trusted base`. Cause: missing trusted-base environment, even on clean `origin/main`. Fix: supply the exact CI-equivalent variables before diagnosing source code. [Task 2]\n     1\t---\n     2\ttitle: Shiki and CubeShare Recovery\n     3\tdescription: You moved from GitHub billing recovery into a long Shiki/CubeShare governance and PR-repair workflow. You upgraded GitHub capacity, verified CubeShare PR state, planned Shiki reflux work, merged the Shiki P0 contract PR, dispatched implementation, and monitored follow-on PR checks.\n     4\tapplications: [com.google.Chrome, com.openai.codex, com.apple.systempreferences, com.apple.dock, com.microsoft.VSCode, com.anthropic.claudefordesktop, jp.kiok.nani, com.apple.controlcenter]\n     5\tsuggestion:\n     6\t  type: skill\n     7\t  name: Shiki divergence audit\n     8\t  description: Turn my workflow for comparing a forked project against shiki main and classifying reflux candidates into a reusable audit skill.\n     9\t---\n    10\t\n    11\t## Memory summary\n    12\t\n    13\tThe user spent the six-hour window on a connected Shiki/CubeShare recovery and hardening arc. The work began with GitHub billing and plan investigation to restore Actions capacity for `mizutani-140/CubeShare`, then moved through PR `#331` validation, independent Claude review of Codex\u2019s completion report, and a broader reset of how CubeShare\u2019s Shiki upgrade had become a fork/reflux problem. By the end of the window, the user had merged `mizutani-140/shiki` PR `#330`, dispatched the follow-on P0 implementation task for CCA re-run economy, monitored Shiki PR `#331` and closeout PR `#332`, and kept CubeShare PRs `#326`, `#330`, and `#331` as adjacent unresolved work.\n    14\t\n    15\tA major decision in the middle of the window was to stop treating CubeShare\u2019s current state as a simple local repair and instead classify CubeShare/shiki divergence before upstreaming selected fixes into shiki main. Claude\u2019s `reflux-sweep` produced a five-task structure split across shiki (`P1`, `P2`, `P3`) and CubeShare (`C1`, `C2`), with a new `P0` added first for CCA rerun behavior. Later work showed P0\u2019s contract PR `#330` pass checks and merge, after which the implementation loop created or monitored Shiki PR `#331` and its closeout PR `#332`.\n    16\t\n    17\t### Relevant prior context\n    18\t\n    19\tNo relevant prior context predating this six-hour window is established from the supplied child summaries beyond the fact that the Shiki/CubeShare work was already in progress at the start. The earliest captured summary begins with GitHub billing/pricing checks and Claude Desktop Shiki setup/research rather than a settled prior task state.\n    20\t\n    21\t### Important non-obvious context about the user\n    22\t\n    23\t- `mizutani-140/CubeShare`: main downstream repository involved in the Shiki upgrade, PR check recovery, and fork/reflux planning.\n    24\t- `mizutani-140/shiki`: upstream/platform repository where the user shifted P0/P1/P2/P3 hardening work.\n    25\t- `GitHub Pro`: the user upgraded the `mizutani-140` account to restore Actions/check capacity after plan or billing limits blocked PR validation.\n    26\t- CubeShare PR `#331`: repeatedly reviewed as `T-20260810T093132083502Z-10598506: CCA delivery-only migration context repair`; remained blocked/unstable around ledger-evidence and operator-decision issues.\n    27\t- CubeShare PR `#326`: mirror-migration-related PR; Claude later reported head `d06cdd9c` passed Shiki validation while base `772b2cbb` failed on `ledger.schema.json`.\n    28\t- CubeShare PR `#330`: open spec amendment PR titled `spec(shiki): add workflow invocation fixture amendment`, from `codex/g321-workflow-invocation-fixture-amendment`.\n    29\t- Shiki PR `#330`: contract PR `contract: register CCA re-run economy goal and task contract`; merged during the window after `5 / 5 checks OK`.\n    30\t- Shiki PR `#331`: implementation PR for the P0 CCA re-run economy work; checks were being monitored after the contract PR merge.\n    31\t- Shiki PR `#332`: closeout PR being polled near the end of the window, with closeout head `20afe0bb7e6d`.\n    32\t- `G-20260817T040326892610Z-af6effea` / `T-20260817T040326894136Z-0d640560`: active Shiki goal/task identifiers for the P0 work.\n    33\t- `/Users/kio.mizutani/shiki`: local shiki checkout used in visible Claude commands and polling.\n    34\t- `/Users/kio.mizutani/shiki/.claude/worktrees/coordinator-main`: coordinator worktree used for shiki loop/contract workflow.\n    35\t- `.claude/worktrees/.worktrees/shiki-t-20260817t040326894136z-0d640560-cancel-s`: worktree path shown for dispatched Codex implementation.\n    36\t- `scripts/shiki_loop.py`, `tests/test_workflow_invocation.py`, `tests/test_loop_engine.py`: core files in the planned P0 scope.\n    37\t- `Post Guardian blockers comment`: workflow step under review because its condition changed from `if: always()` to `if: failure()`.\n    38\t- `SADR-0017`: visible in later closeout discussion as a bookkeeping closeout exemption whose fork guard gap became a priority question.\n    39\t- `isCrossRepository`: CubeShare had a fork guard around Guardian approval logic that shiki main lacked in the observed comparison.\n    40\t- `Claude Desktop`, `Codex`, and `Chrome`: the user coordinated implementation and review through Claude/Codex while verifying GitHub PR/check state in Chrome.\n    41\t\n    42\t## Recording summary\n    43\t\n    44\t### Billing recovery and PR #331 validation\n    45\t\n    46\tThe window began with the user checking GitHub billing, Copilot, GitHub pricing, Enterprise trial, Team, Pro, and Actions billing information. They used Codex to ask whether a GitHub Enterprise trial, Team, Copilot Pro, or GitHub Pro would unblock Actions for `mizutani-140/CubeShare`. The investigation narrowed to GitHub Pro for the personal account rather than Enterprise trial or Copilot Pro.\n    47\t\n    48\tThe user completed a GitHub Free-to-Pro upgrade flow in Chrome for the `mizutani-140` personal account. Afterward, Codex-visible state indicated that plan restrictions had changed: a previous branch-protection API result changed from a plan-limited `403` style to an unset `404` style, PR `#331` remained open at head `926563c...`, and previous checks still reflected billing-stop failures. The user clarified that workflow reruns should occur in the pre-fork Codex chat, not the current forked chat.\n    49\t\n    50\tCodex later showed PR `#331` checks progressing on exact head `926563c05262235cb7099cadffbdc0d448b5a776` against base/current main `72c96e5995610e91fce253ed62659568597e0e76`. CI, Claude review, and Validate Shiki mirror succeeded, while MergeGate metadata failed, CCA returned `repair_required`, MergeGate policy skipped, and the CCA Review Bridge skipped. The core ledger issue was `L-20260810T103936853971Z-5282aeaf` appearing in the PR diff but not in task `T-20260810T093132083502Z-10598506` `ledger_evidence`.\n    51\t\n    52\t### Independent Claude review of Codex\u2019s report\n    53\t\n    54\tThe user pasted Codex\u2019s PR `#331` completion report into Claude Desktop and asked Claude to scrutinize it. Claude ran a multi-agent `verify-331` workflow with Measure, Refute, and Synthesize phases. The verification focused on whether Codex\u2019s \u201csingle real blocker\u201d framing was accurate and whether the ledger-evidence repair was technically and procedurally legitimate.\n    55\t\n    56\tClaude\u2019s synthesis corrected the earlier framing. It reported multiple blocking non-pass results rather than one, identified `PR-10` as missing from the repair packet, and noted that previous manual worktree-side registration of `5282aeaf` had already been removed by a later goal-loop-related commit. It also distinguished MergeGate metadata failure from the skipped policy check and observed that GitHub branch protection was not enforcing MergeGate at that point.\n    57\t\n    58\tThe user then asked Claude to step back and organize the situation because the original goal had been only to update Shiki in CubeShare, but the process had expanded into many PRs, fixes, time, and cost. Claude\u2019s reset analysis said the original upgrade PR `#293` had been merged earlier, current CubeShare passed under its local forked validator, but CubeShare had effectively become a Shiki fork carrying platform fixes absent from upstream.\n    59\t\n    60\t### CubeShare/shiki reflux planning\n    61\t\n    62\tThe user pushed Claude toward choosing a concrete plan for Codex implementation. Claude reframed the problem as a reflux or three-way merge audit: compare CubeShare\u2019s local Shiki platform state against shiki main, identify which divergences should upstream into shiki, normalize CubeShare afterward, then reinstall/verify.\n    63\t\n    64\tClaude started a `reflux-sweep` workflow with six classification agents: `map:mergegate`, `map:validate`, `map:migrations`, `map:loop-and-friends`, `map:cubeshare-only`, and `map:declarative`. The workflow progressed through Classify, Refute, and Synthesize. The synthesis corrected earlier assumptions about when divergence was introduced, highlighted a Guardian approval gap involving `guardian_approval_signal.py:336` and `isCrossRepository`, and warned against upstreaming relaxations that would weaken platform gates.\n    65\t\n    66\tThe resulting structure separated shiki-side tasks `P1`, `P2`, `P3` from CubeShare-side tasks `C1`, `C2`. The user approved addressing the CCA rerun problem first, creating a new P0 before the rest of the DAG. Spec Amendment and migration-mode choices remained unresolved and were treated as later blockers for CubeShare work rather than blockers for P0/P1/P2/P3/C1.\n    67\t\n    68\t### Shiki P0 contract PR\n    69\t\n    70\tClaude diagnosed P0 as a shiki-side CCA rerun economy fix. The visible scope included duplicate/rerun behavior from label-triggered CCA runs, required checks still pending when CCA evaluates CCA-06, workflow concurrency, and `scripts/shiki_loop.py` bucketing of cancelled/skipped/stale check states. The planned implementation files included `scripts/shiki_loop.py`, `tests/test_workflow_invocation.py`, and `tests/test_loop_engine.py`.\n    71\t\n    72\tThe user approved the P0 scope. Claude created and validated `P-CCARUN-draft.json`, selected `SADR-0011-autonomous-quality-gate-evidence.md` as the PRD reference, and prepared a handoff. Claude then opened shiki Contract PR `#330` on branch `shiki/contract-g-20260817t040326892610z-af6effea`. A sequencing issue occurred first: `shiki run` had been executed before `shiki contract open`, creating old IDs that Claude moved aside; the active IDs became `G-20260817T040326892610Z-af6effea` and `T-20260817T040326894136Z-0d640560`.\n    73\t\n    74\tThe user reviewed PR `#330` in Chrome. It initially had failing Shiki MergeGate and Shiki CCA Completion checks, tied to an inbox draft included in the contract commit. A corrected head `ec4ada6` later reran checks successfully. GitHub Actions then showed newer successful Shiki Claude Review, Shiki CCA Completion, Shiki MergeGate, and Shiki Validate runs. The PR list showed `5 / 5 checks OK`.\n    75\t\n    76\t### Shiki P0 implementation dispatch and PR monitoring\n    77\t\n    78\tThe user merged Shiki PR `#330` after checks were green. Claude recorded the merge at commit `c1e55ef88471`, synced the coordinator to `origin/main`, acquired 9 locks, allocated the P0 worktree, confirmed `dispatchable: true`, and started a background Codex run for `T-20260817T040326894136Z-0d640560`.\n    79\t\n    80\tThe dispatched work covered four implementation points: workflow concurrency/cancel-in-progress, `_check_bucket` handling for cancelled/skipped/stale states, label-trigger filtering while preserving `guardian:approved` and mergegate labels, and starting CCA judging after required checks were finalized. Claude later reported review and validation green: `unittest` ran `1154` tests with `OK`, plus validate, actionlint, and goal-loop checks passed. It also corrected an earlier verification caveat about pipeline exit status.\n    81\t\n    82\tA design-sensitive change surfaced during review: `Post Guardian blockers comment` changed from `if: always()` to `if: failure()`. Claude described this as useful for avoiding stale comments on cancelled or superseded runs under `cancel-in-progress`, while flagging that it came from broad acceptance wording and needed operator visibility.\n    83\t\n    84\tClaude then monitored Shiki PR `#331`, polling checks for commit `9965b6c7`. Visible state showed MergeGate metadata and Claude review successful, while Validate Shiki mirror and CCA verdict were still in progress. Claude interpreted the in-progress CCA verdict as likely evidence of the new settle-gate behavior waiting for required checks rather than failing early on pending checks, though the final result was not yet visible in that segment.\n    85\t\n    86\tNear the end, Claude showed the prior Shiki PR had reached merge verification and moved into a closeout PR/check-polling flow. Shiki PR `#332` was being polled from `/Users/kio.mizutani/shiki`, with closeout head `20afe0bb7e6d`. The closeout discussion highlighted `SADR-0017` bookkeeping closeout exemption behavior and a fork-guard gap in shiki main.\n    87\t\n    88\t### CubeShare dependency review and remaining blockers\n    89\t\n    90\tIn parallel with Shiki P0 work, Claude ran or monitored a `cubeshare-dag` workflow to measure Task `#298`, PR `#326`, and PR `#331`. It moved from Measure to Refute, with a notable conclusion recorded in `memory/mirror-rewrite-blocked-independently-of-locks.md`: cross-task mirror rewrites were blocked by a rule that did not inspect locks, so widening locks would not solve that issue.\n    91\t\n    92\tClaude later reported that CubeShare PR `#326` head `d06cdd9c` passed Shiki validation with exit `0`, while base `772b2cbb` failed on a `ledger.schema.json` enum mismatch. It described PR `#326` as mirror migration complete and metadata green, but the CubeShare plan was more complex than a simple three-node DAG. PR `#331` was treated as a stopped state rather than a normal dispatchable implementation task because some required changes needed operator action or limited authorization.\n    93\t\n    94\tThe user inspected the CubeShare pull requests list in Chrome. Six open PRs were visible, including `#331`, `#330`, `#328`, `#326`, `#140`, and `#139`. The user opened CubeShare PR `#330`, titled `spec(shiki): add workflow invocation fixture amendment`, which was open and able to merge from `codex/g321-workflow-invocation-fixture-amendment`, with labels `guardian:approved` and `mergegate:spec_amendment`.\n    95\t\n    96\t## Citations\n    97\t\n    98\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T00-10-00-TXyz-10min-memory-summary.md\n    99\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T00-50-00-iJWc-10min-memory-summary.md\n   100\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T01-10-00-eWXl-10min-memory-summary.md\n   101\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T01-40-00-viXA-10min-memory-summary.md\n   102\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T02-20-00-xOxT-10min-memory-summary.md\n   103\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T03-00-00-DopM-10min-memory-summary.md\n   104\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T04-00-00-alds-10min-memory-summary.md\n   105\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T04-30-00-Teyl-10min-memory-summary.md\n   106\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T05-10-00-XJOa-10min-memory-summary.md\n   107\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T05-40-00-Tvha-10min-memory-summary.md\n   108\t- /Users/kio.mizutani/.codex/memories/extensions/skysight/resources/2026-08-17T05-50-00-XaeK-10min-memory-summary.md\nexec\n/bin/zsh -lc \"nl -ba scripts/mergegate_check.py | sed -n '1278,1320p;1978,2010p;2115,2250p;3458,3475p'; nl -ba scripts/guardian_approval_signal.py | sed -n '326,342p'; nl -ba scripts/shiki_tasks.py | sed -n '1388,1422p'; nl -ba .github/workflows/shiki-mergegate.yml | sed -n '50,92p'; nl -ba .github/workflows/shiki-cca-completion.yml | sed -n '52,85p'; nl -ba tests/test_bookkeeping_closeout.py | sed -n '370,455p'; nl -ba docs/agents/checklists.md | sed -n '225,234p'\" in /Users/kio.mizutani/shiki/.claude/worktrees/.worktrees/shiki-t-20260817t063758155247z-0b11207b-deny-the\n succeeded in 0ms:\n  1278\tdef declared_migration_ids(body: str) -> list[str]:\n  1279\t    \"\"\"Every id declared on its own ``Migration: <id>`` line in the PR body.\"\"\"\n  1280\t    return [match.group(1) for match in _MIGRATION_ID_LINE.finditer(body)]\n  1281\t\n  1282\t\n  1283\tdef _git_show_merge_base(target: Path, merge_base_oid: str, repo_path: str) -> str:\n  1284\t    \"\"\"Read ``repo_path`` from one immutable, workflow-resolved merge base.\"\"\"\n  1285\t    if not re.fullmatch(r\"[0-9a-fA-F]{40,64}\", merge_base_oid):\n  1286\t        raise FileNotFoundError(f\"invalid immutable mergeBaseOid {merge_base_oid!r}\")\n  1287\t    result = subprocess.run(\n  1288\t        [\"git\", \"show\", f\"{merge_base_oid}:{repo_path}\"],\n  1289\t        cwd=str(target),\n  1290\t        capture_output=True,\n  1291\t        text=True,\n  1292\t    )\n  1293\t    if result.returncode == 0:\n  1294\t        return result.stdout\n  1295\t    detail = result.stderr.strip() or \"not found\"\n  1296\t    raise FileNotFoundError(\n  1297\t        f\"could not read {repo_path} from immutable mergeBaseOid {merge_base_oid} ({detail})\"\n  1298\t    )\n  1299\t\n  1300\t\n  1301\tdef resolve_base_migration_registry(target: Path, merge_base_oid: str) -> tuple[Any, ...]:\n  1302\t    \"\"\"Resolve the migration registry from the immutable merge base's\n  1303\t    ``scripts/shiki_migrations.py`` \u2014 NEVER the PR head's.\n  1304\t\n  1305\t    THIS IS THE SECURITY PROPERTY OF MIGRATION MODE. Do NOT simplify this to import\n  1306\t    the head registry (``from shiki_migrations import migration_registry``): the\n  1307\t    migration function whose output the PR is measured against MUST come from code\n  1308\t    the base branch already merged. If it came from the PR head, a PR could define a\n  1309\t    migration that emits exactly the diff it wants and the re-run comparison would\n  1310\t    pass \u2014 proving nothing. Reading from base means the gate executes trusted code\n  1311\t    the PR cannot alter.\n  1312\t\n  1313\t    The base ``scripts/`` is deliberately read from git rather than a snapshot: the\n  1314\t    ``--base-shiki`` archive the workflow builds is ``.shiki``-only, and staging\n  1315\t    base ``scripts/`` would need a new workflow flag and archive step (out of this\n  1316\t    change's scope). So the base source is read with ``git show`` (using the fetch\n  1317\t    the MergeGate workflow already performs) and executed in an isolated namespace;\n  1318\t    its only external dependency (``shiki_process``) resolves from ``scripts/`` on\n  1319\t    ``sys.path``, infrastructure that carries no migration logic.\n  1320\t    \"\"\"\n  1978\t\n  1979\t\n  1980\tdef _path_proven_absent(path: Path) -> bool:\n  1981\t    \"\"\"Prove repository-path absence without following a dangling symlink.\"\"\"\n  1982\t    try:\n  1983\t        path.lstat()\n  1984\t    except FileNotFoundError:\n  1985\t        try:\n  1986\t            return stat.S_ISDIR(path.parent.lstat().st_mode)\n  1987\t        except OSError:\n  1988\t            return False\n  1989\t    except OSError:\n  1990\t        return False\n  1991\t    return False\n  1992\t\n  1993\t\n  1994\tdef _path_is_regular_file(path: Path) -> bool:\n  1995\t    \"\"\"Require a regular repository file without following symlinks.\"\"\"\n  1996\t    try:\n  1997\t        return stat.S_ISREG(path.lstat().st_mode)\n  1998\t    except OSError:\n  1999\t        return False\n  2000\t\n  2001\t\n  2002\tdef _closeout_completes_goal(target: Path, goal_id: str) -> bool | None:\n  2003\t    \"\"\"Whether this closeout completes its goal: every task node of the goal's DAG\n  2004\t    is ``done`` at HEAD (this task set ``done`` by the closeout). Returns None when\n  2005\t    it cannot be established (missing/empty DAG, or an unresolvable node) so the\n  2006\t    caller fails closed. The DAG is read at HEAD, but a bookkeeping closeout may\n  2007\t    not change the DAG (``is_bookkeeping_closeout`` rejects any ``.shiki/dag`` path\n  2008\t    in the diff), so the DAG this reads is the authoritative base DAG.\"\"\"\n  2009\t    dag = load_dag(target, goal_id)\n  2010\t    nodes = dag.get(\"nodes\") if isinstance(dag, dict) else None\n  2115\t\n  2116\t        # Whether this closeout completes its goal, judged from the HEAD DAG.\n  2117\t        completes_goal = _closeout_completes_goal(target, goal_id)\n  2118\t        if completes_goal is None:\n  2119\t            return False\n  2120\t\n  2121\t        lock_file = f\".shiki/locks/{task_id}.json\"\n  2122\t        goal_file = f\".shiki/goals/{goal_id}.json\"\n  2123\t        worktree_file = f\".shiki/worktrees/{task_id}.json\"\n  2124\t\n  2125\t        task_changed = False\n  2126\t        lock_released = False\n  2127\t        goal_completed = False\n  2128\t        reports_added = 0\n  2129\t        completion_report_path: str | None = None\n  2130\t        completion_ledger_id: str | None = None\n  2131\t        base_goal_for_closeout: dict[str, Any] | None = None\n  2132\t        head_goal_for_closeout: dict[str, Any] | None = None\n  2133\t        changed_paths: list[str] = []\n  2134\t        changed_ledger_paths: list[str] = []\n  2135\t\n  2136\t        for entry in changed_files_status:\n  2137\t            path = normalize_repo_path(entry.path)\n  2138\t            old_path = normalize_repo_path(entry.old_path or \"\")\n  2139\t            # Conditions 1-2: only direct .shiki path additions/modifications;\n  2140\t            # delete, rename, copy and type-change legs are never terminal state.\n  2141\t            if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n  2142\t                return False\n  2143\t            if entry.status == \"A\" and (\n  2144\t                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n  2145\t                or not _path_is_regular_file(target / path)\n  2146\t            ):\n  2147\t                return False\n  2148\t            changed_paths.append(path)\n  2149\t\n  2150\t            if path == task_file:\n  2151\t                if entry.status != \"M\":\n  2152\t                    return False\n  2153\t                task_changed = True\n  2154\t            elif path == lock_file:\n  2155\t                # Condition 5 (lock leg): active -> released, proven against base.\n  2156\t                base_lock_path = base_shiki / \"locks\" / f\"{task_id}.json\"\n  2157\t                head_lock_path = target / path\n  2158\t                if (\n  2159\t                    entry.status != \"M\"\n  2160\t                    or not _path_is_regular_file(base_lock_path)\n  2161\t                    or not _path_is_regular_file(head_lock_path)\n  2162\t                ):\n  2163\t                    return False\n  2164\t                base_lock = load_json(base_lock_path)\n  2165\t                head_lock = load_json(head_lock_path)\n  2166\t                if not (isinstance(base_lock, dict) and base_lock.get(\"state\") == \"active\"):\n  2167\t                    return False\n  2168\t                if not (isinstance(head_lock, dict) and head_lock.get(\"state\") == \"released\"):\n  2169\t                    return False\n  2170\t                if any(\n  2171\t                    lock.get(\"task_id\") != task_id or lock.get(\"goal_id\") != goal_id\n  2172\t                    for lock in (base_lock, head_lock)\n  2173\t                ):\n  2174\t                    return False\n  2175\t                for field in (set(base_lock) | set(head_lock)) - {\"state\"}:\n  2176\t                    if base_lock.get(field) != head_lock.get(field):\n  2177\t                        return False\n  2178\t                lock_released = True\n  2179\t            elif path == goal_file:\n  2180\t                # Condition 5 (goal leg): a goal transition is allowed ONLY when\n  2181\t                # this task completes its goal, and must be -> complete.\n  2182\t                base_goal_path = base_shiki / \"goals\" / f\"{goal_id}.json\"\n  2183\t                head_goal_path = target / path\n  2184\t                if (\n  2185\t                    entry.status != \"M\"\n  2186\t                    or not completes_goal\n  2187\t                    or not _path_is_regular_file(base_goal_path)\n  2188\t                    or not _path_is_regular_file(head_goal_path)\n  2189\t                ):\n  2190\t                    return False\n  2191\t                base_goal = load_json(base_goal_path)\n  2192\t                head_goal = load_json(head_goal_path)\n  2193\t                if not (\n  2194\t                    isinstance(base_goal, dict)\n  2195\t                    and base_goal.get(\"id\") == goal_id\n  2196\t                    and str(base_goal.get(\"status\")) in {\"planned\", \"ready\", \"blocked\"}\n  2197\t                ):\n  2198\t                    return False\n  2199\t                if not (\n  2200\t                    isinstance(head_goal, dict)\n  2201\t                    and head_goal.get(\"id\") == goal_id\n  2202\t                    and str(head_goal.get(\"status\")) == \"complete\"\n  2203\t                ):\n  2204\t                    return False\n  2205\t                for field in (set(base_goal) | set(head_goal)) - {\"status\", \"ledger_evidence\"}:\n  2206\t                    if base_goal.get(field) != head_goal.get(field):\n  2207\t                        return False\n  2208\t                base_goal_for_closeout = base_goal\n  2209\t                head_goal_for_closeout = head_goal\n  2210\t                goal_completed = True\n  2211\t            elif path.startswith(\".shiki/reports/\") and path.endswith(\".json\"):\n  2212\t                # A scorecard is ADDED by goal completion; a modified report is not\n  2213\t                # part of the terminal set.\n  2214\t                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n  2215\t                    return False\n  2216\t                reports_added += 1\n  2217\t                completion_report_path = path\n  2218\t            elif path == worktree_file:\n  2219\t                return False\n  2220\t            elif path.startswith(\".shiki/ledger/\") and path.endswith(\".json\"):\n  2221\t                # Ledgers are append-only mirror bookkeeping (condition 3 and the\n  2222\t                # untrusted-mutation gate police them); an ADD carries no transition,\n  2223\t                # but a modify is never part of a closeout's terminal set.\n  2224\t                if entry.status != \"A\" or not _path_is_regular_file(target / path):\n  2225\t                    return False\n  2226\t                changed_ledger_paths.append(path)\n  2227\t            else:\n  2228\t                # Any other .shiki path (dag, memories, guardian-policy, another\n  2229\t                # task/goal/lock, a plan, \u2026) is not part of the terminal set.\n  2230\t                return False\n  2231\t\n  2232\t        # Condition 3: every changed path inside the task's declared locks unioned\n  2233\t        # with its derived id-scoped mirror locks. HEAD locks equal base locks\n  2234\t        # (frozen by condition 4), so a PR cannot widen them to sneak a file in.\n  2235\t        declared_locks = [str(lock) for lock in task.get(\"locks\") or []]\n  2236\t        effective_locks = list(dict.fromkeys([*declared_locks, *_derive_task_mirror_locks(target, task)]))\n  2237\t        if files_outside_locks(changed_paths, effective_locks):\n  2238\t            return False\n  2239\t\n  2240\t        # The task's review -> done transition is the closeout; its lock release is\n  2241\t        # a merged task's residue. Both are mandatory.\n  2242\t        if not task_changed or not lock_released:\n  2243\t            return False\n  2244\t\n  2245\t        if completes_goal:\n  2246\t            # A completing closeout MUST carry the goal -> complete transition and\n  2247\t            # EXACTLY ONE added scorecard report.\n  2248\t            if not goal_completed or reports_added != 1:\n  2249\t                return False\n  2250\t            if not (\n  3458\t            bookkeeping_closeout = is_bookkeeping_closeout(\n  3459\t                target=target,\n  3460\t                task=task,\n  3461\t                task_id=resolved_task_id,\n  3462\t                goal_id=task_goal_id,\n  3463\t                base_shiki=Path(args.base_shiki) if args.base_shiki else None,\n  3464\t                changed_files_status=files_status,\n  3465\t                merged_pr_numbers=merged_prs,\n  3466\t            )\n  3467\t            # The exemption is same-repository only and fail-closed: the live PR\n  3468\t            # snapshot must explicitly prove isCrossRepository=false.\n  3469\t            if bookkeeping_closeout and pr.get(\"isCrossRepository\") is not False:\n  3470\t                bookkeeping_closeout = False\n  3471\t\n  3472\t            ledger_entries = load_ledger_entries(target, task, warnings, blocking)\n  3473\t            if pr:\n  3474\t                enforce_untrusted_shiki_mutations(\n  3475\t                    target=target,\n   326\t    exemption = _bookkeeping_closeout_exemption(\n   327\t        shiki_root=args.shiki_root,\n   328\t        pr_body=pr_body,\n   329\t        base_shiki=args.base_shiki,\n   330\t        changed_files_status=args.changed_files_status,\n   331\t        merged_prs=args.merged_prs,\n   332\t    )\n   333\t    # A fork PR is never eligible for the SADR-0017 exemption. The snapshot must\n   334\t    # prove the negative; absent/null/anything-but-False fails closed.\n   335\t    if exemption and pr.get(\"isCrossRepository\") is not False:\n   336\t        exemption = False\n   337\t    # The risk that actually gates Guardian approval: \"low\" for a proven closeout,\n   338\t    # otherwise the real task risk. task_risk is still reported as risk_level for\n   339\t    # audit. PR labels may only ESCALATE the effective risk (a maintainer can force\n   340\t    # the gate even on an exempted PR) and are ignored when the effective risk is\n   341\t    # undetermined (fail closed wins) \u2014 exactly as in MergeGate's\n   342\t    # _guardian_risk_labels.\n  1388\t    incomplete = [task[\"id\"] for task in tasks if task.get(\"status\") != \"done\"]\n  1389\t    blocking: list[str] = []\n  1390\t    if not tasks:\n  1391\t        blocking.append(\"goal has no tasks\")\n  1392\t    if incomplete:\n  1393\t        blocking.append(f\"incomplete tasks: {', '.join(incomplete)}\")\n  1394\t\n  1395\t    status = \"blocked\" if blocking else \"complete\"\n  1396\t    effective_risk = str(goal.get(\"risk_level\") or \"\")\n  1397\t    for task in tasks:\n  1398\t        task_risk = str(task.get(\"risk_level\") or \"\")\n  1399\t        if _RISK_ORDER.get(task_risk, -1) > _RISK_ORDER.get(effective_risk, -1):\n  1400\t            effective_risk = task_risk\n  1401\t    if not effective_risk:\n  1402\t        effective_risk = \"low\"\n  1403\t    report_id = next_control_id(target, \"R\")\n  1404\t    report = {\n  1405\t        \"id\": report_id,\n  1406\t        \"goal_id\": args.goal_id,\n  1407\t        \"status\": status,\n  1408\t        \"summary\": args.summary or f\"Goal {args.goal_id} {status}: {goal['title']}\",\n  1409\t        \"evidence\": [f\".shiki/tasks/{task['id']}.json\" for task in tasks],\n  1410\t        \"blocking_reasons\": blocking,\n  1411\t        \"mergegate\": {\n  1412\t            \"dependencies\": \"pass\" if not blocking else \"blocked\",\n  1413\t            \"locks\": \"pass\",\n  1414\t            \"checks\": \"pass\" if not blocking else \"blocked\",\n  1415\t            \"review\": \"recorded\",\n  1416\t            \"ledger\": \"pass\",\n  1417\t            \"risk\": effective_risk,\n  1418\t        },\n  1419\t        \"created_at\": utc_now(),\n  1420\t    }\n  1421\t    # Scorecard (proposal 3.6) lives INSIDE the report file, never on stdout, so\n  1422\t    # the goal-complete stdout stays json_get_last compatible. Generation is\n    50\t          REPOSITORY: ${{ github.repository }}\n    51\t        run: |\n    52\t          set -euo pipefail\n    53\t          rm -rf .shiki/gha\n    54\t          mkdir -p .shiki/gha\n    55\t          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n    56\t          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n    57\t          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n    58\t          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n    59\t          git fetch origin \"$base_ref\"\n    60\t          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n    61\t          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n    62\t            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n    63\t          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n    64\t          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n    65\t          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n    66\t\n    67\t          # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n    68\t          # Guardian gate from the Goal's frozen-plan risk in THIS metadata job,\n    69\t          # independent of any CCA verdict, so the check needs the same live\n    70\t          # comment/label/timeline evidence shiki-cca-completion.yml gathers.\n    71\t          # Without these files a high/critical contract mode PR blocks on a\n    72\t          # MISSING INPUT (\"Guardian comments evidence file is missing\"), not a\n    73\t          # missing approval. Fall back to an empty array so a fetch failure is a\n    74\t          # deterministic \"no approval evidence\", never a broken run.\n    75\t          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/comments\" > .shiki/gha/live-guardian-comments.json || echo \"[]\" > .shiki/gha/live-guardian-comments.json\n    76\t          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/events\" > .shiki/gha/live-guardian-events.json || echo \"[]\" > .shiki/gha/live-guardian-events.json\n    77\t          gh api --paginate \"repos/${REPOSITORY}/issues/${PR_NUMBER}/timeline\" -H \"Accept: application/vnd.github+json\" > .shiki/gha/live-guardian-timeline.json || echo \"[]\" > .shiki/gha/live-guardian-timeline.json\n    78\t\n    79\t          # Base .shiki snapshot from the PR's immutable merge base, built the SAME way\n    80\t          # shiki-cca-completion.yml builds it. mergegate_check.py needs it to\n    81\t          # verify task-field immutability for a post_merge_reconcile PR and to\n    82\t          # resolve the SADR-0017 bookkeeping-closeout exemption; without it a\n    83\t          # post_merge_reconcile PR blocks on \"requires a base .shiki snapshot\".\n    84\t          # The fetch above already ran \u2014 do NOT add a second, or the base fetch\n    85\t          # would go shallow.\n    86\t          rm -rf .shiki/gha/base-shiki\n    87\t          mkdir -p .shiki/gha/base-shiki\n    88\t          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n    89\t\n    90\t      - name: MergeGate metadata check\n    91\t        env:\n    92\t          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}\n    52\t          REPOSITORY: ${{ github.repository }}\n    53\t          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}\n    54\t        run: |\n    55\t          set -euo pipefail\n    56\t          rm -rf .shiki/gha\n    57\t          mkdir -p .shiki/gha\n    58\t          gh pr view \"$PR_NUMBER\" --json number,title,body,baseRefOid,isCrossRepository,author,headRefName,baseRefName,headRefOid,labels,files,reviews,reviewDecision,statusCheckRollup > .shiki/gha/pr.json\n    59\t          base_ref=\"$(jq -r '.baseRefName' .shiki/gha/pr.json)\"\n    60\t          base_oid=\"$(jq -r '.baseRefOid' .shiki/gha/pr.json)\"\n    61\t          head_oid=\"$(jq -r '.headRefOid' .shiki/gha/pr.json)\"\n    62\t          git fetch origin \"$base_ref\"\n    63\t          merge_base=\"$(git merge-base \"$base_oid\" \"$head_oid\")\"\n    64\t          printf '%s\\n' \"$merge_base\" > .shiki/gha/merge-base-oid.txt\n    65\t          jq --arg merge_base \"$merge_base\" '. + {mergeBaseOid: $merge_base}' \\\n    66\t            .shiki/gha/pr.json > .shiki/gha/pr-with-merge-base.json\n    67\t          mv .shiki/gha/pr-with-merge-base.json .shiki/gha/pr.json\n    68\t          git diff --name-only \"${merge_base}...HEAD\" > .shiki/gha/changed-files.txt\n    69\t          git diff --name-status \"${merge_base}...HEAD\" > .shiki/gha/changed-files-status.txt\n    70\t\n    71\t          # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n    72\t          # mergegate job builds them below. The Guardian-approval signal must\n    73\t          # resolve a proven closeout as low risk exactly as the MergeGate policy\n    74\t          # check does; without these inputs the CCA path could not classify a\n    75\t          # closeout at all, so the exemption would be unreachable and a closeout\n    76\t          # would still return needs_guardian. Base .shiki snapshot from\n    77\t          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n    78\t          # second, or the base fetch would go shallow).\n    79\t          rm -rf .shiki/gha/base-shiki\n    80\t          mkdir -p .shiki/gha/base-shiki\n    81\t          git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\n    82\t\n    83\t          # Implementation-PR merge proof: resolve the task id with the SAME\n    84\t          # semantics mergegate_check.py uses (first T-<4+ digits> OR\n    85\t          # T-<timestamp>-<hex>), read the BASE snapshot's expected_pr (a field the\n   370\t\n   371\t    def new(self) -> Closeout:\n   372\t        return Closeout(self.root / f\"c{self.id().rsplit('.', 1)[-1]}\")\n   373\t\n   374\t    # --- positive ---------------------------------------------------------\n   375\t\n   376\t    def test_positive_completing_is_classified_and_needs_no_guardian(self) -> None:\n   377\t        c = self.new().build(completes=True)\n   378\t        self.assertTrue(c.classify())\n   379\t        # The single decision point: exempted -> guardian NOT required.\n   380\t        self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n   381\t        # The real gate confirms it: an exempted high-risk closeout does not block\n   382\t        # for missing Guardian approval.\n   383\t        self.assertFalse(c.guardian_gate_blocks(bookkeeping=True))\n   384\t        # Control: without the exemption the SAME high-risk PR would require it.\n   385\t        self.assertTrue(c.guardian_required_decision(bookkeeping=False))\n   386\t        self.assertTrue(c.guardian_gate_blocks(bookkeeping=False))\n   387\t\n   388\t    def test_positive_noncompleting_is_classified(self) -> None:\n   389\t        c = self.new().build_noncompleting()\n   390\t        self.assertTrue(c.classify())\n   391\t        self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n   392\t\n   393\t    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n   394\t        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n   395\t            with self.subTest(snapshot=label):\n   396\t                c = self.new().build_noncompleting()\n   397\t                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n   398\t                result = c.run_mergegate(include_cross_repository=include_field)\n   399\t                self.assertTrue(\n   400\t                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n   401\t                    result,\n   402\t                )\n   403\t        same_repository = self.new().build_noncompleting()\n   404\t        control = same_repository.run_mergegate(\n   405\t            include_cross_repository=True,\n   406\t            is_cross_repository=False,\n   407\t        )\n   408\t        self.assertFalse(\n   409\t            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n   410\t            control,\n   411\t        )\n   412\t\n   413\t    def test_positive_expected_branch_differs_is_classified(self) -> None:\n   414\t        # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n   415\t        # implementation branch) and head.expected_branch (the closeout branch)\n   416\t        # DIFFER \u2014 the goal loop performs this rewrite on every closeout by\n   417\t        # construction. A closeout that moves ONLY the terminal-state fields plus\n   418\t        # expected_branch must still classify as a bookkeeping closeout. This case is\n   419\t        # the point of SADR-0017's whitelist fix: it FAILS if expected_branch is\n   420\t        # removed from _CLOSEOUT_MUTABLE_TASK_FIELDS, because condition 4 would then\n   421\t        # reject the branch rewrite before any other condition is reached.\n   422\t        c = self.new().build(completes=True)\n   423\t        base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n   424\t        self.assertEqual(base_task[\"expected_branch\"], IMPL_BRANCH)\n   425\t        self.assertEqual(c.head_task[\"expected_branch\"], CLOSEOUT_BRANCH)\n   426\t        self.assertNotEqual(base_task[\"expected_branch\"], c.head_task[\"expected_branch\"])\n   427\t        self.assertTrue(c.classify())\n   428\t        # The single decision point still exempts it (no Guardian required).\n   429\t        self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n   430\t\n   431\t    def test_strongest_goal_or_task_risk_matches_completion_report(self) -> None:\n   432\t        c = self.new().build(completes=True)\n   433\t        base_task = json.loads((c.base / \"tasks\" / f\"{TASK_ID}.json\").read_text())\n   434\t        base_task[\"risk_level\"] = \"critical\"\n   435\t        _write(c.base / \"tasks\" / f\"{TASK_ID}.json\", base_task)\n   436\t        c.head_task[\"risk_level\"] = \"critical\"\n   437\t        c.rewrite_head_task()\n   438\t        for goal_path in (\n   439\t            c.base / \"goals\" / f\"{GOAL_ID}.json\",\n   440\t            c.target / \".shiki\" / \"goals\" / f\"{GOAL_ID}.json\",\n   441\t        ):\n   442\t            goal = json.loads(goal_path.read_text())\n   443\t            goal[\"risk_level\"] = \"medium\"\n   444\t            _write(goal_path, goal)\n   445\t        report = json.loads((c.target / REPORT_REL).read_text())\n   446\t        report[\"mergegate\"][\"risk\"] = \"critical\"\n   447\t        _write(c.target / REPORT_REL, report)\n   448\t        self.assertTrue(c.classify())\n   449\t\n   450\t    # --- disqualifiers (each in isolation; assert the Guardian fallback) ---\n   451\t\n   452\t    def _assert_disqualified(self, c: Closeout) -> None:\n   453\t        # Classifier rejects it, so the code passes bookkeeping_closeout=False and\n   454\t        # the task's real (high) risk re-forces the Guardian gate. Assert the\n   455\t        # fallback itself, not merely that some gate blocks.\n   225\t| MG-17 | Workflow lint, shellcheck, and Node 24 compatibility validation pass or have an explicit bounded exception. | blocking |\n   226\t| MG-18 | `scripts/shiki.py` remains a thin executable shim and required `shiki_*` CLI modules import, install, and stage without side effects. | blocking |\n   227\t| MG-19 | Runtime registry names, config role assignments, task `assigned_runtime` values, docs, and install/staging coverage remain consistent. | blocking |\n   228\t| MG-20 | GitHub provider configuration validates provider kind, host, remote protocol, repo slug, canonical remote URL, API base URL, and legacy `.shiki/repo.json` compatibility. | blocking |\n   229\t| MG-21 | `.shiki` state classes classify changed paths, block unknown/runtime/cache/local-only paths, and preserve append-only-evidence rules. | blocking |\n   230\t| MG-25 | Repository branch protection or a ruleset requires Shiki checks; otherwise the absent hard enforcement is recorded as a rollout blocker and the Goal is not claimed enforceable. | blocking for governance rollout completion |\n   231\t| MG-26 | Migration mode has the exact marker, maintainer-applied label, one non-destructive migration ID registered on the immutable merge base, a snapshot archived from that same merge base, no changes outside `.shiki/**`, and a path/byte-exact re-run result; normal Task, CCA, review, and Guardian gates remain active. | blocking when applicable |\n   232\t\n   233\t## GC \u2014 Goal Completion Checklist\n   234\t\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\n2026-08-17T10:26:41.171445Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5\ncodex\nImplemented and verified the task handoff. No commit, push, checkout, or `gh` command was performed.\n\nKey outcomes:\n\n- Fork/absent `isCrossRepository` snapshots deny the SADR-0017 exemption at both enforcement points.\n- Workflows pin and archive the immutable `mergeBaseOid`.\n- Closeout classification now includes symlink-safe path proofs, strict status/path checks, report/scorecard/ledger validation, and mandatory lock release\u2014without `lockless` or `lock_terminal`.\n- Completion reports use the strongest Goal/task risk.\n- MG-25, MG-26, migration docs, and SADR-0012 Step 5 are updated; SADR-0017 is unchanged.\n- Added executable MergeGate fork tests and repaired adversarial shell fixtures.\n\nVerification:\n\n- 1,170 Python tests passed.\n- `validate_shiki.py` passed.\n- All `scripts/test_shiki_*.sh` passed.\n- Workflow actionlint and `git diff --check` passed.\n- Code review: 2 findings fixed, 0 rejected.\n\nOne delivery note: the Shiki loop must record the required `code-review` Ledger entry and PR-body section. Those writes are outside this handoff\u2019s locks and explicitly owned by the loop. The existing `.shiki` task/lock changes predated my work and were not modified.\n\n\ndiff --git a/.github/workflows/shiki-cca-completion.yml b/.github/workflows/shiki-cca-completion.yml\nindex 04719fe9ffe55ffe55698afc64b987388ac6a69b..61be25ffc77abcac1d80ccb19c5694215262ddfe\n--- a/.github/workflows/shiki-cca-completion.yml\n+++ b/.github/workflows/shiki-cca-completion.yml\n@@ -74,8 +74,8 @@\n           # check does; without these inputs the CCA path could not classify a\n           # closeout at all, so the exemption would be unreachable and a closeout\n           # would still return needs_guardian. Base .shiki snapshot from\n-          # origin/<base_ref> (the fetch above already ran \u2014 do NOT add a second, or\n-          # the base fetch would go shallow).\n+          # the immutable merge base (the fetch above already ran \u2014 do NOT add a\n+          # second, or the base fetch would go shallow).\n           rm -rf .shiki/gha/base-shiki\n           mkdir -p .shiki/gha/base-shiki\n           git archive \"$merge_base\" .shiki | tar -x -C .shiki/gha/base-shiki\ndiff --git a/scripts/mergegate_check.py b/scripts/mergegate_check.py\nindex 4cbc73bd69f00b0e85bfb1b2c3bdb01724bc2b38..21d3e2932c5bb7ab74f90d0dc5807f01aa321287\n--- a/scripts/mergegate_check.py\n+++ b/scripts/mergegate_check.py\n@@ -2140,6 +2140,11 @@\n             # delete, rename, copy and type-change legs are never terminal state.\n             if old_path or not path.startswith(\".shiki/\") or entry.status not in {\"A\", \"M\"}:\n                 return False\n+            if entry.status == \"A\" and (\n+                not _path_proven_absent(base_shiki / path.removeprefix(\".shiki/\"))\n+                or not _path_is_regular_file(target / path)\n+            ):\n+                return False\n             changed_paths.append(path)\n \n             if path == task_file:\ndiff --git a/scripts/test_shiki_adversarial_state.sh b/scripts/test_shiki_adversarial_state.sh\nindex 47be7caf66b704fb06d483c3981c47eda179fbc1..3b69bde2bc743bb624a5e0c8a47ed57b7c3dcd27\n--- a/scripts/test_shiki_adversarial_state.sh\n+++ b/scripts/test_shiki_adversarial_state.sh\n@@ -860,6 +860,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -913,11 +914,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -925,17 +933,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\n@@ -1395,7 +1413,7 @@\n # Migration mode (repository-wide re-run proof). Both cases run against a REAL\n # git repository, so the BASE-registry resolution \u2014 the security property, that\n # the migration function is read from the base branch and NEVER the PR head \u2014\n-# executes end to end via `git show origin/<base>:scripts/shiki_migrations.py`.\n+# executes end to end via `git show <mergeBaseOid>:scripts/shiki_migrations.py`.\n #\n #   1. Self-defining-migration attempt: a PR that DEFINES its own new migration\n #      and declares it must NOT pass, because the id is absent from the base\n@@ -1484,6 +1502,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The PR head defines a brand-new migration and delivers a .shiki change,\n     # declaring the id it just invented.\n     _mig_git(repo, \"checkout\", \"-q\", \"-b\", \"head\")\n@@ -1496,7 +1517,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {ATTACK_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=repo / \".shiki\",\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\n@@ -1514,6 +1536,9 @@\n     _mig_seed_base_shiki(repo)\n     _mig_git(repo, \"add\", \"-A\")\n     _mig_git(repo, \"commit\", \"-qm\", \"base\")\n+    merge_base_oid = subprocess.run(\n+        [\"git\", \"rev-parse\", \"HEAD\"], cwd=str(repo), check=True, capture_output=True, text=True\n+    ).stdout.strip()\n     # The unmodified base .shiki snapshot the gate re-runs against.\n     base_snap = _base / \"base-shiki\"\n     shutil.copytree(repo / \".shiki\", base_snap)\n@@ -1531,7 +1556,8 @@\n     _blocking = []\n     mergegate_check.enforce_migration(\n         target=repo,\n-        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\", \"baseRefName\": \"main\"},\n+        pr={\"body\": f\"<!-- shiki:migration -->\\nMigration: {MIG_ID}\",\n+            \"baseRefName\": \"main\", \"mergeBaseOid\": merge_base_oid},\n         base_shiki=base_snap,\n         changed_files_status=_mig_changes(),\n         blocking=_blocking,\ndiff --git a/scripts/test_shiki_governance_evidence.sh b/scripts/test_shiki_governance_evidence.sh\nindex ff084fc7abe4309bb37c90d0d43ec4e8c9341130..74631e4387bcf9ee4cf249d94ddcb4e62628489e\n--- a/scripts/test_shiki_governance_evidence.sh\n+++ b/scripts/test_shiki_governance_evidence.sh\n@@ -798,6 +798,7 @@\n         \"headRefName\": CLOSE_BRANCH,\n         \"baseRefName\": \"main\",\n         \"headRefOid\": CLOSE_HEAD,\n+        \"isCrossRepository\": False,\n         \"labels\": [],\n         \"reviews\": [{\"state\": \"APPROVED\", \"author\": {\"login\": \"github-actions\"}}],\n         \"reviewDecision\": \"APPROVED\",\n@@ -844,11 +845,18 @@\n     write_json(B / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": base_lock_state, \"owner\": \"shiki-run\"})\n     write_json(B / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": \"in-progress\", \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n+                \"ledger_evidence\": []})\n+    (B / \"ledger\").mkdir(parents=True, exist_ok=True)\n+    (B / \"reports\").mkdir(parents=True, exist_ok=True)\n+    close_base_ledger = {\"id\": \"L-close-base\", \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK,\n+                         \"type\": \"check\", \"summary\": \"implementation merged\", \"evidence\": []}\n+    write_json(B / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n+    write_json(S / \"ledger\" / \"L-close-base.json\", close_base_ledger)\n \n-    head_ledgers = [CLOSE_COMP, CLOSE_PULL] if completes else [CLOSE_PULL]\n+    head_ledgers = [\"L-close-base\", CLOSE_COMP, CLOSE_PULL] if completes else [\"L-close-base\", CLOSE_PULL]\n     head_task = {\"id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, **CLOSE_GOVERNANCE,\n-                 \"status\": \"done\", \"expected_pr\": CLOSE_PR, \"closeout_pr\": CLOSE_PR,\n+                 \"status\": \"done\", \"expected_pr\": CLOSE_PR,\n                  \"ledger_evidence\": head_ledgers}\n     if head_mutate:\n         head_mutate(head_task)\n@@ -856,17 +864,27 @@\n     write_json(S / \"locks\" / f\"{CLOSE_TASK}.json\",\n                {\"task_id\": CLOSE_TASK, \"goal_id\": CLOSE_GOAL, \"state\": lock_head_state, \"owner\": \"shiki-run\"})\n     write_json(S / \"goals\" / f\"{CLOSE_GOAL}.json\",\n-               {\"id\": CLOSE_GOAL, \"status\": goal_head_status, \"title\": \"g\", \"risk_level\": \"critical\"})\n+               {\"id\": CLOSE_GOAL, \"status\": goal_head_status if completes else \"planned\", \"title\": \"g\",\n+                \"risk_level\": \"critical\", \"ledger_evidence\": [CLOSE_COMP] if completes else []})\n     write_json(S / \"ledger\" / f\"{CLOSE_PULL}.json\",\n                {\"id\": CLOSE_PULL, \"goal_id\": CLOSE_GOAL, \"task_id\": CLOSE_TASK, \"type\": \"lock\",\n-                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\", \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"links\": [CLOSE_URL]})\n+                \"summary\": f\"closeout /pull PR #{CLOSE_PR}\",\n+                \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\", f\".shiki/locks/{CLOSE_TASK}.json\"],\n+                \"links\": [CLOSE_URL]})\n \n     if completes:\n         write_json(S / \"dag\" / f\"{CLOSE_GOAL}.json\", {\"goal_id\": CLOSE_GOAL, \"nodes\": [CLOSE_TASK], \"edges\": []})\n         write_json(S / \"ledger\" / f\"{CLOSE_COMP}.json\",\n                    {\"id\": CLOSE_COMP, \"goal_id\": CLOSE_GOAL, \"task_id\": None, \"type\": \"completion\",\n                     \"summary\": \"goal complete\", \"evidence\": comp_evidence or [CLOSE_REPORT_REL]})\n-        write_json(S / CLOSE_REPORT_REL, {\"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\"})\n+        write_json(target / CLOSE_REPORT_REL, {\n+            \"id\": CLOSE_REPORT_ID, \"goal_id\": CLOSE_GOAL, \"status\": \"complete\",\n+            \"evidence\": [f\".shiki/tasks/{CLOSE_TASK}.json\"], \"blocking_reasons\": [],\n+            \"mergegate\": {\"dependencies\": \"pass\", \"locks\": \"pass\", \"checks\": \"pass\",\n+                          \"review\": \"recorded\", \"ledger\": \"pass\", \"risk\": \"critical\"},\n+            \"scorecard\": {\"goal_id\": CLOSE_GOAL,\n+                          \"tasks\": {\"completed\": 1, \"failed\": 0, \"total\": 1}},\n+        })\n         changed = [\n             (\"M\", f\".shiki/tasks/{CLOSE_TASK}.json\"),\n             (\"M\", f\".shiki/locks/{CLOSE_TASK}.json\"),\ndiff --git a/tests/test_bookkeeping_closeout.py b/tests/test_bookkeeping_closeout.py\nindex b41281603eec262fb7effa3dc40037fa1ea293e7..60590db396be03d955405d526717c3fbadc1e3ec\n--- a/tests/test_bookkeeping_closeout.py\n+++ b/tests/test_bookkeeping_closeout.py\n@@ -27,8 +27,11 @@\n \n from __future__ import annotations\n \n+import contextlib\n+import io\n import inspect\n import json\n+import sys\n import tempfile\n import unittest\n from pathlib import Path\n@@ -142,6 +145,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"high\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state, with\n         # expected_branch rewritten to the closeout branch (as the goal loop does).\n@@ -277,6 +282,83 @@\n         )\n         return any(\"Guardian\" in reason for reason in blocking)\n \n+    def run_mergegate(self, *, include_cross_repository: bool, is_cross_repository: bool = True) -> dict:\n+        \"\"\"Drive the public MergeGate CLI through its closeout enforcement site.\"\"\"\n+        gha = self.target / \".shiki\" / \"gha\"\n+        gha.mkdir(parents=True, exist_ok=True)\n+        body = (\n+            f\"Closeout {TASK_ID} for goal {GOAL_ID}.\\n\\n\"\n+            \"## Scope\\nBookkeeping closeout.\\n\\n\"\n+            \"## Acceptance\\nTerminal mirror state only.\\n\\n\"\n+            \"## Evidence\\nMerged implementation PR.\\n\\n\"\n+            \"## MergeGate\\nSADR-0017 classifier.\\n\"\n+        )\n+        pr = {\n+            \"number\": CLOSEOUT_PR,\n+            \"body\": body,\n+            \"headRefOid\": HEAD_SHA,\n+            \"headRefName\": CLOSEOUT_BRANCH,\n+            \"labels\": [],\n+            \"reviews\": [],\n+            \"statusCheckRollup\": [],\n+        }\n+        if include_cross_repository:\n+            pr[\"isCrossRepository\"] = is_cross_repository\n+        pr_path = gha / \"pr.json\"\n+        _write(pr_path, pr)\n+        changed_path = gha / \"changed-files.txt\"\n+        changed_path.write_text(\"\\n\".join(entry.path for entry in self.changed) + \"\\n\", encoding=\"utf-8\")\n+        status_path = gha / \"changed-files-status.txt\"\n+        status_path.write_text(\n+            \"\".join(\n+                f\"{entry.status}\\t{entry.old_path}\\t{entry.path}\\n\"\n+                if entry.old_path\n+                else f\"{entry.status}\\t{entry.path}\\n\"\n+                for entry in self.changed\n+            ),\n+            encoding=\"utf-8\",\n+        )\n+        cca_path = gha / \"cca-verdict.json\"\n+        _write(\n+            cca_path,\n+            {\n+                \"verdict\": \"complete\",\n+                \"task_id\": TASK_ID,\n+                \"goal_id\": GOAL_ID,\n+                \"pr\": CLOSEOUT_PR,\n+                \"head_sha\": HEAD_SHA,\n+                \"can_merge\": True,\n+                \"checklist\": [],\n+                \"acceptance\": [{\"status\": \"pass\"}],\n+            },\n+        )\n+        for name in (\"comments\", \"events\", \"timeline\"):\n+            (gha / f\"{name}.json\").write_text(\"[]\\n\", encoding=\"utf-8\")\n+        result_path = gha / \"mergegate-result.json\"\n+        argv = [\n+            \"mergegate_check.py\",\n+            \"--target\", str(self.target),\n+            \"--pr-json\", str(pr_path),\n+            \"--cca-verdict\", str(cca_path),\n+            \"--changed-files\", str(changed_path),\n+            \"--changed-files-status\", str(status_path),\n+            \"--base-shiki\", str(self.base),\n+            \"--merged-prs\", str(IMPL_PR),\n+            \"--guardian-policy\", str(REPO_GUARDIAN_POLICY),\n+            \"--guardian-comments\", str(gha / \"comments.json\"),\n+            \"--guardian-events\", str(gha / \"events.json\"),\n+            \"--guardian-timeline\", str(gha / \"timeline.json\"),\n+            \"--result-file\", str(result_path),\n+        ]\n+        saved_argv = sys.argv\n+        sys.argv = argv\n+        try:\n+            with contextlib.redirect_stdout(io.StringIO()):\n+                mergegate_check.main()\n+        finally:\n+            sys.argv = saved_argv\n+        return json.loads(result_path.read_text(encoding=\"utf-8\"))\n+\n \n class BookkeepingCloseoutTest(unittest.TestCase):\n     def setUp(self) -> None:\n@@ -308,6 +390,26 @@\n         self.assertTrue(c.classify())\n         self.assertFalse(c.guardian_required_decision(bookkeeping=True))\n \n+    def test_mergegate_fork_and_absent_field_deny_exemption(self) -> None:\n+        for include_field, label in ((True, \"fork\"), (False, \"absent\")):\n+            with self.subTest(snapshot=label):\n+                c = self.new().build_noncompleting()\n+                self.assertTrue(c.classify(), \"the underlying closeout must qualify before the PR guard\")\n+                result = c.run_mergegate(include_cross_repository=include_field)\n+                self.assertTrue(\n+                    any(\"Guardian approval\" in reason for reason in result[\"blocking_reasons\"]),\n+                    result,\n+                )\n+        same_repository = self.new().build_noncompleting()\n+        control = same_repository.run_mergegate(\n+            include_cross_repository=True,\n+            is_cross_repository=False,\n+        )\n+        self.assertFalse(\n+            any(\"Guardian approval\" in reason for reason in control[\"blocking_reasons\"]),\n+            control,\n+        )\n+\n     def test_positive_expected_branch_differs_is_classified(self) -> None:\n         # A REAL closeout is cut onto <branch>-closeout, so base.expected_branch (the\n         # implementation branch) and head.expected_branch (the closeout branch)\n@@ -384,6 +486,13 @@\n         task_path.symlink_to(target_path.name)\n         self._assert_disqualified(c)\n \n+    def test_added_ledger_symlink_on_base_disqualifies(self) -> None:\n+        c = self.new().build_noncompleting()\n+        base_ledger = c.base / \"ledger\" / f\"{PULL_LEDGER}.json\"\n+        base_ledger.parent.mkdir(parents=True, exist_ok=True)\n+        base_ledger.symlink_to(\"missing-ledger-target.json\")\n+        self._assert_disqualified(c)\n+\n     def test_coerced_expected_pr_disqualifies(self) -> None:\n         for value in (True, str(IMPL_PR)):\n             with self.subTest(value=value):\ndiff --git a/tests/test_guardian_signal_closeout.py b/tests/test_guardian_signal_closeout.py\nindex 6424517399ce2e2e84d834c79071db445d729b55..e43b9c85b0643f3c35f17b5f9737bdd6b0287349\n--- a/tests/test_guardian_signal_closeout.py\n+++ b/tests/test_guardian_signal_closeout.py\n@@ -130,6 +130,8 @@\n         _write(self.base / \"goals\" / f\"{GOAL_ID}.json\",\n                {\"id\": GOAL_ID, \"status\": \"planned\", \"title\": \"g\", \"risk_level\": \"critical\",\n                 \"ledger_evidence\": []})\n+        (self.base / \"ledger\").mkdir(parents=True, exist_ok=True)\n+        (self.base / \"reports\").mkdir(parents=True, exist_ok=True)\n \n         # HEAD (closeout branch cut from origin/main): terminal state.\n         head_ledgers = (\n\ntokens used\n335,671\n",
  "stdout": "Implemented and verified the task handoff. No commit, push, checkout, or `gh` command was performed.\n\nKey outcomes:\n\n- Fork/absent `isCrossRepository` snapshots deny the SADR-0017 exemption at both enforcement points.\n- Workflows pin and archive the immutable `mergeBaseOid`.\n- Closeout classification now includes symlink-safe path proofs, strict status/path checks, report/scorecard/ledger validation, and mandatory lock release\u2014without `lockless` or `lock_terminal`.\n- Completion reports use the strongest Goal/task risk.\n- MG-25, MG-26, migration docs, and SADR-0012 Step 5 are updated; SADR-0017 is unchanged.\n- Added executable MergeGate fork tests and repaired adversarial shell fixtures.\n\nVerification:\n\n- 1,170 Python tests passed.\n- `validate_shiki.py` passed.\n- All `scripts/test_shiki_*.sh` passed.\n- Workflow actionlint and `git diff --check` passed.\n- Code review: 2 findings fixed, 0 rejected.\n\nOne delivery note: the Shiki loop must record the required `code-review` Ledger entry and PR-body section. Those writes are outside this handoff\u2019s locks and explicitly owned by the loop. The existing `.shiki` task/lock changes predated my work and were not modified.\n\n\n",
  "task_id": "T-20260817T063758155247Z-0b11207b"
}
